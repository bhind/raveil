# 2026-09-18

## T-0191 evidence availability incident

- PR #252 is merged as `3f26f07`; process-observation validation is integrated.
- Read-only inventory found `/private/tmp/raveil-t0191-run-20260909-v9`
  empty and `/private/tmp/raveil-t0191-rtl-sealed-20260909-v5` without its
  marker receipt. Remaining content was not changed or removed.
- On macOS arm64 / Python 3.14.6, both commands exited 1:
  `python3 experiments/openasip/verify_evidence.py run /private/tmp/raveil-t0191-run-20260909-v9`
  and `python3 experiments/openasip/verify_evidence.py rtl /private/tmp/raveil-t0191-rtl-sealed-20260909-v5`.
  Errors identify missing `run-receipt.json` and `rtl-receipt.json`.
- Filename inventory of root and main-clean artifact trees found no matching
  T-0191 / EXP-0012 receipt bundles. This is not an exhaustive backup search.
- Evidence class: local filesystem observation, not a scientific rerun.
  Cause unknown. Historical results retained; promotion pauses at the
  ADR-0051 evidence-identity boundary. Next action: recover exact originals or
  approve a separately identified new collection into persistent storage.

## T-0191 process observation diagnosis and correction

- Main `880cc06` and Issue #239 In Progress confirmed; no open PRs at kickoff.
  New branch `test/t-0191-process-observation` preserves all dirty worktrees.
- The previous witness discarded Docker top errors and used `-eo args`.
  Diagnosis captured `Couldn't find PID field in ps output`; this explains the
  failed observations without assuming a workload-duration problem.
- New tracked `check_cancellation.py` uses `-eo pid,args`, a numeric-PID target
  check and explicit unexpected-error failure. `ttasim` must use `SIM_SCRIPT`,
  not the load-only probe. Scoped monkeypatches are restored after each check.
- Actual commands `python3 experiments/openasip/check_cancellation.py oacc`
  and the same command with `ttasim` both exited 0, reporting process_observed
  and owned_container_absent true with status cancelled, candidate_started true
  and published false. The existing image identity remains `c78fee2f...`.
- Focused `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest
  tests.test_t0191_openasip -q` passed 31 tests in 7.248s on macOS arm64 /
  Python 3.14.6. Two regressions cover PID-column/error handling and exclusion
  of load-only ttasim. This is Host Functional process-lifecycle evidence;
  simulated instruction progress and performance are not inferred.
- Earlier failures remain above/below as provenance. Durable raw evidence and
  independent final task closeout remain open; no T-0044 gate changes.

## T-0191 recovery and cancellation validation

- GitHub main remains `08ff864`; T-0121 is Closed/Done and T-0191 In Progress.
- The old temporary worktree lost its `.git` link and baseline files including
  oracle/manifest. Its 29 test errors therefore did not diagnose the candidate.
  Preserved the original directory; recreated a clean worktree from main and
  restored only five surviving changed files with patches. The historical root
  worktree and unrelated user edits were untouched. Cause of loss is unknown.
- Recovery branch: `test/t-0191-cancel-recovery`. Focused command
  `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_t0191_openasip -q`
  passed 29 tests in 8.185 seconds before final strict-ID tightening.
- Prior security findings addressed: timeout cannot publish CPU fallback;
  failed create cannot delete a container without proven ownership. Returned
  IDs must be full 64-character lowercase hexadecimal identities.
- The earlier actual fixed-image normal run returned accepted oracle results
  neighborhood=170, elementwise=740, reduction=374. A container-only sleep
  cancellation was reported by the implementer; direct `oacc` observation
  failed. Today's `ttasim` observation also failed, so no direct interruption
  witness is claimed. Stop repeating this observation method; it requires a
  revised deterministic witness before that requirement can be closed.
- The previous ttasim attempt was rejected by automatic approval review due
  to service usage limits; today's permitted attempt returned no process
  observation. Weekly usage monitoring remains disabled per owner instruction.
- Evidence is Host Functional and container-lifecycle testing only; no new
  performance, hardware, gate or experiment conclusion. Durable raw-artifact
  promotion and independent final T-0191 closeout remain open.

## Integration receipt

- Exact `2658b0a` passed the final 29-test focused suite in 5.584s and full
  `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -q`:
  860 tests in 180.086s, OK with two skips. macOS 26.6.2 arm64 / Python 3.14.6.
  Record checker passed 211 Markdown files, 157 tasks, 110 records; diff check
  passed. Security and exact-candidate Vreji reviews returned GO.
- PR #250 head/base were verified; no checks were configured/reported and no
  review threads existed. Combined delegated Product Owner/PM acceptance is
  recorded on the PR. Merge readback: `5491bc73a3334df7b8b44cfca160fb6ab2a81f23`,
  MERGED. Issue #239 received the evidence and residual-scope receipt and
  remains In Progress; no full-task completion is claimed.
