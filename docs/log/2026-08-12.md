# 2026-08-12

## T-0105 pinned Rocket reference functional smoke

- Continued on dedicated branch `build/t-0105-rocket-smoke` after the T-0057A
  prior-art boundary milestone. T-0105 remains a Gate 5 substrate task under
  RFC-0004; it does not authorize Graph RTL or performance measurement.
- Used the ignored unmodified Rocket checkout at
  `749a3eae9678bc70b029c5b9091fae33fad539c4` with CDE
  `52768c97a27b254c0cc0ac9401feb55b29e18c28`, Chisel
  `e3bcc90db37f1aec9f8048813f4f0666098d9bee`, and HardFloat
  `d93aa570806013dea479a92ba9bb33d1f2d4f69f`. The upstream license locators are
  `LICENSE.Berkeley`, `LICENSE.SiFive`, `LICENSE.jtag`, and each submodule's
  `LICENSE`. This records source provenance, not patent/FTO clearance.
- Ran the upstream full `nix develop` path diagnostically. It completed the
  first Rocket checks but also ran an unpinned Python shell hook, created a
  50 MiB untracked `venv/`, and pulled irrelevant dependencies including a
  cross-GDB. The generated venv was moved out of the checkout; no upstream
  source was changed. The owned path now uses only named locked Nix attributes.
- A path-based flake later failed on a generated Mill Unix socket under `out/`.
  The owned wrapper instead uses
  `git+file:///rocket?rev=749a3eae9678bc70b029c5b9091fae33fad539c4`, so the
  committed Git tree and `flake.lock` define evaluation while ignored and
  untracked outputs remain disposable cache state.
- Nix's builder seccomp BPF could not load under linux/amd64 emulation on the
  Apple Silicon Docker host. The wrapper passes `filter-syscalls=false` only to
  Nix while preserving Docker's default seccomp and adding
  `no-new-privileges`. Accepted ADR-0038 for this dependency and security
  boundary.
- The first empty-cache host-wrapper run downloaded the fixed package graph but
  then inherited host `out/` created against another Nix store. Mill failed to
  load `ZincWorkerImpl`. Mill 0.11.1 has no supported `--out` option, so the
  corrected wrapper pairs the named Nix-store volume with its own Mill-output
  volume mounted directly over `/rocket/out`; it never uses host or
  foreign-store Mill state. This failure is retained in
  `docs/FAILURE_KNOWLEDGE.md`.
- The corrected `./hardware/chisel/run-rocket-reference.sh` user-facing path
  then completed from the isolated volume pair and exited 0. It compiled the
  Scala/Chisel/Rocket sources, elaborated the small reference, built the full
  Verilator emulator, executed all 16 tests, and emitted the required
  `passed=16 failed=0` non-claiming marker. Upstream deprecation, device-tree
  empty-range, and generated-C++ boolean-negation warnings were observed but
  did not fail the upstream build or functional tests.
- A subsequent ephemeral-container rerun exposed one more absolute-cache
  dependency: Mill output retained a reference to the first container's
  vanished `/root/.cache` Zinc worker. The final wrapper therefore manages a
  version-matched three-volume set for the Nix store, `/rocket/out`, and
  `/root/.cache`. The corrected set then passed a clean compile/execution and a
  second separate-container rerun, both with the exact 16/0 marker. The cached
  rerun duration is not performance evidence.
- Fixed container input: `nixos/nix:2.13.3` manifest-list digest
  `sha256:1f8fa57de6f2f9ea5ea8d115b339fa68d2f98f20b59438bdb9d3a082ad64d4bf`,
  `linux/amd64`; the observed platform-manifest digest was
  `sha256:c3db4c484f6b1ee6c9bb8ca90307cfbeca8ef88156840911356a677eeaff4845`.
  Docker client/server were 29.6.2 on Darwin arm64/Linux arm64.
- `hardware/chisel/rocket-reference-in-container.sh` exited 0. It reported Nix
  2.13.3, Mill 0.11.1/OpenJDK 19.0.2, Rocket Scala 2.13.12/Chisel 5.1.0,
  CIRCT firtool 1.56.1, Verilator 5.012, clang 11.1.0, CMake 3.26.4, Ninja
  1.11.1, and DTC 1.7.0. Unmodified `DefaultSmallConfig` elaboration succeeded;
  unmodified `DefaultConfig` then passed all 16 `rv64mi-p` tests with no failed
  logs. The aggregate passed-log manifest SHA-256 was
  `285ac10334062bfb08ef76f55e193acc97fbbd0c714feed8f6ce1a08cbb3f687`.
- Generated `TestHarness.fir` and the corresponding
  `DefaultSmallConfig.dts` had SHA-256
  `b4ebdfd5755d9f58ee14620acdc252367ddd901a524a91d49867e6c463a14749` and
  `dba33baaa975dfc8fd07e36d0481fd9ed52ce537dfd8a1717d234fddd8463ddd`.
  These ignored outputs are functional-run receipts, not tracked source or
  immutable release assets.
- Added a fixed host wrapper, in-container revision/package/test validator, and
  five repository tests covering executable entrypoints, volume-name input,
  pin agreement, immutable-environment rules, and non-claiming output.
  `python3 -m unittest tests.test_chisel_substrate -v`,
  both scripts' `sh -n` checks, and `git diff --check` exited 0.
- T-0105 is complete as RTL functional simulation substrate evidence only.
  No Graph mechanism, cycle comparison, performance, energy, area, timing,
  OoO-removal, FPGA, silicon, or CPU/ISA advantage is claimed. T-0057B remains
  the next architecture-contract gate before T-0042; T-0044 owns a later fresh
  matched measurement contract.
- Final repository verification: `scripts/ci-local.sh` exited 0 on macOS arm64
  with Python 3.14.6, Apple clang 21.0.0, `riscv64-elf-gcc` 16.1.0, binutils
  2.47.20260726, and QEMU 11.0.3. All 152 tests passed in 53.363 seconds with
  one opt-in real-QEMU integration skipped; RV64 release/debug/DWARF, normal
  and interrupt QEMU smokes, telemetry replay, Sonatine graph execution, and
  native/QEMU checksum differential also passed. The record checker passed
  103 Markdown files, 85 tasks, and 45 records; `git diff --check` passed.

## T-0057B bounded static Graph contract draft

- Continued on dedicated branch `research/t-0057b-native-graph-contract` under
  RFC-0001 and RFC-0004. This is literature-backed contract work, not an EXP or
  implementation result.
- Rechecked the locator-backed T-0057A matrix and primary TRIPS/EDGE,
  WaveScalar, DySER, and BOOM mechanism sources. The review reinforces that
  explicit dependencies, block graphs, resident graph instructions, static
  routes, phase reuse, and hybrid fallback are prior art, not Raveil novelty.
- Added Proposed RFC-0005. Its first candidate is an operation-level,
  fixed-latency, statically scheduled uint32 five-point stencil region behind
  an internal simulation interface. It deliberately excludes dynamic token
  firing, direct-consumer ISA fields, general alias speculation, ROB/rename,
  general LSU, and graph-side precise architectural commit.
- Fixed compiler-versus-validator responsibility, affine/disjoint object
  admission, private-output invalidation, RV64IM fallback, exact independent
  oracle, configuration SHA-256 and invalidation fields, repetition points,
  matched controls, complete system accounting, and pre-registered no-go
  thresholds. These are proposed engineering decisions, not measured facts.
- Updated the prior-art matrix with a feature-level draft disposition. Static
  installed configuration and a retained CPU remain highly similar to
  DySER/CGRA and potentially relevant WaveCache claim scope; private-output
  publication may still require comparison with EDGE commit claims. Avoiding a
  mechanism is not treated as non-infringement.
- US7490218B2, US10824429B2, and WO2015069583A1 remain discovery-only,
  unreviewed risks. No claim scope, family/status, jurisdiction, ownership,
  licensing, expiry, infringement, or FTO conclusion was made.
- RFC-0005 remains Proposed, T-0057 remains open, and T-0042 Graph RTL remains
  blocked pending workload/threshold review and a mechanism-specific
  accept/revise/stop IP disposition. No Graph RTL or performance collection was
  performed.

## T-0057B simulation-only acceptance

- The user directed the project to continue after the RFC-0005 draft. ADR-0039
  records accept outcome 1 only for bounded repository-owned RTL functional
  simulation. It is not a product, novelty, patent, or performance decision.
- Inspected claim text for US7490218B2 claims 1, 10, 21 and dependents;
  US10824429B2 claims 1, 7 and 14; and WO2015069583A1 claims 1, 3 and 6 plus the
  linked US9547496B2 family record. Google Patents status/family fields remain
  unverified and are not legal conclusions.
- Added the T-0057B feature-to-document review. RFC-0005 excludes wave tags and
  wave-ordered memory, dependency-ready issue, commit frontier and precise
  resume, and in-order/OoO mode switching. Those differences are engineering
  boundaries, not non-infringement findings.
- DySER, CGRA, VLIW, loop-accelerator, HLS, static-schedule, scratchpad and
  private-publication patent landscapes remain incompletely searched. No
  external Graph/dataflow RTL or compiler source may be copied into T-0042.
- Updated the reference catalog with exact inspected locators while retaining
  `patent_review_status=unreviewed`. Broader implementation, variable-latency
  scheduling, custom ISA, FPGA, silicon, external reuse, or commercial use
  still requires new review and qualified legal escalation.
- RFC-0005 is now Accepted, T-0057 remains open through functional validation,
  and T-0042 may start only the fixed-schedule static-region slice. T-0044
  performance collection remains unauthorized by this decision.

## T-0042 first owned static-region RTL slice

- Created dedicated branch `feat/t-0042-static-graph-rtl` from the accepted
  T-0057B milestone. Scope remains ADR-0039 repository-owned functional
  simulation; no comparative measurement was started.
- Added `raveil.static-region/v1`: a deterministic Python compiler emits the
  exact ten-node uint32 stencil graph, nine SSA edges, affine READ/WRITE effects,
  disjoint input/private-output objects, a six-cycle schedule, resource bounds,
  RV64IM fallback declaration, and the excluded dynamic mechanisms. The
  independent validator recomputes graph, address, object, schedule, resource,
  bound, fallback, and exclusion invariants.
- Canonical descriptor SHA-256 is
  `d4bf9395a510385f42ba4a193ae2c747f308ad502a8fe807843ed19c2fa4d1e2`;
  the RTL exposes `d4bf9395a510385f` only as a binding tag.
- Added owned Chisel `StaticStencilRegion`, a Verilator C++ testbench with its
  own stencil implementation, container and host runners, seven Python
  compiler/validator/oracle/source-boundary tests, and operator documentation.
- The first container compile failed with seven missing `switch/is` symbols
  because `chisel3.util` was not imported. Added the explicit import and
  retained the recurrence here.
- The next executable RTL failed at output address 15: expected `669790390`,
  observed `3063294118`. A four-bit `x` slice plus one wrapped to zero before
  assignment to a five-bit wire; the same width error threatened the last row.
  Explicitly padded both coordinate slices to five bits before addition and
  added the reusable failure-knowledge entry.
- Final `python3 -m unittest tests.test_static_region -v` passed 7/7. Both shell
  scripts passed `sh -n`, and `git diff --check` passed.
- The final user-facing `./hardware/chisel/run-static-stencil-rtl.sh` completed
  through Docker build, Chisel elaboration, Verilator build, and execution. It
  emitted `STATIC-STENCIL-RTL-V1 status=OK runs=2 cancelled=1 outputs=512
  cycles_per_run=1536 configuration_tag=d4bf9395a510385f
  evidence=rtl-simulation-functional performance=not-measured`.
- Observed functional-smoke tools: Scala CLI 1.10.1 with the Scala 2.13.17 and
  Chisel 7.2.0 source directives, OpenJDK Temurin 17.0.19, and Verilator 4.038
  inside linux/amd64 Docker emulation on the Apple Silicon host. The resolved
  base image manifest was
  `sha256:29467857e8bde40ab1f7befecbda0ea764b95afec1cc7f89aa90f7a766577e19`.
- The 1,536-cycle value proves only the six-phase schedule completed within the
  8,192-cycle contract bound. No Rocket/BOOM, performance, energy, area, timing,
  FPGA, silicon, CPU/ISA, novelty, non-infringement, or FTO claim is made.
- T-0057 closes as prior-art/contract/functional-schema work. T-0042 remains
  open for pinned BOOM, the same-core diagnostic, and a common adapter/resource
  boundary; T-0044 owns later matched measurements.
- Final integration review found that the validator accepted a structurally
  legal alternate six-cycle schedule and unknown fields even though this RTL is
  bound to exactly one descriptor. Tightened the schema, objects, nodes, edges,
  effect kinds, schedule, resources, bounds, fallback, and exclusion checks to
  fail closed. Three regression cases cover unknown fields, schedule changes,
  and effect-kind changes; the canonical descriptor and its hash are unchanged.
- `python3 -m unittest tests.test_static_region tests.test_chisel_substrate -v`
  passed 15/15 after the tightening. A fresh full `scripts/ci-local.sh` completed
  with exit 0 across the 162-test suite, RV64 release/debug/DWARF builds, QEMU
  normal and interrupt smokes, completion telemetry, the Sonatine operator
  demo, and native/QEMU Graph checksum differential. The record checker passed
  106 Markdown files, 85 task definitions, and 47 records.

## T-0042 common simulation adapter

- Continued on dedicated branch `feat/t-0042-common-sim-adapter` after tagging
  the first RTL slice. The pinned Rocket checkout exists and remains clean at
  `749a3eae...539c4`; BOOM is not present yet, so no BOOM execution is claimed.
- Added strict owned `raveil.simulation-adapter/v1` contract SHA-256
  `8b12245438e4a5ffef23bfd4d29a86f6959307a79a437fe517960d043ed51968`.
  It normalizes the RFC-0005 descriptor, semantic workload, disjoint private
  scratchpad, useful-operation counts, implementation identity, completion
  state, and six lifecycle accounting phases without importing upstream types.
- Functional records are accepted with null phases but must enumerate them in
  canonical order, set `accounting_complete=false`, and leave `total_cycles`
  null. A total is valid only when every lifecycle phase is non-negative and
  their exact sum matches. Premature publication, altered useful-operation
  counts, unknown fields, and mismatched identities fail closed.
- Updated the user-facing RTL wrapper to emit completed, cancelled, and restart
  JSON records after the Verilator smoke. A fresh Docker run passed all 512
  output words, two checksums, cancel invalidation, and restart, then emitted
  the adapter marker and all three validated functional records. Current
  unknown phases are installation, completion, validation, and publication;
  no end-to-end timing or performance comparison exists.
- `python3 -m unittest tests.test_simulation_adapter tests.test_static_region
  tests.test_chisel_substrate -v` passed 24/24. A fresh full
  `scripts/ci-local.sh` passed all 171 tests and the downstream RV64/QEMU,
  completion-telemetry, Sonatine demo, and native/QEMU Graph differential
  checks with exit 0. The record checker passed 106 Markdown files, 85 task
  definitions, and 47 records. The first unprivileged Docker attempt was denied access to
  the local daemon; the authorized rerun completed. Docker also reported the
  already pinned Chisel coordinate as older than current, which is not a reason
  to mutate the reproducibility coordinate during this functional milestone.

## T-0042 pinned BOOM reference source

- Created dedicated branch `build/t-0042-pinned-boom-reference` after the
  common-adapter milestone. Cloned the official Chipyard source into ignored
  `external/chipyard`, detached it at existing pin
  `ac58f38d77c99e9d1cafa64dfd6d4b00bdcd43e1`/tag 1.11.0, and initialized only
  its BOOM gitlink `9459af0c1f6847f8411622dac770ac78fe10847c`.
- Added exact `boom-pin.env`, fail-closed fetch and source-verification scripts,
  source/license review, and ADR-0040. The verifier checks origins, clean
  state, parent gitlink, `SmallBoomConfig`, license hashes, source-defined
  chicken CSR `0x7c1` mask `0x8`, and the core consumption of `disableOOO`.
- The source check passed and emitted `BOOM-SOURCE-REFERENCE-V1 status=OK ...
  diagnostic=serialize-dispatch structures=retained evidence=source-verification
  performance=not-measured`. Five owned script/pin tests passed.
- The diagnostic waits for an empty ROB/LSU before dispatch; it does not remove
  the OoO structures and cannot be called an in-order or structural ablation.
  Rocket remains the separate in-order control. No BOOM elaboration, execution,
  performance, area, energy, patent, or FTO result exists yet.
- `python3 -m unittest tests.test_boom_reference
  tests.test_simulation_adapter tests.test_static_region
  tests.test_chisel_substrate -v` passed 29/29. A fresh full
  `scripts/ci-local.sh` passed all 176 tests and downstream RV64/QEMU,
  completion-telemetry, Sonatine demo, and native/QEMU Graph differential
  checks with exit 0. The record checker passed 108 Markdown files, 85 task
  definitions, and 48 records; `git diff --check` passed.

## T-0042 BOOM Scala-project compile bootstrap

- Initialized only the Chipyard gitlinks needed to load the BOOM project:
  Rocket `749a3eae...539c4`, HardFloat `d93aa570...2d4f69f`, CDE
  `384c06b8...d2c1b51`, and FireSim `535dcdc2...4c6871` for its SBT project
  definition. The parent Chipyard gitlinks remain source authority.
- The first fixed-Temurin-17 container attempt downloaded SBT 1.8.2 and then
  failed while loading settings because the image lacked `git`. SBT's Git
  plugin invokes that external executable even for a Scala-only project
  compile. No BOOM source error or compile result was reached.
- Added a dedicated pinned-base compile image and wrapper. It mounts external
  source read-only, copies it into ephemeral container storage, reuses only
  disposable SBT/Ivy caches, prints the JDK/Git versions, and compiles only the
  `boom` project. Maven coordinates are not backed by a repository-owned
  lockfile, so the marker explicitly limits this to functional source evidence.
- `run-boom-project-compile.sh` passed with SBT 1.8.2, Temurin 17.0.19, Git
  2.34.1, and all 56 BOOM Scala sources compiled. It emitted
  `BOOM-PROJECT-COMPILE-V1 status=OK ... elaboration=not-run
  performance=not-measured` after 168 seconds.

## T-0042 BOOM elaboration bootstrap

- A recursive Chipyard submodule attempt was rejected after it created nested
  checkout collisions and reached an unrelated private Mentor integration.
  Restored the exact parent gitlinks, deinitialized partial out-of-scope
  checkouts, and replaced recursion with an explicit list of required public
  gitlinks. `fetch-boom-elaboration-deps.sh` now passes with the detached parent
  clean under `--ignore-submodules=none`.
- The first direct generator attempt compiled the integrated Chipyard assembly
  and printed the selected BOOM tile parameters, then failed because the target
  directory lacked `bootrom.rv64.img`. The direct Scala route had bypassed the
  boot-image copy performed by Chipyard's normal Make flow.
- After explicitly copying the checked-in rv64/rv32 boot images, generation
  reached device-tree creation and failed because the minimal JDK image lacked
  `dtc`. Added `device-tree-compiler`, print its version, and retained both
  failures in `FAILURE_KNOWLEDGE.md`. The base image digest is fixed, but APT
  packages and Maven coordinates are unlocked; this remains functional
  bootstrap and cannot become measurement evidence.
- The corrected run used DTC 1.6.1, rebuilt the integrated Chipyard assembly in
  241 seconds, generated the selected BOOM tile, emitted non-empty FIRRTL and
  annotations, found `BoomCore`, and completed with
  `BOOM-ELABORATION-V1 status=OK config=chipyard.SmallBoomConfig output=firrtl
  boom_core=present evidence=rtl-elaboration-functional execution=not-run
  performance=not-measured`.
- The elaborated configuration reports decode width 1, issue width 3, ROB 32,
  three 8-entry age-priority issue windows, 52 integer and 48 FP physical
  registers, and 8/8 load/store queues. These are emitted configuration facts,
  not performance, energy, area, timing, FPGA, or silicon measurements. No
  RISC-V workload executed and no common-adapter record exists yet.
- `python3 -m unittest tests.test_boom_reference
  tests.test_simulation_adapter tests.test_static_region
  tests.test_chisel_substrate -v` passed 31/31; all new shell entrypoints passed
  `sh -n`. A fresh `scripts/ci-local.sh` completed with exit 0 across 178 tests
  and the downstream RV64 release/debug/DWARF, QEMU normal and interrupt,
  completion telemetry, Sonatine demo, and native/QEMU Graph differential
  checks. The record checker passed 108 Markdown files, 85 task definitions,
  and 48 records; `git diff --check` passed.

## T-0042 BOOM functional execution smoke

- Continued on dedicated branch `build/t-0042-boom-functional-smoke` after the
  elaboration milestone. Initialized only three additional public parent
  gitlinks needed by the simulator path: Spike/FESVR
  `4d8651be...17a6`, DRAMSim2 `44322e2f...6acb`, and install-circt
  `3f8dda6e...eae4`. The ignored Chipyard tree remained clean under
  `--ignore-submodules=none`.
- Fixed the Chipyard lean simulator conda lockfile SHA-256
  `5248d0e4...da3d`; the locked environment supplied Verilator 5.020,
  RISC-V GCC 12.2.0, DTC 1.6.1, and the RISC-V tools prefix. Installed CIRCT
  firtool 1.61.0 was accepted only after its binary matched SHA-256
  `e09cfe2f...56b`. The Miniforge base manifest is pinned, but installing the
  `conda-lock` 1.4 reader is an unlocked bootstrap solve and is not measurement
  authority.
- Built only the FESVR static archive and headers from the exact Spike gitlink,
  then used Chipyard's normal Verilator Make flow for
  `CONFIG=SmallBoomConfig CONFIG_PACKAGE=chipyard`. An earlier wider full-Spike
  build was stopped because it was unnecessary. The first executable check
  guessed a shortened simulator filename after a successful link; the actual
  artifact is `simulator-chipyard.harness-SmallBoomConfig`. Both lessons are
  retained in `FAILURE_KNOWLEDGE.md`. A later fail-closed check also rejected
  the cached tree until inspection showed only CDE's untracked `target/` build
  output; the final check rejects tracked changes while allowing such generated
  cache content. The read-only source checkout remains completely clean.
- Compiled the tracked RV64 assembly at entry `0x80000000`. It sums 1 through
  16, stores and reloads the result, checks 136, and writes success to `tohost`.
  The BOOM Verilator simulator reached its normal `$finish` and emitted
  `BOOM-FUNCTIONAL-SMOKE-V1 status=OK ...
  evidence=rtl-simulation-functional adapter=not-emitted
  performance=not-measured`.
- This is the first owned RISC-V program execution on the pinned BOOM control.
  It is not the RFC-0005 Graph workload and therefore does not fabricate a
  common-adapter record. It supplies no performance, energy, area, timing,
  same-core diagnostic, Graph comparison, FPGA, or silicon evidence.
- `python3 -m unittest tests.test_boom_reference
  tests.test_simulation_adapter tests.test_static_region
  tests.test_chisel_substrate -v` passed 32/32; both new shell entrypoints
  passed `sh -n`. A fresh `scripts/ci-local.sh` completed with exit 0 across
  179 tests and the downstream RV64 release/debug/DWARF, QEMU normal and
  interrupt, completion telemetry, Sonatine demo, and native/QEMU Graph
  differential checks. The record checker passed 108 Markdown files, 85 task
  definitions, and 48 records; `git diff --check` passed.

## T-0042 BOOM serialize-dispatch functional diagnostic

- Continued on dedicated branch `feat/t-0042-boom-serialize-dispatch` from the
  tagged normal BOOM functional-smoke commit. Kept the simulator, source,
  toolchain, linker script, and sum/store/load/check workload unchanged.
- Added a conditional prologue to the same tracked assembly. The diagnostic ELF
  uses `csrrs` to set CSR `0x7c1` mask `0x8`, reads the CSR back, checks bit 3,
  and takes a failing `tohost` code if the bit is absent before entering the
  shared workload. Objdump must also contain the custom CSR access.
- Both the normal and diagnostic ELFs completed on the same pinned
  `chipyard.SmallBoomConfig` Verilator simulator. The latter emitted
  `BOOM-SERIALIZE-DISPATCH-SMOKE-V1 status=OK ...
  diagnostic=serialize-dispatch structures=retained ...
  performance=not-measured`.
- This proves functional execution under the verified setting only. It does
  not show dynamic overlap, make BOOM in-order, remove ROB/rename/issue/LSU
  structures, or provide latency, energy, area, Graph-comparison, FPGA, or
  silicon evidence. `OPEN_QUESTIONS.md` remains unresolved on a scientifically
  fair structural ablation and matched resource boundary; no ADR or EXP changed.
- `python3 -m unittest tests.test_boom_reference
  tests.test_simulation_adapter tests.test_static_region
  tests.test_chisel_substrate -v` passed 32/32; the runner passed `sh -n`.
  A fresh `scripts/ci-local.sh` completed with exit 0 across 179 tests and all
  downstream RV64/QEMU, telemetry, Sonatine, and native/QEMU differential
  checks. The record checker passed 108 Markdown files, 85 task definitions,
  and 48 records; `git diff --check` passed.

## T-0042 simulation adapter v2 resource boundary correction

- Before emitting CPU stencil records, compared the accepted RFC-0005 memory
  contract with the executable v1 adapter and pinned Chipyard configurations.
  RFC-0005 requires one fixed-latency banked scratchpad interface for Rocket,
  BOOM, and the candidate and forbids silently substituting cache-backed
  variable-latency memory. The v1 observation had no field proving which memory
  model actually executed.
- Source inspection also found that Chipyard's existing
  `WithScratchpadsOnly` matches `RocketTileAttachParams`; it does not configure
  BOOM tiles. A normal `RocketConfig`/`SmallBoomConfig` functional run therefore
  cannot honestly claim the RFC-0005 matched memory boundary.
- Added ADR-0041 and `raveil.simulation-adapter/v2`. Every observation now
  reports actual memory model, resource-match verification, and matched-
  comparison readiness. Readiness is true only when the required memory model,
  verified resource match, and complete lifecycle accounting all hold. Current
  static-Graph records are explicitly owned-private-scratchpad, unmatched, and
  not comparison-ready. The new contract SHA-256 is
  `56dbe3f2ab479233eb5e4fe1c79eb06e07458b42ea77acebb471a101afd24c1e`.
- This corrects evidence classification before adding CPU records. It is not a
  CPU, Graph, memory-latency, performance, energy, area, FPGA, silicon, or IP
  result. Historical v1 evidence remains in Git and the earlier dated log.
- `./hardware/chisel/run-static-stencil-rtl.sh` completed in local Docker and
  emitted the v2 marker plus completed/cancelled/restarted JSON observations;
  all three validated with comparison readiness false. The focused adapter and
  static-region suite passed 21/21. A fresh `scripts/ci-local.sh` completed with
  exit 0 across 181 tests and all downstream RV64/QEMU, telemetry, Sonatine,
  and native/QEMU differential checks. The record checker passed 109 Markdown
  files, 85 task definitions, and 49 records; `git diff --check` passed.

## T-0042 BOOM RFC-0005 semantic stencil records

- Added one tracked RV64 C/assembly fallback for the exact RFC-0005 semantic
  workload. It deterministically initializes 324 uint32 input words, computes
  the row-major 16-by-16 five-point stencil with uint32 wrap, and retains all
  256 output words between ELF `begin_signature`/`end_signature` symbols.
- Added an independent host parser that requires exactly 256 eight-hex-digit
  words and compares every word with `static_stencil_oracle`; malformed shape
  or the first differing word fails closed. Unit tests cover exact acceptance,
  truncation, mutation, and tracked workload shape.
- The pinned `SmallBoomConfig` simulator executed the workload in normal and
  CSR-readback serialize-dispatch modes. FESVR extracted both signatures; both
  matched all 256 oracle outputs with checksum `0000007f11ba2640`. Each run then
  emitted a validated adapter v2 record for its exact implementation identity.
- Both records explicitly say cache-backed variable-latency memory, all six
  lifecycle cycle fields unknown, resource matching false, comparison readiness
  false, and performance claim false. This does not satisfy the RFC-0005
  isolation memory contract and supplies no latency, energy, area, FPGA,
  silicon, OoO-benefit, or structural-ablation evidence.
- `python3 -m unittest tests.test_riscv_stencil_signature
  tests.test_simulation_adapter tests.test_boom_reference
  tests.test_static_region -v` passed 34/34. A fresh `scripts/ci-local.sh`
  completed with exit 0 across 186 tests (`OK (skipped=1)`) and all downstream
  RV64/QEMU, telemetry, Sonatine, and native/QEMU differential checks. The
  record checker and `git diff --check` passed before closeout.

## T-0042 Rocket RFC-0005 semantic stencil record

- Continued on dedicated branch `feat/t-0042-rocket-stencil-functional` from
  the tagged BOOM semantic-stencil milestone. Added a Rocket wrapper without
  changing the workload, linker script, signature parser, oracle, adapter
  schema, pinned Chipyard revision, or functional toolchain.
- Built `chipyard.RocketConfig` with Verilator 5.020 in a separate named Docker
  build volume. The simulator executed the tracked 324-input/256-output RV64
  stencil ELF to normal finish. FESVR extracted exactly 256 words; all matched
  the independent host oracle with checksum `0000007f11ba2640`, identical to
  both BOOM functional modes.
- The validated `rocket-in-order` adapter v2 record says
  `memory_model=cache-backed-variable-latency`, leaves all six lifecycle cycle
  fields unknown, and sets resource matching, comparison readiness, and
  performance claim false. This is RTL simulation functional evidence only.
- Read-only source survey found no upstream config that already satisfies the
  RFC-0005 common memory boundary. Rocket's tile-internal
  `WithScratchpadsOnly` rewrites only `RocketTileAttachParams`; the generic
  subsystem TileLink banked scratchpad is the smallest shared starting point
  but does not by itself prove identical ports, arbitration, buffering, or
  fixed latency for Rocket, BOOM, and the owned Graph RTL. No architecture
  decision or performance experiment follows from that survey.
- `python3 -m unittest tests.test_boom_reference
  tests.test_riscv_stencil_signature tests.test_simulation_adapter
  tests.test_static_region -v` passed 35/35; the Rocket wrapper passed `sh -n`.
  A fresh `scripts/ci-local.sh` completed with exit 0 across 187 tests
  (`OK (skipped=1)`) and all downstream RV64/QEMU, telemetry, Sonatine, and
  native/QEMU differential checks. The record checker passed 109 Markdown
  files, 85 task definitions, and 49 records; `git diff --check` passed.

## T-0042 shared subsystem TLRAM functional prototype

- Continued on dedicated branch `feat/t-0042-common-scratchpad-prototype`.
  Source review found that pinned Chipyard `AbstractConfig` already attaches a
  common 64 KiB Mbus TLRAM at `0x08000000` to both Rocket and BOOM configs.
  ADR-0042 accepts this as an unmatched functional prototype only.
- Added a conditional assembly placement and dedicated linker. Code, ordinary
  data, and `tohost` remain at `0x80000000`; the linker and wrapper require
  `input_words=0x08000000` and `begin_signature=0x08000510`, within the pinned
  one-bank/one-partition/one-subbank device.
- The first run stopped before the prototype simulations because `readelf -S`
  abbreviated `.scratchpad_signature` and the exact grep failed. No RTL failure
  or result was claimed. Changing the static check to `readelf -SW` preserved
  fail-closed behavior and is retained in failure knowledge.
- The corrected wrapper executed Rocket, BOOM normal, and BOOM's
  serialize-dispatch diagnostic. Every 256-word signature matched the
  independent oracle with checksum `0000007f11ba2640`. Each validated adapter
  v2 record reports
  `memory_model=shared-tilelink-banked-scratchpad-unverified-latency`, all
  lifecycle cycles unknown, resource matching false, comparison readiness
  false, and no performance claim.
- This removes normal cache-backed buffer placement as one functional
  confounder. It does not prove constant latency: upstream TLRAM exposes a
  minimum latency and the CPU LSU, TileLink fragmenter/buffers/xbar/arbitration,
  and Graph-private storage remain different. No EXP record or performance,
  energy, area, FPGA, silicon, novelty, patent, or FTO result was created.
- `python3 -m unittest tests.test_boom_reference
  tests.test_riscv_stencil_signature tests.test_simulation_adapter
  tests.test_static_region -v` passed 38/38; the wrapper passed `sh -n`. A
  fresh `scripts/ci-local.sh` completed with exit 0 across 190 tests
  (`OK (skipped=1)`) and all downstream RV64/QEMU, telemetry, Sonatine, and
  native/QEMU differential checks. The record checker passed 110 Markdown
  files, 85 task definitions, and 50 records; `git diff --check` passed.

## T-0042 TLRAM endpoint functional observer

- Continued on dedicated branch `feat/t-0042-tlram-latency-observer`. Added a
  passive SystemVerilog bind, a fail-closed log parser, tests, and a wrapper
  that builds separate non-tracing models for pinned `RocketConfig` and
  `SmallBoomConfig`. The validated normal simulator binaries were not replaced.
- The first build attempt stopped because the cached generated file list kept
  its original `/build/chipyard` absolute locator while the source volume was
  mounted at `/rocket`. The wrapper now recreates that locator only inside the
  disposable container. A debug/tracing build then reached C++ compilation but
  failed in generated trace code for an existing `channel` signal; functional
  text telemetry does not need waveforms, so the isolated build switched to a
  non-tracing model.
- The first observer placement above `TLFragmenter` was invalid for one-to-one
  source matching: it accepted 37 logical A requests but saw 296 D response
  beats. Moving the passive bind to TLRAM's post-fragmenter single-beat boundary
  removed that granularity mismatch. Verilator required blocking assignments
  for reset of the 256-entry diagnostic arrays. A later run exposed and fixed
  a monitor-only pending-count error caused by two nonblocking updates during
  simultaneous A and D handshakes. All failed attempts stopped before any
  architecture or performance conclusion.
- Final command `hardware/chisel/run-tlram-latency-observer.sh` ran in the
  pinned `linux/amd64` functional container with Verilator 5.020, Chipyard
  revision `ac58f38...43e1`, and lock identity `5248d0e4...a3d`. Rocket, BOOM
  normal, and BOOM serialize-dispatch diagnostic each retained the exact
  256-word oracle checksum `0000007f11ba2640`.
- Every final observer emitted exactly 296 completed beats, all classified as
  reads, with 162 accepted addresses in the input region, 128 in the
  private-output region, and 6 elsewhere. Every accepted beat had a matching D
  completion; unmatched, premature source reuse, and pending counts were zero. All observed bank-local
  intervals were one cycle. No write beat was observed, and the bind cannot
  distinguish CPU/cache traffic from FESVR load or signature-recovery phases.
- Evidence class remains `rtl-simulation-functional-diagnostic`. No EXP record,
  fixed end-to-end latency claim, matched-resource claim, comparison readiness,
  benchmark, performance, energy, area, FPGA, silicon, novelty, patent, or FTO
  conclusion was created. T-0042 remains open for an owned CPU/Graph request
  interface with phase and read/write coverage.
- The final wrapper rerun completed from cached isolated observer models with
  exit 0. The focused observer/adapter/signature/static-region/BOOM suite passed
  44/44 before full CI; `scripts/ci-local.sh` then completed with exit 0 across
  198 tests (`OK (skipped=1)`) and all downstream RV64/QEMU, telemetry,
  Sonatine, and native/QEMU differential checks. The record checker passed 110
  Markdown files, 85 task definitions, and 50 records; `git diff --check`
  passed.
- The wrapper disables container networking and verifies source/lock identities
  plus tool versions. Its local image tag and persistent generated-model volumes
  remain mutable functional caches, so they are explicitly not T-0044
  measurement authority.

## T-0042 owned local scratchpad transaction boundary

- Continued on dedicated branch `feat/t-0042-owned-memory-interface` after the
  TLRAM observer milestone. Read-only source survey of pinned Chipyard
  `ac58f38...43e1` found the Mbus scratchpad attachment in
  `generators/chipyard/src/main/scala/config/AbstractConfig.scala`, its
  non-cacheable parameters in
  `generators/testchipip/src/main/scala/soc/Configs.scala`, and Get/Put endpoint
  support in pinned Rocket Chip
  `generators/rocket-chip/src/main/scala/tilelink/SRAM.scala`. The endpoint
  observer still cannot distinguish CPU/FESVR phases, and the absence of
  observed writes is not proof that the tracked workload issued none.
- Accepted ADR-0043. Added an upstream-type-free, single-client,
  maximum-one-outstanding request/response scratchpad target with explicit
  operation, word address, 32-bit data, byte mask, initiator, lifecycle phase,
  deterministic error response, backpressure, and transaction counters.
  Operation-disable parameters are present but were not exercised in this
  milestone; only address-range rejection is part of the recorded harness
  coverage.
- `hardware/chisel/run-owned-fixed-latency-scratchpad-rtl.sh` completed with
  exit 0 in the existing `linux/amd64` functional Docker tool host. Chisel
  7.2.0 emitted RTL; Verilator 4.038 ran with assertions enabled. Six accepted
  and six completed requests covered zero-initialized read, full and partial
  writes, readback, request/response backpressure, attribution, and out-of-range
  rejection with no pending transaction.
- The verified one-cycle property is response availability from this module's
  local acceptance boundary. The marker sets
  `fixed_end_to_end_latency_claim=0`, `resource_match_verified=0`,
  `matched_comparison_ready=0`, and `performance=not-measured`. No EXP record or
  CPU/Graph performance, energy, area, FPGA, silicon, novelty, patent, or FTO
  conclusion was created.
- Vreji's read-only triage classified generic ready/valid, banked scratchpad,
  byte-mask, and accounting plumbing as established interface engineering, not
  a novelty basis. Resident graph reuse, static routing, commit-like
  publication, dynamic issue, transitive licenses, patent-family status, and
  freedom to operate remain unreviewed; no external Graph/dataflow source was
  copied.
- Focused owned-memory/static-region/adapter/observer tests passed 34/34.
  `python3 -m unittest discover -s tests -q` and the broader
  `scripts/ci-local.sh` each completed with 201 tests, `OK (skipped=1)`; the
  latter also completed the RV64 release/debug builds, QEMU normal and
  interrupt smokes, completion telemetry, Sonatine demo, and native/QEMU Graph
  checksum differential. Three independent read-only reviews found no blocking
  RTL, ready/valid, claim, record, IP-language, or reproducibility issue.

## T-0042 static Graph adapter over the owned memory boundary

- Continued on `feat/t-0042-graph-memory-adapter`. Replaced the static
  region's direct register-vector accesses with disjoint input and private-
  output `OwnedFixedLatencyScratchpad` instances. Control stages all 324 input
  words with staging attribution; Graph execution issues five reads and one
  private-output write per point with Graph/execution attribution; control
  validates all 256 outputs with validation attribution.
- Added explicit request/response FSM states, per-run accepted Graph read/write
  counts, pending visibility, and cancel-drain behavior. Cancellation stops new
  requests, retires an already accepted response, invalidates private output,
  and permits a clean restart. Emitted-RTL assertions verify operation direction
  plus initiator/phase attribution for staging, execution, and validation.
- The first integration build stopped at Scala parsing because a multiline
  cons-pattern state declaration was invalid; it was replaced with explicit
  `Seq` destructuring. The second emitted Chisel RTL but stopped because the
  older Verilator command passed only the top SV and omitted generated child
  modules; the runner now compiles the bounded generated SV set with assertions.
  Neither failed attempt produced functional or performance evidence.
- The first successful integration exposed stale host accounting: RTL reported
  3,072 execution cycles but adapter v2 still reported the former direct-
  storage 1,536. Updated the strict observation to 648 staging, 3,072
  execution, and 512 validation cycles; a cancellation after 17 execution
  cycles now records seven consumed loads, five adds, one completed store, and
  zero validation cycles. Installation, completion, publication, and total
  remain unknown.
- Final assert-enabled Docker/Verilator execution passed two complete runs plus
  cancel/drain/restart. Each full run accepted 1,280 Graph reads and 256 Graph
  writes; all 512 outputs and checksums matched the independent C++ oracle.
  The marker retains `cpu_connected=0`,
  `fixed_end_to_end_latency_claim=0`, `resource_match_verified=0`,
  `matched_comparison_ready=0`, and `performance=not-measured`. This is
  functional RTL evidence, not a CPU comparison or performance result.
- Focused Graph/owned-memory/adapter tests passed 26/26. A fresh
  `scripts/ci-local.sh` completed with 201 tests, `OK (skipped=1)`, then passed
  the RV64 release/debug builds, QEMU normal and interrupt smokes, completion
  telemetry, Sonatine demo, and native/QEMU Graph checksum differential. The
  record checker passed 111 Markdown files, 85 task definitions, and 51
  records; `git diff --check` passed. Three independent read-only reviews found
  no blocking FSM, handshake, cancel/drain, accumulator, test, reproduction,
  claim, record, or IP-language issue after three stale documentation statements
  were corrected to say Graph connected and CPU unconnected.

## T-0042 CPU-owned TileLink elaboration target

- Continued on dedicated branch `feat/t-0042-cpu-memory-adapter`. Accepted
  ADR-0044 and added `RaveilOwnedTLMemory.scala`, a repository-owned Chipyard
  overlay for dedicated Rocket and Small BOOM configurations. It removes the
  inherited subsystem scratchpad from those configurations and adds a 32-bit,
  maximum-one-outstanding manager at data base `0x08000000` plus a bounded
  phase/counter control page at `0x08010000` on the uncached PBUS path.
- The first MBUS elaboration failed after Scala compilation because the
  inclusive cache tried to add acquire support to an idempotent manager, an
  invalid TileLink region/capability combination. Moving the intentionally
  non-cacheable translation target to PBUS resolved that topology mismatch.
  The next attempt reached the address map but failed with `No implicit clock`;
  wrapping the stateful manager in `ClockSinkDomain` and connecting the selected
  bus fixed clock corrected the missing domain. Both failures produced no
  execution or performance evidence and are retained in
  `docs/FAILURE_KNOWLEDGE.md`.
- Independent static review found that denied invalid phase writes were not
  advertised, partial phase writes ignored byte lane zero, write responses
  could expose irrelevant SRAM data, and the live A-channel address could be
  reused for a next-cycle control response. The overlay now advertises
  `mayDenyPut`, validates the phase byte mask, drives write-response data zero,
  and captures control read data at request acceptance. Direct TileLink
  simulation remains required because source/static and top-level elaboration
  do not exercise these protocol paths.
- `./hardware/chisel/run-owned-cpu-memory-elaboration.sh` is the exact
  acceptance command. It verifies pinned Chipyard
  `ac58f38d77c99e9d1cafa64dfd6d4b00bdcd43e1` and BOOM
  `9459af0c1f6847f8411622dac770ac78fe10847c`, hashes the overlay, uses the
  functional `linux/amd64` Docker bootstrap image, copies the external source
  ephemerally, and generates both target FIRRTL/annotation sets. The final run
  completed with exit 0 for `RaveilOwnedRocketConfig` and
  `RaveilOwnedSmallBoomConfig`; both emitted the owned manager and the expected
  core. Overlay SHA-256 was
  `15bdd1236018ab30c48c07750500dd8b8ce6549e0e926adf77476f77b24de93b`.
  The generated address map exposed `raveil-owned-memory@8000000` through
  `0x08010fff` in both targets.
- Evidence remains `rtl-elaboration-functional`: program execution is not run,
  software phase is only an aggregate label, initiator attribution is
  unverified, the overlay is not yet the ADR-0043 request/response-correlated
  common-contract adapter, and resource matching, comparison readiness, fixed
  end-to-end latency, performance, energy, area, FPGA, silicon, novelty,
  patent, non-infringement, and FTO conclusions remain false or unreviewed.
- Focused owned-memory/static-region/adapter tests passed 27/27. The full
  Python suite passed 202 tests with one skip. `scripts/ci-local.sh` also
  completed with those 202 tests and one skip, then passed the RV64
  release/debug builds, QEMU normal and interrupt smokes, completion telemetry,
  Sonatine demo, and native/QEMU Graph checksum differential. The record checker
  passed 112 Markdown files, 85 task definitions, and 52 records; `git diff
  --check` passed. No EXP record was created because no benchmark or performance
  observation exists.

## T-0042 owned TileLink manager protocol simulation

- Continued on dedicated branch `test/t-0042-owned-tl-protocol`. Added a raw
  bounded TileLink client/harness, an assert-enabled Verilator driver, and a
  pinned Docker runner that copies Chipyard ephemerally, emits FIRRTL and
  Verilog, and caches the assembly by the two overlay sources' content hash.
- Failed attempts were retained as engineering evidence: the first harness
  lost its typed child IO at the `LazyModuleImp` boundary; the next incorrectly
  used master `supports*` fields and failed pinned Rocket Chip requirements;
  an unconstrained assembly was killed for memory pressure; the first Verilator
  passes used a missing and then wrong `plusarg_reader`, lacked
  `sc_time_stamp`, and finally presented two monitor-illegal A-channel test
  sequences. Each was corrected against the fixed source and upstream monitor,
  not hidden by disabling assertions.
- Final command `./hardware/chisel/run-owned-tl-protocol.sh` exited 0. The
  source verifier confirmed Chipyard
  `ac58f38d77c99e9d1cafa64dfd6d4b00bdcd43e1` and BOOM
  `9459af0c1f6847f8411622dac770ac78fe10847c`. Host input SHA-256 was
  `56ee9e011734d0dc2b2a25f00885eef091b4365e3f085fbdb35fb019c42368ad`;
  overlay assembly-key SHA-256 was
  `15dccb29162a4eca3e5ff17752cf52975e27393eec38b2a3dca896a657e6d4fe`.
  The final marker reported 16 transactions: one `PutFull`, five
  `PutPartial`, ten `Get`, masks `0x5`/`0xa`, invalid-phase denial, response
  backpressure, maximum-one-outstanding admission, D metadata, reset phase,
  and the explicitly bounded aggregate counter scope.
- Evidence is `rtl-simulation-functional`. The harness bypasses Rocket and
  BOOM; CPU execution is not run, initiator attribution is unverified, the
  ADR-0043 common contract is not implemented, and resource matching,
  comparison readiness, fixed end-to-end latency, performance, energy, area,
  FPGA, silicon, novelty, patent, non-infringement, and FTO conclusions remain
  false or unreviewed. No EXP record was created.
- Focused owned-memory boundary tests passed 5/5. The full local CI passed 203
  tests with one skip, then completed the RV64 release/debug builds, QEMU normal
  and interrupt smokes, completion telemetry, Sonatine demo, and native/QEMU
  Graph checksum differential. The record checker passed 112 Markdown files,
  85 task definitions, and 52 records; `git diff --check` passed. Two
  independent Low-model read-only reviews found no blocking protocol, driver,
  shell, record, claim, or IP-language issue. Unversioned APT packages and
  mutable dependency caches remain acceptable only for this bounded functional
  development runner and must be pinned before stronger evidence use.

## T-0042 Rocket CPU execution through the owned manager

- Continued on dedicated branch `test/t-0042-rocket-owned-memory-smoke` after
  the owned TileLink protocol milestone. Added a bare-metal RV64 workload, an
  exact host signature verifier, and a pinned Docker runner for the dedicated
  `chipyard.raveil.RaveilOwnedRocketConfig` Verilator system.
- The generated device tree exposed `raveil-owned-memory@8000000`; the ELF used
  CPU load/store instructions and `fence iorw,iorw` to access data at
  `0x08000000` and control at `0x08010000`. It covered a full word write,
  byte-lane writes, reset phase, phase transition to execution, data readback,
  and accepted/completed plus installation/execution read/write counters.
- The first execution failed after the manager had correctly returned
  `cafebabe`. The diagnostic signature proved that the workload's `lw` had
  sign-extended the word while its `li` comparison value was zero-extended.
  Replacing that unsigned comparison load with `lwu` fixed the test; the
  failure and prevention are retained in `docs/FAILURE_KNOWLEDGE.md`.
- Exact command `./hardware/chisel/run-owned-rocket-memory-smoke.sh` exited 0.
  The host signature marker reported reset phase 0, data
  `11223344,5522aa44,cafebabe`, final phase 2, accepted/completed 8/8,
  installation reads/writes 2/3, and execution reads/writes 2/1. The final
  verifier also emits the exact raw-signature SHA-256; the workload runner
  reports a composite input SHA-256 over overlay, ELF source, and verifier.
  Final values were signature SHA-256
  `0e9769fb2f4a3f66bdbd2077fdd933a3d2fa045e6b7823211bd73cebe2880161`
  and composite input SHA-256
  `d3c8375f2a1682d6e84c5b8fe90f0cf17d09f124ace11d26da9efb1d3e9d2138`.
- Evidence is `rtl-simulation-functional`: Rocket CPU instructions executed the
  intended mapped path. Initiator attribution remains intended but unproven,
  software phase is not owned request/response correlation, BOOM has not yet
  replayed the exact workload, and resource matching, comparison readiness,
  fixed end-to-end latency, performance, energy, area, FPGA, silicon, novelty,
  patent, non-infringement, and FTO conclusions remain false or unreviewed. No
  EXP record was created.
- A final runner review added fail-closed checks for unexpected tracked,
  untracked, or shifted-submodule content in the persistent simulator source
  cache. The functional Docker image build and dependency channels are still
  not immutable measurement provenance; T-0044 must use a stronger pinned
  environment before any comparative claim.
- Focused owned-memory tests passed 6/6. The full local CI passed 204 tests
  with one skip, then completed the RV64 release/debug builds, QEMU normal and
  interrupt smokes, completion telemetry, Sonatine demo, and native/QEMU Graph
  checksum differential. The record checker passed 112 Markdown files, 85
  task definitions, and 52 records; `git diff --check` passed. Three
  independent Low-model read-only reviews found no blocking code, record,
  claim, IP-language, or bounded functional-runner issue.

## T-0042 BOOM CPU execution through the owned manager

- Continued on dedicated branch `test/t-0042-boom-owned-memory-smoke`. Refactored
  the Rocket-only runner into one allowlisted CPU runner plus thin Rocket and
  BOOM entrypoints. The configurations and persistent build volumes remain
  separate; the ELF source and host signature verifier are identical.
- Exact commands `./hardware/chisel/run-owned-boom-memory-smoke.sh` and
  `./hardware/chisel/run-owned-rocket-memory-smoke.sh` both exited 0 after the
  refactor. Each independently decoded signature reported reset phase 0, data
  `11223344,5522aa44,cafebabe`, final phase 2, accepted/completed 8/8,
  installation reads/writes 2/3, and execution reads/writes 2/1. Both raw
  signatures had SHA-256
  `0e9769fb2f4a3f66bdbd2077fdd933a3d2fa045e6b7823211bd73cebe2880161`;
  The final shared overlay/workload/linker/verifier/runner payload SHA-256 was
  `3ea825a601f98b3f74a508b348b8d473b542ef8db648203c396f4b1b9432bf93`.
- Evidence is `rtl-simulation-functional` for the two mapped CPU paths and this
  workload only. Identical signatures establish bounded semantic agreement,
  not latency, throughput, energy, area, cache, pipeline, OoO, Graph, resource-
  matching, novelty, patent, non-infringement, FPGA, silicon, or FTO evidence.
  Semantic initiator identity and per-request/response ADR-0043 owned metadata
  remain unimplemented. No EXP record or new ADR was created.
- `sh -n` passed for all three runners. Focused owned-memory tests passed 6/6;
  the record checker passed 112 Markdown files, 85 task definitions, and 52
  records; `git diff --check` passed. Final full local CI passed 204 tests with
  one skip, then completed RV64 release/debug builds, QEMU normal and interrupt
  smokes, completion telemetry, Sonatine demo, and native/QEMU Graph checksum
  differential. Two independent Low-model read-only reviews found no blocker;
  their volume-binding and input-hash cautions were incorporated before the
  final two RTL runs and CI.

## T-0042 generated source-class and A/D phase audit

- Continued on dedicated branch `test/t-0042-cpu-source-attribution` under
  ADR-0043 and ADR-0044. Added a fail-closed verifier for each exact generated
  Chipyard graph. Rocket exposes DCache-MMIO input range `[257,258)` expanded
  to `[8224,8256)`; BOOM exposes `[259,260)` expanded to `[8288,8320)`.
  Both graphs retain the SimTSI/FESVR serial range `[0,8192)`. These are
  config/Xbar/fragmenter-dependent TileLink client IDs, not ISA identifiers.
- The manager now latches a data request's source, expected-client class, and
  software-declared phase at A acceptance and retains them through D
  completion. Separate expected/unexpected accepted/completed counters and
  last-source/last-phase registers are checked by the direct protocol driver
  and CPU signature verifier. This is A/D metadata correlation at the
  unmatched adapter, not ADR-0043 owned semantic initiator attribution.
- `./hardware/chisel/run-owned-tl-protocol.sh` exited 0. Its V2 marker reported
  26 legal transactions, expected accepted/completed 7/7, unexpected 0/0,
  last source 3, and last phase 2 under monitor assertions and D backpressure.
  Host input SHA-256 was
  `4d8bb2d0cbeee3c7ceef7aef2e282607742f4d1adf95312b588bc4fc9cbaa396`;
  overlay assembly-key SHA-256 was
  `a36f27b3339055a5657395849da3938f39a9d8d67a5912c39af753a88822900d`.
- `./hardware/chisel/run-owned-rocket-memory-smoke.sh` and
  `./hardware/chisel/run-owned-boom-memory-smoke.sh` each exited 0. Graph
  SHA-256 values were
  `78437f56a45d477e249f3086ab9f1191d69d935bfdb8d852f401a3e1fbbcd2a1`
  and
  `4361d730fcece11911cab11c3c8a2f25df753b21bfaf42a0631af5377ea272d8`.
  Runtime signatures reported expected 8/8, unexpected 0/0, last sources
  `8224/8224` and `8288/8288`, and phases 2/2. Existing data, phase, aggregate,
  and per-phase counters remained unchanged. Raw signature SHA-256 values were
  `884054743eb48cb0016353124cf20b8136b03dd95c5a2de2fb3fff287dacce01`
  and
  `7b960788f71a95fd92264e3a1884921444597490e3c529277436e6c3f4fd8e69`;
  shared payload SHA-256 was
  `cf16d34b01572d01d0b704c2f15efb5f2cc2b28623b91414307d6d4c408721c4`.
- Evidence remains bounded `rtl-elaboration-topology` plus
  `rtl-simulation-functional`. It does not prove semantic initiator identity,
  resource matching, OoO effects/removal, performance, power, area, FPGA,
  silicon, novelty, non-infringement, patent clearance, or FTO. No EXP or new
  ADR was created.
- `sh -n` passed for the protocol and three CPU runners; both Python verifiers
  passed `py_compile`; focused owned-memory tests passed 6/6. Final
  `scripts/ci-local.sh` exited 0 on macOS arm64 with Python 3.14.6, Apple clang
  21.0.0, `riscv64-elf-gcc` 16.1.0, binutils 2.47.20260726, and QEMU 11.0.3.
  All 204 tests passed with one opt-in real-QEMU integration skipped, followed
  by successful RV64 release/debug/DWARF builds, normal and interrupt QEMU
  smokes, completion telemetry, Sonatine Graph execution, and native/QEMU
  checksum differential. The record checker passed 112 Markdown files, 85
  task definitions, and 52 records; `git diff --check` passed.
- Two independent Low-model read-only reviews found no blocking RTL, assembly,
  verifier, runner, record, claim, or IP-boundary issue. The source-map parser's
  dependence on the pinned GraphML text format remains an intentional
  fail-closed reproducibility boundary; format drift will require explicit
  verifier review rather than silently accepting a changed topology.

## T-0042 standalone TileLink-to-owned-contract bridge

- Continued on dedicated branch `feat/t-0042-tl-owned-contract-bridge` under
  ADR-0043 and ADR-0044. Added a real post-fragmenter TileLink manager that
  translates negotiated `Get`, `PutFull`, and `PutPartial` requests into a
  Raveil-owned request/response shape. TileLink source and size remain in the
  bridge while operation, word address, data, byte mask, explicit adapter-
  supplied initiator, and lifecycle phase cross the owned boundary.
- The first pinned Chipyard assembly stopped with 25 Scala errors because the
  parent harness could not see child-specific ports on anonymous
  `LazyModuleImp` values. Named bridge and raw-client module implementations
  expose the concrete port types; this is retained in
  `docs/FAILURE_KNOWLEDGE.md`.
- Final `./hardware/chisel/run-owned-tl-contract-bridge.sh` execution and an
  exact cache rerun both exited 0. The harness completed six owned requests and
  responses: one full write, two partial writes with masks `0x5`/`0xa`, three
  reads including deterministic range denial, readback, single-outstanding
  request blocking, D backpressure, TileLink source/size routing, and owned
  initiator/phase retention. Accepted/completed accounting was 6/6. The rerun
  printed `chipyard-assembly.jar: OK` before reproducing the same marker.
- Final identities were pinned Chipyard
  `ac58f38d77c99e9d1cafa64dfd6d4b00bdcd43e1`, Docker image ID
  `sha256:8f16d307b4e472302ed4a32e50290dab785026dad4218a2978f4af78e2654a40`,
  input SHA-256
  `5127d50279b1b256e6fb81a6b96969d4b6ef9bc7d05b03758dbb59e3c420cca6`,
  overlay SHA-256
  `34f861d65539b9da5c0b8353b636dd3a8c5348f31eab03032ec6f5a711e87cc8`,
  and assembly key
  `64453a2743baef07e868480c20e74a03d1a8630a8c620cb8a850c8ab02aad679`.
- The marker deliberately reports `attribution=adapter-input-only`,
  `semantic_initiator=not-proven`, `cpu_execution=not-run`,
  `resource_match_verified=0`, `matched_comparison_ready=0`, and
  `performance=not-measured`. This is bounded RTL functional evidence for the
  translation mechanics, not CPU/ELF/loader/debug identity, matched resources,
  end-to-end latency, OoO effects/removal, performance, power, area, FPGA,
  silicon, novelty, non-infringement, patent clearance, or FTO. No EXP or new
  ADR was created.
- `sh -n`, `git diff --check`, and the focused owned-memory suite passed; the
  latter now reports 7/7. Three Low-model read-only reviews found no critical
  handshake, driver, claim, or runner blocker. Their marker-scope and cache-
  identity/checksum findings were incorporated before the final two runs.
- The existing direct manager protocol V2 regression also exited 0 with 26
  transactions, expected-source accepted/completed 7/7, unexpected 0/0, last
  source 3, and last phase 2. Final `scripts/ci-local.sh` exited 0 on macOS
  arm64 with Python 3.14.6, Apple clang 21.0.0, `riscv64-elf-gcc` 16.1.0,
  binutils 2.47.20260726, and QEMU 11.0.3. All 205 tests passed with one opt-in
  integration skipped, followed by successful RV64 release/debug/DWARF builds,
  QEMU normal and interrupt smokes, completion telemetry, Sonatine Graph
  execution, and native/QEMU checksum differential. The record checker passed
  112 Markdown files, 85 task definitions, and 52 records; `git diff --check`
  passed.

## T-0042 source-class negative control and structural-origin survey

- Continued on `research/t-0042-semantic-initiator-boundary` under ADR-0043 and
  ADR-0044. Direct protocol V2 classified its entire legal `[0,4)` client range
  as expected, so its unexpected 0/0 result never exercised the classifier's
  negative path. Protocol V3 narrows the half-open expected range to `[1,3)`.
- `./hardware/chisel/run-owned-tl-protocol.sh` exited 0 with monitor and RTL
  assertions enabled. Its 26-transaction marker reported expected
  accepted/completed 3/3, deliberate boundary sources 0 and 3 as unexpected
  accepted/completed 4/4, last source 3, last phase 2, D backpressure metadata
  stability, and same-source reuse blocking while a response was pending.
  This is harness-range-only negative classifier and A/D conservation evidence;
  no CPU executed and semantic initiator remains unproven.
- The runner now hashes itself, both overlays, the driver, and Dockerfile. Its
  assembly key includes overlay SHA-256, pinned Chipyard revision, and stable
  Docker image ID; cache reuse requires a matching SHA-256 sidecar, while an
  incomplete cache stops closed. The final host identities are recorded by the
  `OWNED-TL-PROTOCOL-HOST-V2` marker: Chipyard
  `ac58f38d77c99e9d1cafa64dfd6d4b00bdcd43e1`, image ID
  `sha256:8f16d307b4e472302ed4a32e50290dab785026dad4218a2978f4af78e2654a40`,
  input SHA-256
  `5002a498fb8af00089e6d1002063d1d7e3448af8a014c88d5297e575387ec305`,
  overlay SHA-256
  `7342413ea88dcff7b021993475e60a6108724becfa5c534078d3ddb5fe4e0174`,
  and assembly key
  `1f9a4c0efa09b58114a96c53579d1c6f54b29ad5ee6b4b9a4c15ddb0d46a2e44`.
  An exact rerun printed `chipyard-assembly.jar: OK` and reproduced the V3
  marker.
- Three Low-model pinned-source reviews found the narrowest common conceptual
  CPU-origin boundary immediately after the DCache and before the shared tile
  master Xbar. BOOM exposes an existing `dCacheTap`; Rocket's
  `HasHellaCache` connects directly and needs a small explicit attach point.
  SimTSI/FESVR is a separate TileLink master, so this position could exclude it
  structurally without relying on final source numbering. However, it would
  prove only DCache origin, not a target ELF instruction, PC, or semantic
  intent. The hook is not implemented or adopted; a durable metadata assignment
  boundary requires a later ADR and functional plus loader/debug negative
  evidence.
- No EXP or new ADR was created. No resource match, OoO effect/removal,
  performance, power, area, FPGA, silicon, novelty, non-infringement,
  patent-clearance, or FTO claim follows.
- Focused owned-memory tests passed 7/7. Final `scripts/ci-local.sh` exited 0
  on macOS arm64 with Python 3.14.6, Apple clang 21.0.0,
  `riscv64-elf-gcc` 16.1.0, binutils 2.47.20260726, and QEMU 11.0.3. All 205
  tests passed with one opt-in integration skipped, followed by successful
  RV64 release/debug/DWARF builds, QEMU normal and interrupt smokes, completion
  telemetry, Sonatine Graph execution, and native/QEMU checksum differential.
  The record checker passed 112 Markdown files, 85 task definitions, and 52
  records; `git diff --check` passed. Three final Low-model read-only diff,
  runner, and records reviews found no critical blocker.
