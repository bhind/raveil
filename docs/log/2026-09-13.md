# 2026-09-13

## S-0003 owner review and conditional acceptance

The owner completed the delayed Saturday Sprint Review from the Mac terminal
and explicitly recorded `Conditional Accept`. The review used a disposable
project at `/private/tmp/raveil-review-20260912.dIMOCu/work`; that path is
ephemeral and is recorded only to identify the observed session.

Review environment: macOS 26.5.1 arm64, Python 3.14.6 and Git 2.50.1. The
`rtl-sim` commands used the repository's offline Docker/Verilator path. The
following command forms were run from the reviewed Raveil checkout; successful
runs returned normally, while the deliberately missing input returned failure:

```sh
python3 -m raveil project run logs --backend native --project /private/tmp/raveil-review-20260912.dIMOCu/work
python3 -m raveil project run gemm --backend native --project /private/tmp/raveil-review-20260912.dIMOCu/work
python3 -m raveil garden --fixture tests/fixtures/garden/minimal.json --width 150
python3 -m raveil project run bias-grid --backend rtl-sim --project /private/tmp/raveil-review-20260912.dIMOCu/work
python3 -m raveil project garden 20260912T093224Z-d65fe310c563 --project /private/tmp/raveil-review-20260912.dIMOCu/work
python3 -m raveil project diff 20260912T093043Z-d67b219f1a1b 20260912T093224Z-d65fe310c563 --project /private/tmp/raveil-review-20260912.dIMOCu/work
python3 -m raveil project run neighborhood-data --backend rtl-sim --project /private/tmp/raveil-review-20260912.dIMOCu/work
python3 -m raveil project diff 20260912T093642Z-1205c4fcbfd7 20260913T012734Z-6cecc7b91ca7 --project /private/tmp/raveil-review-20260912.dIMOCu/work
python3 -m raveil project run logs --backend native --project /private/tmp/raveil-review-failure.0hesAM/work
```

Observed evidence:

- native log-summary runs `20260912T092255Z-28d26139898f` and
  `20260912T092317Z-15801fa7044f` preserved history and showed an edited input
  plus result `2 -> 3`;
- native GEMM run `20260912T092336Z-73bcd2368016` passed its oracle and
  correctly reported an abstained Sonatine outcome;
- Garden displayed materialized and fused plans, including the removal of the
  512-byte intermediate WRITE and READ without changing the semantic Graph;
- actual RTL-simulation bias-grid runs
  `20260912T093043Z-d67b219f1a1b` and
  `20260912T093224Z-d65fe310c563` passed descriptor oracle, C++ fallback and
  RTL byte equality. Changing immediate 5 to 7 changed all 64 active outputs,
  including `(0,0) 16 -> 18`, while simulator and RTL-manifest identities
  remained equal;
- saved-run Garden rendered the selected encoded immediate as `0x60000007`;
- actual RTL-simulation neighborhood runs
  `20260912T093642Z-1205c4fcbfd7` and
  `20260913T012734Z-6cecc7b91ca7` passed all three output checks. Changing
  `ADD_U32` to `MAX_U32` changed all 64 active outputs, including
  `(0,0) 2802362300 -> 1788458059`, while simulator and RTL-manifest identities
  remained equal;
- missing-input run `20260913T012900Z-bf1089295561` failed before publication
  with `path does not exist: /missing.log`, retained empty outputs, and created
  no requested output file.

The owner twice entered `1` after Garden rendered numbered stages. Garden
accepts only `j`, `k`, `g`, `G`, `d`, and `q`, then replaced the useful view
with `No graph state was accepted.` This is durable product feedback, not a
review failure. It is routed to T-0192 / Issue #242, P1, two SP, Host
Functional, as an early S-0004 candidate. The Project review outcome and
Keep/Problem/Try retrospective were recorded and the S-0003 ceremony moved to
Done.

Evidence boundaries: these observations establish Host Functional and RTL
Simulation Functional behavior. They make no QEMU/native speed, FPGA, ASIC,
silicon, cache, isolation, energy, area or general-performance claim.

Remote reconciliation also verified that T-0176, T-0177 and T-0190 were
already integrated by PRs #226, #227 and #228 as merge commits `69b6177`,
`4dd044b` and `68d7253`, that Issues #174, #175 and #208 are closed, and that
their canonical Project items are Done. Their stale unchecked TODO entries
were corrected in the S-0003 closeout change.

## T-0158 scheduled-observation closeout

The Project Manager rechecked the live private Project after S-0003 review.
T-0158 remained in Review even though its reopened evidence conditions had
become observable. GitHub Actions reported seven consecutive successful
`schedule` events for the Private Project burndown workflow: #34035639139,
#34138050210, #34235348804, #34360885596, #34485805909, #34607175815, and
#34695878177 from September 6 through 12.

The live private Draft title reads
`Raveil iteration burndown — S-0003: 3 issues / 11 SP (2026-09-12)` and its
retained data contains five S-0003 observations. Remaining Issues are 3 on
every observed day. Remaining SP is 6 on September 8, then truthfully records
the scope increase to 11 on September 9 and remains 11 through September 12.
This satisfies the required real scheduled event and multiple live observations
without inventing progress. It does not assert decreasing throughput,
productivity, or any hardware result.

The task boundary rechecked the 76 focused tests recorded at PR #150, the
earlier manual hosted correction run #34015052717, the private Draft, and the
actual scheduled run list. T-0158 may therefore close through a
documentation-only receipt PR that closes Issue #135, followed by the canonical
Project completion transition. No Project visibility, secret, paid service,
hosted permission, or workflow implementation changes are part of this
closeout.

## T-0192 Garden navigation implementation

After T-0158 reached canonical Done, the queue had one active research item and
T-0192 was prepared then started through `project_queue.py` as the S-0004 P0
product successor. The HCI-09 reading immediately before start was a 10,080-
minute window observed at 2026-09-13T01:59:31Z with 26% used and 74% remaining.

The bounded implementation accepts exactly the number rendered by each Garden
view. The three-stage fixture uses `1`, `2`, and `3`; dynamic and saved-project
views use their displayed zero-based instruction indices. Direct selection is
advertised only when the entire range fits one decimal digit. Existing
j/k/g/G, supported d, and q inputs remain compatible. An invalid interactive
input preserves and redraws the last valid Graph and selection, appending a
bounded `Invalid navigation` diagnostic instead of the snapshot-load error
`No graph state was accepted.` No execution or evidence authority changes.

Command on macOS arm64 with Python 3.14.6:

```sh
python3 -m unittest tests.test_garden_tui tests.test_project tests.test_project_graph -q
```

Result after adding the saved-project TTY preservation regression: 100 tests
passed in 45.992 seconds. This is Host
Functional evidence only; it is not Graph execution, compiler, RTL,
performance, isolation, device, FPGA, ASIC or silicon evidence.

The full host regression then ran at the same candidate:

```sh
python3 -m unittest discover -s tests -q
```

Result: 845 tests passed in 208.573 seconds with two skips. Existing negative
tests emitted their expected rejection diagnostics; the suite exited zero.

Final hardening added an actual saved-project TTY regression for `1`, invalid
`x`, then `q`; it verifies that bias remains selected and the false snapshot
error is absent. Vreji and Tester independently returned GO for exact candidate
`d2c1186b5acc88a63e8fa3c1d07a6c0b1ecdac46`. PR #245 had an exact matching
remote head and base, CLEAN merge state, no configured hosted checks, and zero
review threads. It merged as `cbba88f8405ff3969893bdb0a5b1f3ac753d135b`
at 2026-09-13T08:02:56Z. Issue #242 closed two seconds later, and the canonical
completion command recorded the acceptance, unsealed delivery cycle, resource
use and Status Done last. T-0192 is complete; this ordinary task merge does not
constitute or require another weekly Sprint Review.
