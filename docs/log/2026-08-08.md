# 2026-08-08 work log

## Goal

Move the accumulated Raveil design and development context from ChatGPT into repository-resident records so a new
Codex session can continue without relying on hidden conversation history.

## macOS/IntelliJ observations

- IntelliJ IDEA with the C/C++ plugin exposed Toolchains and Custom Build Targets.
- The observed Toolchains screen did not expose CMake or Build Tool fields.
- The observed settings tree had no Makefile settings page and no Debug Profiles page.
- A custom target using `/usr/bin/make` and `CROSS_COMPILE=/opt/homebrew/bin/riscv64-elf-` built the kernel.
- `Build -> Clean` remained disabled; `Build Project` and `Rebuild Project` were usable.
- The generated ELF was a 64-bit RISC-V executable.
- Symbol inspection found `_start`, `trap_entry`, and `kmain`; the previously assumed `kernel_main` name was wrong.
- The ELF contained `.symtab`/`.strtab` but no `.debug_info`/`.debug_line` because current flags omit `-g`.
- Command-line GDB/QEMU and IDE-driven remote attach remain to be verified.

## Documentation changes

- Added `docs/handoff/` as the canonical new-session entry point.
- Consolidated project context, decisions, implementation state, development setup, design history, and next work.
- Added a copyable Codex bootstrap prompt.
- Linked the handoff from `AGENTS.md`, root README, and docs index.
- Updated STATUS with the observed native RV64 link state.
- Added T-0006 through T-0008 for debug mode, IntelliJ verification, and macOS smoke evidence.

## Codex workflow configuration

- Added repository-scoped `raveil-explorer`, `raveil-reviewer`, and
  `raveil-verifier` custom-agent definitions.
- Added instruction-only `raveil-task-governance` and
  `raveil-gate0-evidence` skills under `.agents/skills/`.
- Kept `.codex/config.toml` machine-local because it contains the local IDEA
  endpoint; only `.codex/agents/*.toml` is tracked.
- The configuration changes development workflow only. It does not add a
  product architecture decision or any performance evidence.

## Caution

The handoff source directory was an extracted artifact without `.git`. These changes must be applied to the actual
Git working tree, reviewed with `git diff`, and committed there. No claim is made that the QEMU smoke test or
source-level debugger has passed.

## Integration into the real Git tree

Later on 2026-08-08, the handoff overlay and reconstructed conversation digest
were applied to the actual `raveil-clean` Git tree. Missing canonical records,
ADRs, experiment records, templates, open questions, and the proposed native
graph-machine RFC were added so the overlay's links and claims are internally
complete.

Verification on the same macOS host:

- all eight Python/host-C acceptance tests passed;
- the ToyDaphnis benchmark reproduced the documented analytical result;
- the existing RV64 ELF passed native QEMU `make smoke` with exit status 0;
- `.debug_info` and `.debug_line` remain absent;
- clean rebuild, Docker reproduction, and GDB attach remain open.

The detailed environment and smoke transcript are recorded in `EXP-0002`.

## Documentation and progress-governance reorganization

- Removed `docs/handoff/` as a daily entry point because it duplicated STATUS,
  TODO, ROADMAP, architecture, and ADR records.
- Made `docs/README.md` the single document router and isolated provenance
  under `docs/archive/`.
- Promoted reusable macOS/QEMU/GDB instructions to
  `docs/guides/SONATINE_MACOS_DEBUGGING.md` and design evolution to
  `docs/history/DESIGN-EVOLUTION.md`.
- Preserved the reconstructed 2026-08-07–08 conversation digest unchanged in
  `docs/archive/conversations/`.
- Extended `raveil-task-governance` with kickoff, progress/issue audit, and
  closeout phases plus a deterministic record checker.
- Kept progress authority with the primary agent. Existing read-only reviewer,
  explorer, and verifier roles remain advisory; no always-running agent or
  automatic external issue update was introduced.

This was a documentation and project-operation change. It did not change the
implemented Sonatine Microkernel or Experience behavior, so STATUS required no
implementation update.

The governance forward-test audited Gate 0 without editing files. It confirmed
that EXP-0002 satisfies T-0008's recorded native build/smoke evidence, so
T-0008 was marked complete. It also found that the roadmap's command-line GDB
exit condition had no actionable task; T-0061 now tracks that missing work.
Gate 0 remains In progress.

## Native debug build and command-line GDB

- Added a separate `DEBUG=1` build directory so release `-Os` objects cannot
  be reused accidentally in `-Og -g3` builds.
- Added `make check-debug`, `make debug`, and `make gdb` entry points.
- Performed a clean native release build and confirmed it has no DWARF debug
  sections.
- Built the debug ELF and confirmed `.debug_info` and `.debug_line`.
- Re-ran release QEMU smoke successfully after the debug build.
- Connected GDB 17.2 to QEMU 11.0.3, set `break kmain`, continued, and stopped
  at `src/kernel.c:25` with source lines visible.
- Recorded the ignored GDB raw-log path and SHA-256 in EXP-0002.

T-0006 and T-0061 are complete. Gate 0 remains In progress because Docker,
clean-clone/CI, public-repository hygiene, and IDE-driven attachment are not yet
verified.

## Component formal names

- Established the Glossary as the source of formal component names and their
  short forms.
- Updated active records to use the formal name in headings, tables, diagrams,
  and first use within a document or independent section.
- Preserved code identifiers, terminal output, stable record IDs, and archived
  conversation text. This is a documentation naming convention only; it does
  not change an implementation fact, architecture boundary, or evidence class.

## Docker, CI, and IntelliJ Gate 0 work

- Added a Gate 0 GitHub Actions workflow covering nine host tests, RV64
  release/debug builds, DWARF inspection, QEMU smoke, and smoke-log upload.
- Rejected the first Docker build as evidence because host `build/` artifacts
  entered the context and prevented a real container compile.
- Added `sonatine/.dockerignore` and defensive `make clean && make` in the
  image build.
- Rebuilt without cache, observed every C/assembly compile and link, and passed
  container QEMU smoke with exit status 0.
- Inspected the installed IntelliJ/Native Debug plugins and identified
  `Remote Debug` (`CLion_Remote`) as the correct QEMU-stub configuration.
  No IDE attachment result is claimed.

T-0001 and T-0007 are complete. T-0003 remains open until the workflow runs on
GitHub; T-0002, T-0004, and the final public-tree hygiene check also remain.

## Local-only CI/CD policy correction

- One GitHub Actions run (`31248762110`) had already completed before the
  project owner clarified that hosted CI/CD must not be used because of cost.
- Removed the hosted workflow so future pushes and pull requests cannot start
  it.
- Replaced it with `scripts/ci-local.sh`, which runs the nine tests, clean RV64
  release build, isolated debug build and DWARF checks, and QEMU smoke locally.
- Preserved the existing public version tag and Release as historical artifacts;
  no new tag or Release is created under the local-only policy.
- Deferred reconsideration of GitHub CI/CD to T-0063, requiring multiple
  contributors and explicit owner approval.

## Gate 1 measurement-contract implementation

- Created branch `research/exp-0003-gate1-measurement` and adopted
  `<type>/<record-id>-<short-slug>` lowercase branch names for post-Gate-0
  tracked work.
- Added RFC-0002, ADR-0009, and the in-progress EXP-0003 plan without making a
  latency or energy claim.
- Added versioned benchmark/environment/measurement/policy contracts, a
  baseline-first randomized schedule, a 24-holdout manifest, and a fixed native
  C checksum harness for GEMM, GEMM+bias+ReLU, and two-stage MLP.
- Added fail-closed powermetrics parsing, Gate statistics, and the local
  run/analyze/seal/sync lifecycle. Immutable rclone copy, download verification,
  overwrite refusal, and completion-marker-last behavior were exercised with a
  fake local command boundary; no real Google Drive sync is claimed.
- Replaced three generic Codex roles with eight research workflow roles and
  added a ninth read-only Librarian. The `raveil-context-librarian` skill emits
  a small prioritized reading packet; its official skill validator passed from
  a temporary PyYAML 6.0.3 environment. PyYAML was not added to Raveil.
- `python3 -m unittest discover -s tests -v` passed all 26 host tests. Native C
  results in this suite establish checksum/harness behavior only. No
  powermetrics dataset, completed bundle, bounded-policy outcome, independent
  repeat, or TVM comparison exists.
- `scripts/ci-local.sh` then passed with exit status 0 on base Git revision
  `f413c558f1e67fc004f6b087e74330adb7610387` plus this uncommitted branch diff:
  26 host tests, clean RV64 release/debug builds, DWARF checks, and QEMU smoke.
  Versions were Python 3.14.6, Apple Clang 21.0.0, riscv64-elf-gcc 16.1.0, and
  QEMU 11.0.3. The ignored emulation log
  `sonatine/build/smoke.log` had SHA-256
  `10e4a6001f534498fb35525d723ea19905d80cdc9339eccfc55d8a94753fd551`.
