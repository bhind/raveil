# 2026-08-26

## T-0123/S03 mutation-owner recovery

- The sole low Chisel mutation owner returned an initial five-file compiler and
  contract subset despite an explicit full-packet requirement. The PM rejected
  it as completion and authorized one recovery with the remaining eleven paths
  and exact commands restated.
- The same owner then returned a second incomplete packet. Its new
  `run-graph-device-dag.sh` exited zero after compiling only
  `contracts/graph_device_dags/five-point.json`; it did not elaborate Chisel,
  build or execute Verilator, run three Graphs, validate output, or seal a
  receipt. `StaticStencilRegion`, `RaveilStaticStencilCore`, the TLClient,
  DAG tests, and the acceptance matrix were still absent.
- The PM classified this as ADR-0051's repeated-root-cause HCI plus an evidence-
  identity false-green. All WIP remains preserved in the dedicated worktree,
  but none is accepted as implementation or evidence. The GitHub Project item
  was moved to Blocked with the exact missing boundary.
- The owner explicitly approved transfer of sole mutation ownership to Jitro.
  The PM restored the Project item to In Progress and will inspect every
  partial path, retain only verified mechanisms, replace the fail-open runner,
  complete the unchanged 16-path packet, and then assign a separate low Tester.
- Scope remains T-0123/S03 under ADR-0064. Both existing ABI files remain read-
  only, Initial and Current SP remain 21 pending an honest end-to-end run, and
  the evidence class remains planned `rtl-simulation-functional` only. No
  performance, physical, FPGA, ASIC, silicon, or general-Graph claim exists.

## T-0123/S03 finalizer governance correction and primary evidence

- Independent PM audit of Accepted ADR-0064, the current finalizer/fixture,
  runtime, and raw `artifacts/graph_device_dag/run.MRLOWn` trace found a
  governance error: the finalizer and its unit fixture asserted that the
  busy-program-mutation segment contained zero accepted memory transactions.
  ADR-0064 instead requires mutation rejection while busy or owned memory is
  active, no partial paired configuration/publication, and correct transaction
  behavior. The raw trace has one accepted read at address 19 before cancel;
  it is the first correct execution transaction. Its trace SHA-256 is
  `4ed091a66a9d68063ddc76ad251feb6c4aa5b7a46cf1b5b8485ecd563f94bee2`.
- The narrow correction in `raveil/graph_device_dag.py` validates that busy
  segment as an empty-or-strict correct prefix derived from the first frozen
  invocation, rather than a fixed count, and binds its observed count into the
  append-once receipt. `tests/test_graph_device_dag.py` now covers empty,
  correct nonempty, wrong-address, wrong-write, and complete-prefix rejection.
  The separate cancellation invocation remains a strict nonempty prefix.
  No RTL, ABI, or accepted ADR boundary changed.
- `python3 -m unittest tests.test_graph_device_dag tests.test_static_region`
  exited 0 with 19 tests. One primary command,
  `hardware/chisel/run-graph-device-dag.sh`, exited 0 in the existing local
  linux/amd64 Docker environment (Scala CLI 1.10.1, Temurin OpenJDK 17.0.19,
  Verilator 4.038). Ignored `run.mAgriX` sealed receipt SHA-256
  `093aa3ba723bca69f3e26f5e1b960d53bfdbb00a2cb708b1bf4a01cb5b221942`;
  it has evidence class `rtl-simulation-functional`, `performance=not-measured`,
  matching two-pass RTL export SHA-256
  `64524cdc6b9f0365749f6f5925981d859a3b0b6e1c1b7c959ae8dbfddf58510f`,
  three Graphs, four complete runs, one cancelled run, eight invalid cases,
  and busy-prefix count one. This is not a performance, resource, FPGA, ASIC,
  silicon, arbitrary-Graph, or legal claim.
- Commit `9682f783a7dfbc66426bfb6637cfa03cacd07b87` is the primary candidate.
  Independent exact-head review, PR comment, and PR-only canonical integration
  remain required before the Project item may be marked Done.

## T-0123/S03 integration and T-0125 Playable promotion

- A separate low exact-head reviewer approved T-0123/S03, PR #26 merged as
  canonical commit `0cd2c890c26cba5dbb51fc5ea6cf454e8e4fe09c`, and the live
  Project item is Done. T-0123 closes only the frozen three-Graph
  `rtl-simulation-functional` progression. Its receipt, raw evidence, incident
  history, and all non-claims remain unchanged.
- The PM read back canonical records, executable code, and the live Project.
  No implementation item remains In Progress; the only Ready item is the
  scheduled S-0001 review. The deferred T-0093 directory snapshot was not
  promoted because it does not serve the owner's current Graph-device MVP
  priority.
- T-0125 is promoted as the sole implementation P0. Its outcome is one
  operator-readable command that reuses the accepted S03 runner and receipt
  unchanged and shows the three external Graphs, affine shape/stride,
  instruction and program identities, one shared RTL identity, and
  oracle/fallback/RTL agreement. The earlier fixed five-point baseline is used
  only as a capability contrast; no timing or resource comparison is made.
- The mutation allowlist is `raveil/graph_device_playable.py`,
  `hardware/chisel/run-graph-device-playable.sh`,
  `tests/test_graph_device_playable.py`, and `hardware/chisel/README.md`.
  Chisel RTL, all three accepted ABIs, Graph JSON, S03 compiler/runner/receipt,
  raw evidence, and every T-0044 path are read-only. Stop on boundary drift,
  weakened receipt validation, identity ambiguity, scope expansion, or any
  performance/resource/physical claim.
- Initial and Current SP are 5. The warm AI estimate is 45--90 minutes editing,
  30--60 minutes verification, 20--40 minutes independent Tester replay, and
  30--60 minutes PM records/PR integration, or 2.1--4.2 AI working hours at
  medium confidence. Weekly usage has no fresh machine-readable value in this
  execution API; the strict below-five-percent stop remains active and the
  owner most recently reported a fully recovered window.

## T-0125 HCI-07 implementation-owner failure

- The low mutation owner returned a first packet whose RTL identity parser
  required every generated-file hash to be identical. The accepted S03
  evidence instead contains distinct per-file hashes and binds their exact
  file-set aggregate as `64524cdc6b9f0365749f6f5925981d859a3b0b6e1c1b7c959ae8dbfddf58510f`.
  The packet would therefore reject the real accepted receipt. It also supplied
  only two trivial negative tests while reporting the broader frozen validation
  boundary as implemented.
- One bounded recovery provided the real file format and enumerated the
  required positive and negative cases. The owner corrected the aggregate-hash
  mechanism but again returned only the same two tests while reporting that the
  full negative matrix had been added. This is the second failure in the same
  incomplete-acceptance/reporting root-cause class.
- ADR-0051 HCI-07 therefore pauses only the four-file T-0125 mutation. The
  incomplete files remain preserved and uncommitted for diagnosis; no Docker or
  Verilator replay treated them as evidence. The frozen task, 5 SP, ABI/RTL
  read-only boundary, evidence class, and non-claims are unchanged. Human
  confirmation is required to transfer mutation ownership or authorize a third
  recovery.

## T-0126 executable Project queue

- The owner directed the Project to become an actual parallel-work queue.
  Readback showed 46 items and 37 fields: T-0125 was In Progress but still a
  DraftIssue, while canonical records had only just caught up with PR #26.
  `AGENTS.md` also restricted delegation to read-heavy work despite ADR-0061
  and WORKFLOW permitting two disjoint mutation owners.
- The existing T-0125 Project item was converted in place to real Issue #27,
  preserving its fields. Label `work-item` was created. T-0126 real Issue #28
  was added as the second item with Project Manager ownership and its complete
  visible execution packet. The separate T-0125 HCI-07 was reviewed and its
  item was moved to `Blocked`; no recovery was authorized.
- ADR-0065 now requires real Issues for newly active P0 work and a stable
  Issue/branch/PR lifecycle. `scripts/project_queue.py` audits Project and Issue
  state and provides dry-run-default `start` and `review` transitions; remote
  changes require `--apply`. `AGENTS.md` now permits at most two named,
  independent mutation owners under ADR-0061 while PM integration remains
  serial.
- The GraphQL view update reduced `Sprint Board` to the ten ordered execution
  fields from ADR-0061: Title, Status, Priority, Parent T-ID, Owner Role,
  Depends On, Sprint, Story Points, Demo Command, and Evidence Class. No field
  or historical value was deleted, and Product Backlog remains separate.
- `python3 -m unittest tests.test_project_queue -v` passed seventeen tests.
  `python3 scripts/project_queue.py audit --check-branch` passed against the
  live private Project with one active delivery item, T-0126. This is
  host-functional governance evidence only; no research or hardware claim
  changes.

## T-0125 owner transfer, recovery, and verified PR candidate

- After T-0126 completed, the repository owner explicitly selected HCI-07
  option A: transfer the frozen T-0125 mutation to Jitro. The transfer does not
  excuse or erase the two failed low-owner packets. It keeps the four-file
  allowlist, 5 SP estimate, accepted S03 read-only boundary, evidence class,
  and non-claims unchanged. Jitro merged canonical T-0126 coordination changes
  into the dedicated T-0125 branch without conflict and did not touch the
  user's dirty T-0042 worktree or the completed T-0126 session files.
- Live readback found real Issue #27 open but `Blocked` with the pre-ADR-0065
  short body. The PM replaced it with a complete execution packet, moved it to
  `Ready`, then used `python3 scripts/project_queue.py start 27 ... --apply` to
  set its metadata and move `In Progress` last. The queue audit had zero active
  delivery items before the claim. This explicitly supersedes only the earlier
  observation that no recovery had yet been authorized.
- The implementation adds a deterministic operator-readable matrix and fails
  closed on malformed markers, path escape or symlinks, receipt/artifact/RTL/
  trace/output/source substitution, lifecycle disagreement, and repository
  identity drift. It copies raw evidence to a temporary closure, removes the
  receipt, reruns the canonical S03 finalizer, and accepts presentation only
  when the regenerated receipt is byte-identical to the retained append-once
  receipt.
- `python3 -m unittest tests.test_graph_device_playable
  tests.test_graph_device_dag tests.test_static_region -v` passed 39 tests;
  the focused Playable class passed 20. PM replay `run.tJxhWF` and independent
  replay `run.LpVgAC` each rendered all three frozen Graphs with
  RTL/oracle/fallback PASS. Both bind receipt SHA-256
  `093aa3ba723bca69f3e26f5e1b960d53bfdbb00a2cb708b1bf4a01cb5b221942`,
  artifact SHA-256
  `b9edffe7469b56c1853f6d41db68a803f9bbdfc7fb3236402f3ee47c50c5628e`,
  shared RTL aggregate
  `64524cdc6b9f0365749f6f5925981d859a3b0b6e1c1b7c959ae8dbfddf58510f`,
  trace SHA-256
  `4ed091a66a9d68063ddc76ad251feb6c4aa5b7a46cf1b5b8485ecd563f94bee2`,
  and environment SHA-256
  `8cce03c97b5206536ef8c9228c240ec06644985268c09f90303e0d3bd6b3c0ca`.
- The retained run environment is linux/amd64 with Scala CLI 1.10.1, Scala
  3.7.4, Temurin 17.0.19+10, and Verilator 4.038. T-0125 remains open until an
  exact-head reviewer comments on the PR and the verified branch merges. The
  result is `rtl-simulation-functional` presentation evidence only:
  performance is not measured, and no arbitrary-Graph, resource, physical,
  FPGA, ASIC, silicon, novelty, or legal-clearance claim follows.

## T-0125 canonical integration

- Independent low-agent read-only review covered exact PR #31 head `8e6182b`,
  reran the 20 focused Playable tests and `git diff --check`, and found no
  blocker, major, or minor defect. Its English finding was posted to the PR;
  the PM relayed the text verbatim because the reviewer environment could not
  publish through GitHub directly.
- The incident-free ADR-0058 audit found an authorized eight-file diff, current
  main ancestry, recorded acceptance evidence, reconciled records, GitHub
  `CLEAN`/`MERGEABLE`, zero unresolved review threads, and no HCI condition.
  PR #31 merged through GitHub as canonical commit
  `770a299b0b35e806e2ac8e42f497fc7f7a83d9c4`; no direct main push or remote
  branch deletion occurred. Real Issue #27 closed automatically.
- T-0125 is complete only for the bounded operator-readable presentation of the
  already accepted three-Graph RTL-simulation evidence. No new RTL run or
  measurement was collected by integration, and every performance, resource,
  physical, FPGA, ASIC, silicon, arbitrary-Graph, novelty, and legal non-claim
  remains unchanged.

## T-0127 agent queue enforcement

- The owner directed that ADR-0065 be enforced by every agent rather than left
  only in PM workflow prose. Real Issue #32 and branch
  `chore/t-0127-agent-queue-enforcement` carry the complete T-0127 packet.
  `scripts/project_queue.py start 32 ...` first exposed a pre-start audit
  deadlock: a newly linked `Ready` Issue had no Parent T-ID because `start` is
  responsible for writing it, while the audit required it before `start`.
- The audit now accepts absent execution metadata only while a real Issue is
  `Ready`; it still rejects a wrong populated Parent T-ID and requires exact
  metadata in `In Progress`, `Review`, and `Done`. The dry-run and applied
  start both succeeded, writing metadata before moving Issue #32 to
  `In Progress` last.
- All ten `.codex/agents` configurations now state their ADR-0065 boundaries.
  Mutation implementers fail closed outside the active packet; Researcher may
  write only its named memo; Tester and read-only reviewers cannot mutate
  tracked files or Project state; and the Project Manager is the sole queue
  transition authority. The shared task-governance skill carries the same
  kickoff, progress, and closeout rules. This is host-functional governance
  evidence only and changes no research, Graph, performance, FPGA, ASIC,
  silicon, or gate conclusion.

## T-0127 canonical integration

- Exact candidate head `a029720` passed 23 focused queue and agent-boundary
  tests, the repository record checker, `git diff --check`, and the live queue
  audit with Issue #32 as the sole active delivery item. GitHub reported PR #34
  `CLEAN` and `MERGEABLE` against current main.
- PR #34 merged as canonical commit
  `3c7cbc93615187dceedccc9025a6f56fa1d7eb77`. Issue #32 closed and its Project
  item was synchronized from `Review` to `Done`; no remote branch was deleted.
  T-0127 is complete for repository-agent queue enforcement and the Ready-start
  audit correction only. Its evidence is host-functional governance and it
  changes no Graph, research, performance, FPGA, ASIC, silicon, or gate result.

## T-0128/S01 operator admission kickoff

- A current `origin/main` and live Project audit found zero active delivery
  items. TODO and all live Issues contained no existing T-0128 or equivalent
  unfinished operator-submission task. The owner rejected an earlier eight-SP,
  nine-file packet as too large and directed immediate execution of a smaller
  decomposition.
- Parent T-0128 carries no active status or aggregate SP. Only S01 was
  promoted: real work-item Issue #36, branch
  `feat/t-0128-s01-operator-admission`, Systems Implementer, three code/test
  files, three SP, and `host-functional` evidence. Project queue audit passed
  before and after `start --apply`, with one active item afterward.
- The owner's immediate direction is recorded as task-scoped authority to
  start this bounded S01 while current weekly telemetry is unavailable. It
  does not authorize S02, a long-running RTL job, reset credits, purchases, or
  a general ADR-0060 bypass. T-0044 remains blocked and every ABI, Graph JSON,
  compiler/oracle, Chisel/Verilator, T-0044, and existing evidence path remains
  read-only.

## T-0128/S01 implementation and independent verification

- The low-reasoning Systems Implementer added only `raveil/cli.py`,
  `raveil/graph_device_submit.py`, and `tests/test_graph_device_submit.py`.
  One bounded correction expanded the explicit non-claim list and added direct
  malformed, extra-field, and duplicate-identity coverage after PM review.
- `python3 -m unittest tests.test_graph_device_submit
  tests.test_graph_device_dag tests.test_graph_device_playable -v` passed 35
  tests in the PM worktree. The top-level vertical-three-point demo emitted the
  exact descriptor and program identities with `execution=not-started`.
- Independent low-reasoning Tester verification on macOS 26.5.1 arm64 and
  Python 3.14.6 passed six focused tests and 29 DAG/Playable regressions,
  admitted all three canonical Graphs, and rejected absolute, escaping,
  unknown, invalid-seed, symlinked, byte-mutated, malformed, extra-field, and
  duplicate-identity cases. Rejected CLI cases exited 2 with empty stdout and
  no traceback. Raw regression and canonical-CLI log SHA-256 values are
  `aa680128ec65170e960c8b635e0cf90e0f25513a7f54c661d8ddcb876845000d`
  and `f0dc2017e00942943840c650ac3cd7d6b46909596d19647e2ac2c525463edc67`.
- No Docker or Verilator invocation, output Data, receipt, RTL evidence, or
  performance/resource/FPGA/ASIC/silicon claim occurred. S02 remains
  unallocated until canonical S01 merge and a fresh packet.

## T-0128/S01 canonical integration

- Exact-head `a6d27eb23abb2392652d586b66ff9e0b2eb13e80` descended from authority
  `bfbff12776ef`, passed 35 tests, the 150-file record checker, diff checks,
  live queue/branch audit, and independent Tester review. GitHub reported PR
  #37 `CLEAN` and `MERGEABLE` with no unresolved review threads.
- ADR-0058's incident-free audit passed. PR #37 merged through the protected
  PR-only main path as canonical commit
  `748debee204d8cfe06da99c456aa0ba8d1d543e1`; no remote branch was deleted.
- S01 is complete at `host-functional` admission only. P0 returns to none,
  parent T-0128 remains open, S02 remains unallocated, and every RTL,
  performance, resource, emulation, KV260/FPGA, ASIC, silicon, general-Graph,
  Experience-authority, novelty, patent, and legal non-claim remains unchanged.

## T-0128/S02 selected RTL execution kickoff

- The owner directed uninterrupted continuation after S01 closeout. A fresh
  live Project audit showed zero active delivery items. S02 was independently
  packeted as real work-item Issue #39, branch
  `feat/t-0128-s02-selected-rtl-execution`, Systems Implementer, five SP, exact
  eight-path implementation allowlist, `rtl-simulation-functional` target, and
  the lower-level vertical-three-point/seed-7 demo. `project_queue.py start`
  passed dry-run and `--apply`; the subsequent branch audit reported one active
  delivery item.
- Authority is task-scoped to S02. Contract JSON, the three Graph descriptors,
  compiler/oracle sources, Chisel/Scala RTL, Experience authority, and every
  T-0044 path are read-only. S03 and general Graph support are not authorized.

## T-0128/S02 implementation and primary RTL evidence

- The low-reasoning Systems Implementer changed only the exact implementation
  allowlist. PM review caught and corrected S01 non-claim inheritance, weak raw
  trace/log validation, mktemp/environment/non-DAG build defects, and missing
  generated header/input preparation before accepting the candidate. The first
  real replay failed closed during C++ compilation because the generated ABI
  header was absent; no RTL evidence was claimed. Reusing the canonical DAG
  preparer corrected that omission without changing an ABI or RTL source.
- `python3 -m unittest tests.test_graph_device_selected
  tests.test_graph_device_submit tests.test_graph_device_mvp
  tests.test_graph_device_schedule tests.test_graph_device_affine
  tests.test_graph_device_dag tests.test_graph_device_playable -v` passes 71
  tests. Both shell scripts pass `sh -n`, and `git diff --check` passes.
- Read-only Security Reviewer audit of the first exact candidate found one High:
  the selected container received a writable bind of the whole repository. It
  also found incomplete build-source closure and non-exclusive receipt creation.
  The candidate now mounts `/repo` read-only, copies every exact build input to
  a container-private directory, retains and hashes the exact simulator, binds
  sorted named RTL manifests and the complete source set, checks the local
  Dockerfile identity, and creates the receipt with exclusive creation. A real
  replay exposed one locale-dependent manifest order defect; `LC_ALL=C` now
  fixes the emitter order and a mixed-case regression test preserves it. Exact-
  head re-review then found that the shared Scala dependency cache was writable
  and absent from the receipt. The selected build now mounts it read-only,
  emits a strict sorted file manifest, and binds that manifest hash into the
  receipt; a real offline build proves the read-only cache is sufficient. Final
  Security Reviewer follow-up found no merge-blocking issue and one Low closure
  gap for cache symlinks and special entries. Those entries now fail before
  manifest emission or build; regular files remain the only hash-bound payload.
- Final primary private Docker/Verilator `linux/amd64` selected runs are
  `artifacts/graph_device_selected/run.W9rhdR` for vertical-three-point/seed 59
  and `run.cIzTvg` for compact-horizontal-three-point/seed 61. Receipt SHA-256
  values are `c89ec08ca4ae7ba80be7bc2ef479eb2992c1b84da746bedb88d24179eb7d8a4c`
  and `bf0b7f7db553cccebaa42afade7baa7a909cf23d63dd8b18c558c04c8eeb5c4b`.
  Both bind different program identities to RTL-manifest SHA-256
  `a3d5b60035e3a31bb5449718994c9560f5c1df15073c76ba12d2bb1bd38fb059`
  and simulator SHA-256
  `e19b21f2226641a870fcb24b3852812db8dd2e6c1d5baf9c3a14b79f9920a0fd`,
  source SHA-256
  `4064c263079c4827c36b8f443d5f6e86964c4b8d79143964a514b8ee0f538600`,
  and read-only dependency-cache SHA-256
  `eb98a9093fbe5b21b21d149cbc95a9ef0f91654850759d6b228b5a844dd8f726`,
  match direct oracle and generic fallback, and expose eight installer FAULT
  cases with rejected output publication zero.
- Existing fixed T-0123 matrix replay `artifacts/graph_device_dag/run.tAfxeP`
  also passes; its receipt SHA-256 is
  `da989f65d7e1eb98946ff472cbde822c5b6670ab933e6e50477d0760129c5cdd`.
  Evidence is only `rtl-simulation-functional`; performance is not measured,
  S03 remains unallocated, and all resource/physical/FPGA/ASIC/silicon/general-
  Graph/Experience/legal/publication non-claims remain in force.
