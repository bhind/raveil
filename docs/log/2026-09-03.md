# 2026-09-03

## T-0141 single dynamic Graph kickoff

- The owner authorized four sequential P0 phases and directed the Project
  Manager to continue between accepted integrations without acknowledgement
  pauses. Phase 1 is the smallest continuation of canonical T-0140.
- `origin/main` is exactly
  `299f8680675e5baf5875d52a4e5044950014eb47`, which contains merged PR #96.
  The root worktree is an unrelated dirty T-0042 worktree and remains
  untouched. The clean task worktree is `/private/tmp/raveil-t-0141` on
  `feat/t-0141-single-dynamic-graph` from that canonical revision.
- TODO, accepted decisions, all work-item Issues and Project #1 contain no
  prior T-0141 or ADR-0078. PM allocated T-0141 and ADR-0078 monotonically.
  The task is Gate 5 bounded simulation work; no RFC or EXP applies and no gate
  transition or performance measurement is authorized.
- Real Issue #97 and its Project item fix Systems Implementer ownership,
  Tester/Security/Performance/PM support, eight SP, Sprint S-0003, dependency
  T-0140, exact mutation allowlist, artifacts, acceptance, evidence class,
  stop rule and non-claims. Project readback reports P0 and In Progress. The
  live queue audit passes with one active delivery item.
- ADR-0078 adds one non-catalogue `dynamic-run` command while preserving the
  existing pair command. The existing two shell runners may accept exactly one
  or two requests; RTL, C++ executor, device ABIs, opcode/selector alphabet,
  affine profiles and capacities remain unchanged. A sealed UIO-adjacent
  request is deferred to the separately reviewed next phase.
- Initial read-only Security review requires malformed input to remain before
  Verilated-model construction and AXI transcript creation, and identifies
  descriptor/request filesystem races as explicit Phase 2 work. Initial
  Performance review requires real one-emit/build/invocation evidence and
  preserves `polls=` as a termination diagnostic rather than a cycle or timing
  measurement. These reviews make no implementation or functional claim.

## T-0141 bounded implementation and verification

- The Systems Implementer added one `dynamic-run` CLI, the
  `fanout-five-live` descriptor and focused tests, and generalized only the two
  existing dynamic shell runners from exactly two requests to exactly one or
  two. No Chisel, C++, ABI, opcode, selector, affine-profile or capacity file
  changed. The old pair admission remains catalogue then non-catalogue.
- The descriptor compiles to 11 instructions: five `LOAD_U32`, five `ADD_U32`
  and one `STORE_U32`. Its maximum live-value count is five, `a0` is consumed
  by both `a2` and later `a4`, and the program SHA-256 is
  `ec13f9f0d376233b49b2d647088f71bf208ddea68e7a4d09732f660b9770ea39`.
- The exact single command passes in the reviewed offline container. Retained
  session `artifacts/graph_device_axi4lite_dynamic/run.izlk2jgs` reports
  `requests=1`, `rtl_emitted_once=1`, `simulator_built_once=1`,
  `invoked_once=1` and `rejected_before_axi=1`. Simulator SHA-256 is
  `b20eee47e38d8a8603e3e11d8fbb0ee71113099638c0cdd42ce7f9f252fd6ba4`.
  The append-once receipt SHA-256 is
  `d0f0e1864abf8b6d18814c0c570ff2fc20a171c9161978864237ee9977ab2030`;
  its RTL output, descriptor oracle and C++ fallback are byte-equal at
  `6f77bb673b34cf0b1370ab8da18d377c121f0bb55bf2e934d17b2691978062ff`.
  The malformed internal request reports an invalid header and creates no AXI
  transcript.
- The existing exact pair command also passes after the runner generalization
  with `requests=2`, `invoked_twice=1`, one RTL emission/build and the same
  simulator identity. This is backward-compatible regression evidence, not a
  performance comparison.
- Independent Tester verification ran on macOS 26.5.1 / Darwin 25.5.0 arm64,
  Python 3.14.6, Git 2.50.1 and Docker 29.6.2 using immutable image ID
  `sha256:2efc059cf07eb054d93fc1fa32decd7a13c2cdb97069dac29138275b22e5c57c`.
  Seven focused tests, 131 Graph-device tests, CLI help and negative paths,
  both runner syntax checks and `git diff --check` exit zero. Evidence is RTL
  Simulation Functional only. `polls=` is only a bounded termination
  diagnostic; performance was not measured, and no UIO, KV260, FPGA, area,
  energy, ASIC, silicon or general-Graph claim follows.

## T-0141 integration and T-0142 sealed-request kickoff

- Security and Performance Reviewers approved exact T-0141 head
  `038c45c9037175e141b1c792aacc5ca56de3c4b4`. PR #98 squash-merged it as
  canonical main `bb3c5f6ed815f8177217b94f6f62c8346bd74f7f`; Issue #97 is
  Closed, its Project item is Done and the closeout queue audit reports zero
  active delivery items. Sixteen focused tests pass from the post-merge
  checkout. Project README records the functional-only result.
- Owner-authorized Phase 2 has no T-ID or ADR collision. PM allocated T-0142
  and ADR-0079, created real Issue #99 and clean branch
  `feat/t-0142-sealed-dynamic-request` from canonical main, and promoted its
  Project item to the sole P0 In Progress item in Sprint S-0003. Project
  readback fixes Systems Implementer ownership, Tester/Security/Performance/PM
  support, 13 SP, dependency T-0141, exact allowlist, artifacts, acceptance,
  evidence classes, stop rule and non-claims. The live queue audit passes.
- ADR-0079 requires one-read descriptor compilation, an exact sealed inventory
  with exclusive creation and marker-last completion, fd-relative no-follow
  verification into retained bytes, and replay through a separate private root
  without descriptor reinterpretation. Its UIO dry-run is a pure relative-plan
  conversion and must stop before `open`, `mmap`, MMIO or a device transcript.
- Read-only Phase 2 security design identified pathname TOCTOU, execution from
  a sealed root and pre-existing destination reuse as the principal risks.
  Host sealing/verification and RTL replay evidence remain distinct. No target
  host, UIO, FPGA, performance, production-security or general-Graph claim is
  authorized.

## T-0142 sealed-request implementation and verification

- The implementation reads the descriptor and compiler/source set through
  component-wise no-follow directory descriptors, compiles the retained
  descriptor bytes once and exclusively writes a digest-named closed bundle.
  Every payload has an exact byte count and SHA-256; the manifest digest names
  the directory and `SEALED` is fsynced last. Verification rejects mutation,
  missing/extra entries, symlinks, hard links, path escape, external copies,
  source drift and observed same-name replacement.
- Final-source `dynamic-seal` exited zero with seal
  `0beaba3fe6638516aca532104d05cf412d8b3dae0658d93397c8d45ce67b24c8`,
  compiler/source identity `34911cfe...2f290`, request identity
  `e1d8b8c5...ef5af` and required program identity `ec13f9f0...ea39`.
  `dynamic-uio-dry-run` exited zero with plan `81f5d0d5...caec2`, the checked
  16 KiB relative aperture and seven ABI-derived payload/control/output
  operations, `device_opened=0`, `mmap=0`, `mmio=0` and
  `performance=not-measured`. This is Host Functional evidence only.
- The first real Docker replay completed the RTL run and produced equal
  oracle/fallback/output bytes, but the host receipt collector rejected the
  289,072-byte simulator and 380,922-byte AXI transcript under its initial
  64 KiB generic evidence cap. That operational failure was not promoted. The
  collector now uses separate explicit 128 MiB simulator and 16 MiB trace
  bounds, retains no-follow reads and has an over-64-KiB regression.
- The corrected fresh replay exited zero. It emitted RTL once, built simulator
  `b20eee47...ba4` once and invoked it once; malformed request admission was
  rejected before an AXI transcript. Descriptor oracle, C++ fallback and RTL
  output are byte-equal at `6f77bb67...62ff`. The append-once receipt binds the
  seal, descriptor, program, request, affine, input, compiler/source, ABI, RTL,
  toolchain, simulator, AXI trace, oracle, fallback and output identities.
  This is RTL Simulation Functional evidence only.
- Exact commands `python3 -m unittest tests.test_graph_device_dynamic_sealed
  tests.test_graph_device_uio_dry_run` (12 tests), `python3 -m unittest
  discover -s tests -p 'test_graph_device*.py' -v` (143 tests), and `python3
  -m unittest -v tests.test_kv260_preflight` (seven tests) exited zero on macOS
  arm64 with Python 3.14.6. CLI help, both dynamic shell syntax checks, Python
  compile, record checker, diff check and the one-active-item queue audit also
  passed. No UIO device, FPGA, performance, area, energy, ASIC, silicon or
  general-Graph result follows.

## T-0142 integration and T-0143 MAX_U32 kickoff

- Tester, Security and Performance Reviewers approved exact T-0142 remote head
  `fe7bea758a93ee7b573d2838d9f91ada4fdbabbc`; Security reported no actionable
  finding. PR #100 squash-merged as canonical main
  `151c481abdd1bed08b9a2ecc9fb28e79b7f0d46b`, Issue #99 closed and its Project
  item moved to Done. The post-merge Project README and canonical main
  readbacks pass. A clean detached checkout of that merge commit passes all 12
  sealed/UIO focused tests, the record checker and `git diff --check`.
- Phase 3 has no T-ID or ADR collision. PM allocated T-0143 and ADR-0080,
  created real Issue #101, Project item `PVTI_lAHOAGTqgs4BhMtczg5PXGc`, and
  clean branch `feat/t-0143-max-u32` from canonical T-0142. It is the sole P0,
  In Progress, Sprint S-0003, 13 SP, with Systems Implementer ownership and
  Tester/Security/Performance/Librarian/Researcher/PM support. The live queue
  audit passes with one active delivery item.
- Independent read-only Librarian, Researcher and Security reviews all
  recommend exactly unsigned `MAX_U32` opcode 4. PM selected that bounded
  option. MUL adds a multiplier and overflow contract without useful constants;
  MIN plus MAX violates one-opcode scope; comparison/select requires predicates
  or another operand form. MAX retains the two-source register encoding and
  enables a five-neighbor dilation demo.
- ADR-0080 requires explicit v2 program, dynamic-request and sealed semantics,
  exact version-pair rejection before model/AXI and byte-compatible v1
  regression. The device transport/install ABI v1, selectors, affine profiles,
  register/capacity bounds and fixed schedule remain unchanged. External CGRA,
  VLIW and ISA mechanisms are similarity context only; no source reuse,
  novelty, patent or legal-clearance conclusion is authorized.
- The first v2 sealed replay completed the lower RTL run but the host finalizer
  rejected its runner `source.manifest`: the sealed expected closure included
  the new v2 request/program semantic contracts while the existing shared
  in-container runner still enumerated only the v1 contracts. This failed
  closed after execution and is not promoted evidence. PM first expanded Issue
  #101's allowlist to the two existing shared runner scripts, then returned the
  narrow source-list correction to the same Systems mutation owner. No duplicate
  runner or weaker identity comparison is allowed.
- The corrected intermediate v2 seal/replay passed after the source-list fix,
  but later Security hardening changed the admitted C++ source. The actual
  final-source direct command passes in session `run.54jwxl34`. The final v2
  seal is `196248bc33ecc8813342c5776b5a3efe78e8d08d82fb1f80c027fb41c15fd1c6`
  with compiler/source `499f2c22...c1a7`, program `418ea472...e89cd3` and
  request `478d370e...526f9`. Its replay session
  `artifacts/graph_device_axi4lite_dynamic/run.410d8584` emits RTL once, builds
  simulator `ecd11c73...37662` once and invokes it once; malformed admission is
  rejected before AXI. Descriptor oracle, C++ fallback and RTL output are
  byte-equal at `46282258...60852`; trace identity is `5e6bd9fa...9a50`.
- A fresh v1 seal `8e37b331...dc496` replays in session `run.869e7617` through
  the same simulator. It retains the exact T-0141 program
  `ec13f9f0...ea39`, request `e1d8b8c5...ef5af`, and equal oracle/fallback/RTL
  output `6f77bb67...62ff`. This is the backward-compatibility RTL Simulation
  Functional result, not a performance comparison.
- The final v2 UIO dry-run emits plan `268f793e...42f46` with seven ABI-derived
  relative operations and `device_opened=0`, `mmap=0`, `mmio=0`. It is Host
  Functional only. Thirty-six focused tests, 151 Graph-device regressions,
  seven T-0139 regressions, runner syntax, Python compile, the record checker
  and diff check exit zero. `polls=` remains a termination diagnostic; no
  performance, area, energy, device, FPGA, ASIC, silicon or general-Graph
  result follows.
- Preliminary Security review then found two admission gaps: standalone Python
  and C++ fallbacks did not independently reject a v1 header paired with
  opcode 4, and the raw C++ request reader did not recompute program digest
  words before model construction. The same Systems owner added independent
  fallback structure/version validation and a bounded in-process SHA-256 check
  over the little-endian instruction count plus active instruction words. A
  digest-only mutated v2 request now fails the host C++ parser harness before
  a model or AXI transcript. The new harness path was missing from the first
  Issue allowlist; PM detected and added it to Issue #101 before staging or
  integration. The Chisel installer continues to treat the live digest as a
  caller-supplied identity after host admission, not as hardware-verified SHA.

## T-0143 integration and T-0144 Garden explanation kickoff

- Tester, Security and Performance Reviewers approved exact T-0143 remote head
  `a7315a0c8af9ac83a2078fef757e4b2ab609136d`. The clean Tester replay passed
  44 focused tests, 151 Graph-device regressions, seven T-0139 regressions, the
  C++ pre-model digest-admission harness, shell/Python checks, records and diff.
  PR #102 squash-merged as canonical main
  `86a6aee3fc9bc6979981d9a1aedbcdc218453b16`; Issue #101 closed and its
  Project item moved to Done. A post-merge clean checkout passes the same 44
  focused tests, record checker and diff check, and the zero-active queue audit.
- Phase 4 has no T-ID or ADR collision. PM allocated T-0144 and ADR-0081,
  created real Issue #103 and branch
  `feat/t-0144-garden-execution-explanation` from canonical T-0143, and set the
  Project item to the sole P0 In Progress item in Sprint S-0003. Project
  readback fixes one Systems Implementer mutation owner, Experience/Tester/
  Security/Performance/PM support, 13 SP, dependency T-0143, exact allowlist,
  artifacts, acceptance, Host Functional evidence, stop rule and non-claims.
  The one-active-item queue audit passes and Project README records T-0143 Done
  plus T-0144 active.
- ADR-0081 keeps `raveil.garden-snapshot/v1` intact and requires a separate
  dynamic explanation. The compiler owns and verifies the deterministic
  lowering trace; Garden only validates and renders it with retained affine,
  execution-identity, agreement and evidence fields. It may not reconstruct
  allocation, compile, execute, invoke a subprocess, reread live execution
  trees, access UIO, mutate, approve or promote evidence.
- Read-only Security design requires a separate component-wise no-follow,
  bounded regular-file loader; strict recursive schema and program/hash/
  topology/register/lifetime/fan-out validation; reference-only retained RTL
  identities; and behavioral denial of every execution/authority path.
  Read-only Performance design requires Host Functional view evidence to stay
  separate from retained RTL Simulation Functional references, program-order
  positions never to be called cycles, `performance=not-measured`, and
  `polls=` to be labelled only as a termination diagnostic.
- The initial Issue demo packet accidentally used `jjllq`, although the
  preserved Garden navigation alphabet is `j`, `k`, `g`, `G`, `q`. Performance
  review detected this before implementation acceptance. PM corrected Issue
  #103 and its Project Demo Command to deterministic `jjq`; no gratuitous new
  navigation key or behavior is added.

## T-0144 bounded explanation implementation and verification

- The single Systems mutation owner added compiler-owned deterministic
  lowering traces, including explicit fan-out consumer IDs, exact encoded
  words, source/destination registers and program-order definition/last-use/
  release positions. The compiler validates each trace against its descriptor,
  program words and SHA-256 before returning it. The T-0141 fan-out regression
  retains program `ec13f9f0...ea39` and names `a2`/`a4` as the two consumers of
  `a0`.
- A separate `raveil.garden-dynamic-explanation/v1` fixture binds the accepted
  T-0143 cross-dilation lowering trace `76f2b6d9...96fc3`, v2 program
  `418ea472...e89cd3` and retained evidence manifest
  `5524d573...76744e8`. The retained request, source, ABI, RTL, toolchain,
  simulator, AXI trace and exact three-way output hashes match the accepted
  Phase 3 receipt. They are display references, not newly generated evidence.
- The dynamic loader admits only one normalized leaf beneath
  `tests/fixtures/garden`, opens every component no-follow, requires a
  nonempty bounded single-link regular file and rejects an observed identity
  change across its exact read. Strict validation covers recursive fields,
  lower-case digests, payload version/digest/padding, topology, words,
  register lifetimes, fan-out, affine configuration, retained-reference
  manifest, agreement, evidence labels, performance and polls classification.
  Symlink leaf/parent, alternate/escaping path, directory/FIFO/hardlink,
  empty/oversize, read mutation and pathname replacement negatives fail before
  rendering.
- Garden renders Graph/dependencies, compiler lowering and retained evidence in
  deterministic bounded panes. The exact corrected `jjq` command exits zero
  twice with byte-identical transcript SHA-256
  `88f117383f7b28055c8292d126125be1bfafe14a5d4d0fea7aa20a9190e9bb86`.
  The view says positions are program order, transaction count is conformance,
  `performance=not-measured`, and `polls=` is not cycles, elapsed time,
  throughput or performance. It retains the legacy Garden schema/navigation
  and calls no compiler or execution path while loading/rendering the dynamic
  fixture.
- PM verification passes 40 focused tests, 29 Garden tests, 153 Graph-device
  tests and seven T-0139 tests, plus Python compile and diff check. This is
  Host Functional branch evidence only. No new RTL execution, performance,
  device/UIO, FPGA/KV260, general Graph, CGRA/VLIW, ASIC or silicon result
  follows.
- Preliminary Security review found that the first dynamic loader treated
  unavailable `O_NOFOLLOW` or `O_DIRECTORY` flags as zero, which would silently
  weaken the promised no-follow boundary on an unsupported Python/platform.
  The same Systems owner changed both flags to mandatory capabilities checked
  before any `os.open`; focused tests patch each capability absent and prove no
  path access occurs. Forty focused tests and 29 Garden tests pass after the
  correction. `O_CLOEXEC` remains an optional hardening flag.

## T-0144 integration and four-phase closeout

- Tester, Security and Performance Reviewers approved exact remote head
  `b31b0fa8d2518615d3b952b3dc7bfc00e08d0af7`. The clean exact-head worktree
  passed 40 focused tests, 29 Garden tests, 153 Graph-device tests, seven
  T-0139 tests, the byte-identical `jjq` demo, Python compile, record checker,
  diff check and the one-active Review queue audit.
- PR #104 squash-merged as canonical main
  `a4e1afcb99e450288d64bf5932b9586f4e3b0b8f`; Issue #103 closed and its
  Project item moved to Done. Project README records T-0141 through T-0144 as
  Done and no active P0 delivery item. A clean post-merge checkout passes 40
  focused, 29 Garden and 153 Graph-device tests, the exact demo, records and
  diff check. The final queue audit passes with zero active delivery items.
- The owner-authorized four-phase sequence is therefore canonical. T-0141
  added one fan-out dynamic Graph, T-0142 sealed and verified requests plus a
  no-device UIO dry-run, T-0143 added exactly one versioned MAX_U32 opcode, and
  T-0144 added the read-only explanation view. Their separate evidence and
  non-claims remain unchanged; no next P0 is inferred or allocated.
