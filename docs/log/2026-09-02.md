# Work log — 2026-09-02

## T-0132/S07 deterministic RTL export

- Work resumed from clean local S06 commit
  `2f58aff8179eeed52f4fdd77aa902de097f0bc2a` after the owner corrected an
  erroneous ordinary-task pause. The dirty historical root worktree remained
  untouched; S07 uses isolated branch `feat/t-0132-s07-rtl-export`.
- Read-only triage found that the earlier T-0112 KV260 branches diverge before
  the present S01--S06 implementation and cannot be merged wholesale without
  deleting current accepted behavior. Their useful deterministic-export idea
  was reimplemented against the current `GraphDeviceAxi4LiteTop` boundary.
- ADR-0072 accepts only a vendor-neutral SystemVerilog handoff. Board, absolute
  base, Vivado, device tree, UIO execution, FPGA evidence, resources, timing,
  performance, and product claims remain outside the slice.
- The first real export attempt stopped after two successful elaborations
  because the bundle copied Chisel auxiliary files while the manifest admitted
  only `.sv`. The exporter was corrected to copy exactly the manifested
  SystemVerilog closure. The second attempt stopped because OpenJDK reports its
  identity as `openjdk`, not literal `java`; the bounded toolchain check now
  accepts either truthful spelling. Both failures occurred before publication
  and left no accepted output.
- Final offline export
  `artifacts/graph_device_axi4lite_export/s07-candidate-v2` passes standalone
  verification. Manifest SHA-256 is
  `e07662214af42343db8714965310e66476b04bbfce4b4ab5b55b1a84a3e9481d`,
  receipt SHA-256 is
  `6f2e5622ec0d42939999df6a842ee4a49f9e5e2acde5e4e13923f5ee87aef45c`,
  and the double-emitted RTL manifest remains
  `209f26facf122d6f5e39c792e4ddafc5b016bb2a924670b2b4464f9f2b2d5f6e`.
- Verification ran on Darwin 25.5.0 arm64 with Python 3.14.6, Apple Git
  2.50.1, Docker 29.6.2, and the receipt-bound Linux/amd64 offline Chisel
  image. The exact closeout commands were:

  ```sh
  python3 -m unittest tests.test_graph_device_axi4lite_export \
    tests.test_graph_device_axi4lite tests.test_graph_device_axi4lite_request \
    tests.test_graph_device_uio tests.test_graph_device_dag \
    tests.test_graph_device_affine tests.test_graph_device_run \
    tests.test_graph_device_selected
  hardware/chisel/export-graph-device-axi4lite-rtl.sh \
    artifacts/graph_device_axi4lite_export/s07-candidate-v2
  hardware/chisel/export-graph-device-axi4lite-rtl.sh --verify \
    artifacts/graph_device_axi4lite_export/s07-candidate-v2
  sh -n hardware/chisel/export-graph-device-axi4lite-in-container.sh \
    hardware/chisel/export-graph-device-axi4lite-rtl.sh
  python3 .agents/skills/raveil-task-governance/scripts/check_records.py
  git diff --check
  ```

- Sixty-one focused S04--S07 Graph/AXI/UIO regressions pass; both shell syntax checks, standalone
  bundle verification, the record checker, and `git diff --check` pass.
  Evidence is only
  `rtl-export-functional-prerequisite`; no real hardware was used.

## T-0132/S04--S07 remote integration and closeout

- The owner explicitly rejected the ordinary post-commit pause and directed
  continuous work. The PM treated that as authority to integrate the already
  bounded stack, not as authority for new FPGA, experiment, paid-resource, or
  claim-bearing scope.
- Live GitHub audit reported zero active delivery items and canonical
  `origin/main` `9705af2`. Issue #81 received the complete S07 integration
  packet, exact 39-path allowlist, 18 SP, S-0003, and RTL Simulation
  coordination class. The first canonical start dry-run rejected an ambiguous
  `T-0132/S04-S07` title because it did not match branch S07; the Issue was
  corrected to S07. The second dry-run rejected noncanonical packet headings;
  exact Mutation owner, Allowlist, Artifacts, Acceptance, Evidence class, Stop
  rule, and Non-claims fields were added. The third dry-run and apply passed.
- PR #82 exposed exact head
  `a6e7fa2afdd53c685f82f5f19983b5ee04df4a88` over base `9705af2`, matched the
  allowlist, and was `MERGEABLE/CLEAN` with no hosted checks or unresolved
  reviews. The canonical Review transition passed, and ADR-0058 merged it as
  `276469a1fe55b18d6778dfe6b8a6c16bbfdbdf19`. Issue #81 closed automatically.
- The first post-merge queue audit correctly rejected the still-Review Project
  item. The PM recorded Technical Accept, observed cycle, role packets, and
  resource use, then set Done last. The following audit passed with zero active
  items. The private Project README now records canonical S01--S07 and keeps the
  weekly S-0003 Sprint Review separate.
- Issue #83 and branch `docs/t-0132-s07-closeout` own only the final stale-record
  correction. This closeout changes no implementation or evidence class.

## T-0132/S08 bounded runtime request admission kickoff

- The owner rejected the post-S07 acknowledgement pause and directed work to
  continue. The PM selected the previously recommended software-only S08:
  remove request-specific Linux runner rebuilding while remaining inside the
  frozen three-Graph catalogue. This does not authorize board or FPGA work.
- Issue #85 is the real `work-item`; it is linked to Project #1 with Systems
  Implementer, eight SP, Sprint S-0003, Host Functional evidence, exact
  allowlist, demo command, and stop/non-claim boundaries. Canonical start and
  branch audit passed on `feat/t-0132-s08-dynamic-admission`.
- ADR-0073 accepts strict runtime reading of the existing 20-byte request
  envelope and seed-derived input verification before UIO open. RTL, ABIs,
  aperture, frozen catalogue, and `run_selected_dag` remain unchanged.
- Initial Security review found that the unchanged runtime also consumes
  `inputs/seed-1.bin` for its malformed-program matrix after UIO open. Admission
  was corrected to validate that fixed prerequisite too; seed-not-1 mismatch,
  missing, and symlink regressions were added. Re-review accepts the fix. The
  known admission metadata/read and runtime validation/reopen races remain
  explicitly non-production.
- Primary verification on Darwin 25.5.0 arm64, Python 3.14.6, and Apple Clang
  21.0.0 passed 62 Graph/AXI/DAG/UIO tests, the record checker, and diff check.
  The independent Tester passed 60 tests; raw local logs hash to
  `35a9716c0b1e5e2c5cd1852eca6c4468ad6bbdbe0b699384c79bc58f8073d72c`
  and `c1edccf02c50bc3a505c2e5ab0baf49c4e39bb0d2be8ceb32f3c58a6ccb8f386`.
- The warning-free ARM64 Linux build produced local image
  `sha256:89b4bd24f6db672989059f01a12d6501aafc39ec4f22ae1a4152c6af9a0630b5`.
  Unchanged S05 Verilator replay `run.VjxcvK` passed with 2,976 AXI records,
  RTL manifest
  `209f26facf122d6f5e39c792e4ddafc5b016bb2a924670b2b4464f9f2b2d5f6e`,
  and byte-equal output/oracle SHA-256
  `fc0834021afa134f461191580efbab917368bb034f9b3cc007967dc96457037f`.
  These are host-functional/build and RTL-simulation facts only; no real UIO or
  FPGA execution occurred.

## T-0132/S09 shared runtime-admission simulator kickoff

- After PR #86 merged exact S08 head `32fc1d1` as `b6b264f`, Issue #85 was
  Closed and its Project item corrected from In Progress to Done. The live
  queue audit passed with zero active delivery items.
- The owner directed work not to stop. PM selected the smallest simulation-first
  continuation: share S08 request-root admission with the existing AXI4-Lite
  Verilator bridge and prove two requests through one build without changing
  RTL, any ABI, or the frozen catalogue. Issue #87, branch
  `feat/t-0132-s09-runtime-sim-demo`, eight SP, Systems Implementer, and Sprint
  S-0003 are active under ADR-0074.
- The first actual demo checkpoint passes. Five-point seed 1 and
  vertical-three-point seed UINT32_MAX use simulator SHA-256
  `d3fcc5b3def9f9c4cc22185e0cf49d2f3943a4cc5e510f4aebbdc93a468050b7`;
  both private outputs pass independent receipt/oracle verification. A
  corrupt-magic request returns nonzero before `axi-transcript.log` creation.
  Evidence is RTL Simulation Functional only; performance is not measured.
- Sixty-four focused Graph/AXI/DAG/UIO regressions, shell syntax, record, and
  diff checks pass. The unchanged one-request CLI passes for
  vertical-three-point seed 7 with receipt SHA-256
  `f71f13ea806a4d49a70aa45120dd1509baa7e191bbbbbce6646cd10f58c11a40`
  and byte-equal private output/oracle SHA-256
  `fc0834021afa134f461191580efbab917368bb034f9b3cc007967dc96457037f`.

## T-0132/S10 operator runtime-pair CLI kickoff

- PR #88 merged exact S09 head `d65c480` as `de55cac`; Issue #87 is Closed,
  its Project item is Done, and closeout queue audit passed with zero active
  delivery items. The owner directed continuous work, so PM selected the
  smallest operator-facing continuation rather than a cache or daemon.
- Issue #89, branch `feat/t-0132-s10-runtime-pair-cli`, eight SP, Systems
  Implementer, and Sprint S-0003 are active under ADR-0075. The command accepts
  exactly two repeated Graph/seed pairs, pre-admits both, then independently
  validates the lower session marker, ordered receipts, one simulator identity,
  and rejection-before-AXI evidence.
- The first actual command passes for five-point seed 1 and
  vertical-three-point seed UINT32_MAX. Both report independent oracle PASS,
  the rejected request has no AXI transcript, and both receipts bind simulator
  SHA-256
  `d3fcc5b3def9f9c4cc22185e0cf49d2f3943a4cc5e510f4aebbdc93a468050b7`.
  Evidence remains RTL Simulation Functional and performance is not measured.
- Seventy focused Graph/AXI/DAG/UIO/operator regressions, shell syntax, record,
  diff, secret-pattern, and live queue checks pass. Exact-head PR review and
  integration remain.

## T-0132/S10 integration and T-0138 KV260 transition packet

- PR #90 merged exact reviewed S10 head `d70fe7d` as canonical commit
  `c0c33db`; Issue #89 is Closed and its Project item is Done. The closeout
  queue audit passed with zero active delivery items.
- The owner directed continuous work. PM allocated T-0138 as the bounded
  RFC-0004/S01 planning continuation rather than starting a vendor tool or
  board implementation. Real Issue #91, branch
  `research/t-0138-kv260-transition`, Systems Implementer, five SP, Sprint
  S-0003, Planning evidence, exact allowlist, stop rule and non-claims are
  active in Project #1.
- Official-source review tentatively selects KV260, Ubuntu Server 24.04 arm64,
  and Vivado ML Standard 2025.1 on a supported Windows 11 x86-64 host. AMD's
  2025.1 device table includes all Kria devices and says ML Standard is
  license-free; 2026.1 requires a valid license and is not the default.
- The packet maps the S07 relative 16 KiB AXI4-Lite export to a proposed 32-bit
  PS HPM control path, one PL clock/reset domain, polling-only UIO map 0 and the
  unchanged S10 operator/oracle. It separates physical-owner actions from
  Jitro actions and fixes checks 1--7 before implementation and 8--12 before
  FPGA-functional promotion.
- No address, frequency, reset circuit, DT compatible string, bitstream or load
  command is invented. No tool installation, license acceptance, purchase,
  vendor-source copying, device access, FPGA evidence, synthesis/timing/resource
  result or performance claim occurs.

## T-0139 read-only KV260 readiness preflight

- Continuous-work policy pulled the smallest safe executable continuation of
  T-0138. Real Issue #93, branch
  `feat/t-0139-kv260-readiness-preflight`, Systems Implementer, five SP,
  Sprint S-0003, Host Functional target observation, a fixed allowlist and the
  no-device-access stop rule are active in Project #1.
- ADR-0076 adds `graph-device kv260-preflight --device /dev/uioN`. The candidate
  checks Linux aarch64, bounded kernel and KV260 model identities, FPGA-manager
  state, exact UIO character-device/sysfs identity, and an aligned 0x4000-byte
  map 0 using only fixed metadata reads and `lstat`.
- The command never opens or maps the device and issues no MMIO. Passing output
  is labelled `target-host-observation`, `device_opened=0`, `mmio=0`, and
  `performance=not-measured`; no FPGA, execution or deployment fact follows.
- The first six focused tests exposed only a test-harness mistake: Python
  `print` writes the message and newline separately, while the mock expected
  one `write` call. The production output was correct. The test now captures
  and compares the complete stderr string.
- On macOS 26.5.1 arm64 with Python 3.14.6, seven focused tests and the full
  131-test Graph-device regression set pass. Bytecode compilation, CLI help,
  the Markdown record checker and `git diff --check` also pass. The actual host
  command returns exit 2 with `KV260 preflight requires Linux` before device
  inspection, which is the expected non-target result. Live Project audit
  passes with one active item when invoked with explicit owner `@me`; exact-
  head review and integration remain.
