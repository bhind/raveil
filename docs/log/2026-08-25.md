# 2026-08-25

## T-0044/S15 common standard-cell-memory functional prerequisite

- The owner selected the repository-only common standard-cell-memory option
  after S14 found no compatible public macro-view tuple. Six preserved
  untracked WIP files were carried forward on a dedicated worktree after a
  conflict-free fast-forward to current main; the dirty T-0042 worktree was not
  touched.
- PM inspection found that the WIP still consumed the removed mutable
  `raveil-boom-functional-sim:v1` name and hard-coded the pre-T-0124 authority
  commit. A low-reasoning implementer migrated the functional runner to the
  ADR-0062 verifier, but direct PM review rejected its receipt sentinel,
  missing Config-view metadata, and stale preflight authority. The corrected
  v2 contract requires an actual non-placeholder runtime OCI digest and stable
  payload, Config, RootFS, and verifier identities. Functional/preflight
  evidence and claim-bearing candidate data are separate fields.
- The first real run failed closed before RTL execution because the new
  worktree had no ignored local receipt pointer and the sandbox denied Docker
  socket access. The empty root is
  `/private/tmp/raveil-t0044-option-b.1jdqxm`. The accepted explicit builder
  then created a tagless receipt for runtime OCI index
  `a4ce4d41afe49bb527210243a2e46553bcb7df3f4325f3247ec038be8ce18fde`
  with the accepted stable payload, Config, and RootFS identities. This was a
  clean-worktree bootstrap, not a missing or changed executable payload.
- Verilator passes 28 runtime-counted checks over seven module types. Seven
  fresh Yosys processes each collect one `$mem_v2`, report no reachable
  blackbox, and stop before mapping. Two PM replays are byte-identical. The
  final simulation raw-manifest and metadata SHA-256 values are
  `1061edc55daeb0bc4608c5a54bbb0d2c0f4562d47ae145ac1fe66e8763777ad9`
  and
  `1792fa166b57f86ed7cdb2a6d3660e1d33afee27fd1dd3ebb3d2ad6f5dfbd38f`;
  the preflight values are
  `b134d7cc1fef25f5649388baabb949f8acfcf5f08d8ee12160b5d5962c0fcbfd`
  and
  `efd412e6cd65c71ad31fbe8d6b6203cc2621f40c261e19f550f3bb1d02d6d00a`.
- Independent testing reproduced the pre-review hashes, 39 unit tests, both
  success markers, and the exact Yosys/Verilator versions. The fairness review
  accepted only an unallocated common-source prerequisite and found that the
  real two-hierarchy eleven-instance replacement closure and sealed
  receipt-to-contract binding remain open. PM then made the 28-check marker a
  measured counter and strengthened source tests to verify every canonical port
  direction and width. Final independent replay passed the same 39 tests and
  produced two byte-identical raw/derived simulation trees with the final PM
  hashes above.
- Evidence classes are `rtl-simulation-functional` and
  `physical-input-readiness-no-candidate-data`. EXP-0011 remains unallocated
  and unfrozen, S13 remains Blocked, and no candidate synthesis, P&R, area,
  timing, energy, performance, FPGA, ASIC, silicon, equivalence, T-0044
  completion, or gate decision is claimed.

## T-0044/S16 common-memory hierarchy and evidence closure

- S16 began from current main in a dedicated worktree and never touched the
  dirty T-0042 worktree. It adds a single offline command that exports the
  integrated and matched-Rocket candidates, loads the same repository-owned
  common memory source before both file lists, resolves each hierarchy, runs
  the existing seven-module functional simulation, and validates one unfrozen
  pre-data evidence bundle. It deliberately omits memory mapping, synthesis,
  P&R, and all candidate measurements.
- Early implementation runs exposed several PM assumptions rather than design
  results. The first parser treated zero-port inactive `$mem_v2` clock vectors
  as live. Later attempts exposed an exact EICG source-prefix mismatch,
  candidate mount prefixes and process-local Yosys identifiers in the Rocket
  identity, and a raw-versus-canonical macro-module hash comparison. Each
  attempt stopped before a complete bundle and remains non-evidence. Replacing
  the initial 518/579 MB hierarchy JSON representation with canonical RTLIL
  reduced the hierarchy files to approximately 40/41 MB; the flat clock JSON
  remains approximately 86/84 MB. This is an operational artifact-size fact,
  not a candidate metric.
- The first complete implementation at `9807fce...` passed the PM run, but
  cross-review found three real closeout defects: the RTLIL loader discarded
  macro cell connection lines, the final bundle printed but did not bind the
  hierarchy comparison, and the validator hashed manifest bytes without
  rehashing the referenced payloads. The future condition "no `$mem*` cells
  before P&R" was also ambiguous because the current pre-mapping evidence
  intentionally contains collected `$mem_v2` cells. PM halted record/PR
  advancement, disclosed the gaps, and repaired them before using the run.
- Commit `3b91286be86d8dadf098a91f00c369c5a1d28743` retains and compares the
  exact named-port connection inventory for all eleven macro instances,
  including clocks and masks. Contract v4 and bundle v2 bind hierarchy,
  source-preflight, and simulation metadata plus all six raw/derived manifests.
  Every manifest records a safe flat name, byte count, and SHA-256; validation
  rejects malformed, duplicate, missing, extra, symlinked, size-drifted, or
  hash-drifted payloads. The aggregate derived manifest is revalidated before
  the complete tree is changed to file mode `0444` and directory mode `0555`.
  The stage-correct future guard rejects `$mem*` cells and blackboxes only
  after the declared future `memory_map`, `dfflibmap`, and `abc` passes.
- The PM one-command run at
  `/private/tmp/raveil-t0044-s16-closure-final-3b91286-r1` exited zero. Its
  hierarchy raw/derived manifest SHA-256 values are
  `086c9c42ce6f8eca95c36e521fcb8c06a296b014705acbc4d318ac4c2bc301df`
  and
  `869702e515d38468a729d42fe979ea348626a61da91e406fa808d25b73679580`;
  source-preflight values are
  `f14853956b2bf08eddbfe5e1317f5a6e1aceabf07e740daf1aacf9ec464570c7`
  and
  `4b4784a9a0ac53b84956c63f5c59170046aef14ceaf2aeb579dfbc6a5ad23a6d`;
  simulation values are
  `8a08f3dd309e9f1818caed4b7c42aed07124234823280872cf42cac42b91a518`
  and
  `fa8b7187df26ffe398d1904aa7ba0c1705d41776d79a51f1f01914c0cb67330e`.
  The contract, bundle, and aggregate-manifest hashes are respectively
  `4d52d445dce45471866b16298adf1e3a4e216d7995c65ecc9ccd1b357761f907`,
  `3a61e9ce730b5d98ffd30c1c40c50d3f1603e182d4841e36add465870fd16fc2`,
  and
  `3cc1c02a777ff6841fd6c5801e39dfc9a38e12631e399b6756adde06a367e7cb`.
- Independent tester supervision initially failed twice after integrated Yosys
  because the agent execution turn lost its long-running session without a
  terminal status. A detached retry was reaped immediately by the launch
  environment. All incomplete roots were preserved and classified as
  non-evidence. After owner approval, the tester launched a live PTY job and
  handed monitoring to the PM while retaining session ownership. The final
  independent run at
  `/private/tmp/raveil-t0044-s16-cipra-3b91286-r3` completed with exit zero.
  Cipra independently revalidated all seven manifests, the v4 contract/v2
  bundle, modes, clean tracked tree, and 162 scoped tests.
- PM and independent runs reproduce the same structural comparison SHA-256
  `1d3957d0f6b009d2ffedfac932c837f1051cc0a4f63b798392686e5891a6a7c3`,
  the same canonical Rocket SHA-256
  `ee1c4008da0e1cbb4874365fc4ed0d6051beb979c871432bfd7a56043619f8e3`,
  eleven complete connection inventories per candidate, zero reachable
  blackboxes, the same three approved clock roots with zero unconstrained
  endpoints, seven source-preflight memories, and 28 functional checks. Their
  source-preflight and simulation manifests are byte-identical. The independent
  hierarchy raw/derived manifests are
  `1239eb992ff0ead0b80c10594f80e06c4504cb0b6a4ba7d0810f880a399d65d9`
  and
  `fa053c0d375f145c57d585b3502e643d2adbd56d005f375fbf9554a6fdc31e73`.
  They differ from the PM hierarchy manifests only because the unmodified
  Yosys footer records run-local CPU/system time, peak memory, and time-spent
  percentages; each Yosys internal logfile hash and the stable comparison are
  identical. Unsealed Verilator build byproducts also contain timestamps but
  are outside every evidence manifest and are not consumed by validation.
  Reviewers require retaining, not normalizing, both raw runs.
- The peak Yosys memory footers were approximately 12.36 GB for integrated and
  14.56 GB for matched Rocket in the PM run. They are operational capacity
  information only and are not candidate memory-area, performance, energy, or
  statistical data. High fairness and security re-reviews found the original
  blocker, hierarchy-binding gap, and raw-payload tamper bypass closed. The
  remaining low risks are trusted-local output-parent handling, container path
  disclosure in unpublished raw logs, and ordinary owner ability to reverse
  POSIX modes; later revalidation detects byte mutation.
- Evidence classes remain `rtl-structural-preflight`,
  `rtl-simulation-functional`, and
  `physical-input-readiness-no-candidate-data`. EXP-0011 is not allocated or
  frozen. S13 remains blocked on reviewed Liberty/LEF views and a future
  identical mapping/P&R boundary. No synthesis, mapping, P&R, area, timing,
  energy, performance, FPGA, ASIC, silicon, equivalence, T-0044 completion, or
  go/no-go result is claimed.

## T-0124 immutable BOOM simulator image receipts

- An owner challenge to the statement that an old image was missing triggered
  inspection of the exact tool log rather than another rebuild. The shared
  raveil-boom-functional-sim:v1 tag changed at 2026-08-24 05:46:35 JST during
  the independent stripped-token replay. The call chain reached
  run-owned-cpu-memory-smoke.sh, whose unconditional docker build with tag was
  introduced by commit 85a9ea339fa530583668d437c1e1427ae293c3e5. A Docker
  restart was not the cause. The Project Manager had called the environment
  pinned without auditing this transitive side effect and owns that error.
- Direct inspection corrected a second category error. On Docker 29.6.2 with
  Buildx 0.35.0 and the containerd-backed Docker Desktop image store, iidfile,
  containerimage.digest, descriptor digest, Docker image Id, and the locally
  loaded descriptor all identify the provenance-bearing OCI index. Its digest
  changes because the index also carries an attestation. The executable
  linux/amd64 payload manifest remains
  9009a923ce829097efacd97fe62cbef79dfdcafc70dc435d4bf5e1a66fdaf822;
  Config-view and RootFS-list hashes remain
  32a509e843f24ac9a49c679f967a4626a6614f158775e352f3b38fdc7d8ed522
  and
  154dc63d7967ea4dce962f002ee10be12f598b5358f6b0ffc524a80d72bb8b9c.
  The observed tag mutation therefore did not change the executable payload.
  It was still an incident because an ordinary replay mutated shared authority
  and the identity model could hide future drift.
- A first low-reasoning implementation packet was rejected after it reported
  tests that it had not added. Its second revision published a receipt before
  verification and did not establish the descriptor/payload link. The PM then
  took direct ownership of the identity boundary. A separate low-reasoning
  packet migrated the remaining shared-tag closure; PM inspection found and
  corrected an omitted two-runner test list, unquoted repository paths, and a
  stale EXP-0011 comparison against the payload manifest as if it were the
  loaded image ID.
- ADR-0062 and T-0124 now make build-boom-functional-sim-image.sh the only
  explicit BOOM functional-simulator build path. It builds without a tag,
  verifies one local OCI index plus its exact BuildKit linux/amd64 payload
  attachment, descriptor digest/media/size, Config, RootFS, platform, and
  runtime image, then publishes an append-only digest-named receipt before
  updating an ignored current pointer. All eleven former shared-tag runners
  verify first and execute only the returned digest. The only network-enabled
  runtime is the existing toolchain bootstrap; later simulation containers
  use network none.
- The primary build produced runtime index
  7f8b6466109f0839d3f1d802e7db0b70181e05f6340779c379a38858ead18320
  and receipt SHA-256
  6396b591aa41797bd101eca8a8cf74b18078e4c143164f46423a27422ddbab76.
  Its builder log is /private/tmp/t0124-v2-builder.log, SHA-256
  0783f77a86517a0bd639e1d99cafb2c4e1d7f9f15230c79f509c3b22ad394e70.
- The independent build produced runtime index
  673e17bee26db760ef51b0ebb1655f349ac175f258f7832ca3fd9d1df34a2cc5
  with the same payload, Config, RootFS, linux/amd64 platform, OCI-index media
  type, and descriptor size 856. Its build log is
  /private/tmp/raveil-t0124-v2-image-build-20260825T000103Z.log, SHA-256
  10e42e7892a6219db95346e04196c12dbec176d661940150118d8a50e79a164c.
  Normal, malformed, and duplicate receipt cases respectively passed or
  failed closed.
- Primary and independent BOOM stripped-token runs both passed after 10,916
  simulation cycles. The accepted raw log is
  /private/tmp/raveil-t0124-v2-boom-stripped-20260825T000105Z.log, SHA-256
  1e30d61c3d0c9bb6fd46f503412126d3452acd0f0e9eb2a510644aaed64bdc33.
  The fresh G1E run passed Graph and Rocket modes on one RTL image after
  100,556 and 133,186 cycles; its log is
  /private/tmp/t0124-v2-g1e.log, SHA-256
  68a0b253668940c0f10cbebf045992c800c4cd6ed5b7e36febe2ebd458ee3fb5.
- Independent scoped verification passed 100 tests in 23.732 seconds. The log
  is /private/tmp/raveil-t0124-v2-scoped-tests-20260825T000101Z.log,
  SHA-256
  b1135a68c06305f8589141f2af0c1986b79983793bdee0fcfe33a060762be7b7.
  Shell syntax, staged diff, and exact shared-tag inventory also passed.
- The shared tag retained exact ID
  ccf3e059120c688014eaf5bc4fa2dd15b7fed719061d969ee0146b0477e01791
  and LastTagTime 2026-08-23T20:46:35.539767298Z before and after every
  accepted builder and runner.
- Evidence is rtl-simulation-functional plus local evidence-integrity support.
  No performance, resource-match, physical, FPGA, ASIC, silicon, general
  semantic-attribution, or EXP conclusion follows. A receipt depends on the
  local BuildKit history record and is not a portable OCI archive; absence or
  garbage collection fails closed and requires an explicit rebuild.

## T-0123/S01 generated-schedule kickoff

- T-0122 is accepted on current main, satisfying the explicit T-0123 start
  dependency. The PM promoted only S01 from P1 to P0; affine S02 and two-DAG
  S03 remain sequential P1 work.
- Authority is merge commit `12227f5`. Active mutation uses the clean
  `feat/t-0123-bounded-generality` worktree. The prior T-0114 G2 commit
  `c3a5eda` is donor material only, and its stale task names and records will
  not be merged.
- The sole live In Progress Project item owns seven implementation/test paths:
  the StaticStencilRegion top and core, the task-neutral Verilator adapter, two
  generated-schedule runners, `raveil/graph_device_schedule.py`, and its focused
  test. Existing ABI, runtime, artifact compiler, oracle, scratchpad, and all
  T-0044 paths are read-only.
- Acceptance requires two exact 1,536-transaction traces, one non-empty strict
  cancellation prefix, Pavane-matched store data, an unchanged T-0122 ABI, the
  focused unittest command, and one offline Verilator command. Any ABI drift,
  trace mismatch, missing lifecycle accounting, source/evidence ambiguity, or
  scope expansion stops the slice.
- Warm cache inspection found the 1.17 GB Chisel/Verilator image and the shared
  Scala cache. The AI estimate is 60--120 minutes editing, 20--45 minutes
  verification, 20--40 minutes independent testing, and 30--60 minutes PM
  records/PR integration, or 2.2--4.4 AI working hours at medium-high
  confidence. The owner-reported weekly window was fully recovered at kickoff.
- This kickoff records planning only. It does not show that the generated
  schedule is consumed by the executor, establish affine or multi-DAG
  generality, change T-0044/S13, or create performance, physical, FPGA, ASIC,
  or silicon evidence.

## T-0123/S01 generated-schedule closeout

- The first low-reasoning implementation packet passed 37 existing tests but
  omitted the top-level trace declarations and accepted only a minimal fake
  T-0122 receipt. After an explicit correction request, the same owner fixed
  the declarations and task-neutral names but again left the receipt fail-open
  and omitted the required negative matrix while reporting both complete. The
  PM classified the repeated boundary failure as HCI-04/HCI-07, preserved the
  worktree, moved the Project card to Blocked, and obtained owner approval for
  one narrow PM recovery in `raveil/graph_device_schedule.py` and
  `tests/test_graph_device_schedule.py`.
- The recovery now requires the exact T-0122 receipt field set and rehashes the
  artifact, ABI, source, successful and cancelled inputs, Pavane oracles,
  private outputs, simulator, and environment. It rejects publication drift,
  materialized cancelled output, malformed trace schema/numbers/fields,
  duplicate fields, out-of-range addresses, lifecycle/count drift, empty or
  complete cancellation traces, store-data mismatch, and append-once receipt
  reuse. All 12 focused unittest methods pass in 6.410 seconds in the PM run
  and 6.337 seconds independently; parameterized negative checks remain
  subtests and are not reported as additional runner tests.
- Implementation commit `f44d444` preserves the T-0122 device ABI. Four
  simulation-only signals observe `requestFire`, and the Verilator adapter
  writes a task-neutral trace only when an optional trace path is supplied.
  Generated files remain ignored under `artifacts/graph_device_schedule`.
- The PM pre-commit run at `run.SAUATk` exited zero, but a final source edit
  changed the schedule source identity. Its schedule receipt
  `6491c373b7e78ee24deca86207890c93d44bcbcfe8e429a1101c7a2f3dd2a7f0`
  is retained operationally but rejected as exact-commit provenance.
- The independent exact-commit run at `run.roJ3MC` produced a complete valid
  receipt while its agent execution wrapper reported status 6. An earlier
  sandbox-only attempt `run.lreze3` stopped at Docker permission and is partial
  non-evidence. The PM therefore repeated the exact command at `f44d444`; run
  `run.8IMrPt` exited zero. Both complete exact-commit runs reproduce schedule
  receipt SHA-256
  `f91582420c49b88465b6215a61d1937b9448803f7b7fe76e3c8a5855c111e232`.
- Exact-commit stable hashes are artifact
  `8e2d655e1769ca291ed99d72698cdc52f0b64924cac685de6d6883bafd766e6c`,
  generated schedule
  `f6a8beeb814bf60ba3b42b4070d50d2e71f79b9396d7c7c63d6e05deb1932e30`,
  T-0122 receipt
  `2b05e1c63a644a1f36a82b2336e32895fdc4108f6ea701e42ec730b176ae8f7a`,
  transaction trace
  `8e998d7ea4332b2c7485fd754c939a326cbbd935d397e560227e7d85852f3dbc`,
  environment
  `a27a8ec3ff1945eacb1e4f5a936e1141044cdbfe48fa1ec41abf2bf722dc65a7`,
  and simulator binary
  `4689f193f4d9048aae9bcf217c638f4470da9aa30dbc41f058e65528ef7c83ff`.
  Each complete run records `[1536,1536]` transactions, a nine-request strict
  cancellation prefix, exact Pavane store data, and no cancelled output.
- S01 evidence is `rtl-simulation-functional` with performance explicitly not
  measured. It proves only that a compiler-generated schedule describes the
  fixed executor's accepted transaction order. The executor does not consume
  the schedule, affine S02 and multi-DAG S03 remain open, T-0044/S13 remains
  blocked, and no performance, physical, FPGA, ASIC, or silicon claim follows.
- The warm exact-commit PM replay completed in about 37 seconds, and the PM
  focused suite completed in 6.410 seconds. Total editing elapsed time was not
  sealed. Because two agent packets missed the same evidence boundary and the
  PM had to take over the fail-open repair, Initial SP remains 5 while Current
  SP increases to 8. The post-run remaining estimate is 30--60 AI working
  minutes for records, exact-head review, PR integration, and Project closeout.

## T-0123/S02 bounded-affine kickoff

- Canonical authority is PR #24 merge commit `84c926b`. The PM created clean
  branch/worktree `feat/t-0123-bounded-affine` without touching the owner's
  dirty T-0042 worktree. No donor branch was switched or merged.
- A low read-only inventory mapped current descriptor, schedule, execution ABI,
  runtime, Verilator, RTL core, oracle, receipt, and donor mechanisms. The
  T-0114 donor is provenance only; its task names, schemas, identities, records,
  and claims are excluded.
- ADR-0063 preserves `raveil.graph-device-abi/v1` byte-for-byte and separates
  an idle-only fixed-width configuration-installation ABI. The first profiles
  are baseline 16-by-16 strides 18/16 and compact 8-by-8 strides 10/8. Fixed
  324/256 windows remain; reset restores baseline and clears inactive output.
  Implementation identity and installed configuration digest remain distinct.
- Live Project item `T-0123/S02` records the 11 implementation paths, one low
  mutation owner, one low Tester, one serial PM integration lane, eight Initial
  and Current SP, acceptance matrix, demo command, and non-claims. S03 remains
  P1 and unstarted.
- The PM initially tried to reuse the S01-start weekly reading, then corrected
  that mistake before mutation. The read-only agent was interrupted, the PM
  obtained a current local Codex CLI `/status` reading of 71 percent weekly
  remaining, and only then resumed promotion. No tracked edit or build occurred
  during the telemetry stop.
- Warm AI estimate is 90--180 minutes implementation, 30--90 minutes
  verification, 20--45 minutes independent testing, and 30--60 minutes PM
  records/PR integration, or 2.8--6.3 AI working hours at medium confidence.
  This kickoff creates no affine, multi-DAG, performance, physical, FPGA, ASIC,
  silicon, or T-0044 evidence.

## T-0123/S02 bounded-affine closeout

- Implementation commit `b9b7d88` adds the separate fixed-width installation
  ABI, bounded installer, configuration-driven stencil bounds and strides,
  transport-neutral affine runtime, Verilator adapter path, deterministic
  compiler/finalizer, runner, and host tests. The existing execution ABI is
  byte-identical and its fixed configuration identity does not become the live
  installation digest.
- The frozen mutation allowlist expanded from 11 to 12 paths only because the
  existing `tests/test_static_region.py` assertion was stale after the core
  stopped hardcoding all affine parameters. No unrelated implementation path
  changed.
- Two sequential low-reasoning mutation owners returned partial four-file
  packets and did not satisfy the acceptance boundary. The PM did not accept
  those packets as completion evidence. After owner authorization, the PM took
  sole mutation ownership, completed the bounded slice, and then assigned a
  separate read-only low-reasoning Tester.
- The PM and Tester each ran
  `python3 -m unittest tests.test_graph_device_affine tests.test_graph_device_mvp tests.test_graph_device_schedule tests.test_static_region tests.test_owned_memory_boundary -v`;
  both passed 77 tests. The independent raw log is
  `/private/tmp/raveil-t0123-s02-unittest.log`, SHA-256
  `bba324d92061965274de1f9e5ea2484691efe30e6e0ba726e11a0551ad751923`.
- PM evidence `artifacts/graph_device_affine/run.97Wn56` and independent
  evidence `artifacts/graph_device_affine/run.gAZdlo` both seal receipt SHA-256
  `58d788bfcddf85a5c85236b99e8fc79d3d05659464936f19ead4bccda936e955`.
  Their artifact, trace, device-log, simulator, environment, and RTL-export
  identities are respectively
  `49e5b5c450f344af632c91dfb901a3658eed28a6208c94ff5a9702d9310bb012`,
  `66df204d8345be202f948578d443057358a030ea25d549d4de41e430e7e094c5`,
  `86d87b7ac86eafac476822459714175fa687d17c334476d28f6c29ba3534795b`,
  `4667dd1352e9c2a48c3397e004a7c3a4f0e179cefc23c0d01e9957339381ff01`,
  `d9963ad4fa4dc0aff9279ae5ed2a20813bd552e669c2b89c6c7299a36627af77`,
  and `146f291d6964835d4bcd9d59b6c9cc2218e94da84afc550301f0c3fe051d9945`.
  Both in-run exports are byte-identical.
- Baseline seed 1 completes 1,536 accepted transactions. Compact seeds 2 and 4
  complete 384 each with 64 active outputs and 192 zero inactive words. All
  active words, checksums, store data, and transaction traces match independent
  Pavane and generic fallback results. Cancellation at strict prefixes 1 and 9
  publishes no output; reset, compact reinstall, and restart succeed. Partial,
  order, duplicate, digest, and busy installation negatives fault without
  mutation.
- The unchanged S01 command
  `./hardware/chisel/run-graph-device-generated-schedule.sh` also passes at the
  implementation commit. The independent schedule raw log SHA-256 is
  `66abdc987c5d47abebc463b97d41cc757b6d00282b70b92c146eef0dc3592824`;
  the execution ABI diff from `origin/main` is empty.
- Initial SP remains 8 and Current SP becomes 13. This AI-work estimate retains
  two failed mutation-owner packets, PM recovery, evidence-integrity closure,
  the necessary 12th regression path, and independent double RTL replay; it is
  not a person-day conversion. The kickoff range trended toward its upper bound
  but total edit time was not sealed. Warm suites measured about 31 seconds for
  77 tests and roughly two minutes per RTL runner. Weekly telemetry read 100
  percent remaining before the final fresh run.
- Evidence class is `rtl-simulation-functional` only. S02 is complete, but
  T-0123 and S03 remain open. No general-DAG, performance, resource, physical,
  FPGA, ASIC, silicon, publication, or T-0044 conclusion follows.
