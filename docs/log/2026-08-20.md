# 2026-08-20

## T-0110 continuous execution and human-confirmation incidents

- The owner directed that Human-confirmation incidents become project rules,
  not a one-off Jitro prompt, and that authorized work continue while none is
  present.
- Accepted ADR-0051. `AGENTS.md` and `docs/WORKFLOW.md` now define eight
  exhaustive incident classes covering authority or scope expansion,
  irreversible experiment or gate boundaries, destructive or remote actions,
  evidence ambiguity, user-work overlap, cost/credentials/legal risk, repeated
  blockers or resource overruns, and material design forks.
- Ordinary bounded local work, local atomic commits, tests, regressions,
  candidate-independent smokes, record updates, and a first operational repair
  are explicit non-incidents. Status reports are non-blocking.
- The Project Manager agent carries the same stop/report rule, and the agent-
  boundary test requires the ADR, non-blocking-status language, exact-approval
  report field, and all eight workflow markers.
- Work occurred in an isolated branch from `origin/main` at `e5264ff`; the
  user's older dirty T-0042 worktree was not edited, stashed, rebased, or
  discarded.
- This governance change modifies no executable Raveil behavior, task P0,
  experiment result, evidence artifact, performance claim, or remote state.
- Closeout on macOS arm64 used repository Python 3. `python3 -m unittest
  tests.test_experiment -v` passed 38 tests in 28.293 seconds;
  `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  passed with 133 Markdown files, 90 task definitions, and 65 records; and
  `git diff --check` exited zero. Evidence class is repository workflow and
  regression verification; no simulator, synthesis, hardware measurement,
  network transfer, or remote mutation ran.
