# Work log — 2026-08-31

## T-0132/S03 AXI execution path kickoff

- T-0132/S02 merged through implementation PR #76 at canonical commit
  `f2c283449ce9071755a9ff1d0ba35834368a7ca0` and closeout PR #77 at
  `fff91700d1d7a4417f37047b165acdaf4b12f5f8`. Issue #75 and its Project item
  are Done; the live queue audit passed with zero active delivery items.
- No Project item was Ready. The PM refined the separately reserved ADR-0067
  S03 boundary instead of returning to blocked physical measurement or jumping
  to Linux/FPGA before an AXI data plane exists. Real Issue #78 and branch
  `feat/t-0132-s03-axi-execution` now bind S-0003, eight SP, one Systems
  Implementer, Tester/Security/Performance/PM support, exact mutation and
  record allowlists, and RTL Simulation evidence.
- The canonical start helper could not use GitHub CLI owner alias `@me` in its
  GraphQL `-F login=@me` argument because `gh` interpreted the value as a file.
  The PM preserved the failure, applied the same validated required fields
  manually with Status last, and reran the live branch audit. It passed with
  T-0132/S03 as the sole active P0.
- Acceptance is deliberately smaller than a platform runtime: 324 execution
  input writes, start, terminal status, 256 authorized private output reads,
  independent-oracle equality, cancel denial, and reset/restart through actual
  AXI pins. Descriptor/implementation digest promotion, arbitrary Graphs,
  Linux, board addresses, FPGA tools, DMA/IRQ, performance, and resource claims
  remain outside the slice.
- Read-only FSM and runtime exploration found two requirements that the S01/S02
  control-only wrapper had not needed: execution output byte `0x1000` requires
  address bit 12 rather than the install namespaces' ten-bit word index, and
  the nested `done`/`cancelled` signals are pulses that require wrapper-sticky
  terminal state for AXI polling. The candidate also holds input/output core
  requests through their complete scratchpad handshakes and blocks AW/W/AR
  admission throughout them.
- The first S02 regression attempt stopped during Scala compilation in private
  artifact `artifacts/graph_device_axi4lite_install/run.BYquSP` because the new
  execution decode referenced `exec` before defining it in the write path. The
  definition was added next to the existing namespace decoders. Corrected S02
  replay `run.OEy5ia` exits zero.
- The first complete S03 offline run
  `artifacts/graph_device_axi4lite_execute/run.QJswNx` exits zero. Through actual
  AXI pins it stages 324 words, completes seed 1, cancels seed 3 without output,
  resets/restarts seed 2, reads two authorized 256-word private outputs, and
  matches every output byte with the independently generated oracle. The exact
  marker labels `rtl-simulation-functional` and `performance=not-measured`.
  S01 private replay `artifacts/graph_device_axi4lite_control/run.ksiVqs` also
  exits zero. S01, S02, and S03 each record identical double-emitted RTL
  manifest SHA-256
  `209f26facf122d6f5e39c792e4ddafc5b016bb2a924670b2b4464f9f2b2d5f6e`.
  Twenty-four focused/regression host tests and shell syntax pass. The
  candidate proceeds to independent review; no platform or performance claim
  follows.
- Independent security and performance review requested three evidence
  corrections before integration: retain the full AXI transaction transcript,
  exercise R backpressure on the new private-output path, and make the claimed
  completion-during-held-cancel-B race executable rather than inferred. The
  harness now records every completed read/write transaction with response and
  held-response cycles, retains the first authorized output R response for four
  cycles while checking stability and blocked admission, and retains cancel B
  for 4,096 cycles so the core finishes before cancellation is applied.
- Intermediate corrected run `run.ImAKuK` was superseded after the verifier
  gained bounded reads plus exact semantic phase and payload ordering checks.
  Final private run `artifacts/graph_device_axi4lite_execute/run.NH03sk` exits
  zero and its
  append-once receipt binds 7,160 strict AXI records under transcript SHA-256
  `fb040b957d9cc42b3ddeae90da4d31db25835b7866bf28c833ba564d4cb433e9`.
  The coverage summary records 972 admitted input writes, 512 authorized output
  reads, three denied output reads, three starts, one cancel, three resets,
  4,100 held-B cycles, and four held-R cycles. Both oracle output hashes remain
  unchanged and the double RTL manifest remains
  `209f26facf122d6f5e39c792e4ddafc5b016bb2a924670b2b4464f9f2b2d5f6e`.
  This remains RTL Simulation Functional evidence with no performance claim.
- Independent Security and Performance re-review both pass with no blocking
  finding. Residual scope is explicit: the transaction trace is not an internal
  waveform or AXI certification, and the result is not Linux, FPGA, physical,
  resource, timing, performance, or arbitrary-Graph evidence.
- Both reviewers then approved immutable implementation head `6cf910e`; PR #79
  merged as canonical commit
  `fc52e47a2770b5751b99117816bb87dff2582c01`. Issue #78 is Closed, its Project
  item is Done, and the live queue audit passes with zero active delivery
  items. T-0132/S03 is complete; this task closeout does not accept the weekly
  S-0003 Sprint Review ceremony.

## T-0132/S04 local AXI-selected candidate

- After S03 closeout, the queue audit passed with zero active delivery items.
  Read-only context routing found the smallest simulation-first continuation:
  reuse the existing T-0123 three-Graph compiler and transport-neutral runtime
  behind `GraphDeviceAxi4LiteTop`, then select it explicitly from the existing
  T-0128 `graph-device run` CLI. Code inspection confirmed that the core already
  consumes installed affine and program payloads; S04 needs an AXI transport
  bridge and evidence/CLI integration, not a new executor or Graph semantics.
- The PM initially stopped because no fresh weekly usage reading was available.
  The owner explicitly instructed the work not to stop. This is retained as a
  bounded HCI-09 owner override for the existing plan and fixed local resources
  only; it grants no credit purchase, plan change, network build, external cost,
  measurement, performance claim, or scope expansion.
- The attempted detailed GitHub work-item publication was rejected by the tool
  safety boundary because it would externally disclose repository architecture
  without a new explicit approval. The safer local alternative uses branch
  `feat/t-0132-s04-axi-selected` from canonical main `9705af2`; it creates no
  Issue/Project item and will not push until that approval exists.
- The Systems implementation added a thin C++ AXI bridge over the existing
  transport-neutral DAG runtime, an offline fixed-image runner and private
  finalizer, and one explicit CLI transport. Primary verification on Darwin
  25.5.0 arm64, Python 3.14.6, Git 2.50.1, and Docker 29.6.2 passed 48 focused
  tests, shell syntax, and `git diff --check`.
- Fixed offline Docker/Verilator run
  `artifacts/graph_device_axi4lite_selected/run.jlX28a` passed 3 Graphs, 4
  completions, 1 cancellation, 1 factory-default restart, 8 rejected invalid
  cases, 4 independent oracle comparisons, and 11,444 complete AXI4-Lite
  transcript records. Receipt SHA-256 is
  `36744413b00b1a2ed554442e2662b72ce76832f1da87e58ec9b41e9180632798`;
  transcript SHA-256 is
  `e266bb9ac12a97613e3c041aff73b012fa421fe40a4077e21ad83ae607e6db5b`.
  A second end-to-end invocation through `python3 -m raveil graph-device run`
  also passed. The CLI now explicitly separates the admitted Graph/seed from
  the full frozen-catalogue execution so it cannot be mistaken for selected
  per-request AXI execution. Evidence is `rtl-simulation-functional` and
  performance is `not-measured`; independent review and remote integration
  remain pending.
- Independent Tester review passed the intended diff, 48 focused regressions,
  and read-only receipt replay. Its one test-isolation finding was corrected by
  replacing an accidental Docker-running unit test with a parser-only test.
  Security review initially found no High and three Medium evidence-integrity
  gaps. The candidate removed an unvalidated trailing reset, fixed the exact
  11,444-record transcript digest and schema, requires the exact runtime log,
  rejects nested links/special files and unregistered evidence files, and
  verifies the ABI identity export. The same-owner deletion/recreation limit is
  now stated explicitly.
- Exact-head Tester re-review passed 48 focused tests, isolated the parser-only
  test without creating an artifact, and replayed `run.jlX28a`. Exact-head
  Security re-review reports High 0 and Medium 0; the explicit same-owner local
  receipt limitation remains Low. A fresh operator-path run
  `artifacts/graph_device_axi4lite_selected/run.yhxZKj` exits zero and renders
  the admitted `vertical-three-point`/seed 7 separately from
  `Execution scope=frozen-catalogue (not one selected invocation)`, followed by
  RTL, oracle, private output, cancel, and factory-restart PASS. Its receipt is
  identical to `run.jlX28a`.
- The repository-wide `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover
  -s tests -q` ran 586 tests with 2 skips but failed one unchanged
  agent-skill contract assertion because the locally installed Sprint reference
  wraps the expected sentence across a newline. The S04 diff does not modify
  that test or external skill reference. This environment/baseline failure is
  retained rather than reported as an S04 pass; all 48 S04 regressions pass.
