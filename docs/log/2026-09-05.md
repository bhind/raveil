# 2026-09-05

## T-0148 simulation-first Graph generality kickoff

- The owner directed the Project Manager to finish the Graph path in
  simulation before returning to physical integration. The latest canonical
  authority is `82b196cb6093aceed5be06d19162660fd20a01f4`.
- Current Codex telemetry reported the 10,080-minute window at 43 percent used
  and 57 percent remaining. No reset credit, paid resource, credential, device
  or vendor tool was used.
- The live queue correctly failed `audit --require-horizon` at zero active and
  zero Ready items. PM allocated real Issue #113 / T-0148, corrected its first
  impossible nine-input acceptance to an exact 16-instruction eight-neighbor
  reduction, added it to Sprint S-0002, and transitioned it through complete
  P1/Ready metadata to P0/In Progress.
- ADR-0084 chooses descriptor schema v2 and program/request version 3 for
  signed relative loads. Direct per-load coordinates avoid a new table
  installer or device aperture. Version 1 and 2 retain byte-identical fixed
  selector behavior.
- The first fixture uses eight loads, seven unsigned maxima and one final
  store. A nine-load binary reduction would require 18 instructions and is
  retained as an explicit capacity limit rather than hidden by the demo.
- Evidence remains pending and, if successful, will be RTL Simulation
  Functional only with performance not measured. No device, FPGA, ASIC,
  silicon, novelty, patent or freedom-to-operate claim follows.

## T-0148 implementation and RTL verification

- One low-tier implementation packet delivered the descriptor/compiler Python
  slice and 25 passing tests, then stopped before hardware. PM retained the
  same task and completed C++ admission/fallback, Chisel installer and executor,
  regressions and records rather than treating partial work as completion.
- The first RTL attempt correctly failed at Chisel elaboration because an
  intermediate `asUInt` result was sliced with invalid Scala syntax. The next
  attempt exposed and fixed an independent Python fallback bug: a decoded
  relative row had shadowed the outer output-row variable. Oracle equality
  detected the shifted first output row before acceptance.
- `python3 -m unittest tests.test_graph_device_dag
  tests.test_graph_device_dynamic tests.test_graph_device_dynamic_sealed`
  passed 41 tests on macOS 26.5.1 arm64 with Python 3.14.6 and the available
  host C++ compiler.
- The exact v3 demo completed `eight-neighbor-dilation-u32`, seed 9, through
  one AXI4-Lite Verilator build. Independent oracle, C++ fallback and RTL output
  all have SHA-256
  `204c0d4f6a480ecef869427d0ab4f86cecee0b2319d08ce4eab05e6eace364f1`.
  The RTL installer also rejected one signed row delta of 2 with
  `output_published=0`.
- A second one-build/two-invocation run passed v1 `five-point` seed 1 and v2
  `cross-dilation-u32` seed 3. Both that pair and the v3 demo used simulator
  SHA-256
  `5be848283e8c0ba455b098f35c35daf2f1ea5afd3b34719660f0b2f92ca6f30b`,
  demonstrating versioned programs rather than per-Graph RTL regeneration.
- `python3 -m unittest discover -s tests` ran 679 tests with two documented
  skips and one unrelated existing failure: the unchanged
  `test_graph_device_uio_dry_run` source-import assertion still rejects the
  `os.open` calls added previously by T-0145. No T-0148-owned file participates
  in that failure. Focused T-0148 and affected Graph-device suites pass.
- Evidence is RTL Simulation Functional and performance remains not measured.
  No external CGRA source was copied, no device or vendor tool was accessed,
  and no FPGA, ASIC, silicon, novelty, patent or freedom-to-operate claim is
  created.
- Independent security review found that `seal()` could initially create a v3
  manifest that the v1/v2-only verifier and UIO projection could never admit.
  The bounded correction now rejects program version 3 before creating any
  sealed bundle, with a regression proving the destination remains empty.

## T-0149 shell-first playable workspace kickoff and first execution

- The owner redirected the immediate product slice toward a system that can be
  edited and repeatedly used from ordinary bash or zsh. CPU/RTL and physical
  research records remain retained, but this development unit is judged by an
  edit-run-observe-change-rerun experience rather than another isolated
  mechanism or user-interview-only activity.
- The dirty root worktree contains an untracked 379-line `raveil/project.py`
  draft on `feat/t-0106-project-workspace`. T-0106 is already allocated to a
  different conditional CPU research task, so PM treated the file only as a
  donor, left the root and its T-0042/agent changes untouched, allocated
  T-0149 and ADR-0085, and created clean branch
  `feat/t-0149-project-workspace` from reviewed T-0148 candidate `bf81269`.
- The donor now connects to the existing CLI in the clean worktree. Thirteen new
  focused tests pass for initialization, Graph inspection, editable/repeated
  command runs, exact recipe retention, history integrity failure, bounded
  command rejection, backend admission, CLI dispatch, console launch,
  private modes, no path-following chmod and project-directory launch.
- On macOS 26.5.1 arm64 / Python 3.14.6, a real project counted two `ERROR`
  lines, retained the first output, accepted an edited third line, retained the
  second output, and displayed changed input/output hashes. Native 8-by-8 GEMM
  and Sonatine/QEMU 8-by-8 GEMM both succeeded. `make -C sonatine` built the
  current RV64 kernel; the actual console booted, returned its bounded `help`
  list and exited from the U-mode shell.
- In the existing local image `raveil-sonatine-graph:t-0085` with networking
  disabled, the same `init`, `show`, command `run`, edit, rerun, `diff`, Native
  GEMM, Sonatine/QEMU GEMM and console flow succeeded. The Linux allowlisted
  tool identities produce a different Command Graph ID from macOS, as expected;
  the operator grammar and results agree.
- Evidence is Host Functional or QEMU Emulation Correctness. No Native/QEMU
  timing comparison, production cache, hostile-input isolation, CPU/RTL
  extension, FPGA, ASIC, silicon, novelty or commercial-readiness claim is
  made. Full regression, independent review and remote integration remain
  pending. `gh auth status` reports the current GitHub token invalid, so no
  real Issue or Project state is fabricated; local work continues while remote
  synchronization stays pending.
- `python3 -m unittest discover -s tests` completed 690 tests with two skips
  and one failure. The failure is the pre-existing T-0145/T-0148-unrelated
  `test_graph_device_uio_dry_run` assertion that scans the unchanged dry-run
  module for `os.open`; no T-0149-owned file participates. The new project
  suite itself remains green.
- UX review initially rejected opaque hash-only output and the checkout-only
  invocation. The corrected CLI prints the bounded text output on each run,
  shows text before/after in `diff`, explains what validation matched, and
  exposes a POSIX `scripts/raveil` launcher that works from the project
  directory on macOS and Linux after one explicit PATH setup.
- Security review initially rejected umask-dependent `0755`/`0644` state, then
  rejected a path-following `chmod` correction. The accepted correction creates
  subdirectories with mode `0700`, artifacts with `0600`, and tightens an
  existing root only through `O_DIRECTORY|O_NOFOLLOW`, identity checks and
  `fchmod`. Security re-review is GO; residual same-user snapshot races remain
  the documented ADR-0035/T-0100 isolation boundary.
- Final hands-on UX re-review is GO after independently exercising three runs:
  input change produced output `2` to `3`, recipe change reduced the Graph from
  three nodes/two edges to two nodes/one edge and produced `4`, with both
  transitions visible in the CLI. Tester review is GO for the focused Native,
  Command Graph, GEMM and QEMU paths, including network-disabled Linux.
- After the final launcher, preview and private-mode changes,
  `python3 -m unittest discover -s tests` ran 693 tests with two skips and the
  same single T-0149-unrelated UIO source-scan failure. The focused project
  suite remains 13/13 passing.
- The final focused command
  `python3 -m unittest tests.test_project tests.test_command_graph
  tests.test_graph_mvp tests.test_native_backend tests.test_sonatine_backend
  tests.test_sonatine_demo` passes 70 tests with one existing opt-in skip.
  Python compilation, POSIX shell syntax, the record checker and
  `git diff --check` also pass.
- Final-code Mac and network-disabled Linux reruns of `raveil project run gemm
  --backend sonatine-qemu` both succeed with trusted baseline checksum
  `c102cc36c72f8e5c`, outcome `abstained`, and explicit
  `qemu-emulation-correctness` evidence. This is correctness/fallback behavior,
  not a speed result.

## T-0148/S02: connect editable project Graphs to RTL simulation

- Owner asked about Graph generality, then approved the next concrete step:
  connect edited Graph JSON to the existing Chisel dynamic executor through
  the T-0149 project loop. Continued the existing Graph lane in clean worktree
  `/private/tmp/raveil-t0148-s02`, branch
  `feat/t-0148-project-dynamic-graph`, on local commits `bf81269` and `31262f1`.
  Primary owns mutations and record integration; independent Security Reviewer
  and Tester performed read-only checks. ADR-0086 records the added backend.
- Fresh account telemetry verified the 10,080-minute window at 54% used,
  46% remaining. No reset credit or paid resource was consumed.
- GitHub authentication recovered. `python3 scripts/project_queue.py audit`
  passes: two active delivery items, zero Ready. They are T-0148 (#113) and
  independent T-0151 (#115). A replacement continuation packet preserving the
  old Issue body was prepared at `/private/tmp/raveil-t0148-continuation-issue.md`.
  The attempted `gh issue edit 113 --repo bhind/raveil --body-file ...` was
  rejected by automatic approval review for lacking explicit approval of that
  Issue mutation. No Issue edit, queue transition, push or merge followed.
  The local implementation remains a reviewable candidate, not a claim of
  completed remote coordination or a third active mutation lane.
- Added `graph-device` recipes with flat descriptor filename and uint32 seed,
  `--backend rtl-sim`, editable relative-load sample, compiled-node explanation,
  private snapshots, copied generated input/full output, active-row text view,
  raw-evidence locator, receipt checks and changed-node/cell/hardware-identity
  diff. Reused existing request compiler/runner/oracle/C++ fallback; Chisel,
  C++ executor, capacity, ISA and transport ABIs were not edited.
- First actual run `20260905T062238Z-61400700c8d5` passed RTL/oracle/fallback
  comparison but the new text adapter wrongly expected only 64 output words.
  It retained a failed project record; the fixed device window is 256 words.
  Corrected projection to active rows/stride and added the full-window test.
  The failed record and raw evidence remain intact.
- Security review found a separate descriptor reread could let the saved
  snapshot, compiled Graph and receipt disagree. Captured bytes once, bound
  them to the initial input manifest and receipt, and checked the input tree
  again before success. A controlled mutation regression now fails closed
  without output publication; independent re-review is GO.
- Actual operator project: `/private/tmp/raveil-graph-play.euOicK`.
  Commands from the candidate checkout:
  `scripts/raveil project init /private/tmp/raveil-graph-play.euOicK`,
  `scripts/raveil project show neighborhood --project /private/tmp/raveil-graph-play.euOicK`,
  and `scripts/raveil project run neighborhood --backend rtl-sim --project /private/tmp/raveil-graph-play.euOicK`.
  The final command was repeated after ordinary descriptor edits:
  - ADD, run `20260905T062511Z-c185dad61195`, raw session `run.uf3qumz1`,
    output SHA-256 `1e04e64bcf847ee61a517d63124dddec6de6702fa914ce71f819de693acd4ddc`.
  - MAX, run `20260905T062719Z-37aa9defdbc4`, raw session `run.o41c78y9`,
    output SHA-256 `e2e622c59a990f2961d831c95b9bafc86e0553ca782ec7ec780af61c247832c7`.
  - MAX with north-east load, run `20260905T063045Z-c9bacdd33a45`, raw session
    `run.8va7ymsh`, output SHA-256
    `fa88fc5b158cf34a4d07640e7104ee6a82a2d0e4ec7a0b7853a2590818d56fcc`.
  Raw roots are under candidate `artifacts/graph_device_axi4lite_dynamic/`;
  each has `request-1/dynamic-receipt.json`, raw logs and simulator artifacts.
  All three share simulator `5be84828...f30b` and RTL manifest `13a274ff...3eb0`.
  The respective first output values are 2802362300, 1788458059 and 3668339974.
  `project diff` for the first pair reports 64/64 changed active cells; the
  second pair reports 36/64. All output/oracle/fallback comparisons pass.
- Primary command `python3 -m unittest tests.test_project_graph
  tests.test_project tests.test_graph_device_dynamic tests.test_graph_device_dag
  tests.test_graph_device_dynamic_sealed tests.test_command_graph` passes 86
  tests. Independent host subset passes 67. Host: macOS 26.5.1 arm64, Python
  3.14.6. Linux read-only/network-none tests in existing
  `raveil-sonatine-graph:t-0085` pass 65/67 with two C++-compiler skips.
  Linux actual `scripts/raveil project init` and `project show neighborhood`
  also pass; this is Host Functional CLI evidence, not another RTL execution.
- After the last diff presentation change, `python3 -m unittest
  tests.test_project_graph tests.test_project` passes 26/26 on Mac and Linux.
  Final independent Linux log:
  `/private/tmp/raveil-t0148-s02-linux-project-final-20260905.log`, SHA-256
  `43e975593df0a18e323c03347974735c26e4f7e9c21f2c23a7a2c04dfa91c273`.
  Debian 12 arm64/Python 3.11.2, offline image
  `sha256:389e7b0ce10e03adaee54d2fb1b7f59782e3b05a1c0f5a8e9781df445fdbd439`.
- Evidence is RTL Simulation Functional for actual Graph runs and Host
  Functional for adapter/CLI checks. The generic simulator is rebuilt each
  run, with equal binary/RTL identities verified; no cache, speed, physical,
  FPGA, ASIC, silicon or general-purpose compiler claim is made. Broader
  inputs, capacity and opcodes remain explicit open questions.

## T-0148/S02: owner-approved Issue continuation publication

- Owner explicitly approved the pending Issue #113 continuation update.
  Fresh weekly telemetry reads 70% used / 30% remaining in the 10,080-minute
  window; no reset or paid resource was used.
- Before writing, reread Issue #113 and found a concurrent inventory handoff
  referencing Draft PR #118 for predecessor `bf81269` and workspace Issue
  #116. Preserved the complete current Issue body, including that handoff,
  below the approved continuation packet and local `7cc94b2` verification
  summary. This corrects the older observation that all parents were unpushed.
- `gh issue edit 113 --repo bhind/raveil --body-file
  /private/tmp/raveil-t0148-continuation-approved.md` succeeded. A subsequent
  `gh issue view 113 --repo bhind/raveil --json body,state` confirms the Issue
  remains Open and the payload matches apart from one trailing newline.
  No Issue closure, Project transition, code push or merge was performed.
- `python3 scripts/project_queue.py audit` passes after the update: one active,
  one Ready and one pullable-Ready item. Local STATUS/TODO are reconciled with
  the applied packet; the previous rejected attempt remains recorded above.
