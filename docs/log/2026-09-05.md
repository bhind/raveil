# 2026-09-05

## T-0158 GitHub Project progress configuration

- The dirty historical `feat/t-0106-project-workspace` root was inspected and
  left untouched. Work used a clean current-main clone and
  `chore/t-0158-project-insights`.
- GitHub CLI 2.97.0 was reauthenticated through device flow; no token or secret
  entered the repository. The live private Project initially had 102 items,
  37 fields, and two views. Issue #135 entered the canonical queue as T-0158,
  S-0003, three SP, Project Manager, Operations, Host Functional.
- Four fields and seven views were created from returned live identifiers.
  Official documentation and live schema confirm standard current/historical
  Insights and the default Burn up, but expose no chart mutation; the
  documented REST route returned 404 for this user-owned Project.
- Hosted Actions, Pages, visibility, external services, canonical evidence,
  and Gate state were not changed. Evidence is live GitHub coordination and
  host-functional governance only.

## T-0155 verified dependency scheduling

- T-0156 PR #132 merged as `3d1f786` at 11:50:22 UTC. Canonical completion
  succeeded for Issue #131. Its disjoint change was merged into the Graph
  worktree before final integration; no source conflict occurred.
- The compiler now performs stable topological scheduling; ready-node ties
  prefer descriptor list order. Existing valid ordered graphs keep their
  allocation and program/trace bytes. Unknown dependencies, cycles, duplicate
  IDs, STORE-as-operand and non-final STORE remain rejected. ADR-0088 records
  the accepted admission change and schedule-index interpretation without
  replacing the trace schema or hardware ABI.
- Implementer `python3 -m unittest discover -s tests -p 'test_graph_device*.py'`
  passes 169 tests in 33.041 seconds; `python3 -m unittest
  tests.test_project_graph tests.test_garden_tui` passes 44 tests in 0.802s.
  Host: macOS 26.5.1 arm64, Python 3.14.6. Independent read-only review is GO.
- Primary loaded the prior module from `git show
  92ac1ae:raveil/graph_device_dag.py` into a separate Python module and compared
  every previously admitted descriptor in tests/fixtures: four preserve exact
  instructions, payload, program hash and lowering trace. Primary then enumerated
  `itertools.permutations(descriptors()[1]['nodes'][:-1])`, retaining the final
  STORE and seed 5. All 120 schedules match the previous module's direct oracle
  and the current encoded software fallback. This guards shared-scheduler errors
  with a prior implementation and fixed equivalent workloads; it is not RTL data.
- New tests also reject tampered schedule traces and preserve the original
  canonical descriptor hash. No Chisel or C++ execution source changed.
  PM keeps evidence classes, resource boundaries and S-0003 ceremony pending.
- After main reconciliation, primary `python3 -m unittest
  tests.test_graph_device_dag tests.test_project tests.test_project_graph
  tests.test_garden_tui` passes 76 tests in 4.059 seconds, exit 0. Record
  checker passes 187 Markdown files/130 task definitions/99 records;
  `git diff --check` passes. The reviewed source has no further changes.

## T-0155/T-0156 parallel continuation and temporary usage exception

- The owner explicitly requested parallel Graph generalization and workspace
  usability work. Main `92ac1ae` is the clean base; T-0154/#128 is closed after
  PR #129. T-0155/#130 and T-0156/#131 were registered and canonically started
  in S-0003 with disjoint source files and isolated worktrees. PM owns serial
  record integration and merges; sol low owns each implementation/publication.
- At 11:42:55 UTC the 10,080-minute usage window reported 97% used. Both
  workers preserved their state. The owner's subsequent explicit instruction
  temporarily waived the weekly usage stop for this continuation; work resumed
  without applying a reset, buying capacity or changing the standing ADR-0060.
- T-0156 reproducer: init a directory named `work space; false`; the old
  unquoted Next hint fails in `/bin/sh`. `shlex.quote` now renders the target,
  and the regression executes the printed hint through the shell successfully.
- Primary `python3 -m unittest tests.test_project tests.test_project_graph`
  passes 28 tests in 4.137 seconds, exit 0. `git diff --check` passes. Host is
  macOS 26.5.1 arm64/Python 3.14.6. Prior implementer project-only run passed
  15 tests. No RTL or full-suite rebuild is necessary for this rendering fix.
- Live canonical audit passes with two active items and zero Ready. The
  Graph compiler slice still requires its tests, ADR and primary acceptance.

## T-0148/S02 final main integration

- PR #123 was marked ready and merged at 10:14:39 UTC as `4cc28ed`; Issue
  #113 closed and canonical Project completion succeeded. This completes
  T-0148's technical integration. S-0003 owner-visible review remains pending.
- T-0154 / Issue #128 was opened as a one-point documentation-only correction
  after live readback found stale active/unmerged wording in current-authority
  records. It changes no executable, ADR/EXP, research gate or review outcome.

- PR #119 merged workspace head `7aa6ed6` as `3fb6b5c` at 10:02:22 UTC.
  Canonical completion of Issue #116 succeeded; S-0003 ceremony is not
  accepted by technical completion. Continued immediately to PR #123.
- Merged main `3fb6b5c` into the continuation, preserving both dated histories,
  main's nonempty-init regression fix and canonical T-0152 UIO resolution.
  Removed duplicate task definitions. The branch-local Graph ADR-0086 becomes
  ADR-0087 with explicit provenance, unchanged design and original Git history;
  canonical inventory ADR-0086 is untouched.
- Primary inspected the project/adapter diff and retained graph source. No
  Chisel, C++ executor, transport ABI, opcode or performance conclusion changes.
  The earlier three actual edited-Graph RTL runs remain earlier source-labelled
  evidence; they were not rerun or relabelled as new integrated measurements.
- Primary `python3 -m unittest discover -s tests` exits 0: 743 tests,
  two skips, no failures, 123.794 seconds on macOS 26.5.1 arm64/Python 3.14.6.
  Log `/private/tmp/raveil-t0148-s02-integrated-full.log`, SHA-256
  `eaae7fca92e629d7567f52134e2787aec0823ec5f53a81603c14be2f4d59a3fd`.
- Independent `python3 -m unittest -v tests.test_project tests.test_project_graph
  tests.test_graph_device_dynamic tests.test_graph_device_dynamic_sealed`
  passes 54/54 in 11.427 seconds. Log
  `/private/tmp/raveil-t0148-s02-final-focused-20260905.log`, SHA-256
  `40021b265662be5bbd232c1dc0e96e52eeb5ebf1f67cc7309ecc31c6a0c9df9f`.
  Read-only review found no source correctness/security regression.
- Linux `docker run --rm --network none --mount
  type=bind,src=/private/tmp/raveil-t0148-s02,dst=/repo,readonly -w /repo
  raveil-sonatine-graph:t-0085 python3 -m unittest -v tests.test_project`
  passes 14/14. Existing image, Debian 12 arm64/Python 3.11.2; no download,
  build or device access. Log
  `/private/tmp/raveil-t0148-s02-final-linux-project-20260905.log`, SHA-256
  `6266618fa72da9c8b987cf2d8c2059fb0b30d511310822d711244f17cdd7cca4`.
- Record checker and `git diff --check` pass. Live queue audit: one active,
  zero Ready. At 10:08:58 UTC verified weekly telemetry is 92% used / 8%
  remaining, 10,080-minute window. Push is delegated to sol/low; primary
  retains exact-head acceptance, canonical completion and merge authority.

## T-0148 simulation-first Graph generality kickoff

- The owner directed the Project Manager to finish the Graph path in
  simulation before returning to physical integration. The latest canonical
  authority is `82b196cb6093aceed5be06d19162660fd20a01f4`.
- Current Codex telemetry reported the 10,080-minute window at 43 percent used
  and 57 percent remaining. No reset credit, paid resource, credential, device
  or vendor tool was used.
- The live queue correctly failed `audit --require-horizon` at zero active and
  zero Ready items. PM allocated real Issue #113 / T-0148, corrected its first
  impossible nine-input acceptance to an exact 16-instruction eight-neighbor
  reduction, added it to Sprint S-0002, and transitioned it through complete
  P1/Ready metadata to P0/In Progress.
- ADR-0084 chooses descriptor schema v2 and program/request version 3 for
  signed relative loads. Direct per-load coordinates avoid a new table
  installer or device aperture. Version 1 and 2 retain byte-identical fixed
  selector behavior.
- The first fixture uses eight loads, seven unsigned maxima and one final
  store. A nine-load binary reduction would require 18 instructions and is
  retained as an explicit capacity limit rather than hidden by the demo.
- Evidence remains pending and, if successful, will be RTL Simulation
  Functional only with performance not measured. No device, FPGA, ASIC,
  silicon, novelty, patent or freedom-to-operate claim follows.

## T-0148 implementation and RTL verification

- One low-tier implementation packet delivered the descriptor/compiler Python
  slice and 25 passing tests, then stopped before hardware. PM retained the
  same task and completed C++ admission/fallback, Chisel installer and executor,
  regressions and records rather than treating partial work as completion.
- The first RTL attempt correctly failed at Chisel elaboration because an
  intermediate `asUInt` result was sliced with invalid Scala syntax. The next
  attempt exposed and fixed an independent Python fallback bug: a decoded
  relative row had shadowed the outer output-row variable. Oracle equality
  detected the shifted first output row before acceptance.
- `python3 -m unittest tests.test_graph_device_dag
  tests.test_graph_device_dynamic tests.test_graph_device_dynamic_sealed`
  passed 41 tests on macOS 26.5.1 arm64 with Python 3.14.6 and the available
  host C++ compiler.
- The exact v3 demo completed `eight-neighbor-dilation-u32`, seed 9, through
  one AXI4-Lite Verilator build. Independent oracle, C++ fallback and RTL output
  all have SHA-256
  `204c0d4f6a480ecef869427d0ab4f86cecee0b2319d08ce4eab05e6eace364f1`.
  The RTL installer also rejected one signed row delta of 2 with
  `output_published=0`.
- A second one-build/two-invocation run passed v1 `five-point` seed 1 and v2
  `cross-dilation-u32` seed 3. Both that pair and the v3 demo used simulator
  SHA-256
  `5be848283e8c0ba455b098f35c35daf2f1ea5afd3b34719660f0b2f92ca6f30b`,
  demonstrating versioned programs rather than per-Graph RTL regeneration.
- `python3 -m unittest discover -s tests` ran 679 tests with two documented
  skips and one unrelated existing failure: the unchanged
  `test_graph_device_uio_dry_run` source-import assertion still rejects the
  `os.open` calls added previously by T-0145. No T-0148-owned file participates
  in that failure. Focused T-0148 and affected Graph-device suites pass.
- Evidence is RTL Simulation Functional and performance remains not measured.
  No external CGRA source was copied, no device or vendor tool was accessed,
  and no FPGA, ASIC, silicon, novelty, patent or freedom-to-operate claim is
  created.
- Independent security review found that `seal()` could initially create a v3
  manifest that the v1/v2-only verifier and UIO projection could never admit.
  The bounded correction now rejects program version 3 before creating any
  sealed bundle, with a regression proving the destination remains empty.

## T-0149 shell-first playable workspace kickoff and first execution

- The owner redirected the immediate product slice toward a system that can be
  edited and repeatedly used from ordinary bash or zsh. CPU/RTL and physical
  research records remain retained, but this development unit is judged by an
  edit-run-observe-change-rerun experience rather than another isolated
  mechanism or user-interview-only activity.
- The dirty root worktree contains an untracked 379-line `raveil/project.py`
  draft on `feat/t-0106-project-workspace`. T-0106 is already allocated to a
  different conditional CPU research task, so PM treated the file only as a
  donor, left the root and its T-0042/agent changes untouched, allocated
  T-0149 and ADR-0085, and created clean branch
  `feat/t-0149-project-workspace` from reviewed T-0148 candidate `bf81269`.
- The donor now connects to the existing CLI in the clean worktree. Thirteen new
  focused tests pass for initialization, Graph inspection, editable/repeated
  command runs, exact recipe retention, history integrity failure, bounded
  command rejection, backend admission, CLI dispatch, console launch,
  private modes, no path-following chmod and project-directory launch.
- On macOS 26.5.1 arm64 / Python 3.14.6, a real project counted two `ERROR`
  lines, retained the first output, accepted an edited third line, retained the
  second output, and displayed changed input/output hashes. Native 8-by-8 GEMM
  and Sonatine/QEMU 8-by-8 GEMM both succeeded. `make -C sonatine` built the
  current RV64 kernel; the actual console booted, returned its bounded `help`
  list and exited from the U-mode shell.
- In the existing local image `raveil-sonatine-graph:t-0085` with networking
  disabled, the same `init`, `show`, command `run`, edit, rerun, `diff`, Native
  GEMM, Sonatine/QEMU GEMM and console flow succeeded. The Linux allowlisted
  tool identities produce a different Command Graph ID from macOS, as expected;
  the operator grammar and results agree.
- Evidence is Host Functional or QEMU Emulation Correctness. No Native/QEMU
  timing comparison, production cache, hostile-input isolation, CPU/RTL
  extension, FPGA, ASIC, silicon, novelty or commercial-readiness claim is
  made. Full regression, independent review and remote integration remain
  pending. `gh auth status` reports the current GitHub token invalid, so no
  real Issue or Project state is fabricated; local work continues while remote
  synchronization stays pending.
- `python3 -m unittest discover -s tests` completed 690 tests with two skips
  and one failure. The failure is the pre-existing T-0145/T-0148-unrelated
  `test_graph_device_uio_dry_run` assertion that scans the unchanged dry-run
  module for `os.open`; no T-0149-owned file participates. The new project
  suite itself remains green.
- UX review initially rejected opaque hash-only output and the checkout-only
  invocation. The corrected CLI prints the bounded text output on each run,
  shows text before/after in `diff`, explains what validation matched, and
  exposes a POSIX `scripts/raveil` launcher that works from the project
  directory on macOS and Linux after one explicit PATH setup.
- Security review initially rejected umask-dependent `0755`/`0644` state, then
  rejected a path-following `chmod` correction. The accepted correction creates
  subdirectories with mode `0700`, artifacts with `0600`, and tightens an
  existing root only through `O_DIRECTORY|O_NOFOLLOW`, identity checks and
  `fchmod`. Security re-review is GO; residual same-user snapshot races remain
  the documented ADR-0035/T-0100 isolation boundary.
- Final hands-on UX re-review is GO after independently exercising three runs:
  input change produced output `2` to `3`, recipe change reduced the Graph from
  three nodes/two edges to two nodes/one edge and produced `4`, with both
  transitions visible in the CLI. Tester review is GO for the focused Native,
  Command Graph, GEMM and QEMU paths, including network-disabled Linux.
- After the final launcher, preview and private-mode changes,
  `python3 -m unittest discover -s tests` ran 693 tests with two skips and the
  same single T-0149-unrelated UIO source-scan failure. The focused project
  suite remains 13/13 passing.
- The final focused command
  `python3 -m unittest tests.test_project tests.test_command_graph
  tests.test_graph_mvp tests.test_native_backend tests.test_sonatine_backend
  tests.test_sonatine_demo` passes 70 tests with one existing opt-in skip.
  Python compilation, POSIX shell syntax, the record checker and
  `git diff --check` also pass.
- Final-code Mac and network-disabled Linux reruns of `raveil project run gemm
  --backend sonatine-qemu` both succeed with trusted baseline checksum
  `c102cc36c72f8e5c`, outcome `abstained`, and explicit
  `qemu-emulation-correctness` evidence. This is correctness/fallback behavior,
  not a speed result.

## T-0148/S02: connect editable project Graphs to RTL simulation

- Owner asked about Graph generality, then approved the next concrete step:
  connect edited Graph JSON to the existing Chisel dynamic executor through
  the T-0149 project loop. Continued the existing Graph lane in clean worktree
  `/private/tmp/raveil-t0148-s02`, branch
  `feat/t-0148-project-dynamic-graph`, on local commits `bf81269` and `31262f1`.
  Primary owns mutations and record integration; independent Security Reviewer
  and Tester performed read-only checks. ADR-0086 records the added backend.
- Fresh account telemetry verified the 10,080-minute window at 54% used,
  46% remaining. No reset credit or paid resource was consumed.
- GitHub authentication recovered. `python3 scripts/project_queue.py audit`
  passes: two active delivery items, zero Ready. They are T-0148 (#113) and
  independent T-0151 (#115). A replacement continuation packet preserving the
  old Issue body was prepared at `/private/tmp/raveil-t0148-continuation-issue.md`.
  The attempted `gh issue edit 113 --repo bhind/raveil --body-file ...` was
  rejected by automatic approval review for lacking explicit approval of that
  Issue mutation. No Issue edit, queue transition, push or merge followed.
  The local implementation remains a reviewable candidate, not a claim of
  completed remote coordination or a third active mutation lane.
- Added `graph-device` recipes with flat descriptor filename and uint32 seed,
  `--backend rtl-sim`, editable relative-load sample, compiled-node explanation,
  private snapshots, copied generated input/full output, active-row text view,
  raw-evidence locator, receipt checks and changed-node/cell/hardware-identity
  diff. Reused existing request compiler/runner/oracle/C++ fallback; Chisel,
  C++ executor, capacity, ISA and transport ABIs were not edited.
- First actual run `20260905T062238Z-61400700c8d5` passed RTL/oracle/fallback
  comparison but the new text adapter wrongly expected only 64 output words.
  It retained a failed project record; the fixed device window is 256 words.
  Corrected projection to active rows/stride and added the full-window test.
  The failed record and raw evidence remain intact.
- Security review found a separate descriptor reread could let the saved
  snapshot, compiled Graph and receipt disagree. Captured bytes once, bound
  them to the initial input manifest and receipt, and checked the input tree
  again before success. A controlled mutation regression now fails closed
  without output publication; independent re-review is GO.
- Actual operator project: `/private/tmp/raveil-graph-play.euOicK`.
  Commands from the candidate checkout:
  `scripts/raveil project init /private/tmp/raveil-graph-play.euOicK`,
  `scripts/raveil project show neighborhood --project /private/tmp/raveil-graph-play.euOicK`,
  and `scripts/raveil project run neighborhood --backend rtl-sim --project /private/tmp/raveil-graph-play.euOicK`.
  The final command was repeated after ordinary descriptor edits:
  - ADD, run `20260905T062511Z-c185dad61195`, raw session `run.uf3qumz1`,
    output SHA-256 `1e04e64bcf847ee61a517d63124dddec6de6702fa914ce71f819de693acd4ddc`.
  - MAX, run `20260905T062719Z-37aa9defdbc4`, raw session `run.o41c78y9`,
    output SHA-256 `e2e622c59a990f2961d831c95b9bafc86e0553ca782ec7ec780af61c247832c7`.
  - MAX with north-east load, run `20260905T063045Z-c9bacdd33a45`, raw session
    `run.8va7ymsh`, output SHA-256
    `fa88fc5b158cf34a4d07640e7104ee6a82a2d0e4ec7a0b7853a2590818d56fcc`.
  Raw roots are under candidate `artifacts/graph_device_axi4lite_dynamic/`;
  each has `request-1/dynamic-receipt.json`, raw logs and simulator artifacts.
  All three share simulator `5be84828...f30b` and RTL manifest `13a274ff...3eb0`.
  The respective first output values are 2802362300, 1788458059 and 3668339974.
  `project diff` for the first pair reports 64/64 changed active cells; the
  second pair reports 36/64. All output/oracle/fallback comparisons pass.
- Primary command `python3 -m unittest tests.test_project_graph
  tests.test_project tests.test_graph_device_dynamic tests.test_graph_device_dag
  tests.test_graph_device_dynamic_sealed tests.test_command_graph` passes 86
  tests. Independent host subset passes 67. Host: macOS 26.5.1 arm64, Python
  3.14.6. Linux read-only/network-none tests in existing
  `raveil-sonatine-graph:t-0085` pass 65/67 with two C++-compiler skips.
  Linux actual `scripts/raveil project init` and `project show neighborhood`
  also pass; this is Host Functional CLI evidence, not another RTL execution.
- After the last diff presentation change, `python3 -m unittest
  tests.test_project_graph tests.test_project` passes 26/26 on Mac and Linux.
  Final independent Linux log:
  `/private/tmp/raveil-t0148-s02-linux-project-final-20260905.log`, SHA-256
  `43e975593df0a18e323c03347974735c26e4f7e9c21f2c23a7a2c04dfa91c273`.
  Debian 12 arm64/Python 3.11.2, offline image
  `sha256:389e7b0ce10e03adaee54d2fb1b7f59782e3b05a1c0f5a8e9781df445fdbd439`.
- Evidence is RTL Simulation Functional for actual Graph runs and Host
  Functional for adapter/CLI checks. The generic simulator is rebuilt each
  run, with equal binary/RTL identities verified; no cache, speed, physical,
  FPGA, ASIC, silicon or general-purpose compiler claim is made. Broader
  inputs, capacity and opcodes remain explicit open questions.

## T-0148/S02: owner-approved Issue continuation publication

- Owner explicitly approved the pending Issue #113 continuation update.
  Fresh weekly telemetry reads 70% used / 30% remaining in the 10,080-minute
  window; no reset or paid resource was used.
- Before writing, reread Issue #113 and found a concurrent inventory handoff
  referencing Draft PR #118 for predecessor `bf81269` and workspace Issue
  #116. Preserved the complete current Issue body, including that handoff,
  below the approved continuation packet and local `7cc94b2` verification
  summary. This corrects the older observation that all parents were unpushed.
- `gh issue edit 113 --repo bhind/raveil --body-file
  /private/tmp/raveil-t0148-continuation-approved.md` succeeded. A subsequent
  `gh issue view 113 --repo bhind/raveil --json body,state` confirms the Issue
  remains Open and the payload matches apart from one trailing newline.
  No Issue closure, Project transition, code push or merge was performed.
- `python3 scripts/project_queue.py audit` passes after the update: one active,
  one Ready and one pullable-Ready item. Local STATUS/TODO are reconciled with
  the applied packet; the previous rejected attempt remains recorded above.
## S-0002 owner-visible review and daily-operation correction

- Authority is main `82b196cb6093aceed5be06d19162660fd20a01f4`, confirmed
  against live remote main. The dirty historical root remains unchanged.
- The initial conversation review incorrectly used that historical root;
  its assessment was explicitly withdrawn after checking current main and
  the live Project. Garden fusion was already accepted on 2026-08-29 and
  is not counted as new September delivery.
- The owner navigated Garden on Mac Terminal, reported clipped/verbose
  display and difficulty seeing the change, then inspected the actual native
  result. `python3 -m raveil graph-mvp --family gemm_bias_relu --m 8 --n 8
  --k 8 --output RESULT_JSON` exited 0 on macOS arm64/Python 3.14.6.
  Both baseline and loop-ikj-fused reported semantic validity and checksum
  `ab4fc57fa8a47521`; outcome was `rolled-back` because the short development
  observation did not improve. No performance result follows.
- `python3 -m unittest tests.test_graph_device_dynamic_uio -q` passed 4
  tests in 3.325 seconds. The prior 179-file/121-task/94-record checker
  passed. These checks did not rerun RTL.
- The owner explicitly said `レビュー承認`, requested more intuitive Garden
  presentation, a Graph generality demo next Sprint, Project updates and
  daily task-consumption reconciliation without reminders.
- T-0150 / Issue #114 retains the visual-feedback task; T-0151 / Issue #115
  implements daily factual reconciliation; existing T-0148 / Issue #113
  retains its independent Chisel implementation with S-0003 demo forecast.
  T-0149 is already reserved by a separate local branch and is not reused.
- S-0003 dates are 2026-09-07 through 2026-09-13; next Saturday ceremony
  is 2026-09-12 JST. Do not infer implementation completion from the forecast.
- Current weekly Codex reading at 2026-09-05T06:06:42Z used 52 percent of
  the 10,080-minute window, leaving 48 percent. No model invocation or new
  paid service is required by the deterministic daily reporter.
- Independent PM advice identified canonical complete limitations: multiple
  stale closed items can block each other; merged In Progress items cannot
  complete; existing Done returns before metadata repair. Daily factual
  reporting preserves these as findings rather than bypassing lifecycle
  authority. The existing 39 queue tests passed in the read-only review.
- Keep: actual owner-visible interaction and evidence-grounded explanation.
  Problem: stale-root review and reminder-driven metadata maintenance.
  Try: T-0151 daily reconciliation plus immediate per-task closeout. T-0150
  is separately tracked product feedback, not a second process experiment.

## Project reflection and daily delivery implementation

- The S-0002 ceremony now records the owner's explicit Accept and is Done.
  S-0003's 2026-09-12 Graph/visual-fusion demo is scheduled in Backlog while
  its dependencies remain unfinished. T-0148 retains its original active
  Sprint; Forecast Sprint/Date name the owner-directed next demo.
- T-0150 was prepared through the canonical queue as the one P1/Ready
  successor; T-0151 was prepared and started as the independent second
  active lane. Live horizon audit passes: two active, one Ready and one
  pullable-Ready. No direct lifecycle bypass was used.
- The Project README's stale no-active-P0 paragraph was replaced by current
  review/daily-operation routing. T-0146's missing review summary was
  backfilled from canonical acceptance evidence, without inventing an
  acceptance time.
- The remote Project Review Outcome field rejected an overlong summary;
  a bounded summary was written before ceremony Done. The daily reporter
  consequently preserves and reports evidence overflow rather than truncating
  existing text.
- Independent PM scheduler review recommends RunAtLoad and a 900-second local
  due check, with the due date calculated at 19:00 Asia/Tokyo, one held lock,
  last-attempt persistence before execution, atomic success state, bounded
  timeout and hourly failure retry. A local stub smoke passed due-date,
  duplicate-suppression, forced-replay and failure-state checks. This was not
  a remote scheduled execution.
- ROADMAP, OPEN_QUESTIONS and accepted ADR/EXP boundaries were reviewed; no
  gate, architecture, experiment, physical-device or evidence promotion is
  changed by this daily factual reporting work.

- The first live read-only daily inventory planned 33 factual event updates
  and exposed 12 Done items with empty Review Outcome plus Issue #99 without
  a closing-reference PR association. PM inspected canonical records and
  backfilled the 12 existing acceptance summaries; no new acceptance or
  event date was invented. Canonical STATUS explicitly maps Issue #99 to
  merged PR #100, so the reporter must describe the missing API association
  without denying that retained evidence.
- Independent Tester reproduced two rejected candidate defects before live
  apply: invalid usage timestamps were accepted by the mutation preflight,
  and text-buffered JSON-RPC could hide a second response already read into
  memory. Failed reproduction receipts remain in ignored artifacts. Both
  require regression fixes before scheduled execution.

- Corrected candidate passes 16 daily tests plus all 39 existing queue
  tests. Independent testing verified rejection of malformed usage time
  before API calls and successful coalesced JSON-RPC response handling.
- A broad `gh project item-list --limit 1000` hit the live GraphQL quota
  before writes. The reporter now uses a narrow paginated inventory, a
  narrow per-item text preflight and one final batch readback. A generated
  GraphQL brace error was rejected in a read-only run and corrected before
  any write. The final live dry-run exited 0, planned 32 event-field updates
  and retained one explicit missing closing-reference association for
  Issue #99. No API failure was labelled a successful reconciliation.
- Fresh weekly telemetry at 2026-09-05T06:30:45Z reports 60 percent used
  and 40 percent remaining in the 10,080-minute window.

- Final narrow GraphQL view, field lookup and mutations passed independent
  review and `python3 -m unittest tests.test_project_daily
  tests.test_project_queue` (56 tests, exit 0). The governance record checker
  passed 180 Markdown files, 124 tasks and 94 records; `git diff --check`
  passed. Independent daily-test log SHA-256 is
  `ed58c886187ddf3ca1a449ecef9f545cf493175efb5f2c56b14edefb43a91231`.
  Environment: macOS 26.5.1 build 25F80, Python 3.14.6.
- Project API writes paused after the actual GraphQL rate-limit response
  showed reset at 2026-09-05T06:53:54Z. The subsequent Mac installation was
  staged with `plutil -lint` exit 0 before enabling scheduled execution.

## T-0151 initial Project inventory and mandatory session synchronization

- Owner explicitly authorized actual Project registration, repair, consolidation,
  preservation and session-level upkeep. Used task-governance and current
  queue/WORKFLOW records. Base is canonical `82b196c`; dedicated PM branch is
  `docs/t-0151-project-inventory`. Existing dirty root, committed workspace
  donor and separate uncommitted daily automation are untouched.
- Repository-linked Project discovery returns only bhind Project #1, private
  Raveil Weekly Sprints. Authorized `gh auth status` succeeds with repo/project
  permissions; earlier sandbox-only invalid-token output is not a confirmed
  expired credential. No secret is retained here.
- Current account telemetry reports weekly window 10080 minutes, 55% used and
  45% remaining. No new paid service or build is used. Evidence class is Host
  Functional governance/read-only metadata, not research performance.
- T-0106 and ADR-0046 are already allocated to conditional CPU hardening and
  controlled-run closure. Workspace is local T-0149/ADR-0085. Local T-0150
  defect collides with real Garden #114, so the defect is T-0152/#117.
- Exact baseline command: `python3 -m unittest
  tests.test_graph_device_uio_dry_run` on macOS/arm64, Python 3.14.6, canonical
  `82b196c`; exit 1, three tests, one source-scan failure at line 48. No fix is
  claimed. The failure checks blanket `os.open` text; the source opens the
  repository directory for identity verification. T-0152 must verify the
  correct boundary before changing the assertion.
- Actual mutations and per-item state/record mapping are retained in
  `docs/guides/PROJECT-INVENTORY-2026-09-05.md`. No new Done is awarded by this
  inventory. Parent T-0151 stays unfinished until daily automation and local
  schedule acceptance are independently verified.

- Initial canonical `review 113 --pr 118` dry-run passed, but its apply failed
  during the stock `gh project item-list --limit 200` read with a GraphQL quota
  error. The Project state was unchanged. The GraphQL endpoint then reported
  83/5000 remaining, reset at 2026-09-05 06:53:54 UTC; a minimal field query
  succeeded at cost 1. REST's rate-limit response conflicted and was not used
  to override the GraphQL result.
- The Issue #115 packet was expanded before mutation for a bounded correction
  to `scripts/project_queue.py` and its existing tests, owned by PM in this
  isolated branch. The explicit read preserves all lifecycle functions and
  normalizes the same fields while rejecting incomplete or drifting pages.
  `python3 -m unittest tests.test_project_queue`: 44 tests, exit 0.
- `python3 -m unittest tests.test_experiment.AgentBoundaryTests`: four tests,
  exit 0. An earlier guessed module name `tests.test_agent_queue_boundaries`
  failed import; the actual four-test class above is the corrected command.
- Record checker passes (182 Markdown files, 126 task definitions, 95 records)
  and `git diff --check` passes. No full product/RTL suite is rerun for the
  documentation and queue-read correction.

- First explicit-field live read failed closed on GitHub repeating the built-in
  Title field. The corrected reader allows only identical content/field titles
  and rejects disagreement. The regression fixture includes that representation.
  The rerun of `project_queue.py audit --check-branch` passes: two active,
  one Ready, one pullable Ready. This does not start another product lane.
- Small Issue #113/#116/#117 body corrections succeeded within the remaining
  quota; the earlier failed attempt is retained as provenance.

- A second canonical Review apply failed before mutation at the separate stock
  `gh project field-list --limit 100` call. The same bounded correction now
  reads the explicit field schema and option IDs, refusing truncation or
  duplicate/unknown fields. `python3 -m unittest tests.test_project_queue
  tests.test_experiment.AgentBoundaryTests`: 50 tests, exit 0. Lifecycle
  transition methods remain unchanged.
- Project README now retains its existing daily-review content and an appended
  dated inventory section with actual Issue/PR links and next-workspace focus.
  This is an applied remote write; neither a promise nor task completion.

- Independent Tester rejected malformed field/option IDs that could survive
  preflight and fail only after an earlier metadata write. PM corrected the
  complete schema validation and added a start-path regression asserting zero
  edits. Independent re-review passes: 51 tests in normal and clean
  environments; clean-log SHA-256
  `26ba8d1468e62ebe1cf2a29144335017b4cb98edcb8465e3e8cba53b6dcb946f`.
- The canonical `review 113 --pr 118 --apply` now succeeds. Final
  `audit --check-branch` passes and readback at 06:45 UTC / 15:45 JST has 97
  visible items: 52 Done, 40 Backlog, 2 Blocked, 1 Review, 1 Ready, 1 In
  Progress. All unfinished TODO IDs map to a card/child set. No new Done was
  awarded by this inventory; #115 remains In Progress for its daily acceptance.
- Final account reading: weekly 10080 minutes, 65% used / 35% remaining. No
  permission is missing; initial GraphQL budget failures are resolved for the
  inventory transitions through the bounded canonical read correction.

## T-0151 inventory integration closeout

- Exact candidate `01a427c9020b77befd56b1dd1ae031283af34237` received independent
  Tester GO. PR #121 was non-draft, mergeable/CLEAN, based on unchanged
  canonical `82b196c`, with no unresolved review threads or pending checks.
  Its inspected diff contains only the declared queue/tests and records.
  Owner-authorized scope and ADR-0058 permit the incident-free merge.
- PR #121 squash-merged as canonical
  `75bf2757016debdfc8aa2fabcfffa5b584277ce5`; `git fetch origin` confirmed it.
  `git diff --exit-code 01a427c origin/main -- scripts/project_queue.py
  tests/test_project_queue.py AGENTS.md
  .agents/skills/raveil-task-governance/SKILL.md` exits 0, confirming the
  canonical blobs are the reviewed ones. The original dirty root's 18-file
  status inventory remains present, including four agent changes, T-0042
  changes and the untracked project.py donor. No file was discarded/stashed.
- Post-merge attempt to read Issue #115 before appending the integration
  receipt failed with GraphQL API quota already exceeded. Target: #115 body;
  intended delta: PR #121/main hash, tested inventory slice, link to this
  receipt, and the still-unfinished #120 daily implementation. No body update
  occurred at this attempt. No permission is missing; retry only after the
  reported reset, 2026-09-05 06:53:54 UTC / 15:53:54 JST.
- Dedicated `docs/t-0151-inventory-closeout` starts from canonical `75bf275`
  to reconcile the merged state without touching the independently dirty
  daily or product worktrees. This is still the same #115 PM inventory scope.

- While awaiting API reset, PM independently verified the untouched T-0149
  candidate `31262f14e37090df500ab30eb13819c9872a92ed` with
  `python3 -m unittest tests.test_project tests.test_command_graph
  tests.test_graph_mvp tests.test_native_backend tests.test_sonatine_backend
  tests.test_sonatine_demo`: exit 0, 70 tests, one existing skip, 8.175 seconds.
  Log SHA-256 `1b49af3bb7a6fe66f3e4444d5fe79c5365015801ae61dbb767d6d9d1462a964d`.
  Candidate worktree remains clean. This is focused Host Functional test
  evidence; mocked/opt-in paths are not a new complete QEMU or hardware run.
  It does not resolve the existing full-suite UIO failure or PR integration.

- After the reported reset, GraphQL explicitly returned 4928 remaining. The
  fresh-read/append updates to #115 (canonical inventory integration plus
  unfinished daily boundary) and #116 (published #119 and the independently
  run 70-test receipt) succeeded. The final canonical `audit --check-branch`
  passes. The earlier failed body write is therefore resolved, with its
  failure and retry preserved rather than erased. No item becomes Done.

## Serial integration of the inventory and daily slices

- Current main advanced to `75bf275` through PR #121 while the daily slice
  was under acceptance. Both histories were merged, preserving ADR-0086,
  mandatory session checkpoints, the inventory, canonical queue improvements
  and all unfinished task definitions. T-0151 remains open for actual daily
  execution and local registration acceptance.
- Integrated acceptance: `python3 -m unittest tests.test_project_daily
  tests.test_project_queue tests.test_experiment.AgentBoundaryTests` passes
  68 tests; record checker passes 182 Markdown files, 126 tasks and 95 records;
  `git diff --check` passes. No product, device or research gate is changed.

- Daily active-work rendering also includes Review items. Integrated 68-test
  acceptance remains green. Independent integration review is GO; a reviewer
  initially invoked the historical-root record checker, then corrected the
  absolute script path and confirmed the candidate's 182/126/95 counts.
- The live GraphQL quota reset was confirmed at 06:53:58Z with 5000 remaining;
  bounded factual reconciliation resumed using fresh weekly telemetry.

## Live daily acceptance and scheduler activation

- `python3 scripts/project_daily.py --apply --output-dir
  /private/tmp/raveil-review-20260905/daily-live-final` exited 0, updated 32
  actual-event fields and the marked Project README, and verified readback.
  Receipt `project-daily-20260905T155408+0900-229e0f2aa04c442d830b9748edbecb3b.json`
  records script SHA-256
  `7cafd18358bc19d267cb2336dd75d26c836f7253dd59c0b6d14b938b39bd217c`.
  Weekly telemetry at 06:54:09Z was 67% used / 33% remaining.
- `launchctl bootstrap gui/501` for the installed
  `com.raveil.project-daily.plist` exited 0. `launchctl print
  gui/501/com.raveil.project-daily` confirms RunAtLoad, 900-second interval,
  one run and last exit 0. The real scheduled execution ran 15:55:15--15:55:23
  JST; its receipt has success=true, zero planned event updates, no README
  change and no repeated edits. The atomic state records last success against
  the latest already-elapsed due date, September 4; it does not claim that
  September 5's future 19:00 run has occurred. Mac availability is required.
- The installed directory contains the source revision/hash manifest,
  supervisor, logs and receipts. The sole retained finding is Issue #99's
  absent API closing-reference association; canonical PR #100 evidence was
  inspected and preserved. No synthetic association, acceptance time or Done
  transition was generated by the reporter.

## Concurrent daily integration verified by the inventory PM

- The first #122 merge attempt was refused because canonical main advanced
  concurrently through PR #120 to `9e3a405`. PM merged current main into the
  isolated closeout branch, retained both dated histories and the verified
  inventory/read correction, and resolved TODO/STATUS using the newly
  integrated daily acceptance. No implementation file was edited in resolution.
- PM independently inspected the live-apply JSON receipt: success=true,
  dry_run=false, 32 planned event updates, README changed, and script hash
  `7cafd18358bc19d267cb2336dd75d26c836f7253dd59c0b6d14b938b39bd217c`,
  which equals the canonical `scripts/project_daily.py` hash. The installed
  LaunchAgent has RunAtLoad and a 900-second interval; read-only `launchctl
  print` returns exit 0, one run, last exit 0. The 15:55:15 JST scheduled
  receipt succeeds with zero repeated updates/README change. A separate
  15:59:20 receipt also succeeds. No future 19:00 execution is claimed.
- PM ran `python3 -m unittest tests.test_project_daily
  tests.test_project_queue tests.test_experiment.AgentBoundaryTests`: 68
  tests pass after conflict resolution. Record checker and diff check pass;
  live canonical queue audit passes with one active review item and one
  pullable Ready. The already-accepted T-0151 completion on current main is
  retained based on inspected code, tests and execution receipts, rather than
  a conversation or subagent completion report.
- The remaining #99/PR #100 native API closing-reference gap was investigated.
  PR #100's original body had 18 literal newline escapes and zero real
  newlines, including its existing Closes reference. PM normalized formatting
  and added a dated explanation without changing evidence or event timestamps.
  The subsequent closingIssuesReferences query still returns an empty list.
  Keep that historical API association gap explicit; canonical T-0142/PR #100
  evidence and the now-readable body remain linked, and no synthetic closure
  event or accepted timestamp is invented.

## T-0150: readable fusion comparison while publication is delegated

- Owner directed that pushes be performed by sol-or-lower subagents while
  development continues. A GPT-5.6-sol low-reasoning publication agent pushed
  only the reviewed Graph continuation branch and created Draft PR #123 atop
  workspace PR #119. Primary independently verified the remote head is
  `e31064b9b0c7da1327c50a5cee39ca1df7952d22` and the base is
  `feat/t-0149-project-workspace`. No merge or code mutation was delegated.
- Latest weekly 10,080-minute guard reading: 71% used / 29% remaining.
  Fetched main `60b57f5`; identified Ready T-0150/#114 as the existing approved
  visual-feedback task. No new Graph semantic scope was selected. Created
  `/private/tmp/raveil-t0150-garden`, branch
  `feat/t-0150-garden-fusion-view`, and used canonical `project_queue.py start
  114 --owner-role 'Experience Implementer' --depends-on
  'T-0135, T-0144; independent of T-0148' --sprint S-0003 --story-points 5
  --demo 'python3 -m raveil garden --fixture tests/fixtures/garden/minimal.json
  --width 150' --evidence-class 'Host Functional' --apply` successfully.
- Primary owns garden.py and test_garden_tui.py plus serial STATUS/TODO/log/
  Sprint updates. Graph continuation publication is immutable remote-ref work,
  not a concurrent source mutation. T-0148 remains an independent review.
- Implemented a default side-by-side materialized/fused path, data-bound
  intermediate memory labels, selected-node markers/explanation, `d` detail
  toggle, and both-streams-TTY redraw. Existing detailed comparison tests now
  exercise the detail toggle; new tests cover the compact view and redraw.
  No new ADR or EXP applies: this is presentation inside T-0135's existing
  read-only boundary and the accepted T-0150 feedback, not an architecture
  or research decision. ROADMAP/OPEN_QUESTIONS require no gate/design change.
- `python3 -m unittest tests.test_garden_tui` passes 31 tests. Independent
  `python3 -m unittest -v tests.test_garden_tui
  tests.test_graph_device_dynamic_sealed` passes 41/41. Host macOS 26.5.1
  arm64, Python 3.14.6. Raw tester log
  `/private/tmp/raveil-t0150-garden-focused-20260905.log`, SHA-256
  `4a46f13bdf8635f58daf5d8c65c0c278bf4841f22b21de8cb7ab36c8cb4fccd6`.
- Actual Mac PTY command: `stty columns 150 rows 32; python3 -m raveil garden
  --fixture tests/fixtures/garden/minimal.json --width 150`. Sent j, j, q on
  separate lines. Observed selected bias marking both bias_add and fused
  bias_add+relu, selected relu marking both relu and the fused stage, redraw
  control sequence before each frame, and clean closed/exit 0. Default frame
  counts are 24 lines at width 72 and 21 at 100/150/240, before the prompt.
- `cua.getApp("Terminal")` was denied because com.apple.Terminal is not
  allowed for Computer Use. No alternate UI-control mechanism was attempted.
  Actual Mac Terminal visual acceptance remains pending; PTY/test evidence
  does not substitute for the owner-visible S-0003 review. Keep task unchecked.
- Main introduced canonical ADR-0086 for inventory, colliding with the Graph
  continuation's local ADR-0086; its older local UIO defect T-0150 collides
  with canonical Garden T-0150 and is now T-0152/#117. The publication agent
  was instructed to record both reconciliation requirements in Draft PR #123
  before any integration. No accepted ADR was silently renumbered here.
- The same GPT-5.6-sol publication agent pushed Garden implementation
  `80b2006ce41f059aa93546325459ead5f14c0764` and opened Draft PR #124 against
  main. Primary verified the exact remote head and Draft state. The PR body
  links `Closes #114` for eventual integration; the Issue remains open.
  `python3 scripts/project_queue.py review 114 --pr 124 --apply` exited 0
  and moved Issue #114 to Review. No merge, Done, or owner visual acceptance
  is claimed. Publication remains delegated; final record reconciliation is
  primary-owned.
- Continuation audit at 2026-09-05 09:13 UTC read the weekly 10,080-minute
  window at 80% used / 20% remaining. Exact-head independent Garden review
  at `23dce978` passes 31 tests; the previously retained 41-test and actual
  macOS PTY evidence remain valid. Primary inspected the full source/test
  diff and distinguishes technical CLI acceptance from the pending S-0003
  owner ceremony under WORKFLOW/ADR-0069. Native Terminal.app GUI access is
  still unverified; it is not claimed by this technical acceptance. Do not
  require a separate owner-attendance ceremony for the ordinary task merge.
- Canonical `project_queue.py prepare 117 --owner-role 'Systems Implementer'
  --depends-on 'T-0145 complete; independent of T-0148 and T-0150; clears
  T-0149 regression blocker' --sprint S-0003 --story-points 2 --demo
  'python3 -m unittest tests.test_graph_device_uio_dry_run' --evidence-class
  'Host Functional' --apply` prepared the existing T-0152 successor. Baseline
  reproduction remains 3 tests / 1 stale blanket-source-scan failure; no new
  task, device action or production code change was introduced.

## T-0152: continue from integrated Garden to the UIO regression

- PR #124 exact head `ad09fdf` passed the primary ancestry/diff/record/thread
  audit, 41 focused tests and mergeability checks. Merged at 09:19:59 UTC as
  `5ddf460c9b022cce5852e9279491a3ae71503887`. Issue #114 closed; canonical
  `project_queue.py complete 114 --pr 124` with technical review, unsealed
  cycle and resource descriptions exited 0. Owner ceremony/GUI observation
  remains pending, separately from technically verified CLI behavior.
- Created isolated `/private/tmp/raveil-t0152-uio` from that merge. Canonical
  `project_queue.py start 117` with the prepared Systems Implementer,
  S-0003, 2 SP, Host Functional, focused unittest packet exited 0. Initial
  failure was reproduced before mutation; no C++/RTL change is justified.
- Current weekly telemetry at 09:21:29 UTC: 10,080-minute window, 82% used /
  18% remaining. One sol/low worker owns only the test file; primary owns
  STATUS/TODO/log updates, review and integration. Push remains delegated to
  the separate sol/low publication agent. No real device or vendor tool runs.
- ROADMAP, OPEN_QUESTIONS and ADR-0083 retain their existing gates/boundaries;
  no ADR or EXP decision is introduced by this regression task.
- The sol/low worker returned the tests-only candidate. Primary inspected the
  diff and reran `python3 -m unittest tests.test_graph_device_uio_dry_run`:
  6 tests pass. The static check now scopes to the pure planner. Guarded
  os.open, builtins.open, io.open/Path.open and mmap calls check the named
  canonical device paths before any real device syscall; descriptor-relative
  ordinary file operations remain exercised. A valid injected handoff calls
  the fake runner once; tampering calls it zero times. These are bounded
  regression tests, not universal device-isolation claims.
- Record checker passes (182 Markdown files, 126 task definitions, 95 records)
  and `git diff --check` passes before independent full-suite verification.
- A proposed detailed public PR #118 evidence comment was rejected by the
  safety reviewer for local-path/internal-metadata disclosure. It was not
  posted or retried indirectly. That publication is held separately; the
  authorized T-0152 test/record work continued without changing raw evidence.
- Independent verification completed on macOS 26.5.1 arm64 / Python 3.14.6:
  `python3 -m unittest tests.test_graph_device_uio_dry_run
  tests.test_graph_device_dynamic_uio tests.test_graph_device_dynamic_sealed`
  passes 20 tests; `python3 -m unittest discover -s tests` passes 704 tests
  with two skips, zero failures, exit 0 (91.462 seconds in this observation).
  Primary read back the retained tester receipt and exit status. No runtime,
  RTL, ABI or device-access code changed; these results are host regression
  evidence, not a new QEMU/RTL/physical measurement.
- Authoritative tester receipt: `/private/tmp/raveil-t0152-uio-tests-20260905.log`,
  SHA-256 `1a47e4b14a2fbfc14e5068b1efc413e3fce5eb70c1e98ca7ce6d26aa9ea8923e`.
  Tester also inadvertently launched a duplicate discovery while handling a
  command timeout; it finished with the same 704/two-skip result but lacked
  a wrapper exit receipt. It is not counted as additional acceptance evidence.
  No further repetition was requested; use the first complete exit-0 receipt.

## T-0148 predecessor integration continuation

- T-0152 PR #125 passed exact-head primary review, no unresolved threads,
  required checks, records and ancestry checks, and merged at 09:32:18 UTC
  as `2a4da77f7664eaedac2bf426485f09ecea477203`. Canonical completion of
  Issue #117 exited 0. This clears the test-regression blocker for T-0149;
  its predecessor/workspace integration is still pending.
- Continued existing T-0148 review using the clean predecessor branch and
  merged current main without rewriting either history. Conflicts were
  limited to TODO, decision index and the dated log. Both independent record
  histories are retained; the duplicate local T-0148 completion checkbox is
  now an evidence subsection, not whole-task Done. ADR-0084 and canonical
  inventory ADR-0086 remain separate accepted decisions.
- Independent re-review and primary inspection confirmed that the existing
  Issue #113 inventory addendum already covers the single programVersion wire.
  The final v3/v1/v2 retained cohort supports the existing documented shared
  simulator identity and output equality. Earlier receipts are retained as
  a separate cohort. The specific out-of-halo FAULT/no-publication marker is
  present in the final v3 device log. No evidence was rewritten or recollected.
- PR #118 is only the predecessor. Issue #113 also contains an authorized
  editable-Graph continuation, so predecessor merge must reference, not close,
  that Issue. T-0149 and the Graph continuation remain separate integration
  steps. No owner ceremony or performance/physical claim is accepted here.
- At integrated head `448b4dc`, primary
  `python3 -m unittest discover -s tests` completed 710 tests with two skips,
  zero failures, exit 0 (79.466 seconds). The prior tester adapter attempt
  returned only partial progress without a terminal status; it is not counted
  as a pass. Primary used a retained resumable process and checked exit 0.
  Host remains macOS 26.5.1 arm64 / Python 3.14.6. Weekly check at 09:35:42
  UTC is 85% used / 15% remaining in the 10,080-minute window.

## T-0153: canonical recovery for resolved Blocked work

- PR #118 merged at 09:39:48 UTC as `1e17dab44e1640e3c8b81737d52fa9abc1a0a7e2`.
  Its body references rather than closes Issue #113 because the authorized
  continuation still depends on workspace PR #119 and Graph PR #123. No
  whole-task completion was emitted. Source behavior is the verified candidate.
- With both PR #118 and #125 merged, canonical preparation of T-0149/#116
  failed: `prepare requires an unset/Backlog status (or idempotent Ready)`.
  `start` likewise only accepts Ready/In Progress. This is a missing recovery
  transition, not an unresolved technical dependency; no ad hoc field write
  was used. De-duplicated current records and live Issues; allocated T-0153/#126.
- Canonical prepare/start of the new 1-SP PM correction in S-0003 succeeded.
  Branch `fix/t-0153-explicit-queue-unblock` is based on `1e17dab`. One sol/low
  owner is restricted to the queue script and its tests; PM owns the four
  records. Explicit clearing reason, unchanged readiness checks and no new
  Project fields bound this correction under ADR-0086. ROADMAP, OPEN_QUESTIONS,
  ADR and EXP gates/claims do not change.
- Weekly telemetry at 09:42:27 UTC: 10,080-minute window, 87% used / 13%
  remaining. No device, vendor build, paid resource or new research collection.
- Candidate preserves supplied dependencies and appends a trimmed explicit
  unblock reason before Ready. Empty/whitespace reasons and Initial-SP
  mismatch on Blocked fail without writes; Ready retry is deterministic.
  `start`, WIP limits and remote APIs are unchanged. Independent
  `python3 -m unittest -v tests.test_project_queue tests.test_project_daily`
  passes 70 tests, exit 0, on macOS 26.5.1 arm64 / Python 3.14.6.
  Receipt `/private/tmp/raveil-t0153-queue-focused-20260905.log`, SHA-256
  `a14bbe50a9a37b014c10ce9ec92df9f047dc613a69f019676dddc6dbe3589661`.
- Readback confirms T-0149 retains Initial SP = current SP = 13. Recovery
  must preserve those original points, not substitute the 5-SP integration
  guess from the rejected prepare attempt. No field changed in that attempt.

## T-0149: resumed integration and playable verification

- T-0153 merged via PR #127 as `6fc688106dabda7d31e8cded8072482cea9291eb`
  at 09:50:44 UTC; canonical completion exited 0. Its reviewed prepare
  command moved Issue #116 from Blocked to Ready with the verified PR #118/
  #125 clearing reason, then canonical start resumed T-0149 in S-0003 with
  the original 13 points. No manual state workaround or estimate rewrite.
- Merged current main into the clean existing workspace branch. Resolved
  TODO, decision-index and dated-log conflicts by preserving both histories,
  retaining canonical T-0150 Garden/T-0152 defect identities, and keeping one
  T-0149 task entry. Workspace ADR-0085 and inventory ADR-0086 remain distinct.
- Primary found rejected `init` changed a nonempty directory's mode. A
  sol/low owner reproduced 0755 -> 0700 despite rejection, then moved the
  emptiness check before permission mutation and added a regression for
  unchanged mode/content/entries. Descriptor-based no-follow fchmod remains
  on accepted roots. Only project.py and its test changed; no new boundary.
- Independent host command `python3 -m unittest -v tests.test_project
  tests.test_command_graph tests.test_graph_mvp tests.test_native_backend
  tests.test_sonatine_backend tests.test_sonatine_demo` ran 71 tests: 70 pass,
  one expected opt-in QEMU skip, exit 0. Receipt
  `/private/tmp/raveil-t0149-host-focused-20260905.log`, SHA-256
  `5b69b26489d52e7196c97967a59b3d4d3cca5aac49f6caea0346de3f54bf5035`.
- In the existing `raveil-sonatine-graph:t-0085` Linux arm64 image, network
  disabled and repository read-only at /repo, `python3 -m unittest -v
  tests.test_project` passes 14/14, exit 0. Debian 12 / Python 3.11.2;
  receipt `/private/tmp/raveil-t0149-linux-project-parity-20260905.log`, SHA-256
  `64b04f40153d2e9067e286e23d76b32f9eca35e2f9dae6703bc1ce2bc3bb24fe`.
- Actual macOS CLI used `/private/tmp/raveil-workspace-integration.YvKTUY`:
  `scripts/raveil project init DIR`, `project show logs`, and `project run
  logs --project DIR`. After appending one ERROR input, a second run from DIR
  through the repository launcher changed output 2 -> 3 while retaining both
  runs. `project diff 20260905T095524Z-df55adf344fb
  20260905T095537Z-23145907613b` shows both input text and output text changes.
  `project run files` produces both sorted outputs. `project run gemm --backend
  native` and `--backend sonatine-qemu` succeed with baseline checksum
  `c102cc36c72f8e5c`, outcome abstained. Actual `project console sonatine`
  boots; help lists commands and exit closes with status 0. These are Host
  Functional / QEMU Emulation Correctness observations, not speed comparisons.
- No kernel/image build or download was used. Current weekly guard at 09:56:54
  UTC reads 90% used / 10% remaining in the 10,080-minute window. Full host
  discovery is running once through a retained resumable process.
- Full `python3 -m unittest discover -s tests` completed 730 tests with two
  skips, zero failures, exit 0 (105.323 seconds). All documentation conflicts
  are resolved and staged; the earlier review's unmerged index observation
  preceded final staging, not an unresolved source conflict. Record checker
  and staged diff checks pass.
