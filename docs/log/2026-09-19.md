# 2026-09-19

## T-0184 Graph pressure planning, immediate next pull

T-0194 merged via PR #258 as `1a6b18e`; canonical completion closed Issue #256.
Prepared then started existing Issue #183 / T-0184 on S-0005, 3 SP Planning,
branch `research/t-0184-graph-pressure`, one primary mutation owner. Initial
prepare rejected missing colon-form packet markers; corrected the formatting
without expanding scope, then canonical prepare/start succeeded. T-0185 and
T-0155 dependencies are complete. ADR-0088 applies; no new EXP/RFC collection.

`PYTHONDONTWRITEBYTECODE=1 python3 tests/fixtures/graph_pressure/check_pressure.py`
passes all 17 constructed descriptors: six admits and eleven exact rejects.
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_graph_device_dag tests.test_graph_device_affine -q`
passes 28 tests in 3.359s on macOS arm64 / Python 3.14.6. Host compiler/oracle
only: admitted cases have three oracle/fallback seeds; no RTL or performance.
Independent review correctly identified dead/unreachable values in the register
probes. Report labels them retention/order diagnostics, not intrinsic nine-live
pressure. Added exact 17-case coverage and explicit exceptions (no removable
assert checks), and included the input-generator source hash. Not a sealed
source/environment receipt. Current compiler and machine remain unchanged.

Chosen next increment: advisory pressure hints in existing project check.
It preserves acceptance; rejecting unused nodes would unnecessarily shrink
currently accepted programs. Scheduling/retirement, spatial tiling and physical
capacity remain deferred. No new Drive upload or Sprint ceremony acceptance.
Final independent planning re-review GO: normal and `python3 -O` fixture
commands pass exact coverage, with explicit limitations. Record checker passes
216 Markdown / 161 tasks / 112 records before successor registration; diff
check passes. T-0195 is the only selected implementation successor, 2 SP Host
Functional: explain rejections in existing preflight without changing admission.
Successor Issue #259 is registered; final record checker passes 216 Markdown /
162 tasks / 112 records. No runtime/compiler file changed in T-0184.

## T-0194 immediate continuation

Owner instructed completion -> summarize next action -> immediately execute,
not a Ready-only handoff. T-0193 is merged via PR #257 (`0c61ded`); Issue #255
is canonically Done. Started T-0194 / Issue #256, S-0005, 3 SP, via canonical
queue on `feat/t-0194-inspect-export` from that main, primary single owner.
ADR-0100 and Playable apply; no new EXP, RFC or research/hardware gate.
Original dirty root remains untouched. Weekly usage monitoring remains disabled
by explicit owner instruction; no Drive upload, paid resource or device action.

Implemented standalone read-only `project inspect-export`: strict bounded
JSON and payload verification, no extraction/project lookup or authenticity.
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_project_export -q`
passes 21 tests in 8.828s, exit 0, macOS arm64 / Python 3.14.6. Includes actual
export roundtrip, standalone CLI, unchanged source/no created output, malformed
UTF-8/deep JSON/duplicate fields, type and claim rejection, paths/collisions,
hash/size/base64, resource limits, symlink/hardlink/FIFO and changed-file cases.

Read-only successor review recommends existing T-0184 / Issue #183 over further
export hardening: its T-0185 and T-0155 dependencies are complete, and it returns
to user-visible editable Graph pressure. Refine a planning-only fixture/report
packet first; retain all current instruction/register/shape/halo bounds. No
new scheduler, tiling, ISA, transport or RTL implementation is pre-authorized.

Final T-0194 verification:
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_project_export tests.test_project tests.test_project_graph -q`
passes 85 tests in 34.887s, exit 0. Independent security reviewer reruns all
21 export tests and returns GO for bounded self-consistency, not authenticity.
`python3 -m raveil project inspect-export <persistent-artifacts>/t0193-selected-errors-v2.json`
exits 0: consistent, one member, 2 bytes, 19 omitted, unchanged bundle SHA-256
`704f2cdb1f72ed75fe16e1b02f80ed0e9ba2a7ac2fbfb1b447522aa68eab8fc3`.
Source_verified/authenticated/replayable_full_run remain false. No source
project, extraction, writes or execution are involved in this command.
Record checker passes 215 Markdown / 161 tasks / 112 records and diff check
passes. T-0184 existing Issue #183 packet is refined for a 3-SP Planning
successor, with an explicit fixtures/report allowlist and unchanged compiler.

## T-0193 explicit preview and derivative export

T-0180 merged through PR #254 and canonical completion closed Issue #178.
Owner-authorized T-0193 / Issue #255 is In Progress on S-0005 at 5 SP,
single primary mutation owner, `feat/t-0193-selective-export` from `5467832`.
ADR-0100 adopts bounded JSON/base64 selected derivatives, not archive import.
Initial focused suite passed 9 tests in 10.430s, including actual native run,
CLI preview/export, acknowledgement/token, path/selection failures, hardlink
source, symlink destination, source tamper and atomic publication failure.
No new Drive upload and no original-root edits were made.

Final candidate verification on macOS arm64 / Python 3.14.6:
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_project_export tests.test_project tests.test_project_graph -q`
passed 76 tests in 34.660s, exit 0. The new suite contributes 12 tests.
Independent review exposed original-record selection and missing O_NOFOLLOW
fallback; both were fixed and independently re-reviewed GO. Original records
are unselectable; unsupported no-follow reads fail closed. Cooperative
single-writer limitations remain explicit, not a hostile-concurrency claim.

Actual CLI demo:
`python3 -m raveil project export-preview 20260919T045403Z-e64a4000a996 --project <persistent-artifacts>/t0180-inspection-20260919 --member workspace/errors.txt`,
then `python3 -m raveil project export 20260919T045403Z-e64a4000a996 <persistent-artifacts>/t0193-selected-errors-v2.json --project <persistent-artifacts>/t0180-inspection-20260919 --member workspace/errors.txt --expect-preview 5c5e822d99ad9378bb5fc2edaae2a85070887131f54013de8c424e7a07d240b5 --acknowledge-sensitive-data`.
Both exit 0. Persistent artifacts are under the original repository's ignored
`artifacts/`, not tracked source changes. Inspection lists 20 files / 24,097 bytes;
export contains only `workspace/errors.txt` = `2\n`, 2 payload bytes, 19 omitted
members. Envelope is 935 bytes, mode 0600, SHA-256
`704f2cdb1f72ed75fe16e1b02f80ed0e9ba2a7ac2fbfb1b447522aa68eab8fc3`.
`python3 -m json.tool` and `stat -f '%Lp %z'` verified the actual envelope and
mode. The v1 development attempt remains separate, never overwritten.

Record checker passed 215 Markdown files / 159 tasks / 112 records before
successor refinement; `git diff --check` passed. Ordinary live queue audit
passed one active item, but horizon was empty. Ranked bounded next choices:
read-only derivative verification, archive import, broader privacy scanning.
Choose T-0194 verification (3 SP) because it closes the consumer inspection gap
without extraction authority; defer the latter two. No duplicate export issue
was found. T-0194 is prepared only, not implemented in the six-step packet.
Weekly usage monitoring remains disabled by owner instruction. No new Drive
transfer, paid resource, device access or Sprint ceremony acceptance occurred.
Post-refinement record checker passes 215 Markdown files / 160 tasks / 112
records; `git diff --check` exits 0. T-0194 is tracked as Issue #256.

## T-0180 closeout and authorized successor

Owner requested steps 1–6: integrate the design, close the planning task,
then implement/demo a bounded preview and selective export with explicit
derivative lineage. No archive import, automatic whole-run sharing or research
promotion is authorized. Five fixture tests pass; prior reference regressions
passed 64 tests. Root dirty worktree remains untouched; no new Drive transfer.

## T-0180 inspection fixture validation

Inspected the persistent synthetic native run's 20 regular files, recording
exact byte counts separately from the existing hashes. Four planning-only
fixture tests pass via `python3 tests/fixtures/project_export/check_inspection.py -v`:
file-set/total consistency, relative path rejection, casefold duplicate
rejection, and invalid types/budgets/link metadata. No archive is parsed or
extracted; metadata tests do not prove filesystem isolation or privacy.
Recommendation remains inspection-preview first, automatic archive/import
deferred. No Drive upload performed under the weekly preservation policy.

Independent reviewer identified undercounted implicit directories. The model
now counts files plus unique parents and rejects file/directory prefix conflicts;
a fifth regression covers 513 two-component files exceeding 1024 members.
Link-key tests reject JSON metadata only, not real archive headers; this
limitation is explicit. The original four-test result is retained above.

## Owner-directed weekly preservation and resumed delivery

- Owner requested Drive saves approximately weekly and work resumption.
  ADR-0099 records weekly batches before Review, immediate persistent local
  saves/checks, and unchanged remote-promotion integrity. No scheduler added.
- Canonical queue started T-0180 / Issue #178 in S-0005, 3 SP, Planning, from
  main `d0e3bf1` on `docs/t-0180-export-boundary`; dirty root work is untouched.
- Initial export-boundary draft follows actual `Project.load_run` and `tree`
  constraints. Actual-run fixture and final design review remain; no runtime
  exporter/importer or new remote upload was added.
- Sprint Review Accept was not inferred from the owner's work-resume request.
- Actual new native sample run `20260919T045403Z-e64a4000a996` passed reference
  validation and produced `errors.txt` = `2\n`. Existing load_run/tree produced
  the inspection-only member fixture. It reveals unrelated input snapshots,
  so the draft now explicitly defers blind whole-run export as a disclosure risk.
  Evidence remains local under the weekly policy; no new Drive transfer.
- Verification: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest
  tests.test_project tests.test_project_graph -q` passed 64 tests in 46.749s
  (macOS arm64 / Python 3.14.6). Record checker passed 214 Markdown files,
  158 task definitions and 111 records; `git diff --check` passed.

## Final preservation and technical acceptance

- Owner restored existing Drive authentication. An initial directory listing
  showed no EXP-0012 destination, so the new run path did not overwrite a bundle.
- `rclone copy <persistent-run-root> <configured-remote>:Raveil/research-data/EXP-0012/20260919-recollection-01 --immutable`
  and `rclone check <persistent-run-root> <same-remote-path> --download`
  exited 0: 40 matching files, zero differences. No sync/delete was used.
- `REMOTE_COMPLETE.json` was transferred last with immutable `copyto`, then
  the full download check passed again: 41 matching files, zero differences.
  Marker SHA-256:
  `ab3760876c1b7a3043ebb60a5a41de9ff7e293599fb4453a0e2dd730836ee1ca`.
- Independent final reviewer found Issue #239 criteria 1–7 satisfied subject
  to remote verification and record/PM closeout. PM now accepts the bounded
  functional task; no broader research gate or historical recovery claim.
- Focused tests rerun: 31 pass in 6.930 seconds. Garden `--keys 3dq` rerun
  exited 0, showing numeric ReLU selection and directed dependency view.
- S-0005 T-0180 remains Ready; live horizon audit passes one active and one
  pullable Ready item. Sprint Review remains awaiting explicit owner disposition.

## T-0191 separately authorized recollection

The owner approved a new collection after the historical evidence-availability
incident. Historical receipts remain unavailable; no old result or hash is
replaced. Project #1 / Issue #239 resumed In Progress before collection.

- New run ID: `20260919-recollection-01`, under ignored persistent
  `artifacts/research/EXP-0012/` in the original repository, not temporary storage.
- Source snapshot: `source-a729ce1.tar`, SHA-256
  `83a7818c893af79ad362a511ec5954f51a544c62bb12b2f4c948737ff7c80d30`.
- Existing pinned image only; no build, pull, remote artifact upload or device use.
- Generate and verify RTL before compiling the three programs. Same frozen ADF,
  image and normalized RTL identity as EXP-0012. The existing same-image
  Docker bind-copy repair was used and recorded by the generator.
- Three independent oracles matched: neighborhood 170, elementwise 740,
  reduction 374; Raveil accepted the simulator result (no CPU fallback).
- Strict verification passed: 26 run files and 13 RTL files.
- Raw run receipt SHA-256:
  `3149295c139e220eedd570c47bd4c3b2cebed8e121a471e40ef27ee68f0e3ee4`.
- Canonical private-receipt SHA-256 (different JSON serialization):
  `fa135f710f61a83f8400ae5429d4263c3e685a13b4f769aff3f7b989da3cb792`.
- Authorized receipt SHA-256:
  `766ad944778e5c00720d8f62c28407b9a21e843ba77391204d636bb620827cb2`.
- RTL receipt SHA-256:
  `b55c96ee7cc9c7406b3d25b03ff095f4893f7a62621cf69a396e5f806295e357`.

Commands from the isolated worktree, on macOS arm64 / Python 3.14.6:

```sh
python3 experiments/openasip/generate_rtl.py --output <persistent-run-root>/rtl
python3 experiments/openasip/verify_evidence.py rtl <persistent-run-root>/rtl
sh experiments/openasip/run_three_programs.sh --simulate --evidence-dir <persistent-run-root>/run
python3 experiments/openasip/verify_evidence.py run <persistent-run-root>/run
python3 experiments/openasip/verify_evidence.py rtl <persistent-run-root>/rtl
```

All commands exited 0. Evidence class is Host Functional external simulator /
generated RTL, not RTL simulation, performance, FPGA or silicon. Persistent
local storage is not remotely verified durability. Independent final review and
approved remote preservation remain; T-0191 is not Done.

Independent Security Reviewer re-ran both strict verifiers and hashed all three
receipts plus the source archive: GO, exact identities above confirmed.
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_t0191_openasip -q`
passed 31 tests in 5.448 seconds. No runtime code changed in this recollection.

## Integration, successor and Sprint Review candidate

- Remote preservation attempted against the existing `gdrive` research-data
  connection. Read-only listing failed with OAuth `invalid_grant`; no upload
  occurred. Owner reconnection is required before immutable transfer and
  download verification. T-0191 final closure remains pending, not Done.
- Final acceptance review found missing exact cleanup/retry instructions;
  README now specifies full-ID operator reconciliation, no name-based deletion
  or prune, fresh evidence paths, and simulator-versus-fallback distinction.
- RTL generation and strict verification completed before the compile command;
  the normalized identity was already frozen in EXP-0012. This is observed
  invocation order, not a cryptographic ordering attestation.
- Canonical prepare registered existing T-0180 / Issue #178 as S-0005,
  Planning, Experience Implementer, 3 SP, P1/Ready. It is a bounded saved-run
  export contract/inspection fixture, not runtime export/import implementation.
- Garden demo at source `60bae26`: `python3 -m raveil garden --fixture
  tests/fixtures/garden/minimal.json --keys 3dq --width 100` exited 0:
  numeric 3 selects ReLU; d displays admitted dependency edges and selected
  node; q exits. The initial comma-separated keys invocation was invalid;
  corrected invocation is a contiguous key sequence, not comma-separated.
- Saved new run strict verification was also demonstrated (26 files).
  This rechecks saved output; it does not rerun simulation.
- Actual `check_cancellation.py oacc` and `ttasim` demos both exited 0 at
  the Docker-enabled boundary: process observed, unpublished cancelled receipt,
  owned container absent. An earlier restricted-boundary invocation reported
  no witness and was not counted as success.
- Sprint Review remains pending owner disposition. No automatic Accept.
- Retrospective candidate: Keep independent strict verification; Problem
  temporary evidence was unavailable and remote credentials have expired;
  Try require persistent evidence placement plus verified remote preservation
  before research closeout. No claim that historical evidence was recovered.
