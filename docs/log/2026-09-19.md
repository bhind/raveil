# 2026-09-19

## T-0183 multi-output planning after build reuse

T-0196 mergedPR #265, main ecf5655, canonical completion succeeded.
Started `research/t-0183-multiple-output-contract`; Issue #182 prepared and
started on S-0005, preserving3SP. Concrete workload is existing sensor energy
with both raw and biased outputs. Compared manual runs, host composition and
versioned hardware STORE extension; candidate selects bounded host composition.
Current ISA/installer and one-final-STORE rule are unchanged. T-0197 / Issue
#266 registers the separately versioned implementation, no runtime claim.

`python3 -m unittest tests.test_graph_multiple_output_contract tests.test_graph_device_dag tests.test_graph_device_dynamic -q`
passes41 tests in9.113s, including four new planning vectors, on macOS26.6.2
arm64/Python3.14.6. Vectors compare scalar wraparound formulas with current
Graph oracles; zero/two-STORE attempts reject. No new RTL collection or timing
claim. Independent planning review requested before technical acceptance.

Closeout: review requested a precise name grammar/mapping. Added ASCII1-31byte
name rules, duplicate/path/control/Unicode rejections and ordered private-slot
mapping; labels never become paths or parent field keys. Final same command
passes43 tests in8.572s (six new planning tests). Independent reviewer GO and
6/6 tests in0.011s. Primary accepts planning for integration; T-0197 remains
the runtime successor, not implemented. Record checker220 Markdown/167tasks/
114records and diff check pass. No new hardware effects or performance claim.

## T-0196 bounded compiled simulator reuse kickoff

T-0187 merged as PR #264, main0767363; canonical completion succeeded.
T-0196 / Issue #263 is prepared/started on S-0005, 5 SP, dedicated branch
`feat/t-0196-simulator-build-reuse`. ADR-0102 specifies the implementation.
Independent initial contract review requested explicit canonical identity and
bounded dependency hashing; primary added both and requested re-review.
The existing offline dependency volume has436 regular files, approximately
157MiB and no symlinks (readonly Docker inspection, no image pull).
`python3 -m unittest tests.test_graph_device_build_cache -q` passes16 tests
in0.648s on macOS26.6.2 arm64 / Python3.14.6. These are host cache tests only;
runtime integration and real cold/warm Graph execution remain incomplete.

Closeout supersedes that kickoff status. Runtime integration is complete;
independent security review GO after explicit identity/tree consistency checks
and clarifying the 16MiB payload plus8KiB manifest bound. Reviewer ran21 cache
tests in0.820s. Primary final command:

```sh
python3 -m unittest tests.test_graph_device_build_cache tests.test_graph_device_dynamic tests.test_graph_device_runtime_pair tests.test_graph_device_dynamic_sealed tests.test_project_graph tests.test_graph_compare -q
```

All98 tests pass in24.919s. Existing negative-test diagnostics are expected.
`tests/test_build_dependency_manifest.sh` passes in the existing offline image
using `docker run --rm --network none --pull never --platform linux/amd64
--mount type=bind,source=/private/tmp/raveil-t0191-observation,target=/repo,readonly
sha256:2efc059cf07eb054d93fc1fa32decd7a13c2cdb97069dac29138275b22e5c57c
bash /repo/tests/test_build_dependency_manifest.sh`.
It checks stable/content/rename/add/remove/link/hardlink/FIFO/size/name cases.

Actual RTL commands (same working tree; source files unchanged between runs):

```sh
RAVEIL_SIMULATOR_REUSE=1 python3 -c 'from raveil.graph_device_dynamic import _run_dynamic; print(_run_dynamic(["tests/fixtures/graph_device_dynamic/compare-immediate-u32.json"],[0],None,"t0196-cold", input_snapshots=[bytes(1296)]))'
RAVEIL_SIMULATOR_REUSE=1 python3 -c 'from raveil.graph_device_dynamic import _run_dynamic; print(_run_dynamic(["tests/fixtures/graph_device_dynamic/add-immediate-u32.json"],[0],None,"t0196-warm", input_snapshots=[bytes(1296)]))'
```

Cold `run.zxu9n85e`: V1, one elaboration/build, published=true.
Warm `run.z2b4cavx`: V2, zero elaborations/builds, cache_hit=true.
Both freshly execute and pass independent oracle/C++fallback/RTL byte equality
for256 words and malformed-request no-AXI rejection. Changed Graphs produce64
different words; binary bytes equal. This also exercises old ADD program v5
after threshold program v6. Identity invalidation is covered by host tests,
not an additional source-mutated RTL build. No wall-clock speedup measured.

- Simulator SHA256: `8775948419c617659a8003f3f13f5b7abd9aa62f71f77b6de0d5bd307b85da99`.
- Build key: `3a19d6ecd1e565de0966504406cc6fa4b8c02987d94719bd1898713e00b1f58a`.
- Dependencies: `9229c750999d97f314e04069688d551c6fd26d9aedbeed7b6a68eabbb373359c`.
- Persistent raw copies: `artifacts/development/T-0196/20260919/run.zxu9n85e`
  and `run.z2b4cavx` in the original repository. Both `diff -rq` comparisons
  against working-tree artifacts return0 with no differences.

Record checker passes219 Markdown files/165 tasks/114 records; diff check
passes. No weekly Drive upload, device access, new image or paid resource.
Primary technical acceptance is for integration, not owner Sprint Review
acceptance. Next remains T-0183, not a larger cache framework.

## T-0187 restart and acceleration plan

Owner explicitly authorized T-0187, short/long efficiency/generalization
planning and continued work, clearing the previous scope-approval denial.
Branch `research/t-0187-simulator-reuse` starts from merged T-0182 main b5fc667.
Issue #187 packet refined, canonical prepare/start succeeded on S-0005
(configured September21-27). Preserved existing Initial SP3; an initial2SP
attempt was correctly rejected, as was a missing Evidence-class packet marker.
No lifecycle bypass. Weekly monitoring remains disabled by owner; no Drive
upload, paid resource or device action.

Primary inspected dynamic/run-pair/project snapshot code and compared retained
T-0182 bytes: simulator/source/RTL/ABI manifests and four generated headers
all equal; requests and expected outputs differ. Command
`python3 -m unittest tests.test_graph_device_runtime_pair tests.test_graph_device_dynamic`
passes24 tests in8.425s (macOS26.6.2 arm64 / Python3.14.6). No new build/timing
collection. Read-only security reviewer mapped all source/identity/isolation
requirements and initially preferred the two-request batch. Primary rejected
that as the main optimization: one existing-before run plus one new edit still
costs one build, and batching repeats old execution without removing that build.
Selected a bounded compiled-build reuse successor (T-0196), with no result
reuse or daemon. No runtime implementation is claimed by this planning task.

Short/long sequence is recorded in ROADMAP and the decision report: avoided
build, multi-output decision, one standard-IR workload, workload-driven capacity
composition, then separately gated physical backends. No elapsed-time forecast
is inferred from SP. Independent security review is GO, with two stale document
dates corrected by primary. Reviewer independently ran the same 24 tests in
8.820s; this is host test evidence, not a simulator timing result. Primary
accepts the planning decision for integration. Successor Issue #263 records
T-0196 with a bounded implementation packet; no runtime work claimed yet.

## T-0182 unsigned threshold implementation and verification

Owner approved the prior explicit opcode question. Primary implemented on
`feat/t-0182-unsigned-threshold` from `c8dae541`; canonical prepare/start moved
Issue #181 onto S-0005, 5 SP, RTL Simulation. Packet-format corrections were
needed before prepare; implementation was not represented as complete then.
ADR-0101 accepts exact descriptor v5/program v6/request v7 semantics.

Environment: macOS 26.6.2 arm64, Python 3.14.6 / Clang 21.0.0. Offline Linux
amd64 Docker image `sha256:2efc059cf07eb054d93fc1fa32decd7a13c2cdb97069dac29138275b22e5c57c`,
existing readonly Scala cache, no image pull, paid resource or device access.
Three builds were used: initial old/new commissioning pair, final direct-
installer-negative implementation plus first workspace run, and edited run.
Final builds produce byte-identical simulators; old control reused that binary
without rebuilding. No persistent cache/service was added.

Initial pair command (repository root):

```sh
python3 -c 'from raveil.graph_device_dynamic import _run_dynamic; import struct; snapshot=struct.pack("<324I",*([0,99,100,101,0xffffffff,4194303]*54)); print(_run_dynamic(["tests/fixtures/graph_device_dynamic/compare-immediate-u32.json","tests/fixtures/graph_device_dynamic/add-immediate-u32.json"],[0,0],None,"t0182-pair",input_snapshots=[snapshot,snapshot]))'
```

It passes both 256-word comparisons in `run.y9zofbie`, simulator
`70779e867fc3864cf73bf5a0c2731271e770d8adc52c1d920c2591d570faa7a8`.
The final runtime then adds independent installer negatives; do not confuse
that initial binary with the final identity below.

Actual workspace commands:

```sh
python3 -m raveil project init /private/tmp/raveil-t0182-demo
cp examples/graph-workloads/recipes/threshold-cross-dilate.json /private/tmp/raveil-t0182-demo/recipes/
cp examples/graph-workloads/inputs/threshold-cross-dilate-descriptor.json examples/graph-workloads/inputs/threshold-cross-dilate-input.json /private/tmp/raveil-t0182-demo/inputs/
python3 -m raveil project show threshold-cross-dilate --project /private/tmp/raveil-t0182-demo
python3 -m raveil project run threshold-cross-dilate --backend rtl-sim --project /private/tmp/raveil-t0182-demo
# Edit all five immediate fields from 100 to 150, leaving input unchanged.
python3 -m raveil project run threshold-cross-dilate --backend rtl-sim --project /private/tmp/raveil-t0182-demo
python3 -m raveil project diff 20260919T080347Z-06d08ef21ba1 20260919T080521Z-a045bee4a493 --project /private/tmp/raveil-t0182-demo
python3 -m raveil project garden 20260919T080521Z-a045bee4a493 --project /private/tmp/raveil-t0182-demo
```

Both runs succeed. PTY Garden `j` then `q` shows threshold150, canonical 1/0
and no conditional effects. Non-TTY invocation prints the initial node only;
it was not used as evidence of interactive selection. `project output` for
both runs rechecks saved integrity. A mistakenly attempted `project verify`
does not exist and exits2; no verification result is attributed to it.
An initial manual snapshot locator incorrectly included `workspace/inputs`;
corrected check of retained `inputs/` confirms the old five thresholds remain100.

Final evidence:

- First: `run.uqg__pan/request-1`, run `20260919T080347Z-06d08ef21ba1`,
  15 one-valued outputs, output SHA256
  `a5c9b317a9ad8e131ec995719291b6207f1e964e1a0979b2e1f4eb6deacc380c`.
- Edited: `run.u601jxd3/request-1`, run `20260919T080521Z-a045bee4a493`,
  five one-valued outputs, SHA256
  `be97ab894c48c00095733667eb285a16c570112db9fea4361919ecf2caa8a7a0`.
- Diff: 0/324 input words changed, 10/64 active outputs changed; first change
  `(1,2): 1 -> 0`. Fifteen instructions, not a larger capacity.
- Both final simulator SHA256:
  `8775948419c617659a8003f3f13f5b7abd9aa62f71f77b6de0d5bd307b85da99`.
- Final RTL manifest SHA256:
  `0af26da2283283b7ab58a7d09f28807ccac2c4e42fc7d3d08708f3dc5a524627`.
- Final source manifest SHA256:
  `46baf2de8538e1547db569fb81c420523d431226b031bfcacf1c1524dfbb5af6`;
  all45 source entries checked against current sources/generated headers.
- Direct installer log: old-version, unknown-version, undefined-source and
  reserved-load all FAULT, output_published=0. Outer malformed request rejects
  before AXI. All256 transport words, including padding, match each oracle.
- Old ADD_IMM control prepared with the same six-value repeated snapshot and
  executed with the final saved simulator under offline Docker, no rebuild.
  `artifacts/t0182-old-version-control/`: output/oracle/C++ fallback byte-equal,
  output SHA256 `7914ba14be3964437fa0e43421ce8075b494b8a4232fccc29c823ee2235f8dae`.

Durable local copy in the original repository's ignored
`artifacts/development/T-0182/20260919/`: three raw sessions, old control and
workspace-demo, 180 files / 2,752,226 bytes, copy compared byte-for-byte.
Drive upload is deferred to the weekly batch, not claimed complete.

Host verification commands:

```sh
python3 -m unittest tests.test_graph_compare tests.test_graph_immediate tests.test_graph_device_dynamic tests.test_graph_device_dag tests.test_garden_tui tests.test_project_graph tests.test_graph_device_dynamic_uio tests.test_graph_workload_examples
python3 tests/fixtures/graph_device_dynamic/rejected-workloads/check_vectors.py
python3 tests/fixtures/graph_pressure/check_pressure.py
python3 .agents/skills/raveil-task-governance/scripts/check_records.py
git diff --check
```

Before the final Garden malformed-version regression: 131 tests pass in21.102s;
17 pressure probes and historical T-0181 rejection vectors pass. Early failures
were stale source-text assertions (5 versus6), corrected test import naming,
and then a primary-review fix to validate Garden version type before numeric
comparison. No failed run was promoted. Independent reviewer ran103 tests in
15.942s and returned technical GO; final incremental results follow below.

Final primary verification adds `tests.test_project_preflight` to the command
above: all150 tests pass in27.603s, including the Garden malformed-version
regression. Record checker passes217 Markdown /163 tasks /113 records; diff
check passes. Existing UIO negative subprocesses deliberately print errors;
their enclosing rejection tests pass. Runtime manifest sources remain exact.

No EXP performance result, research gate, physical-device support or Sprint
ceremony acceptance is claimed. Continuous-work horizon inspection found
T-0187 (reuse decision), T-0183 (multi-output decision) and T-0186 (standard IR).
Repeated builds make planning-only T-0187 the closest usability successor.
Execution-policy review rejected its Issue update/prepare as scope expansion;
no bypass or new task mutation was performed. T-0182 integration continues,
and explicit owner approval is requested for T-0187 separately.

## T-0195 immediate usability implementation

T-0184 merged through PR #260 as `81d9342`, then canonical completion closed
Issue #183. Started prepared T-0195 / Issue #259 on S-0005, 2 SP Host Functional,
branch `feat/t-0195-pressure-hints`, primary single owner. No EXP or new ADR;
existing ADR-0088/compiler admission remains unchanged. Added only preflight
known-error advice and tests across all 17 retained pressure cases. No auto
edit, node pruning/reordering, execution or new hardware/compiler authority.

Next substantive option inspected: T-0182 / Issue #181 requires an accepted
new opcode/semantic ADR, exact source allowlist and RTL budget; its T-0181
GE_IMM_U32 recommendation explicitly grants no adoption authority. Do not
silently promote that Backlog task into hardware implementation. T-0183 remains
a separate planning-only multi-output question, not multiple-STORE authority.

Verification: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_project_preflight tests.test_project tests.test_project_graph -q`
passes 82 tests in 27.778s, macOS arm64 / Python 3.14.6. Independent reviewer
reruns 18 preflight tests, GO: original admission/error, files/runs and no-tool
boundaries preserved over all 17 probes. Record checker216/162/112 and diff pass.
Actual disposable CLI demo at `/private/tmp/raveil-t0195-demo.b1Jwww/work`:
`python3 -m raveil project check nine-cell-dilation --backend rtl-sim --project <demo>`
and the same command with `retained9` both exit2 as expected. Original failures
remain; hints explain18 versus16 instructions and unused-result retention.
Both show actions=0/runs_created=0 and Docker daemon/image not checked. Demo
inputs are copies of retained T-0184 fixtures, not new research data.

Next pull reaches HCI-01 (architecture/authority): independent reviewer confirms
T-0182 cannot implement GE_IMM_U32 without explicit owner selection plus an
accepted versioned ADR/allowlist/RTL budget. Asked one asynchronous owner
question while continuing unaffected T-0195 integration. No false Ready card
or adoption claim is created. This is not a per-task demo/approval pause.

## T-0184 Graph pressure planning, immediate next pull

T-0194 merged via PR #258 as `1a6b18e`; canonical completion closed Issue #256.
Prepared then started existing Issue #183 / T-0184 on S-0005, 3 SP Planning,
branch `research/t-0184-graph-pressure`, one primary mutation owner. Initial
prepare rejected missing colon-form packet markers; corrected the formatting
without expanding scope, then canonical prepare/start succeeded. T-0185 and
T-0155 dependencies are complete. ADR-0088 applies; no new EXP/RFC collection.

`PYTHONDONTWRITEBYTECODE=1 python3 tests/fixtures/graph_pressure/check_pressure.py`
passes all 17 constructed descriptors: six admits and eleven exact rejects.
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_graph_device_dag tests.test_graph_device_affine -q`
passes 28 tests in 3.359s on macOS arm64 / Python 3.14.6. Host compiler/oracle
only: admitted cases have three oracle/fallback seeds; no RTL or performance.
Independent review correctly identified dead/unreachable values in the register
probes. Report labels them retention/order diagnostics, not intrinsic nine-live
pressure. Added exact 17-case coverage and explicit exceptions (no removable
assert checks), and included the input-generator source hash. Not a sealed
source/environment receipt. Current compiler and machine remain unchanged.

Chosen next increment: advisory pressure hints in existing project check.
It preserves acceptance; rejecting unused nodes would unnecessarily shrink
currently accepted programs. Scheduling/retirement, spatial tiling and physical
capacity remain deferred. No new Drive upload or Sprint ceremony acceptance.
Final independent planning re-review GO: normal and `python3 -O` fixture
commands pass exact coverage, with explicit limitations. Record checker passes
216 Markdown / 161 tasks / 112 records before successor registration; diff
check passes. T-0195 is the only selected implementation successor, 2 SP Host
Functional: explain rejections in existing preflight without changing admission.
Successor Issue #259 is registered; final record checker passes 216 Markdown /
162 tasks / 112 records. No runtime/compiler file changed in T-0184.

## T-0194 immediate continuation

Owner instructed completion -> summarize next action -> immediately execute,
not a Ready-only handoff. T-0193 is merged via PR #257 (`0c61ded`); Issue #255
is canonically Done. Started T-0194 / Issue #256, S-0005, 3 SP, via canonical
queue on `feat/t-0194-inspect-export` from that main, primary single owner.
ADR-0100 and Playable apply; no new EXP, RFC or research/hardware gate.
Original dirty root remains untouched. Weekly usage monitoring remains disabled
by explicit owner instruction; no Drive upload, paid resource or device action.

Implemented standalone read-only `project inspect-export`: strict bounded
JSON and payload verification, no extraction/project lookup or authenticity.
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_project_export -q`
passes 21 tests in 8.828s, exit 0, macOS arm64 / Python 3.14.6. Includes actual
export roundtrip, standalone CLI, unchanged source/no created output, malformed
UTF-8/deep JSON/duplicate fields, type and claim rejection, paths/collisions,
hash/size/base64, resource limits, symlink/hardlink/FIFO and changed-file cases.

Read-only successor review recommends existing T-0184 / Issue #183 over further
export hardening: its T-0185 and T-0155 dependencies are complete, and it returns
to user-visible editable Graph pressure. Refine a planning-only fixture/report
packet first; retain all current instruction/register/shape/halo bounds. No
new scheduler, tiling, ISA, transport or RTL implementation is pre-authorized.

Final T-0194 verification:
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_project_export tests.test_project tests.test_project_graph -q`
passes 85 tests in 34.887s, exit 0. Independent security reviewer reruns all
21 export tests and returns GO for bounded self-consistency, not authenticity.
`python3 -m raveil project inspect-export <persistent-artifacts>/t0193-selected-errors-v2.json`
exits 0: consistent, one member, 2 bytes, 19 omitted, unchanged bundle SHA-256
`704f2cdb1f72ed75fe16e1b02f80ed0e9ba2a7ac2fbfb1b447522aa68eab8fc3`.
Source_verified/authenticated/replayable_full_run remain false. No source
project, extraction, writes or execution are involved in this command.
Record checker passes 215 Markdown / 161 tasks / 112 records and diff check
passes. T-0184 existing Issue #183 packet is refined for a 3-SP Planning
successor, with an explicit fixtures/report allowlist and unchanged compiler.

## T-0193 explicit preview and derivative export

T-0180 merged through PR #254 and canonical completion closed Issue #178.
Owner-authorized T-0193 / Issue #255 is In Progress on S-0005 at 5 SP,
single primary mutation owner, `feat/t-0193-selective-export` from `5467832`.
ADR-0100 adopts bounded JSON/base64 selected derivatives, not archive import.
Initial focused suite passed 9 tests in 10.430s, including actual native run,
CLI preview/export, acknowledgement/token, path/selection failures, hardlink
source, symlink destination, source tamper and atomic publication failure.
No new Drive upload and no original-root edits were made.

Final candidate verification on macOS arm64 / Python 3.14.6:
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_project_export tests.test_project tests.test_project_graph -q`
passed 76 tests in 34.660s, exit 0. The new suite contributes 12 tests.
Independent review exposed original-record selection and missing O_NOFOLLOW
fallback; both were fixed and independently re-reviewed GO. Original records
are unselectable; unsupported no-follow reads fail closed. Cooperative
single-writer limitations remain explicit, not a hostile-concurrency claim.

Actual CLI demo:
`python3 -m raveil project export-preview 20260919T045403Z-e64a4000a996 --project <persistent-artifacts>/t0180-inspection-20260919 --member workspace/errors.txt`,
then `python3 -m raveil project export 20260919T045403Z-e64a4000a996 <persistent-artifacts>/t0193-selected-errors-v2.json --project <persistent-artifacts>/t0180-inspection-20260919 --member workspace/errors.txt --expect-preview 5c5e822d99ad9378bb5fc2edaae2a85070887131f54013de8c424e7a07d240b5 --acknowledge-sensitive-data`.
Both exit 0. Persistent artifacts are under the original repository's ignored
`artifacts/`, not tracked source changes. Inspection lists 20 files / 24,097 bytes;
export contains only `workspace/errors.txt` = `2\n`, 2 payload bytes, 19 omitted
members. Envelope is 935 bytes, mode 0600, SHA-256
`704f2cdb1f72ed75fe16e1b02f80ed0e9ba2a7ac2fbfb1b447522aa68eab8fc3`.
`python3 -m json.tool` and `stat -f '%Lp %z'` verified the actual envelope and
mode. The v1 development attempt remains separate, never overwritten.

Record checker passed 215 Markdown files / 159 tasks / 112 records before
successor refinement; `git diff --check` passed. Ordinary live queue audit
passed one active item, but horizon was empty. Ranked bounded next choices:
read-only derivative verification, archive import, broader privacy scanning.
Choose T-0194 verification (3 SP) because it closes the consumer inspection gap
without extraction authority; defer the latter two. No duplicate export issue
was found. T-0194 is prepared only, not implemented in the six-step packet.
Weekly usage monitoring remains disabled by owner instruction. No new Drive
transfer, paid resource, device access or Sprint ceremony acceptance occurred.
Post-refinement record checker passes 215 Markdown files / 160 tasks / 112
records; `git diff --check` exits 0. T-0194 is tracked as Issue #256.

## T-0180 closeout and authorized successor

Owner requested steps 1–6: integrate the design, close the planning task,
then implement/demo a bounded preview and selective export with explicit
derivative lineage. No archive import, automatic whole-run sharing or research
promotion is authorized. Five fixture tests pass; prior reference regressions
passed 64 tests. Root dirty worktree remains untouched; no new Drive transfer.

## T-0180 inspection fixture validation

Inspected the persistent synthetic native run's 20 regular files, recording
exact byte counts separately from the existing hashes. Four planning-only
fixture tests pass via `python3 tests/fixtures/project_export/check_inspection.py -v`:
file-set/total consistency, relative path rejection, casefold duplicate
rejection, and invalid types/budgets/link metadata. No archive is parsed or
extracted; metadata tests do not prove filesystem isolation or privacy.
Recommendation remains inspection-preview first, automatic archive/import
deferred. No Drive upload performed under the weekly preservation policy.

Independent reviewer identified undercounted implicit directories. The model
now counts files plus unique parents and rejects file/directory prefix conflicts;
a fifth regression covers 513 two-component files exceeding 1024 members.
Link-key tests reject JSON metadata only, not real archive headers; this
limitation is explicit. The original four-test result is retained above.

## Owner-directed weekly preservation and resumed delivery

- Owner requested Drive saves approximately weekly and work resumption.
  ADR-0099 records weekly batches before Review, immediate persistent local
  saves/checks, and unchanged remote-promotion integrity. No scheduler added.
- Canonical queue started T-0180 / Issue #178 in S-0005, 3 SP, Planning, from
  main `d0e3bf1` on `docs/t-0180-export-boundary`; dirty root work is untouched.
- Initial export-boundary draft follows actual `Project.load_run` and `tree`
  constraints. Actual-run fixture and final design review remain; no runtime
  exporter/importer or new remote upload was added.
- Sprint Review Accept was not inferred from the owner's work-resume request.
- Actual new native sample run `20260919T045403Z-e64a4000a996` passed reference
  validation and produced `errors.txt` = `2\n`. Existing load_run/tree produced
  the inspection-only member fixture. It reveals unrelated input snapshots,
  so the draft now explicitly defers blind whole-run export as a disclosure risk.
  Evidence remains local under the weekly policy; no new Drive transfer.
- Verification: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest
  tests.test_project tests.test_project_graph -q` passed 64 tests in 46.749s
  (macOS arm64 / Python 3.14.6). Record checker passed 214 Markdown files,
  158 task definitions and 111 records; `git diff --check` passed.

## Final preservation and technical acceptance

- Owner restored existing Drive authentication. An initial directory listing
  showed no EXP-0012 destination, so the new run path did not overwrite a bundle.
- `rclone copy <persistent-run-root> <configured-remote>:Raveil/research-data/EXP-0012/20260919-recollection-01 --immutable`
  and `rclone check <persistent-run-root> <same-remote-path> --download`
  exited 0: 40 matching files, zero differences. No sync/delete was used.
- `REMOTE_COMPLETE.json` was transferred last with immutable `copyto`, then
  the full download check passed again: 41 matching files, zero differences.
  Marker SHA-256:
  `ab3760876c1b7a3043ebb60a5a41de9ff7e293599fb4453a0e2dd730836ee1ca`.
- Independent final reviewer found Issue #239 criteria 1–7 satisfied subject
  to remote verification and record/PM closeout. PM now accepts the bounded
  functional task; no broader research gate or historical recovery claim.
- Focused tests rerun: 31 pass in 6.930 seconds. Garden `--keys 3dq` rerun
  exited 0, showing numeric ReLU selection and directed dependency view.
- S-0005 T-0180 remains Ready; live horizon audit passes one active and one
  pullable Ready item. Sprint Review remains awaiting explicit owner disposition.

## T-0191 separately authorized recollection

The owner approved a new collection after the historical evidence-availability
incident. Historical receipts remain unavailable; no old result or hash is
replaced. Project #1 / Issue #239 resumed In Progress before collection.

- New run ID: `20260919-recollection-01`, under ignored persistent
  `artifacts/research/EXP-0012/` in the original repository, not temporary storage.
- Source snapshot: `source-a729ce1.tar`, SHA-256
  `83a7818c893af79ad362a511ec5954f51a544c62bb12b2f4c948737ff7c80d30`.
- Existing pinned image only; no build, pull, remote artifact upload or device use.
- Generate and verify RTL before compiling the three programs. Same frozen ADF,
  image and normalized RTL identity as EXP-0012. The existing same-image
  Docker bind-copy repair was used and recorded by the generator.
- Three independent oracles matched: neighborhood 170, elementwise 740,
  reduction 374; Raveil accepted the simulator result (no CPU fallback).
- Strict verification passed: 26 run files and 13 RTL files.
- Raw run receipt SHA-256:
  `3149295c139e220eedd570c47bd4c3b2cebed8e121a471e40ef27ee68f0e3ee4`.
- Canonical private-receipt SHA-256 (different JSON serialization):
  `fa135f710f61a83f8400ae5429d4263c3e685a13b4f769aff3f7b989da3cb792`.
- Authorized receipt SHA-256:
  `766ad944778e5c00720d8f62c28407b9a21e843ba77391204d636bb620827cb2`.
- RTL receipt SHA-256:
  `b55c96ee7cc9c7406b3d25b03ff095f4893f7a62621cf69a396e5f806295e357`.

Commands from the isolated worktree, on macOS arm64 / Python 3.14.6:

```sh
python3 experiments/openasip/generate_rtl.py --output <persistent-run-root>/rtl
python3 experiments/openasip/verify_evidence.py rtl <persistent-run-root>/rtl
sh experiments/openasip/run_three_programs.sh --simulate --evidence-dir <persistent-run-root>/run
python3 experiments/openasip/verify_evidence.py run <persistent-run-root>/run
python3 experiments/openasip/verify_evidence.py rtl <persistent-run-root>/rtl
```

All commands exited 0. Evidence class is Host Functional external simulator /
generated RTL, not RTL simulation, performance, FPGA or silicon. Persistent
local storage is not remotely verified durability. Independent final review and
approved remote preservation remain; T-0191 is not Done.

Independent Security Reviewer re-ran both strict verifiers and hashed all three
receipts plus the source archive: GO, exact identities above confirmed.
`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_t0191_openasip -q`
passed 31 tests in 5.448 seconds. No runtime code changed in this recollection.

## Integration, successor and Sprint Review candidate

- Remote preservation attempted against the existing `gdrive` research-data
  connection. Read-only listing failed with OAuth `invalid_grant`; no upload
  occurred. Owner reconnection is required before immutable transfer and
  download verification. T-0191 final closure remains pending, not Done.
- Final acceptance review found missing exact cleanup/retry instructions;
  README now specifies full-ID operator reconciliation, no name-based deletion
  or prune, fresh evidence paths, and simulator-versus-fallback distinction.
- RTL generation and strict verification completed before the compile command;
  the normalized identity was already frozen in EXP-0012. This is observed
  invocation order, not a cryptographic ordering attestation.
- Canonical prepare registered existing T-0180 / Issue #178 as S-0005,
  Planning, Experience Implementer, 3 SP, P1/Ready. It is a bounded saved-run
  export contract/inspection fixture, not runtime export/import implementation.
- Garden demo at source `60bae26`: `python3 -m raveil garden --fixture
  tests/fixtures/garden/minimal.json --keys 3dq --width 100` exited 0:
  numeric 3 selects ReLU; d displays admitted dependency edges and selected
  node; q exits. The initial comma-separated keys invocation was invalid;
  corrected invocation is a contiguous key sequence, not comma-separated.
- Saved new run strict verification was also demonstrated (26 files).
  This rechecks saved output; it does not rerun simulation.
- Actual `check_cancellation.py oacc` and `ttasim` demos both exited 0 at
  the Docker-enabled boundary: process observed, unpublished cancelled receipt,
  owned container absent. An earlier restricted-boundary invocation reported
  no witness and was not counted as success.
- Sprint Review remains pending owner disposition. No automatic Accept.
- Retrospective candidate: Keep independent strict verification; Problem
  temporary evidence was unavailable and remote credentials have expired;
  Try require persistent evidence placement plus verified remote preservation
  before research closeout. No claim that historical evidence was recovered.
## T-0182 owner-approved kickoff (earlier state)

Owner approved the unsigned threshold comparison implementation after the
explicit HCI-01 question. ADR-0101 fixes descriptor v5/program v6/request v7,
unchanged capacity and simulation-only scope. Primary owns tracked mutations
on feat/t-0182-unsigned-threshold; independent security review mapped native,
RTL and source-manifest gates without editing. No comparison RTL result or
completion is claimed at kickoff. Weekly usage monitoring remains disabled
by owner; Drive preservation remains weekly.
