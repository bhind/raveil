# 2026-09-08

## T-0178 recipe fork resumed

- The 10,080-minute weekly window reset. At 2026-09-08T11:35:44Z it reported
  7% used and 93% remaining. Project audit showed T-0158 Review plus T-0178
  Ready, and the task worktree was clean at canonical main `68d7253`.
- The owner accepted recipe-last publication only with explicit operational
  constraints. ADR-0098 and the workspace guide state single-user/single-writer,
  no concurrent destination editing or shared synchronized mutation, possible
  crash-orphan files, inspection before name reuse and no transaction/sandbox
  claim.
- Canonical start moved Issue #176 to In Progress. Implementation preflights
  sources/destinations, copies Graph inputs before the recipe commit marker and
  cleans only newly created identity-matching files on caught failures.
- `python3 -m unittest tests.test_project tests.test_project_graph` passed 64
  tests in 19.161 s after one corrected test-only missing `shutil` import.
- The first host demo attempt retained failed run
  `20260908T113932Z-09044c00e265`: the sandbox denied access to the Docker
  socket before the dynamic runner could execute. This is environment failure
  evidence, not an RTL or fork result.
- With Docker access, source run `20260908T114052Z-ae68407223ee`
  (`immediate=5`) and fork run `20260908T114213Z-0e1b60c4895c`
  (`immediate=7`) both passed descriptor-oracle, C++ fallback and RTL byte
  equality. Their simulator hash
  `20ad478928f669fbffcef3d841fa2b2acb9e86d32a1dd9dbed9010b469a0f9fa`
  and RTL manifest were identical. `project diff` reported unchanged 324-word
  input, only the bias immediate changed and all 64 active outputs increased by
  two. Evidence is RTL-simulation-functional; performance was not measured.
- `python3 -m unittest discover -s tests` passed 805 tests in 130.254 s with
  two skips.
- The current-candidate Vreji review reran 64 scoped tests, passed
  `git diff --check` and found no material publication concern. It confirmed
  no copied external implementation or new dependency and no authority,
  performance or silicon claim. The guide was narrowed so source admission is
  not mistaken for descriptor/input semantic validation.
- At 2026-09-08T11:49:55Z weekly capacity was 8% used and 92% remaining;
  canonical Project audit passed with T-0158 Review and T-0178 In Progress.
- Final PR review found that direct `O_CREAT` of the destination recipe exposed
  its path before all JSON bytes were written. The candidate now writes a
  non-discoverable temporary recipe and atomically publishes a complete
  no-clobber hard link. A fault-injected partial-write test proves the final
  recipe remains absent and the temporary file is cleaned. After the fix, 64
  scoped tests passed in 19.235 s and the full 805-test suite passed in
  123.164 s with two skips.
- Re-review resolved the Medium publication finding and identified only a Low
  wording mismatch. ADR-0098 and the guide now describe the precise crash
  leftovers: invisible partial temporary data, or a complete destination plus
  its temporary hard link. The reviewer found no remaining implementation
  concern.
- PR #229 merged commits `9118fa4` and `6b85bd7` to canonical main as
  `85e17d6`. Issue #176 closed automatically and `project_queue.py complete`
  recorded the exact acceptance, observed cycle and resources before moving
  T-0178 to Project Done.

## T-0179 read-only project preflight

- Issue #177 was normalized to the canonical independence packet, prepared at
  3 SP in S-0003 and started P0/In Progress on branch
  `feat/t-0179-project-preflight`. Dependencies T-0171 and T-0160 are complete.
- `project check` separates recipe, input, admission and tool availability. A
  non-executing Command registry reuses syntax, allowlist and path admission
  without the normal `--version` subprocess. Graph checks reuse the descriptor,
  transport-profile and optional 324-word snapshot validators.
- Host demo project `/private/tmp/raveil-t0179-demo.3PmC8B` reported all known
  prerequisites present for `logs/native` and `bias-grid/rtl-sim`;
  `gemm/sonatine-qemu` failed only the absent Sonatine kernel prerequisite.
  Every result kept execution readiness not checked, the RTL result also kept
  Docker daemon/image state not checked, and `runs/` remained empty.
- `python3 -m unittest tests.test_project_preflight tests.test_project_graph`
  passed 47 tests in 11.894 s. `python3 -m py_compile` for the three changed
  Python files and `git diff --check` also passed. Evidence is Host Functional;
  no workload tool, QEMU, Docker daemon, RTL simulation, network, device,
  installer or performance measurement ran.
- Security review found that the first candidate accepted a non-executable RTL
  runner and labelled known prerequisites as overall readiness. The revised
  probe requires an executable non-symlink regular runner, and the stable header
  now separates `known_prerequisites` from
  `execution_readiness=not-checked`. Neither a zero exit status nor the summary
  claims that Docker, QEMU, compilation or a workload will execute successfully.
- After those corrections,
  `python3 -m unittest tests.test_project_preflight tests.test_project_graph`
  passed 49 tests in 11.120 s and `python3 -m unittest discover -s tests`
  passed 820 tests in 171.486 s with two skips. Record checking and
  `git diff --check` passed.
- Security re-review reran the 49 scoped tests and found both Medium findings
  resolved with no remaining material security or authority concern. The
  current runner is executable, and the preflight remains advisory and
  non-authoritative across its documented time-of-check/time-of-use gap.
- PR #231 merged exact `e8bdd68` to canonical main as `3537b74`. Issue #177
  closed and `project_queue.py complete` recorded acceptance, observed cycle
  and resources before canonical Project Done was verified.

## T-0185 bounded Graph workload examples

- Issue #184 was refined to exactly two copied-before-use applications and
  started in S-0003 at 3 SP on branch
  `feat/t-0185-graph-workload-examples`. T-0173 and T-0174 are complete.
- The fixed scope reuses the current Graph compiler, snapshot transport,
  Project workflow and RTL simulator: 8x8 cross-neighborhood binary dilation
  and `(x * x + 17) modulo 2^32` sensor-energy-plus-bias.
- Independent domain formulas, actual RTL execution and an edit/rerun/diff/
  Garden demonstration were required before closeout. No performance, FPGA,
  silicon, physical-sensor, signed/saturating, predicate or multi-output claim
  is authorized.
- `python3 -m unittest tests.test_graph_workload_examples
  tests.test_graph_device_dag tests.test_project_graph` passed 58 host tests.
  The new tests independently compute both domain formulas and compare their
  fixed 256-word transport images with the descriptor oracle and installed-program
  software fallback. Halo effects, ignored transport padding and uint32
  overflow boundaries are explicit.
- Actual RTL simulation run `20260908T132753Z-18a122cb4ad7` passed cross
  dilation and `20260908T132850Z-4d8a72aaa5eb` passed energy-plus-bias. Both
  reported byte equality across descriptor oracle, C++ fallback and RTL with
  simulator SHA-256
  `20ad478928f669fbffcef3d841fa2b2acb9e86d32a1dd9dbed9010b469a0f9fa`.
- After input word 23 changed from zero to one, actual RTL run
  `20260908T143619Z-13b85922cbfe` passed the same three-way equality. Project
  diff reported one of 324 input words and three of 64 active outputs changed;
  program, simulator and RTL manifest identities stayed equal. Integrity-
  checked output and read-only Garden inspection passed. Timing was not
  compared and performance was not measured.
