# 2026-08-14

## T-0107 estimate and authority-freshness correction

- The T-0042 post-ADR-0046 estimate of three to six working days was wrong.
  ADR-0046 commit `793973e` was committed at 09:35:53 +09:00; implementation
  candidate `0f4be3b` was committed at 12:24:11 +09:00, an elapsed 2 hours 48
  minutes. The error was an overestimate, not schedule variance.
- Root cause: the estimate was not rebuilt from the reduced exit contract. It
  carried intuition from the superseded token-lifecycle scope, omitted the
  existing Graph scratchpad, CPU manager, workload, oracle, wrappers, Docker
  build cache, and current agent delivery rate, and did not separate edit,
  verification, and integration effort.
- A second process failure occurred concurrently: `0f4be3b` descends from
  `793973e` but not from the later phase/authority commit `cb2d56b`. Exact
  `git merge-base --is-ancestor cb2d56b 0f4be3b` exited 1. Therefore the branch
  is an implementation candidate pending integration, not canonical completion,
  even though its bounded RTL evidence independently replayed.
- T-0107 adds the estimate template, mandatory scope-change re-estimation,
  warm/cold and delivery-rate evidence, separate edit/verification/integration
  ranges, and the authority-ancestry completion check to WORKFLOW and the PM
  role. Failure knowledge retains the observed mismatch and detection rule.
- Regression coverage parses every agent TOML and asserts that the PM role
  names the estimate template, warm/cold build state, and latest authority
  commit; it also asserts the template's authority, cache, and integration
  fields. `python3 -m unittest tests.test_experiment.AgentBoundaryTests -v`
  passed 2/2. Direct Python `tomllib` parsing of the PM TOML exited 0;
  `git diff --check` exited 0 with no output; and
  `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  exited 0 with `record check passed: 119 Markdown files, 87 task definitions,
  54 records`.

## Unfinished-task start phases

- All 23 unfinished TODO tasks are assigned once to a start-timing phase. P0
  immediate contains only T-0042; P1 next contains T-0044; P2
  result-conditioned contains T-0106. P3 retains five future-planned tasks
  without scheduling them, and P4 retains fifteen optional/triggered tasks
  without a default start date.
- The implementation WIP limit is one coherent P0 task. An existing branch,
  patch, local modification, partial milestone, sunk effort, or spare agent does
  not authorize resumption. Promotion requires a dated trigger, dependencies,
  owner, bounded deliverable, stop rule, and previous-P0 disposition.
- The current unfinished stripped/malformed token diagnostic is mapped to P2
  T-0106. It remains unverified local work and must not interrupt the P0
  controlled common-resource T-0042 slice. No local modification was deleted,
  staged, committed, or interpreted as implementation evidence by this
  planning change.
- `TODO.md` is the canonical assignment table;
  `docs/guides/TASK-START-PHASES.md` defines promotion and WIP handling, and
  `docs/WORKFLOW.md` routes implementation kickoff through it. This is
  documentation-only organization and changes no implementation or measurement
  result.
- Closeout's exact AWK assignment audit exited 0 with
  `unfinished_definitions 23` and `phase_assignments 23`; every unfinished task
  appears exactly once and no unknown task appears in the phase table.
  `git diff --check` exited 0 with no output. `python3
  .agents/skills/raveil-task-governance/scripts/check_records.py` exited 0 with
  `record check passed: 118 Markdown files, 86 task definitions, 54 records`.
  No code or RTL test was run because the change only controls task start time.

## T-0042 small-start reset

- A scope audit found that T-0042 had accumulated many CPU token-lifecycle
  diagnostics while the resource-matched Rocket/BOOM/Graph comparison boundary
  remained unimplemented. This repeated the earlier project failure mode of
  improving a prerequisite instead of testing the core hypothesis.
- ADR-0046 accepts a narrower sequencing rule. T-0042 now closes on one frozen,
  quiescent controlled-run slice with exact semantic agreement, equal owned
  ports/buffering/request capacity/arbitration/width/response rules, rejection
  of unaccounted execution-window traffic, and complete six-phase plus total-
  cycle records. It makes no general per-operation semantic-attribution claim.
- T-0106 receives reset/stale/duplicate/exhaustion, replay, post-request
  exception and post-A rollback, arbitrary ELF identity, general non-CPU
  traffic exclusion, multi-live-token, and Rocket/BOOM lifecycle-parity work.
  It starts only after T-0044 survival or an accepted product requirement.
  Existing ADR-0045 evidence and fail-closed semantics are retained; only their
  position on the first-comparison critical path changes.
- `docs/guides/T-0042-SMALL-START.md` is the active implementation packet. It
  explicitly forbids starting another deferred token case before connecting the
  controlled CPU paths to the ADR-0043 resource boundary.
- Documentation closeout ran from the dedicated
  `docs/adr-0046-t-0042-small-start` worktree. `git diff --check` exited 0 with
  no output. `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  exited 0 with `record check passed: 117 Markdown files, 86 task definitions,
  54 records`. No RTL or performance test was run because this change only
  reorganizes accepted scope and makes no implementation or measurement claim.

## T-0042 BOOM store token handoff to owned-manager D

- T-0042 remains in progress on Gate 1 under accepted ADR-0045. This slice adds
  no new ADR or EXP and does not change the ADR-0043 common bridge or its
  one-outstanding contract.
- A repository-owned `{valid, epoch, sequence}` request metadata field now
  follows the already-authorized pinned BOOM store from the LSU request through
  the DCache request pipeline and uncached TileLink A request. The owned manager
  samples it only on the exact target `Put` A acceptance, retains it in the
  existing one-outstanding state, and reports the same fields with the matching
  D completion. ROB, STQ, PC, address, branch mask, lane, and TileLink source
  remain diagnostic or transport context rather than semantic identity.
- Classification fails closed when the metadata is absent or malformed. This
  diagnostic uses fixed epoch 1 and one local sequence and does not yet
  implement epoch rotation, reset-outstanding handling, exhaustion, retry,
  stale/duplicate-token, stripped-field, source-reuse/backpressure, or
  loader/FESVR/Debug negatives. It does not feed `InitiatorCpu` or promote the
  ADR-0043 bridge.
- The first connected RTL run reached the BOOM-local authorization ledger with
  token `(1, 1)`, but the manager observed invalid zero metadata. Source
  inspection found that the pinned DCache partially assigned the request from
  `s2_req` into the MSHR request, so adding members to `BoomDCacheReq` did not
  automatically preserve them. Explicit `s2_req -> MSHR` assignments corrected
  the path. A subsequent successful RTL trace was initially rejected because
  the verifier compared differently zero-padded hexadecimal address strings;
  numeric address comparison corrected that verifier-only failure. Both failed
  attempts remain diagnostic evidence and make no attribution claim.
- Fresh and cached executions of
  `./hardware/chisel/run-owned-boom-store-token-handoff.sh` passed with identical
  `input_sha256=364b956d9b62f3cc7935da50877175f1f12c1e7b9ffc7214a4ab3da50e5d33e2`,
  `source_sha256=fc03e3f8bd5e81a0389a41429d8b89963c2a446c5ff526e5e20620e4fa1ec596`,
  and
  `graph_sha256=8fbee170f393e6ed777484c9fbb3f6e0d6adddbf1802525fa5a1bb680756a948`.
  The post-patch pinned-source hashes were
  `Bundles.scala=f1f8190de5064a50ac184749c965b09259748f7e20954fe2566cdfced9c41586`,
  `lsu.scala=fd57219554cea6be5c30dceed6b590f864cabdd4bf2d5a8db1729f362a258815`,
  `DCache.scala=bb2f712f941368e7be57049fb99378520646054ce47ac31e45bda2ebc01a2f49`,
  and
  `mshrs.scala=92210ec6fca1699c9083c5726363e7ec48dd4d995b6238c664ddb704a2a8c755`.
- Each execution completed after 10,916 simulation cycles. The exact bounded
  trace contains one BOOM authorization/request/response/clear at PC
  `0x8000001c`, address `0x08000100`, ROB 7, STQ 3, and lane 0; one target Put A
  with TileLink source 8304 and token `(valid=1, epoch=1, sequence=1)`; the
  matching non-denied, non-corrupt AccessAck D with source 8304 and the same
  token; and the existing target readback using source 8288. The software
  signature remained `0x51a7c0de`.
- This is bounded `rtl-simulation-functional` plus
  `rtl-elaboration-topology` evidence. It establishes one same-token
  observation from an already-authorized BOOM store request to owned-manager D.
  It does not establish complete ADR-0045 semantic attribution, CPU consumption
  of D, general lifecycle correctness, replay/reset/multi-live behavior,
  resource matching, matched comparison, performance, power, area, timing,
  OoO effects, FPGA, silicon, novelty, non-infringement, patent clearance, or
  FTO. Those measurement questions remain separate in T-0044.
- Closeout passed `sh -n` for the shared and dedicated runners,
  `python3 -m py_compile` for both affected verifiers, focused owned-memory
  boundary tests at 34/34, full discovery at 232/232 with one expected
  real-QEMU integration skip, the record checker over 116 Markdown files and
  53 records, `git diff --check`, and `./scripts/ci-local.sh`. The mutation
  suite rejected changed or missing valid/epoch/sequence fields,
  classification, DCache origin, source, target address, manager ordering,
  denial status, missing or duplicate token events, and source mismatch. Local
  CI used Python 3.14.6, Apple clang 21.0.0, RISC-V GCC 16.1.0, binutils
  2.47.20260726, and QEMU 11.0.3; it completed Sonatine release/debug builds,
  QEMU smokes, and native/QEMU Graph checks. These repository checks do not
  expand the bounded RTL claim.

## T-0042 ADR-0046 controlled equal-resource small-start

- Authority is accepted ADR-0046 plus estimate-governance authority commit
  `ae606dea3f74d6694eecbe91fc368b097f03758c`. Implementation was selectively
  transplanted from non-descendant candidate `0f4be3b` onto dedicated branch
  `feat/t-0042-common-resource-integration` and worktree
  `/private/tmp/raveil-t0042-common-resource-integration`; the candidate was
  not merged wholesale and the existing dirty
  `test/t-0042-boom-token-stripped-after-valid` worktree was not reset,
  overwritten, staged, or committed. Gate 1/T-0042 applies; no new ADR, RFC, or
  EXP is required because this implements ADR-0046 without changing the frozen
  workload, core microarchitectures, or ADR-0043 one-outstanding boundary.
- The minimum connection is Rocket or BOOM DCache -> repository origin tagger
  -> adapter-specific TileLink translation -> one controlled
  `RaveilOwnedTLMemory` ingress, and `StaticStencilRegion` -> one
  `OwnedFixedLatencyScratchpad` ingress. Graph now uses one physical 1,024-word
  bank with logical input `[0,324)` and private output `[324,580)`; the CPU
  manager exposes the same 580 valid words in a 4 KiB mapping. Upstream caches,
  crossbars, fragmenters, and width translation remain adapter-specific and
  outside the compared owned resource.
- The canonical resource tuple fixes one physical bank, 1,024 physical and 580
  valid words, four-byte operations, 32 data bits, four byte-mask bits, one
  request and one response port, maximum one outstanding request, request
  buffer depth zero, response buffer depth one, no arbitration at the owned
  ingress, read and byte-mask-write operations, response availability one
  module-local cycle after acceptance, and stable response until consumption.
  Resource SHA-256 is
  `16664d8ed96865c60ea41c91452b5e6748b055e0dfef3f786b13bd6f90127748`;
  controlled-run contract SHA-256 is
  `f95cc6dc896659a32f1407f0f5a8f74ec475d208632f9dd5ff4d43d9cb2f7978`.
  TileLink diplomacy still advertises the surrounding bus's legal transfer
  range; controlled data admission explicitly rejects non-four-byte requests.
- The frozen seed-1 input digest is
  `65b00605d894f4a6934862137c751e7f25e7c86a41077f6172cb7142c7ab9254`.
  Independent oracle and all three observed-output digests are
  `dd749f0f218c7389730bef5b97af4e9203b0501d5ec57fa48ffa643356f23582`.
  Descriptor SHA-256 remains
  `d4bf9395a510385f42ba4a193ae2c747f308ad502a8fe807843ed19c2fa4d1e2`;
  simulation-adapter SHA-256 remains
  `56dbe3f2ab479233eb5e4fe1c79eb06e07458b42ea77acebb471a101afd24c1e`.
  Integrated CPU source SHA-256 is
  `dac7b3e14a586f9887765ba60ed58a3baffbe9342906b1c20fab303cf3101f6e`;
  static Graph source SHA-256 is
  `005eeaa6889c1948f56593c12ecd68ede61a1aa7b4ba8ef729a1f855ae73cee2`.
  CPU source identity includes the frozen C/assembly workload, linker and
  verifier inputs in addition to the pinned overlay and patch set. The
  aggregate separately binds the compiled ELF or Verilated executable,
  toolchain contract, and derived configuration identity for each peer.
- `./hardware/chisel/run-static-stencil-rtl.sh` passed in linux/amd64 Docker
  using Chisel 7.2.0, Java 17.0.19, Scala CLI 1.10.1, and Verilator 4.038.
  Both seed-1 and seed-3 strict windows reported phases
  `installation/staging/execution/completion/validation/publication =
  0/648/3072/1/512/0`, total 4,233, accepted/completed 1,536/1,536,
  pending zero, quiescence 1/1, and unaccounted traffic zero. The independent
  oracle also passed both seeds; the legacy v2 incomplete/unmatched records are
  retained unchanged as earlier evidence. The separately captured integrated
  Graph raw log is 12,501 bytes with SHA-256
  `cb4f04ba35425a533331434885382e75591a489f76b79729e8397d9c32cb21e2`.
  Two independent rebuild/reruns produced that exact byte hash and identical
  source, artifact, toolchain, and configuration identities.
- `./hardware/chisel/run-controlled-rocket-stencil.sh` passed from the pinned
  source and warm build cache
  for `chipyard.raveil.RaveilMatchedRocketConfig`. The exact execution window
  is cycle 48,495 through 63,116, or 14,621 cycles; it accepted/completed
  1,056/1,056 with 800 reads, 256 writes, unexpected/non-origin/pending zero,
  and quiescence 1/1. Phase vector is
  `44630/3865/14621/410/16513/0`, total 80,039. The persistent raw log is in
  Docker volume `raveil-chipyard-matched-rocket-controlled-v1` at
  `/4912642ba744b1c233d63b22bc4ef75c37e41ee6186880f12801a75fab775e3b/riscv_stencil_controlled.log`;
  it is 3,872,723 bytes with SHA-256
  `e8e630eea0ed7020ee19bf1b2f13fe2288ae55c818c19a4300ed74c628ef376d`.
- `./hardware/chisel/run-controlled-boom-stencil.sh` passed from the pinned
  source and warm build cache
  for `chipyard.raveil.RaveilMatchedSmallBoomConfig`. The exact execution
  window is cycle 48,969 through 70,861, or 21,892 cycles; it
  accepted/completed 1,056/1,056 with 800 reads, 256 writes,
  unexpected/non-origin/pending zero, and quiescence 1/1. Phase vector is
  `44761/4208/21892/425/16513/0`, total 87,799. The persistent raw log is in
  Docker volume `raveil-chipyard-matched-boom-controlled-v1` at the same
  source-hash-relative `riscv_stencil_controlled.log` path; it is 2,363 bytes
  with SHA-256
  `796aa14d84159e5b376ab09d984f6336d7dcb42a7bb61f5f4fe543bd17f9ee2f`.
  Chipyard is pinned
  at `ac58f38d77c99e9d1cafa64dfd6d4b00bdcd43e1`; BOOM is pinned at
  `9459af0c1f6847f8411622dac770ac78fe10847c`.
- `./hardware/chisel/run-controlled-three-way-stencil.sh` then replayed all
  three and emitted `raveil.controlled-run-aggregate/v1` with
  `resource_contract_verified=true`, `resource_equality_verified=true`,
  `semantic_valid=true`, `traffic_conserved=true`, quiescence 1/1, and bounded
  functional `comparison_eligible=true`. Each peer is deliberately ineligible
  alone; only the exact three-record aggregate may promote equality.
  The captured integrated wrapper log is 56,358 bytes with SHA-256
  `8d8afe33af9ca7a4ffc955048115a6a92812db826218e4359befee7e30214139`.
  Graph/Rocket/BOOM artifact hashes are respectively
  `8af26a8e...7686`, `52175480...8d30`, and `bd78fc8a...71aa`;
  toolchain hashes are `20093415...7037` for Graph and
  `ae2488b8...8eee` for both CPUs. Configuration hashes are
  `3ff3df8a...a33b`, `a7899b30...3a16`, and `29659996...3e41`.
- Dynamic execution traffic is not equal: Graph is 1,280 reads plus 256 writes
  (1,536 transactions), while Rocket and BOOM each execute 800 reads plus 256
  writes (1,056 transactions) because the pinned optimized ELF reuses values.
  The aggregate therefore reports `dynamic_memory_traffic_equal=false`,
  `t0044_measurement_claim_ready=false`, `semantic_initiator=not-proven`, and
  `performance_claim=false`. This is bounded `rtl-simulation-functional`
  evidence only. It is not a performance, latency advantage, CPU/ISA advantage,
  OoO-removal, energy, area, timing, FPGA, ASIC, silicon, security, or general
  semantic-identity result. T-0044 owns fairness preregistration and any later
  measurement; T-0106 owns reset/stale/duplicate/exhaustion, replay, post-A
  rollback, arbitrary-ELF identity, and other deferred token hardening.
- Failed attempts remain evidence. An initial CPU execution threshold assumed
  1,280 dynamic reads, but the pinned optimized ELF produced exactly 800; the
  strict record now separates semantic operations from observed manager
  traffic. FESVR signature validation changed origin/source between fragments,
  so validation is instead admitted only as the exact sequential 256-word
  output readback and never treated as semantic identity. Restricting TileLink
  diplomacy itself to `TransferSizes(4,4)` failed elaboration in
  `TLAtomicAutomata`; surrounding bus negotiation was restored and the owned
  controlled ingress now rejects non-four-byte data requests. One parallel
  Rocket/BOOM assembly attempt ended with exit 137 under host memory pressure;
  sequential BOOM retry passed. Finally, the first strict parser rejected
  Chisel's padded decimal marker values; it now accepts exactly one
  whitespace-separated value after each key while retaining exact field and
  cardinality checks.
  Independent review then found that the first CPU aggregate source identity
  omitted the workload/linker/ELF/toolchain binding. The integrated verifier
  now requires source, artifact, toolchain, and derived configuration hashes
  for every peer and mutation tests reject their removal or change. A first
  toolchain scheme used the Docker image manifest and was rejected after
  BuildKit attestation metadata changed the hash across identical rebuilds;
  the accepted scheme binds the pinned base-image Dockerfile, platform, locked
  environment, and exact tool versions. One run started under the rejected
  scheme was interrupted and discarded before aggregation.
- The non-descendant donor candidate had passed its full 244-test/CI closeout;
  that result was not treated as integration authority. On the `ae606de`
  descendant, `python3 -m unittest tests.test_controlled_run -v` passed 10/10,
  the static/simulation-adapter/signature suite passed 27/27,
  `tests.test_owned_memory_boundary` passed 36/36, `sh -n` passed for the five
  affected runners, `python3 -m py_compile raveil/controlled_run.py` passed,
  the governance checker passed over 119 Markdown files, 87 task definitions,
  and 54 records, and both staged and unstaged `git diff --check` passed. These
  repository checks do not expand the bounded RTL claim.

- Integration replay first completed the Graph path but exited 1 before CPU
  execution because the isolated worktree had no local ignored Chipyard/BOOM
  checkout. The worktree then referenced the already pinned clean checkout;
  the exact three-way command exited 0. This was an environment-location
  failure, not semantic, resource, traffic, or source drift.
  The final exact three-way replay with the accepted identity scheme exited 0.

- This verified exit closes T-0042 and displaces it as the sole P0. T-0044 is
  promoted to P0 for only the preregistered 1/4-invocation latency/traffic
  pilot. The trigger is the exact three-way oracle/resource/accounting pass;
  dependencies are RFC-0004, RFC-0005, ADR-0039, ADR-0046, the pinned
  linux/amd64 Docker/Verilator path, and the integrated commit. The primary
  agent owns records and claims. Stop after the 1/4 pilot and fairness finding;
  fail closed on oracle/resource/traffic/accounting/source/config/matrix/window
  failure, and do not infer the RFC-0005 numerical no-go below 64 fresh inputs.

## T-0042 BOOM negotiated default-invalid token negative

- T-0042 remains in progress on Gate 1 under accepted ADR-0045. No new ADR or
  EXP applies. This slice does not change the ADR-0043 bridge or promote
  `InitiatorCpu`.
- A negative-only pinned BOOM patch advertises the repository token request
  fields at the DCache client but deliberately omits LSU token members/minting
  and I/O-MSHR token assignments. The existing TL field definitions and Xbar
  aligned defaults therefore supply `{valid=0, epoch=0, sequence=0}`. Pinned
  post-patch source hashes and exact `raveilTokenValid` absence checks on LSU
  and MSHR source prevent accidental reuse of the positive producer.
- The exact fresh command was
  `./hardware/chisel/run-owned-boom-store-token-default-invalid.sh`; the same
  command was then run again against the completed persistent source cache.
  Both executions passed with identical
  `input_sha256=8d109d8787cb72c6bf542ad8560ab17657343b91f98163c434b7e03ce9802de3`,
  `source_sha256=edcd72365092364af11f80d43107db3cd6ecad3cd6139fcaa6967b1bf8288e3e`,
  and
  `graph_sha256=8fbee170f393e6ed777484c9fbb3f6e0d6adddbf1802525fa5a1bb680756a948`.
  Each completed after 10,916 simulation cycles; the second build reported
  `make: Nothing to be done for 'default'.`
- The mode-specific applied-patch manifest is
  `t-0042-tl-token-metadata.patch`,
  `t-0042-rocket-dcache-origin-hook.patch`,
  `t-0042-boom-dcache-origin-hook.patch`,
  `t-0042-boom-store-authorization.patch`,
  `t-0042-boom-token-fields-only.patch`, and
  `t-0042-tlxbar-request-defaults.patch`. The positive producer patch
  `t-0042-boom-store-token-handoff.patch` is a required shared-runner input but
  is not in this mode's applied manifest and was not applied. The mode and
  applied manifest are included in both input and source hashes so this
  composition cannot alias the positive handoff composition.
- Post-patch source hashes were
  `Bundles.scala=f1f8190de5064a50ac184749c965b09259748f7e20954fe2566cdfced9c41586`,
  `Xbar.scala=4867e293671c4df061637b01f358772595c0ec0efff359deeacb8572dde4cbe2`,
  `lsu.scala=beaf195dfed4457315b14aad5cb054f09bc894a6a997d63467e5fec0570154fb`,
  `dcache.scala=dfe116bdcd69f86d64c10fc29787870eb3244ec173e355c78b2ac0de49d8d1c5`,
  and
  `mshrs.scala=bdbc7eae44feac0bc71cf1769dc89230ddc6623a8c3d0a4ce391c9f7c961f042`.
- The exact bounded trace contains one BOOM-local
  authorization/request/response/clear sequence at PC `0x8000001c`, address
  `0x08000100`, ROB 7, and STQ 3; one manager Put A/D pair with source 8304;
  token A and D records both equal `{valid=0, epoch=0, sequence=0}` with
  classification 0; and one readback Get/D pair with source 8288. The
  transaction is non-denied/non-corrupt and software readback remains
  `0x51a7c0de`.
- This is bounded `rtl-simulation-functional` plus
  `rtl-elaboration-topology` evidence for one absent-producer negotiated
  default. It shows that semantic store attribution remains unknown and is not
  promoted while the underlying store completes; this is not access denial. It
  does not establish stripping after a valid producer, malformed nonzero
  handling, stale/duplicate/exhausted tokens, reset with work outstanding,
  replay/source reuse/backpressure, untagged loader/FESVR/Debug exclusion,
  BOOM loads, Rocket parity, CPU-side D consumption, common-bridge promotion,
  complete ADR-0045 attribution, resource matching, OoO effects, performance,
  power, area, timing, FPGA, or silicon results. T-0044 remains separate.
- Closeout passed `sh -n` for the shared and dedicated runners,
  `python3 -m py_compile` for the default-invalid verifier, the focused
  `python3 -m unittest tests.test_owned_memory_boundary -v` suite at 36/36,
  and `python3 -m unittest discover -s tests -v` at 234/234 with one expected
  real-QEMU integration skip. The record checker passed over 116 Markdown
  files, 85 task definitions, and 53 records; `git diff --check` passed; and
  `./scripts/ci-local.sh` completed successfully, including the same 234-test
  suite, Sonatine release/debug builds, QEMU smokes, and native/QEMU Graph
  checks. Local CI used Python 3.14.6, Apple clang 21.0.0, RISC-V GCC 16.1.0,
  binutils 2.47.20260726, and QEMU 11.0.3. These repository checks do not
  expand the bounded RTL claim.
