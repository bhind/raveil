# 2026-09-06

## T-0168 integration receipt and final workspace smoke

- Existing #154 canonically prepared/started after T-0167, retaining S-0003
  and original 2 SP. Primary owns TODO/STATUS/this log only on
  `docs/t-0168-integration-receipt`, base `219d46b`. Original `57fbe2e`
  implementation entered main in #159 (`606a4f7`); this PR only reconciles
  the remaining stale publication statement and original task receipt.
- macOS arm64 Python 3.14.6 command
  `python3 -m unittest tests.test_project.ProjectWorkspaceTests.test_successful_large_metadata_remains_readable_and_diffable tests.test_project.ProjectWorkspaceTests.test_explicit_text_read_bound_preserves_validation -q`
  passed 2 tests in 4.101 seconds. No code, ADR-0096 limit, EXP/RFC, ROADMAP
  or OPEN_QUESTIONS semantics change; no new RTL/hardware/Sprint acceptance.
- At unchanged integrated code in `11f65f5`, a fresh project was initialized,
  recipes listed and logs shown. `project run logs` saved run
  `20260906T105141Z-1e2842782537` with validated output `2`. After adding one
  ERROR line, run `20260906T105218Z-c47aac141e89` validated output `3`.
  `project diff` showed both the saved input edit and output 2 -> 3, preserving
  the first snapshot. `project run gemm --backend native` saved
  `20260906T105218Z-dc9a3e37db15`, trusted checksum `c102cc36c72f8e5c`,
  outcome `abstained`. Host Functional smoke, not a speedup or new RTL result.
- T-0167 receipt PR #167 merged as `219d46b`, #153 closed and canonical
  completion succeeded. Weekly 10:54:41 UTC: window 10080 minutes, used 18 /
  remaining 82 percent. Monitoring remains active; no paid resources.
- Pre-stop search after the final predecessor receipt found no already-
  accepted implementation successor. T-0158 requires an actual schedule event
  and two live S-0003 observations; the current reporting date precedes S-0003.
  Primary's working GitHub readback still returned only three workflow_dispatch
  runs, not schedule evidence. T-0121 remains P2/unscheduled, other conditional
  work retains its promotion gates, and Sprint ceremony needs owner review.
  Do not fill Ready by fabricating scope, observations or automatic acceptance.

## T-0167 integration receipt reconciliation

- Existing #153 canonically prepared/started after T-0166, retaining S-0003
  and original 1 SP. Primary owns TODO/STATUS/this log only on
  `docs/t-0167-integration-receipt`, base `1692790`. Original `3e3222e`
  entered main through #159 (`606a4f7`); this is a stale-record correction,
  not new implementation. Original Issue closure follows this receipt PR.
- macOS arm64 Python 3.14.6:
  `python3 -m unittest tests.test_project.ProjectWorkspaceTests.test_init_setup_hint_preserves_literal_checkout_and_existing_path -q`
  passed 1 test in 0.250 seconds. No code, ADR/EXP/RFC, ROADMAP or
  OPEN_QUESTIONS semantics change; no new RTL or Sprint acceptance.
- T-0166 receipt PR #166 merged as `1692790`, original #152 closed and
  canonical completion succeeded. Weekly telemetry 10:48:07 UTC: window
  10080 minutes, used 14 / remaining 86 percent. No paid resources.

## T-0166 integration receipt reconciliation

- Existing #152 canonically prepared/started after T-0165, retaining S-0003
  and original 2 SP. Primary owns TODO/STATUS/this log only on
  `docs/t-0166-integration-receipt`, base `6c1890a`. Original `fd30d31`
  implementation entered main through #159 (`606a4f7`); the present change
  corrects the stale publication statement, not the implementation.
- macOS arm64 Python 3.14.6 command
  `python3 -m unittest tests.test_project.ProjectWorkspaceTests.test_malformed_saved_metadata_fails_cleanly_even_with_matching_checksum -q`
  passed 1 test in 0.798 seconds. No source, ADR/EXP/RFC, resource boundary,
  ROADMAP or OPEN_QUESTIONS semantics change. No new RTL or Sprint acceptance.
- T-0165 receipt PR #165 merged as `6c1890a`, original #149 closed and
  canonical completion succeeded. Weekly telemetry at 10:42:32 UTC: window
  10080 minutes, used 12 / remaining 88 percent. No paid resources.

## T-0165 integration receipt reconciliation

- Canonical prepare/start resumed existing #149, S-0003, original 1 SP,
  after T-0163 receipt PR #164 (`3a18dcc`) and canonical completion. Primary
  owns TODO/STATUS/this log only on `docs/t-0165-integration-receipt`.
  Original `fd9797f` and actual implementation integration #159 (`606a4f7`)
  remain explicit; this PR corrects stale records, not implementation.
- macOS arm64 Python 3.14.6:
  `python3 -m unittest tests.test_project_graph.ProjectGraphTests.test_show_names_actual_descriptor_instead_of_inferring_from_graph_id -q`
  passed 1 test in 0.089 seconds. No source, ADR/EXP/RFC, roadmap gate or
  OPEN_QUESTIONS change; no new RTL, hardware or Sprint acceptance.
- Weekly telemetry 10:37:25 UTC: 10080-minute window, used 11 / remaining
  89 percent. Monitoring active; no paid resources or capacity purchases.

## T-0163 integration receipt reconciliation

- Existing task #147 resumed after T-0164 through canonical prepare/start,
  preserving 2 initial SP and S-0003. Primary owns only TODO, STATUS and this
  log on `docs/t-0163-integration-receipt`, base `5adad29`. The real defect in
  these records was a remaining "not integrated" statement despite PR #159
  (`606a4f7`) carrying the implementation. Original `79460b7` is preserved.
- This is genuine record reconciliation, not repeated implementation or a
  new performance result. A same-T-ID receipt PR closes the original Issue
  through the canonical lifecycle; PR #159 is not reused to bypass identity
  checks. Other predecessor cards remain separate until reconciled.
- macOS arm64 Python 3.14.6 command
  `python3 -m unittest tests.test_project_graph.ProjectGraphTests.test_input_diff_is_saved_semantic_and_does_not_execute tests.test_project_graph.ProjectGraphTests.test_input_diff_preserves_legacy_and_rejects_corrupt_history -q`
  passed 2 tests in 0.991 seconds. Source is unchanged. Prior RTL and combined
  Linux evidence retain their original identities. No ADR/EXP/RFC, gate,
  ROADMAP or OPEN_QUESTIONS semantics change.
- T-0164 PR #163 merged as `5adad29` at 10:29:31 UTC; canonical completion
  succeeded. Fresh weekly 10080-minute telemetry at 10:29:35 UTC showed
  used 9 / remaining 91 percent. Monitoring remains active; no purchase.

## T-0164 reviewed integration continuation

- Owner's serial integration direction includes existing governance records.
  Garden PR #144 merged as `a197500` at 10:21:23 UTC; Issue #142 closed
  and canonical completion succeeded. T-0164 was prepared/started retaining
  its original 1 SP. Normal main merge preserves both record histories and
  all executable code. Prior exact local candidate `24dfcbe` is retained.
- The owner reinstated weekly monitoring. At 10:22:01 UTC the 10080-minute
  window showed used 6 / remaining 94 percent; historical waiver mentions
  below describe earlier phases only. HCI-09 applies again. This integration
  changes no runtime setting, scheduler, automatic permission or research gate.
- Current evidence reconciliation confirms exact Garden `a7ae948` passed
  220 tests on offline Linux arm64 Python 3.11.2 uid1000 in 1.662 seconds,
  in addition to the recorded Mac suite and retained-run views. No new RTL
  run was performed. ROADMAP/OPEN_QUESTIONS and accepted ADR-0095 semantics
  are unchanged; Sprint Review remains separate.

## T-0164 routine approval batching

- Continued local preparation on `docs/t-0164-current-main`, based on
  `2a0ace3f4b83456c61feddf459c09c4c64c505b4`. Reapplied the existing seven
  governance-file delta from preserved candidate 54b55b9, retaining main's
  reopened T-0158 and actual-schedule/live-observation requirements. Burndown
  executable/test files are unchanged. No runtime configuration or denied
  operation was retried. Checker passes 194 Markdown files, 136 tasks and
  105 records; `git diff --check` passes. Fresh exact-head audit is required;
  publication remains outside this local preparation.

- Follow-up: the owner explicitly required checking stop necessity and
  automatically searching/starting the next eligible task when no incident
  exists. The live Project had no Ready item; routine publication candidates
  remained blocked and research/physical tasks retained distinct triggers.
  Reopened only local T-0164 refinement through canonical prepare/start;
  the receipt explicitly keeps publication excluded. Primary modifies only
  WORKFLOW, TODO, STATUS and this log; old candidate 28c4ee4 is preserved.
- Added an operational pre-stop checkpoint: classify actual incidents, scope
  denials, search authoritative triggers, perform the next concrete action,
  and state exact external limitations only after bounded search. No new
  scheduler, runtime policy, authority, ADR or research decision is introduced.
  ROADMAP/OPEN_QUESTIONS remain unchanged; this clarifies ADR-0095's existing
  continuous-work procedure. Documentation-only, not executable enforcement.
- The checkpoint was then exercised in the same turn: selected existing
  T-0161 standalone verification at clean candidate
  `50a94bb3460a720a0f4a1e671af777cf5eae895a`, without applying its denied
  patch, integrating or publishing it. `python3 -m unittest
  tests.test_project_graph tests.test_garden_tui -q` passed 57 tests on
  Darwin arm64 / Python 3.14.6. A fresh minimal project was initialized and
  its existing `project run neighborhood-data --backend rtl-sim` started
  with the offline runner for later saved-run Garden inspection. Only
  generated fixture/evidence writes are involved; RTL outcome was pending
  at this checkpoint. Issue #142 retains the exact operational receipt.
- Follow-up record validation: checker passed 194 Markdown files, 136 task
  definitions and 105 records; `git diff --check` passed. Librarian review
  found no checklist authority expansion and requested this actual next-action
  receipt, now recorded. This is not proof of autonomous background execution.

- Owner explicitly requested daily/up-to-ten approval batching and then
  instructed implementation. Issue #148 registered, prepared and started
  through the canonical queue; S-0003, 1 SP, base d935b42. Primary owns seven
  governance files on docs/t-0164-batch-approvals. A concurrent Project update
  invalidated one initial read; a fresh normal queue attempt succeeded.
- ADR-0095 adds request cadence and pending-item receipts; it changes no
  runtime permissions. No approval-batching scheduler or notification is
  installed; existing Project maintenance is unchanged. ROADMAP,
  OPEN_QUESTIONS, architecture and EXP/RFC have no semantic changes.
- T-0161 and T-0163 remain separate publication waits. No denied push is
  retried here. Existing owner weekly-guard waiver retained; no spending,
  reset credits, external builds or new simulation evidence.
- Routine requests already occurred today; no further unsolicited routine
  approval prompt is sent by this slice. Carry pending publication to the
  next packet unless the owner explicitly asks to review approvals earlier.
- `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  passed with 194 Markdown files, 136 tasks and 105 records on Darwin arm64,
  Python 3.14.6; `git diff --check` passed. Documentation-only validation,
  not a test of background execution or runtime approval.
- Librarian review identified ambiguous scheduler wording and a stale P0
  row; corrected both without changing the approval scope. Technical document
  acceptance remains separate from publication and Sprint ceremony acceptance.

## T-0161 current-main integration and restored usage guard

- Owner directed serial integration of the remaining workspace/Garden work.
  T-0172 PR #162 merged as `b690db3` at 10:11:03 UTC, Issue #158 closed
  and canonical completion succeeded. T-0161 was prepared and started with
  its original 3 SP, dependency T-0172, and primary PM as sole mutation owner.
  A prepare attempt refused a changed Initial SP; the successful retry
  preserved the existing value. No Project guard was bypassed.
- Dedicated `feat/t-0161-garden-integration` retains `50a94bb` ancestry;
  normal main merge keeps both Garden and current recipes/output handlers,
  parser arguments, tests and records. ADR-0093 remains Garden's identifier;
  ADR-0092 and T-0158's unfinished observation conditions are preserved.
  Prior publication refusals remain historical, not overridden permissions.
- Owner explicitly reinstated weekly usage monitoring; prior waiver ends at
  that direction. Existing account telemetry through the approved boundary
  returned window 10080, used 0 / remaining 100 at 10:07:23 UTC, then used 2 /
  remaining 98 at 10:10:33 UTC. Apply HCI-09 before new costly boundaries;
  missing/stale telemetry or remaining below 5 percent pauses new work.
  The sol publisher initially returned a usage-limit error, then succeeded
  on the same scoped T-0172 push after fresh telemetry. No credits, purchase,
  model switching or runtime bypass was used.
- Existing saved-run observation scope and ADR-0093 apply; no new EXP/RFC,
  opcode, hardware result or research-gate conclusion. Combined verification
  remains separate from previous standalone tests and original RTL evidence.
- At 10:15:47 UTC the weekly 10080-minute reading was used 4 / remaining 96.
  macOS arm64 Python 3.14.6 command
  `python3 -m unittest tests.test_project tests.test_project_graph tests.test_garden_tui tests.test_graph_device_dag tests.test_command_graph tests.test_shell_completion tests.test_interactive_shell tests.test_project_burndown tests.test_project_queue tests.test_project_daily -q`
  passed 220 tests in 26.666 seconds. Initial focused Graph/Garden 65 tests
  passed before the additional shared-run recipes/output/Garden regression.
- Actual `project garden RUN_ID --keys jjq --width 100` repeated twice for
  retained ADD `20260906T080920Z-245c1f7b26ca` and MUL
  `20260906T081216Z-f2c3b91f85bf` produced identical transcripts per run:
  ADD SHA-256 `6491676fdcb51825242954e7be9110d910ee4b86bc6175bb8523f46da4d5f989`;
  MUL `ab90d88bed283c92f0c29f66183e056325302a01e65823f03569b96706d79266`.
  `project output` and `project recipes` also passed on that retained project.
  Reader verification only: no compiler/simulator was invoked or new RTL
  evidence collected. Exact-head Linux verification and audit follow.

## T-0161 kickoff and local working increment

- User requested the next task. Issue #142 was prepared and started through
  project_queue on `feat/t-0161-project-garden`, base `9ec124d` (T-0159 PR #141).
  S-0003, 3 SP; replaces completed T-0159. T-0158 remains a separate lane.
  Primary owns raveil/project.py, raveil/garden.py, tests/test_project_graph.py
  and the corresponding canonical records/ADR-0093. Read-only reviewer only.
  No compiler/RTL/transport changes. Existing owner weekly-guard waiver applies;
  no paid resource or reset credit used. Dirty historical root remains untouched.
- `python3 -m unittest tests.test_garden_tui tests.test_project_graph
  tests.test_project tests.test_graph_device_dag -q` passed 89 tests on
  macOS arm64/Python 3.14.6. This includes synthetic host-runner tests, not new
  simulation evidence. Initial unchanged focused suite passed 52 tests.
- Actual retained data opened successfully using:
  `python3 -m raveil project garden 20260905T185848Z-eca1e696c41e
  --project PROJECT_DIR --keys jq --width 100` (`PROJECT_DIR` is the retained
  local demo workspace; its machine-local path is omitted from public records).
  The second run `20260905T190016Z-67941ecedb31` also opens with `--keys q`.
  Both exited zero without simulator invocation. North renders
  `input word = center + (-1) * 10 + (0)`. Saved output hashes differ while
  the saved program hash is the same, as established by T-0160.
  This view performs no independent historical execution verification.
- Final focused regression passed 91 tests with exit 0 (same command above).
  A test-placement mistake initially reused a deliberately modified receipt
  in a separate mismatch assertion; corrected placement without weakening
  validation. Independent review ran 21 project tests and found no blockers.
  Additional final negative tests cover missing/conflicting receipt fields
  and clean CLI rejection. Record checker passed 192 Markdown files, 134 tasks,
  103 records; git diff --check passed. Two actual `jjq` views have identical
  SHA-256 `5135284716cb7b3b2ea0796c83dbacef17fd9af6e6801496731b4a713e639f3e`.
  The workspace guide documents the new command; it changes no runtime boundary.
- Owner grants standing conditional approval for ordinary publication of
  authorized Raveil work when Vreji reviews the exact candidate and reports no
  material publication concern, and primary verifies tests/records and the
  incident-free integration audit. Vreji remains read-only triage, not legal
  clearance or gate authority. Unresolved material concerns and separate
  destructive, paid-resource, scope, release and evidence gates still apply.
- Vreji's first T-0161 publication review found a new machine-local demo path
  in this section; generalized it above before publication. No credential,
  third-party content, license change or new hardware/performance claim was
  identified by that scoped review. Re-review is required at the corrected head.

- Integration with main `4e8f381` preserved completed T-0158 records. Its
  already integrated ADR-0092 owns the private burndown decision; this branch's
  Garden ADR was renumbered ADR-0093 before merge. Both log sections remain.

## T-0172 reviewed integration continuation

- Later owner direction includes serial reviewed publication. T-0171 PR #161
  merged as `0c11edc` at 10:01:52 UTC; Issue #157 closed and canonical
  completion passed. T-0172 started; existing T-0161 is the next dependency.
  Main merge preserves all code/tests and both dated log entries.
- Current macOS arm64 Python 3.14.6 focused Graph suite passed 26 tests in
  7.125 seconds; prior 214-test Mac/Linux evidence remains applicable to the
  unchanged implementation. No new RTL execution or Sprint acceptance claimed.

## T-0172 checked saved Graph output

- Human owner approved registration, implementation, tests and documentation
  for `project output RUN_ID`, excluding publication. Issue #158 was created,
  linked to Project #1 and canonically prepared/started for S-0003, 1 SP.
  Primary owns project.py, test_project_graph.py, PROJECT_WORKSPACE guide,
  TODO, STATUS and this log on `feat/t-0172-saved-output`, base `15f918f`.
  Existing weekly waiver applies; dirty root and prior candidates are preserved.
- Reuse existing load_run admission, allow only successful rtl-sim Graph
  records, then read bounded output.txt and compare its bytes to both saved
  artifact and output hashes. Display identity and saved active rows without
  compilation or execution. ADR-0085's cooperative integrity boundary remains;
  no new ADR/RFC/EXP, gate or architecture change applies. ROADMAP and
  OPEN_QUESTIONS remain unchanged. No stronger isolation or signed audit claim.
- `python3 -m unittest tests.test_project_graph -q` passes 26 tests in 6.905s
  on Darwin arm64 / Python 3.14.6. Four new tests cover saved output after
  current-input edits, no backend calls, artifact mutation, changed bytes
  after record admission, and unsupported/missing/failed runs without rows.
- Actual CLI display of retained run `20260906T081216Z-f2c3b91f85bf` from
  `artifacts/verification/t0161-standalone-20260906` passes. The stored 8x8
  MUL output starts `11 24 39 56 75 96 119 144`. This reuses prior real RTL
  evidence; the new command did not rerun simulation. It is Host Functional
  reader evidence, not a new result at this source revision.
- Expanded command `python3 -m unittest tests.test_project tests.test_project_graph tests.test_garden_tui tests.test_graph_device_dag tests.test_command_graph tests.test_shell_completion tests.test_interactive_shell tests.test_project_burndown tests.test_project_queue tests.test_project_daily -q`
  passes 214 tests in 28.308s. Record check passes 194 Markdown files, 144
  tasks and 105 records; diff check passes. Independent draft review finds no
  material concern. Exact-head audit remains separate. Local task acceptance
  remains distinct from publication and Sprint Review acceptance.

## T-0171 reviewed integration continuation

- Owner directed the serial integration flow; T-0170 PR #160 merged as
  `ba5cb36` at 09:55:53 UTC, Issue #156 closed and canonical completion passed.
  T-0171 started through the canonical queue; T-0172 is the next dependency.
  The normal main merge changes records only relative to `15f918f` code/tests.
- macOS arm64 Python 3.14.6: the existing ten-module host command recorded
  below passed 210 tests in 18.133 seconds during this continuation. Prior
  exact-head Linux/nonroot evidence remains distinct; no new RTL run claimed.
  No ADR, roadmap gate or open design question changes. Ordinary task
  integration does not accept the weekly Sprint Review.

## T-0171 recipe discovery

- Initial Issue registration was refused for insufficient exact task authority.
  No workaround was attempted. The human owner then explicitly approved task
  registration, implementation, tests and documentation, excluding publication.
  Issue #157 was created, linked to Project #1, prepared and started through
  project_queue.py for S-0003, 1 SP. Existing weekly usage waiver applies.
- Primary owns project.py, test_project.py, PROJECT_WORKSPACE guide, TODO,
  STATUS and this log on `feat/t-0171-recipe-discovery`, base `5848e1b`.
  Historical dirty root and publication-pending candidates remain untouched.
  Existing workspace scope and ADR-0085 apply; no new ADR/RFC/EXP or research
  gate applies. ROADMAP and OPEN_QUESTIONS retain their existing conditions.
- Added sorted `.json` recipe metadata discovery, custom recipe support,
  backend distinctions and escaped unavailable-entry diagnostics. No tool,
  compiler or simulator execution is added. Referenced inputs, full Graph
  validity and installed tools are intentionally not checked by discovery.
- `python3 -m unittest tests.test_project -q` passes 24 tests in 15.794s
  on Darwin arm64 / Python 3.14.6. Three new tests cover custom/sample backend
  choices, malformed and confined entries, empty discovery and missing Graph
  inputs without execution. Actual `project init` then `project recipes`
  exits zero and lists all five initialized recipes; no run is created.
- Corrected the guide's stale v4 Garden statement against accepted ADR-0091
  and executable parser tests; pending project-Garden integration stays separate.
  Expanded command `python3 -m unittest tests.test_project tests.test_project_graph tests.test_garden_tui tests.test_graph_device_dag tests.test_command_graph tests.test_shell_completion tests.test_interactive_shell tests.test_project_burndown tests.test_project_queue tests.test_project_daily -q`
  passes 210 tests in 25.191s. Actual custom `my-logs` and invalid `broken.json`
  CLI discovery also succeeds and leaves no runs. Record check passes 194
  Markdown files, 143 tasks and 105 records; diff check passes. Independent
  draft review found no material concern; exact-head audit remains separate.
  These are local Host Functional results, not publication, RTL or Sprint
  Review acceptance.

## T-0170 duplicated Command input admission

- Integration continuation: owner directed serial integration without ordinary
  task-boundary pauses. T-0169 PR #159 merged as `606a4f7`; its Issue #155
  closed and canonical completion succeeded. T-0170 was prepared and started
  with that dependency; T-0171 was prepared as successor. Normal main merge
  into T-0170 changed records only, preserving the previously tested code.
  This does not accept the Sprint ceremony or expand resource guarantees.

- Rolling-horizon search found a distinct existing correctness gap. On
  T-0169 `84ff1d3`, 130 extra 65536-byte inputs produced native logs output,
  then full-run sealing raised `project snapshot exceeds 16 MiB`; no record
  existed. Primary reproduced the read-only review finding. T-0168's metadata
  read bound does not solve this duplicated-payload problem.
- Created Issue #156 and linked Project #1; canonical prepare/start succeeded
  for S-0003, 1 SP. Primary owns project.py, test_project.py, TODO, STATUS and
  this log on `fix/t-0170-input-budget`, base `84ff1d3`. Historical dirty root
  and all publication-pending heads remain untouched. Existing weekly waiver
  applies. No new ADR/EXP/RFC or research gate applies: resource limits and
  interfaces are unchanged. ROADMAP and OPEN_QUESTIONS remain unchanged.
- Reject when duplicated Command inputs alone exceed the existing byte or
  entry budget, before creating a run or executing tools. This is a necessary
  lower-bound check only: generated-output reservation, near-limit overhead
  and concurrent input changes are not claimed solved.
- The initial mock yielded a TypeError; changed it to an explicit assertion
  on unexpected execution. Both regression subcases then failed with
  `execution started before budget rejection` before the fix, and pass after
  it. The real 130-file reproduction now reports the explicit duplicated-input
  diagnostic, zero run directories and `No runs yet`.
- Environment: Darwin arm64, Python 3.14.6. Focused command:
  `python3 -m unittest tests.test_project.ProjectWorkspaceTests.test_duplicate_input_budget_rejected_before_execution_or_history -q`.
  Host Functional evidence only. Expanded command:
  `python3 -m unittest tests.test_project tests.test_project_graph tests.test_garden_tui tests.test_graph_device_dag tests.test_command_graph tests.test_shell_completion tests.test_interactive_shell tests.test_project_burndown tests.test_project_queue tests.test_project_daily -q`
  passes 207 tests in 26.631 seconds. Exact-head audit remains pending;
  local acceptance is not publication or Sprint Review acceptance.

## T-0169 local integration preparation

- Integration continuation: after the remaining-work report recommending
  serial main integration, Garden reconciliation and an end-to-end check, the
  human owner directed following that flow without stopping. This supersedes
  the earlier local-preparation exclusion for the scoped integration work,
  not execution review or known denials. Primary updated Issue #155 and used
  canonical unblock/prepare/start for the existing packet. Remote main remains
  `2a0ace3`; exact `84ff1d3` audit finds no material concern. The exact-head
  Linux aarch64/Python 3.11.2 nonroot/network-none suite also passed 206 tests;
  its retained log hash is
  `532d576238babead8416114a5c5cc2f713420afdf22d394f4dfd4fcf3947255b`.
  Only these three records change for PR preparation; no code, data, claims,
  gate, limits or prior evidence are changed. Publication still requires
  normal reviewed execution, exact-head readback and PR-only merge checks.
  Earlier exclusion statements below are historical preparation receipts.

- Owner requested continued authorized work. Issue #155 was created,
  prepared and started through the canonical queue in S-0003, 1 SP. Primary
  owns five code/test files, TODO, STATUS, PROJECT_WORKSPACE guide, this log
  and ADR-0096. Branch `test/t-0169-workspace-integration` starts at current
  main `2a0ace3f4b83456c61feddf459c09c4c64c505b4`; the historical dirty root
  and all old candidate heads remain untouched. No other active lane was
  observed. Existing weekly usage waiver applies; no reset credit is used.
- Trigger: reviewed disposable combination had no exact local Git head.
  Import only T-0163 `79460b7`, T-0165 `fd9797f`, T-0166 `fd30d31`, T-0167
  `3e3222e`, T-0168 `57fbe2e` behavior, preserving main's T-0158 corrections.
  Prior denied T-0161 application and publication are not retried; no push,
  PR, merge, new feature or authority-policy change is part of this packet.
- Five implementation/test files are byte-equal to the tested composite.
  `python3 -m unittest tests.test_project tests.test_project_graph
  tests.test_garden_tui tests.test_graph_device_dag tests.test_command_graph
  tests.test_shell_completion tests.test_interactive_shell -q` passes 129
  tests on Darwin arm64 / Python 3.14.6. Earlier Linux arm64/Python 3.11
  composite verification also passed 129 after correcting read-only test
  fixture setup; that initial harness failure remains in Issue #154.
- Retained prior verification is under ignored
  `artifacts/verification/t0163-t0168-20260906`. Source inventory SHA-256
  `61ac437ee9bcaa1499401646d18de6017065438133783ecf2717858b0c463a28`
  binds 692 base files and five changes; RTL diff log SHA-256
  `6c83e685351fcbf26215c68e17abbd6cc485d3ac37e5cc41b0809a1f05b95b09`.
  These earlier receipts remain disposable-composite evidence, not a claimed
  execution at this new Git revision. Both real RTL runs passed full
  oracle/fallback equality with identical program/RTL/simulator hashes.
- ADR-0096 is imported unchanged for the retained-text bound. No new ADR,
  RFC or EXP applies to integration itself. ROADMAP, ARCHITECTURE and
  OPEN_QUESTIONS remain unchanged: no research gate, opcode, physical access
  or product boundary changes. Stop the affected action on unexpected source
  differences or external refusal; do not bypass denial. Fresh exact-head
  audit and delegated acceptance precede any later authorized publication.
- Record checker passes 194 Markdown files, 141 task definitions and 105
  records; `git diff --check` passes. Five explicit `cmp` checks pass against
  the disposable source, and a Git comparison confirms no changes to main's
  burndown script/test. The local implementation commit is retained; this
  record-only follow-up still requires final exact-head audit.
- Exact `db42a1d` passed an expanded 205-test host suite including
  `tests.test_project_burndown tests.test_project_queue tests.test_project_daily`.
  Librarian review then found one integration seam: snapshot-mode records
  could omit `recipe.input`, pass admission and crash diff. A checksum-
  consistent regression failed in four subcases before the correction.
  Require a nonempty string for that consumed field; the CLI must return
  exit 2 without traceback. This is within the existing project/test allowlist,
  not a new mechanism. Only project.py and test_project_graph.py now differ
  from the earlier tested composite; fresh verification/audit is required.
- Corrected full command (the seven original modules plus burndown, queue and
  daily modules) passes 206 tests in 30.934 seconds on the same host. This is
  Host Functional evidence only; earlier RTL receipts remain separately
  labelled. No source or execution-boundary expansion was necessary.

## T-0162 delegated acceptance procedure

- Human owner explicitly authorized Issue #145, Project registration/start,
  documentation, commit, audit and push/PR/merge for this bounded task.
  Primary owns seven governance files on docs/t-0162-delegated-acceptance,
  base 4e8f381. S-0003, 1 SP; no EXP/RFC or research-gate change.
- The initial Issue creation was denied until explicit authorization. Queue
  preflight exposed missing packet labels, Project linkage and Ready state;
  corrected through the canonical prepare/start path. Issue #145 is active.
  Earlier T-0161 publication denial is not retried by this task.
- A read-only Librarian review of the local proposal found no material concern;
  incorporated candidate-bound terminology, current delegation, exact-head
  verification and explicit external-denial boundaries. Public records use
  functional roles; call signs remain local-only.
- Existing owner weekly-guard waiver applies; no reset credit, purchase,
  hosted build, runtime configuration change or new simulation was used.
  ROADMAP and OPEN_QUESTIONS remain unchanged: no product/research boundary
  or unresolved scientific question is resolved by this operations procedure.
- Verification on Darwin arm64, Python 3.14.6:
  `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  passed (193 Markdown files, 135 task definitions, 104 records), and
  `git diff --check` passed. Documentation only; no new executable behavior
  or runtime auto-approval capability is claimed.
- Exact candidate 63b69d595f944fca6ea8f9ad7592776b4a6a2008 received
  independent Librarian audit with no material concern. The primary, also
  acting as the human-delegated Product Owner, accepted its bounded task
  scope and verified clean ancestry, records and diff. This is not an
  independent human GitHub review. The acceptance-record follow-up requires
  exact-head re-review before integration; live PR state remains authoritative.

## T-0158 rebase and private snapshot continuation

- The owner authorized publication of the Project coordination configuration.
  PR #137 was made ready after its automatic Issue-closing reference was
  removed; T-0158 remains Review until hosted verification succeeds.
- A second private S-0002 snapshot for 2026-09-06 updated the Project Draft
  without exposing aggregate data in the public repository or Actions log.
- Rebase onto `origin/main` `9ec124d` preserved concurrent T-0157/T-0159/T-0160
  records. Their accepted ADR-0089 through ADR-0091 allocations required the
  burndown decision to move monotonically from its branch-local ADR-0089 to
  ADR-0092. No existing ADR was rewritten.
- `RAVEIL_PROJECT_NUMBER=1` is configured as a repository variable. The
  owner installed the dedicated `RAVEIL_PROJECT_TOKEN` repository secret; its
  value was neither read nor stored. Hosted workflow run #34003784228 at merge
  `c87944f95acb4dd389af62072e04596a46cb4b5d` succeeded. The run reported only
  `Contents: read` and `Metadata: read`, masked secrets, and completed the
  private Project update without printing Project aggregates. A following
  read-only dry run returned `changed: false` for the 2026-09-06 snapshot,
  verifying the hosted write was current and idempotent. Failed preflight run
  #34003433766 remains preserved as fail-closed evidence from before secret
  installation.
- Closeout verification ran on Darwin 25.5.0 arm64 with Python 3.14.6 and
  GitHub CLI 2.97.0. `python3 -m unittest tests.test_project_burndown
  tests.test_project_queue tests.test_project_daily -q` passed 73 tests;
  `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`,
  workflow YAML parsing, and `git diff --check` passed. The canonical queue
  returned Issue #135 from Blocked to Ready with the verified credential/run
  reason and started this dedicated closeout branch.
- Owner review found the claimed burndown was not visibly moving. Primary
  reproduced the gap: both retained S-0002 observations were taken after all
  nine items were Done; tests covered rendering tokens and same-day idempotence
  but no multi-day decrease; the only successful workflow event was
  `workflow_dispatch`, not `schedule`; and the Burndown view was a Table view
  containing a Draft whose title exposed no current value. Issue #135/T-0158
  was reopened and returned from Done to In Progress. The correction must not
  close until decreasing synthetic history passes, the live Project card shows
  its remainder, and a scheduled event is observed successfully.
- Independent Tester verification on Darwin 25.6.0 arm64 / Python 3.14.6 ran
  the burndown, queue, and daily suites and observed the synthetic 4 -> 2 issue
  and 8 -> 3 SP decrease. Its 75-test log SHA-256 was
  `f951ac2dd665e22f0d5ceac30a21a82bd60d0be547f5a9033507e3aeeb86a812`.
  Review found and the primary fixed one dynamic-title Draft exclusion gap.
  This is synthetic Host Functional evidence only; live decrease and the first
  scheduled event remain unverified.
- Vreji reviewed exact candidate
  `e287db397a3bc659052c93a427541fab5cc508cc` with no material publication
  concern; PR #150 merged it as `a45336c56d82620eb2c0e44b7481de652568c5a8`.
  `python3 scripts/project_burndown.py --owner bhind --project 1 --apply`
  updated the live card title to `S-0002: 0 issues / 0 SP (2026-09-06)` and a
  following preview returned `changed: false`. Corrected hosted manual run
  #34015052717 succeeded at the same main revision. A read-only
  `--date 2026-09-07` preview selected S-0003 and found 6 remaining Issues / 10
  remaining SP among 33 items; it was not applied, because future snapshots
  must not be fabricated. The first real `schedule` event and two live S-0003
  observations remain the closeout conditions.

## T-0160 integration and T-0159 continuation

- Owner explicitly approved T-0160 publication; low-tier publisher pushed
  `8610f7d249ae9e9691c3d02cdf10286351f58346`. PR #140 merged as `8780fc5`.
  Primary verified both trees equal `2d51ae360aec893ee815709a9c2e8d393d37a4c1`,
  no unresolved review threads, and canonical completion of Issue #139.
- T-0159 / Issue #136 started on `feat/t-0159-garden-relative-mul` from that
  main revision, S-0003, 3 SP, Host Functional. This replaces the completed
  T-0160 lane; T-0158 remains independent. Weekly usage guard is explicitly
  waived by the owner; no reset credits or purchases were used.
- Primary owns parser/renderer `raveil/garden.py`, tests
  `tests/test_garden_tui.py`, and STATUS/TODO/ROADMAP/OPEN_QUESTIONS/
  ARCHITECTURE/ADR-0091/index/this log. A low-tier read-only reviewer maps the
  compiler encoding and version rules. No runtime, RTL, transport or device
  edits; no new EXP/RFC or research gate change. Dirty historical root preserved.
- `python3 -m unittest tests.test_garden_tui tests.test_graph_device_dag -q`
  passed 53 tests on macOS arm64 / Python 3.14.6. Tests exercise actual compiler
  traces in synthetic envelopes, not new RTL runs. The retained v2 fixture is
  unchanged. Per-task integration does not accept the Sprint Review ceremony.
- One initial queue start was run from the preceding branch and rejected
  without transition; rerunning from the correct dedicated branch succeeded.
- Final `python3 -m unittest tests.test_project tests.test_project_graph
  tests.test_garden_tui tests.test_graph_device_dag -q` passed 86 tests.
  `python3 -m raveil garden --fixture
  tests/fixtures/garden/dynamic-explanation.json --keys jjq --width 100`
  exited zero. Independent read-only reviewer reran the 53-test focused suite
  and found no blocking issue. `git diff --check` and record checker passed
  (191 Markdown files, 133 tasks, 102 records).

## T-0160 functional acceptance

- Environment: macOS 26.5.1/Darwin 25.5.0 arm64, Python 3.14.6,
  Git 2.50.1; base `a0769da`. Host commands passed:
  `python3 -m unittest discover -s tests -p 'test_graph_device*.py' -q`
  (173 tests), `python3 -m unittest tests.test_project tests.test_project_graph
  tests.test_garden_tui -q` (64 tests). Additional final single v5 C++ admission
  test covers program versions 1--4; final project tests cover 324-element
  bool/float/negative/overflow and extra-field rejection.
- The first negative downgrade fixture used `range(324)`, which exactly equals
  the existing seed-zero generator; its expected rejection was incorrect.
  Preserved the failed log locally and used distinct explicit data instead.
  No admission rule was weakened. The corrected 62-test focused run passed.
- Actual commands (project was initialized in a fresh temporary directory):

  ```sh
  python3 -m raveil project show neighborhood-data --project /private/tmp/raveil-t0160-demo.7TvHIm
  python3 -m raveil project run neighborhood-data --project /private/tmp/raveil-t0160-demo.7TvHIm --backend rtl-sim
  # Edit only words[11] from 7 to 9; words[1] remains 3.
  python3 -m raveil project run neighborhood-data --project /private/tmp/raveil-t0160-demo.7TvHIm --backend rtl-sim
  python3 -m raveil project diff 20260905T185848Z-eca1e696c41e 20260905T190016Z-67941ecedb31 --project /private/tmp/raveil-t0160-demo.7TvHIm
  ```

- All exited 0. First cell 10 -> 12; 2/64 active outputs changed because the
  same input is reused by the next output row. Prior `load_run` and full raw
  JSON/packed input/output checks passed. Raw RTL artifact roots are
  `artifacts/graph_device_axi4lite_dynamic/run.kznmljtq` and `run.ola7paab`.
  Each contains the 1,024-byte output exactly equal to descriptor oracle and
  C++ fallback, plus matching 1,296-byte input snapshots and request copies.
- Input SHA-256 before/after:
  `8074e8e564ea28a774db9d314d2f41adf69b3d40050782fba40a6e247a16788a` /
  `26d53461b4f9eba3daa11c523f04007a946ef0efc42e3280c5394eccfea621ef`.
  Output SHA-256 before/after:
  `fc8e7e283d22cf9acb7018ac448580d668802a4bf790566672c309d0513131b7` /
  `8b6540871565484c87d4d7c5cc0bb68c916c616c052b924a904dff7630b6c51d`.
  Simulator SHA-256 both:
  `8a1942c8d175c3d471900f1585acd4de47e4f29bd2ae9698dfb1843fb262918e`.
  RTL manifest both (also identical to T-0157):
  `0b3dfa11f91a81b22d9c88f6c76d3b68f393d6b99e8780137af125c964a80de7`.
  Source manifest both:
  `cf24763764093f7d9dfa270783bbb8e74f0bc38833742d09517a26e9f4e2d4e7`.
- Existing offline image only; no Scala/RTL source edits, download or physical
  device access. The simulator was rebuilt once per project invocation, not
  persistently cached. Host tests and actual RTL results remain separate;
  no performance, isolation, physical gate or Sprint ceremony claim.

The delegated source agent entered pending initialization after its first
patch. Primary interrupted it and assumed serial source integration, keeping
the patch and adding confined show reads, request metadata versioning and an
executable projected-UIO rejection regression. No concurrent source owner
remains. T-0159 was subsequently prepared again as the P1 successor.

## T-0160 editable Graph input kickoff

The owner requested further Graph generalization after T-0157 integrated in
PR #138 (`a0769da`). Read-only routing compared constants, user input and
flexible shapes. Seed-only input is the smallest practical bottleneck: remove
it with bounded snapshots while retaining circuit, instruction and memory
limits. ADR-0090 fixes that design; no EXP/RFC or research-gate change applies.

Issue #139 records S-0003, 5 SP. The canonical queue permits only one Ready
successor, so T-0159/#136 was moved to Backlog with history intact to prepare
the new owner-prioritized execution slice; it remains the next display task.
Separate-session T-0158/#135 operations are untouched. Root dirty work remains
donor/provenance only. Dedicated branch: feat/t-0160-graph-input-snapshot.

The source implementer owns only the frozen Python/C++ request/project adapter,
contract, runner inventory and focused tests; primary serially owns records,
verification and integration. No RTL mutation or physical-device action is
authorized. Weekly-stop waiver from the owner continues; no reset credit,
purchase or service-plan change is applied. New verification is pending.
