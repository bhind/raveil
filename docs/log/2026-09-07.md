# 2026-09-07

## T-0176 saved-run filters

- Owner requested execution of all ranked backlog items. With T-0158 still in
  Review, T-0176 uses the only remaining mutation lane; other items remain
  Backlog until WIP permits canonical preparation.
- Issue #174, S-0003, 2 SP, Experience Implementer, branch
  `feat/t-0176-run-filters`, Host Functional evidence. Dependencies T-0149,
  T-0161 and T-0174 are integrated. No ADR, RFC or EXP change applies.
- Weekly guard at 2026-09-07T06:09:39Z: 77% used, 23% remaining.
- The first prepare dry-run rejected headings that lacked the canonical
  packet markers. The same bounded Issue body received explicit marker lines;
  its scope did not change. Prepare then reached Ready. A subsequent start
  read raced the Project update and failed closed; live Ready readback passed,
  and canonical start retry moved the item to In Progress.
- Implementation adds exact recipe/backend/status filters to the existing
  read-only run listing. Invalid entries remain visible only in the unfiltered
  view and never appear as successful. No recipe executes and no history is
  written by inspection.
- Verification on macOS 26.6.2 arm64, Python 3.14.6:
  `python3 -m unittest tests.test_project tests.test_project_graph` passed 60
  tests in 14.141 s; `python3 -m unittest discover -s tests` passed 798 tests
  in 121.510 s with two skips; record checker and `git diff --check` passed.
  A fresh `/private/tmp` project ran `logs`, then the combined successful
  filter returned exactly that run and the `rtl-sim` filter returned no match.
  This is Host Functional evidence, not a timing or hardware result.

## T-0177 project workload examples

- Issue #175, S-0003, 3 SP, Experience Implementer, branch
  `feat/t-0177-workload-pack`, Host Functional. Dependencies T-0101 and T-0149
  are integrated; no ADR, RFC or EXP change applies.
- Weekly guard at 2026-09-07T06:22:57Z: 78% used, 22% remaining. Canonical
  prepare and start completed. T-0158 remains the separate Review item, so the
  two-item WIP boundary is full while T-0177 is active.
- Added two repository-authored synthetic fixtures under
  `examples/project-workloads/`. Tests copy each fixture to a temporary project
  and create `runs/` there; source fixtures are never executed in place.
- `python3 -m unittest tests.test_project_workload_examples
  tests.test_command_graph tests.test_project` passed 48 tests in 33.124 s.
  `python3 -m unittest discover -s tests` passed 801 tests in 169.750 s with
  two skips. A copied host demo produced the exact checked log output and a
  separate retained missing-input failure without output publication.
  The fixtures add no allowlisted tool, grammar, executor, schema, init sample,
  real data, performance evidence, isolation or hardware support.

## T-0190 recipe relationships

- Issue #208, S-0003, 2 SP, Experience Implementer, branch
  `feat/t-0190-recipe-relations`, Host Functional. Dependencies T-0171 and
  T-0174 are integrated; no ADR, RFC or EXP change applies.
- Weekly guard at 2026-09-07T06:37:44Z: 81% used, 19% remaining. Canonical
  prepare/readback/start completed with T-0158 as the separate Review item.
- `Project.recipes` now adds exact recipe paths and graph-device descriptor
  plus seed/input relationships using recipe metadata only. Existing invalid
  entry behavior, ordering and non-execution warning remain.
- `python3 -m unittest tests.test_project` passed 26 tests in 23.512 s.
  `python3 -m unittest discover -s tests` passed 801 tests in 167.050 s with
  two skips. A fresh project listing displayed all intended relationships and
  left `runs/` empty.
  This is Host Functional discovery evidence, not referenced-file validation,
  execution, performance, cache, isolation or hardware evidence.

## T-0178 recipe fork preparation

- T-0178/Issue #176 was prepared in S-0003 at 3 SP after the owner approved
  recipe-last publication provided its limitations become explicit operating
  constraints. Work paused cleanly at Ready due to weekly capacity.
