# 2026-09-07

## T-0176 saved-run filters

- Owner requested execution of all ranked backlog items. With T-0158 still in
  Review, T-0176 uses the only remaining mutation lane; other items remain
  Backlog until WIP permits canonical preparation.
- Issue #174, S-0003, 2 SP, Experience Implementer, branch
  `feat/t-0176-run-filters`, Host Functional evidence. Dependencies T-0149,
  T-0161 and T-0174 are integrated. No ADR, RFC or EXP change applies.
- Weekly guard at 2026-09-07T06:09:39Z: 77% used, 23% remaining.
- The first prepare dry-run rejected headings that lacked the canonical
  packet markers. The same bounded Issue body received explicit marker lines;
  its scope did not change. Prepare then reached Ready. A subsequent start
  read raced the Project update and failed closed; live Ready readback passed,
  and canonical start retry moved the item to In Progress.
- Implementation adds exact recipe/backend/status filters to the existing
  read-only run listing. Invalid entries remain visible only in the unfiltered
  view and never appear as successful. No recipe executes and no history is
  written by inspection.
- Verification on macOS 26.6.2 arm64, Python 3.14.6:
  `python3 -m unittest tests.test_project tests.test_project_graph` passed 60
  tests in 14.141 s; `python3 -m unittest discover -s tests` passed 798 tests
  in 121.510 s with two skips; record checker and `git diff --check` passed.
  A fresh `/private/tmp` project ran `logs`, then the combined successful
  filter returned exactly that run and the `rtl-sim` filter returned no match.
  This is Host Functional evidence, not a timing or hardware result.
