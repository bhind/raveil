# 2026-08-11

## T-0084 Linux driver-development harness

- PM review retained ADR-0003: Linux does not replace the permanent
  RISC-V/Sonatine control and authority path. ADR-0019 makes Linux a
  non-authoritative development and transport-validation host.
- Added a fixed 32-byte request/completion v1 ABI with PING/NOP only and a
  pure-C one-inflight core. Invalid magic, version, size, flags, and opcode
  fail closed; busy and empty states remain distinct.
- Added Linux-only same-UID `SOCK_SEQPACKET` daemon/client sources. The daemon
  requires an absolute `XDG_RUNTIME_DIR`, creates a private endpoint under
  umask 0077, rejects partial messages with `MSG_TRUNC`, and exposes no network,
  root, kernel module, pointer, file-descriptor passing, DMA, MMIO, or
  Experience boundary.
- `scripts/ci-local.sh` exited 0 with all 54 tests, release/debug Sonatine
  builds, DWARF checks, and QEMU smoke on macOS 26.5.1 arm64 using Python
  3.14.6, Apple clang 21.0.0, `riscv64-elf-gcc` 16.1.0, binutils
  2.47.20260726, and QEMU 11.0.3.
- `docker build -f linux/Dockerfile -t raveil-linux-driver linux` exited 0 on
  Docker Linux arm64. Debian bookworm GCC 12.2.0 compiled both Linux-only
  binaries warning-free. The image identity was
  `sha256:1f19fe3b2be57fbb159ded26887afd2986341b05d4d25d91280bc861b3d46231`.
- The non-root smoke used UID/GID 65534 and a mode-0700 `/runtime` tmpfs. The
  Docker frontend left the invocation in Created state in this session, so the
  same configured container was started explicitly with `docker start -a`.
  It exited 0, observed socket mode 600, printed
  `raveil linux driver ping: OK`, and verified endpoint cleanup.
- Host-core and Linux-container results are correctness evidence only.
  Kernel-device, QEMU-device, FPGA, silicon, and performance evidence remain
  separate and are not claimed.

## T-0030 bounded JobDescriptor and CompletionRecord

- Accepted ADR-0020 and added a platform-neutral little-endian, pointer-free
  JobDescriptor/CompletionRecord v1 contract. The schema caps each job at four
  generation/version/range/effect object references and requires explicit
  runtime/input/output/scratch ceilings.
- Completion validation requires nonzero claimed execution epoch, sequence,
  and cookie; T-0031 still owns issuance and replay state. Executed outputs
  must be the job's WRITE objects with advanced versions;
  rejected/cancelled/fault observations carry no outputs.
- Structural validation is deliberately not admission, semantic approval,
  commit, visibility, or Experience authority. ObjectManifest/table lifecycle
  and rings remain T-0031; Linux still transports only PING/NOP.
- `scripts/ci-local.sh` exited 0 with all 55 tests and the unchanged Sonatine
  release/debug/DWARF/QEMU checks. The T-0030 strict-C11 host test passed under
  Apple clang 21.0.0 with `-pedantic-errors -Wall -Wextra -Werror`.
- The default public Debian base metadata lookup timed out in this session.
  The reproducible Dockerfile was therefore verified with the already-audited
  local Debian-derived base override:

      docker build --build-arg BASE_IMAGE=raveil-sonatine:gate0-clean-20260808 -f linux/Dockerfile -t raveil-linux-driver .

  Debian arm64 GCC 12.2.0 compiled the Linux daemon/client and the shared job
  validator with `-Werror`. Image identity was
  `sha256:2619082d2573496975b5423440fff84d02830aa83fb2e7cff8bfb8687f101453`.
  A UID/GID 65534 private-tmpfs smoke again exited 0 with socket mode 600,
  `raveil linux driver ping: OK`, and endpoint cleanup. This does not claim
  Linux transport of JobDescriptor; the driver remains PING/NOP-only.

## T-0031 Sonatine object table and job rings

- Accepted ADR-0021 and added the fixed 64-byte ObjectManifest v1 contract.
  The validator rejects malformed headers, reserved data, invalid access and
  backing combinations, zero identity/version/size fields, and writable
  immutable objects.
- Added a Sonatine-owned eight-entry object table, four-entry submission and
  completion rings, and four-entry inflight ledger. Admission checks exact
  64-bit ID, manifest generation/version, range, and permitted effect without
  treating the ID as a capability or truncating it to 32 bits.
- Sonatine issues boot-scoped epoch, sequence, and non-cryptographic cookie
  bindings. Wrong or duplicate completions do not consume pending state; valid
  completions post and consume once. `EXECUTED` deliberately leaves the
  visible object version unchanged for T-0033 commit/rollback.
- The seed is single-hart kernel-owned state. It adds no U-mode syscall, Linux
  authority, MMIO, DMA, IRQ, shared memory, reset protocol, physical storage,
  Experience write, or performance claim.
- The first full RV64 run exposed an undefined freestanding `memset` generated
  by aggregate zero initialization. `sonatine/src/runtime.c` now supplies the
  same bounded byte-loop primitive as its existing `memcpy`; the complete
  verification was then rerun from the start.
- `scripts/ci-local.sh` exited 0: all 56 Python/host tests passed, RV64 release
  and debug builds linked, DWARF checks passed, and the QEMU `virt` smoke
  emitted the ring/replay marker. Environment: macOS 26.5.1 arm64, Python
  3.14.6, Apple clang 21.0.0, `riscv64-elf-gcc` 16.1.0, binutils
  2.47.20260726, and QEMU 11.0.3. The emulation log SHA-256 is
  `d37d5d70db3f462063d4056912e59dfb71bc23ba6eb82dbfcc047b19b801d7ac`.
- `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  exited 0 with 69 Markdown files, 65 task definitions, and 26 records;
  `git diff --check` also exited 0.

## T-0032 segregated completion telemetry

- Accepted ADR-0022. Completion telemetry is a separate cold-evidence schema,
  not the optimization `ExperienceRecord` or Gate 1 `MeasurementRecord`.
- Sonatine emits a bounded `RAVEIL-COMPLETION-V1` UART frame only after
  successful one-shot completion consumption. It records QEMU machine-timer
  ticks for the full synthetic kernel smoke path as emulation diagnostics,
  never job/Daphnis execution time, silicon latency, or energy.
- Added strict host parsing and a mode-0600, single-writer, hash-chained journal
  with contiguous sequences, raw-log SHA-256/line provenance, full-history
  duplicate detection, exact fields, partial/corrupt-line rejection, leaf
  symlink/non-regular/multi-link rejection, append, and fsync.
- The CLI now provides `experience ingest-completions` and
  `experience inspect-completions`. QEMU smoke ingests its real console log and
  proves a repeated ingest appends zero records.
- This seed does not claim serial authenticity, crash-spanning exactly-once
  delivery, cross-boot uniqueness, active retrieval, semantics, commit, real
  Daphnis, FPGA/silicon evidence, energy, or performance.
- `scripts/ci-local.sh` exited 0 with all 61 tests, RV64 release/debug builds,
  DWARF checks, and QEMU ingestion E2E. Environment: macOS 26.5.1 arm64,
  Python 3.14.6, Apple clang 21.0.0, `riscv64-elf-gcc` 16.1.0, binutils
  2.47.20260726, and QEMU 11.0.3. The ignored smoke-log SHA-256 was
  `5c77d0a396af110884bc95307c7333e8095014e6d62857a4ef5b8f3e23ec12c7`;
  the generated one-record journal SHA-256 was
  `583045f0e6026f1848268dfb08360ce8a01ebc14cc920c5d816ccf83343eb4fe`.
- The governance checker exited 0 with 70 Markdown files, 65 task definitions,
  and 27 records; `git diff --check` also exited 0.

## T-0033 metadata-shadow finalization

- Accepted ADR-0023. Completion consumption now retains a full-binding shadow
  slot until explicit one-shot commit or rollback; job ID alone cannot finalize
  it and `EXECUTED` alone cannot publish state.
- Added injected kernel approval, exact-successor output versions, optimistic
  revalidation of every READ/WRITE reference, and two-phase multi-output
  visible-version publication. A conflict in any target leaves every target
  unchanged.
- Added queued and dispatched cancellation. Cancellation is sticky; a late
  executed completion remains observable but cannot be approved or committed.
- Host tests cover missing approval, explicit commit, duplicate finalization,
  competing writers, dispatched cancel-versus-execute, queued-cancel job-ID
  reuse, stale bindings, version jumps, and a two-output atomic conflict. QEMU
  smoke proves a volatile WRITE manifest remains at version 1 before approval
  and publishes version 2 only on commit; its sole UART telemetry frame carries
  `outputs=1:1:2` and a distinct emulation-only marker.
- This is metadata shadowing, not byte shadowing or semantic verification.
  T-0085 now records bounded object-byte backing/shadow work; T-0043 remains the
  semantic-oracle task and T-0034 remains the write-authority task.
- `scripts/ci-local.sh` exited 0 with all 61 tests, RV64 release/debug builds,
  DWARF checks, the metadata-shadow QEMU marker, and unchanged segregated
  telemetry ingestion. Environment remained macOS 26.5.1 arm64, Python 3.14.6,
  Apple clang 21.0.0, `riscv64-elf-gcc` 16.1.0, binutils 2.47.20260726, and
  QEMU 11.0.3. The ignored smoke-log SHA-256 was
  `46c397a15d2cb17b9713215b7d31403e1f657fdb9e805e8ecb7750bf82516291`;
  the one-record telemetry journal SHA-256 was
  `80efbabb18bbce7d8065ffc6658290fe586fa87e194b5f99f0dd7ca4e6a662f0`.
- The governance checker exited 0 with 71 Markdown files, 66 task definitions,
  and 28 records; `git diff --check` also exited 0.

## T-0087 delivery-line reconciliation

- Paused the manufacturing line after a cross-record audit found that the
  accepted commodity-host, GNU/Linux, and OSS-first pivot remained only on
  preserved divergent branches while main continued the earlier
  RISC-V/Sonatine-first sequence.
- Accepted ADR-0024. The next explicit-restart P0 is the GNU/Linux userspace
  graph vertical slice; Sonatine Microkernel, Linux harness, contracts, job
  rings, telemetry, Experience, EXP-0003 evidence, and metadata-shadow artifacts remain
  implemented and preserved.
- Recorded `feat/t-0086-linux-graph-mvp` as an implementation donor rather than
  a merge target because it predates and deletes later main work. Its useful
  code and tests must be ported onto current main.
- Resolved the Gate 1 prose conflict in TODO and the stale TVM dataset claim in
  STATUS. EXP-0003 remains closed negative and unchanged.
- Clarified feature-release versioning: `v0.0000000000001` is an immutable
  historical release; current work is unreleased until the next accepted
  feature boundary, verification, records, and release evidence are complete.
- Added immutable annotated milestone tags, separate from feature-release
  versions and remote publication. T-0087 closes at
  `milestone/t-0087-delivery-line-reconciled` after verification.
- `python3 -m unittest discover -s tests -v` exited 0 with all 61 tests on the
  current macOS arm64 development host; this is implementation regression
  evidence, not performance evidence. The governance checker passed with 72
  Markdown files, 68 task definitions, and 29 records, and `git diff --check`
  exited 0.

## T-0086 GNU/Linux userspace graph MVP

- Resumed manufacturing from
  `milestone/t-0087-delivery-line-reconciled` and selectively ported only the
  donor graph module/test ideas. The donor branch was never merged and no
  current Sonatine, Linux harness, contract, telemetry, Experience, EXP-0003,
  or metadata-shadow artifact was deleted or weakened.
- Accepted ADR-0025. Added an owned `GraphProgram`, `ExecutionContract`, fixed
  structural compiler, advisory proposal/abstention, baseline-first executor,
  checksum comparison against the baseline, explicit commit/rollback result,
  and exclusive segregated JSON evidence output.
- `python3 -m unittest tests.test_graph_mvp -v` passed 13 tests on macOS 26.5.1
  arm64 with Python 3.14.6 and Apple clang 21.0.0.
- Final `scripts/ci-local.sh` passed all 74 tests, Sonatine RV64 release/debug
  builds, DWARF checks, QEMU smoke, and repeated completion-telemetry ingestion.
  The ignored QEMU smoke SHA-256 was
  `f10b6952caab0e024369539d3e7bda107ca1d59d7d771030367a272ff5d26ec9`;
  its generated one-record telemetry journal SHA-256 was
  `d1a3a52423832e1e2a197aa82cca8439c16df56776003647489d86f69efee392`.
- Built `linux/Dockerfile.graph-mvp` from the locally verified Debian base
  override `raveil-linux-driver:t0030-review`. The resulting Linux arm64 image
  ID was `sha256:0c23cf2ed5c9c53061bd335bd28ca265da5c48d7c2d7583ff2f5b67e9be31853`.
  Its build passed the same 13 tests under Debian 12, Python 3.11.2, and GCC
  12.2.0.
- The GNU/Linux E2E command produced schema
  `raveil.graph-mvp-result/v1`, evidence class `host-correctness`, baseline
  first, two observations, and an explicit `rolled-back` outcome. This is
  correctness evidence only; it creates no latency, energy, FPGA, ASIC, or
  silicon claim and does not reinterpret EXP-0003.

## T-0088 failure knowledge and issue governance

- Audited existing failure evidence. EXP-0003 preserves failed RUN-IDs and the
  falsified Gate result; regression tests, rejected alternatives, and dated
  logs preserve implementation failures, but no compact reusable lesson index
  or GitHub Issue escalation threshold existed.
- Accepted ADR-0026 and added `docs/FAILURE_KNOWLEDGE.md` with evidence-linked
  lessons for sampler readiness, thermal recovery, portable sealed commands,
  divergent-branch integration, and immutable bundle object cost.
- Added a safe structured GitHub bug template. Issues coordinate defects that
  outlive a branch, recur, block release, cross ownership, or threaten users,
  security, data, or evidence. Same-branch fixes with regression coverage,
  expected fail-closed behavior, and negative experiments avoid issue churn.
- TODO and repository records remain canonical; GitHub access is not required
  to make a safe in-scope fix or preserve a candidate for later batch triage.
- Vreji's read-only review identified RUN-ID write isolation and compiler/FFI
  ABI pinning as additional reusable lessons. It also identified public-Issue
  leakage risks from internal locators, absolute paths, unpublished artifacts,
  third-party materials, and patent/standards text. Added those lessons,
  strengthened the template, and routed sensitive reports through
  `SECURITY.md` and an approved private channel without claiming legal review.
- Ruby/Psych parsed both GitHub Issue YAML files successfully. The governance
  checker passed with 76 Markdown files, 69 task definitions, and 31 records;
  `git diff --check` exited 0. This is documentation/configuration validation,
  not a live GitHub Issue submission test. No remote issue or publication was
  created.
- T-0088 closes at `milestone/t-0088-failure-issue-governance` after commit.

## T-0041 owned graph artifact schemas

- Resumed from `milestone/t-0088-failure-issue-governance` on dedicated branch
  `feat/t-0041-owned-graph-contracts`. No tracked artifact was removed and no
  donor branch was merged.
- Accepted ADR-0027. Added strict v1 `GraphVariant`, `MemoryPlan`, and
  `OptimizationProposal` serialization and validation. Program, execution
  contract, and ordered candidate-set lineage is checked before any backend
  invocation; Experience remains advice only.
- `python3 -m unittest tests.test_graph_mvp -v` passed all 16 focused tests on
  the macOS arm64 development host with Python 3.14.6. The new cases cover
  exact round trips, unknown fields, inconsistent plans, and stale proposals
  that execute zero backend calls.
- Built `linux/Dockerfile.graph-mvp` with local audited base override
  `raveil-linux-driver:t0030-review`. Docker ran the same 16 tests under Debian
  12 arm64, Python 3.11.2, and GCC 12.2.0. The resulting image ID was
  `sha256:d132ae0118ad038501c10e0a2dc942cc3aea0c7ac3b829dfe1bac6872f64e20e`.
- The GNU/Linux E2E command produced the three v1 owned schemas with exact
  program/contract lineage and an explicit `rolled-back` outcome. This is
  host-correctness evidence only; it is not a latency, energy, FPGA, ASIC, or
  silicon claim and does not change EXP-0003's falsified result.
- Final `scripts/ci-local.sh` exited 0 with all 77 tests, Sonatine RV64
  release/debug builds, DWARF checks, QEMU smoke, and repeated completion
  telemetry ingestion. The ignored QEMU smoke SHA-256 was
  `5702ae42c8de5c874808bed7a14cf8d7a4f162478c36dae20291d43d9884cd12`;
  its one-record telemetry journal SHA-256 was
  `7bafa9225f363ac7d3800477eae3fbbd33a7d60f4788d85ddcea4a608ef01166`.
  The governance checker passed with 77 Markdown files, 69 task definitions,
  and 32 records, and `git diff --check` exited 0.

## T-0089 U-mode command-line shell

- Preserved unfinished T-0043 work as named stash
  `wip/t-0043-miroirs-pavane-before-command-shell` on its original branch,
  then started `feat/t-0089-u-mode-command-shell` from local `main`. No prior
  artifact or experiment was removed or reinterpreted.
- Accepted ADR-0028. Replaced the normal U-mode single-character dispatcher
  with a bounded eight-byte scalar command state, `raveil-u> ` prompt,
  CR/LF/CRLF and empty-line handling, delete/backspace editing, overflow
  recovery, unknown-command handling, and the six public commands.
- Kept syscall identity current-task-derived and pointer-free. The added output
  operation accepts only one seven-bit scalar byte after console-capability
  resolution; command state stays in the U-mode context across preemption.
- The first direct QEMU editing check exposed a little-endian delete bug: the
  implementation removed the first byte rather than the final byte. The active
  branch fixed it with a length-derived mask and added the exact `infX<DEL>o`
  regression to smoke. No Issue was needed because the defect was contained,
  fixed, and covered before milestone closeout.
- `make -C sonatine CROSS_COMPILE=riscv64-elf- smoke` exited 0 after splitting
  `help` across a one-second interval with repeated CLINT preemption. It then
  covered CRLF, an empty CR line, delete-edited `info`, VFS before and after
  preemption, smoke-only register/capability self-test, `ticks`, `ipc`,
  overflow, unknown command, and `exit`.
- Final `scripts/ci-local.sh` exited 0 with all 77 tests, Sonatine RV64
  release/debug builds, DWARF checks, the command-line QEMU smoke, and repeated
  completion-telemetry ingestion. The ignored QEMU smoke SHA-256 was
  `0ebff5610485c638f7522f2cb6a74ca3d1b5d9db63db0bc76510fefb5cd7fd51`;
  its one-record telemetry journal SHA-256 was
  `37a19ca63c97801921f40ddb41d827a1a68b5737c24e4e4caa26fa504ed08750`.
  The governance checker passed with 78 Markdown files, 70 task definitions,
  and 33 records, and `git diff --check` exited 0.
- All results are QEMU emulation correctness or host regression evidence only;
  no performance, energy, FPGA, ASIC, or silicon claim was added, and EXP-0003
  remains falsified and unchanged.

## T-0090 Sonatine backend for the GNU/Linux graph MVP

- Started `feat/t-0090-sonatine-graph-backend` from the completed T-0089
  milestone. No donor branch was merged and no existing Sonatine, Linux,
  contract, telemetry, Experience, experiment, or metadata-shadow artifact was
  removed.
- Accepted ADR-0029. Added a strict 128-byte pointer-free graph request owned
  contract, an explicit `sonatine-qemu` adapter, and a bounded QEMU result
  frame. The native backend remains the default and retains its prior output.
- The guest validates the request, reuses ObjectManifest, JobDescriptor,
  CompletionRecord, bounded rings, and metadata-shadow approval/finalization,
  and emits a result only after explicit commit. `EXECUTED` alone cannot
  commit. The host rejects malformed, duplicate, stale, unknown, late,
  unapproved, timeout, nonzero-exit, and parse-failure outcomes.
- The first root-context Docker build failed because `.dockerignore` excluded
  `sonatine/`. The same branch corrected the allowlist while continuing to
  exclude `sonatine/build/`; the regression is recorded in
  `docs/FAILURE_KNOWLEDGE.md`.
- Focused host verification passed 32 tests with
  `python3 -m unittest tests.test_graph_mvp tests.test_sonatine_backend tests.test_sonatine -v`.
  `make -C sonatine CROSS_COMPILE=riscv64-elf- graph-smoke` then executed the
  real QEMU path and the native differential path with the same 8x8 GEMM
  baseline checksum `c102cc36c72f8e5c`.
- The direct demo
  `python3 -m raveil graph-mvp --backend sonatine-qemu --family gemm --m 8 --n 8 --k 8 --output /tmp/raveil-sonatine-result.json`
  completed with `abstained`, semantic validity true, no latency, and evidence
  class `qemu-emulation-correctness`.
- `linux/Dockerfile.sonatine-graph-mvp` built and ran on Debian 12 arm64 with
  Python 3.11.2 and GCC 12.2.0. The image identity was
  `sha256:edc8ec8db6fe072920d51a5c93f8054694ec87ab8d9b7c05d99873c5498a0f9e`;
  the default container demo printed an explicit abstention and wrote its
  result under `/tmp`.
- These are GNU/Linux host correctness and QEMU emulation correctness results,
  not latency, energy, FPGA, ASIC, or silicon evidence. EXP-0003 remains
  falsified and unchanged.
- Final `scripts/ci-local.sh` exited 0 with all 81 tests, Sonatine RV64
  release/debug builds, DWARF checks, the command-line QEMU smoke, repeated
  completion-telemetry ingestion, and the native-versus-Sonatine graph
  differential. SHA-256 values were: QEMU smoke
  `cd86f9e274d42443b25ce227b1067c56934c11b485fb1819ec5535f5b116d6a6`,
  completion journal
  `fc13e414073cb7fdfc68dee78680c5b717884b1f47669b12cac1e7910bee01d4`,
  Sonatine graph result
  `01a783fff4ed6273ccc962e2427e25386205d4083897ab495a8a3ff9381f090a`,
  and native graph result
  `859c31354917c85a31607a9eb4fb900210e12e99fd5bb86cc57854b800494c70`.
  The macOS arm64 environment used Python 3.14.6, Apple clang 21.0.0,
  `riscv64-elf-gcc` 16.1.0, Binutils 2.47.20260726, and QEMU 11.0.3.
  The governance checker passed with 79 Markdown files, 71 task definitions,
  and 34 records; `git diff --check` exited 0.
## T-0091 ReactOS portability side-project planning

- Allocated T-0091 after the contemporaneous Sonatine graph-backend work took
  T-0090. The ReactOS planning work stayed in a separate worktree and did not
  modify or reinterpret that implementation.
- Added a non-blocking post-MVP task and roadmap stage for porting the existing
  owned graph loop to a ReactOS user-mode VM. The acceptance boundary preserves
  the v1 graph schemas, baseline-first execution, proposal or abstention,
  semantic verification, commit/rollback, and replayable evidence.
- Kept the first slice out of kernel mode. A direct Win32 CLI is sufficient; a
  message-mode named-pipe adapter is optional. ReactOS does not replace the
  GNU/Linux/AArch64/RK3588 path or Sonatine, and VM execution would be
  host-correctness/portability evidence rather than performance, isolation, or
  production-compatibility evidence.
- Vreji's read-only source check used the official ReactOS repository
  (`https://github.com/reactos/reactos`, inspected 2026-08-11). Upstream labels
  ReactOS Alpha-quality, recommends virtual-machine or non-sensitive test
  hardware, and identifies its source as GPL 2.0. T-0091 therefore requires
  version and license/provenance revalidation at kickoff and does not authorize
  copying ReactOS source or claim patent/FTO clearance; those gaps remain
  `unreviewed` until implementation planning.
- This is a planning-record change only. It adds no ReactOS build, runtime,
  compatibility, security, or performance evidence, changes no Gate, and does
  not require a STATUS, ADR, RFC, EXP, or OPEN_QUESTIONS update.
- `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  passed with 79 Markdown files, 72 task definitions, and 34 records;
  `git diff --check` exited 0. Code tests were not run because this change only
  registers future work and changes no executable behavior.

## T-0043 Miroirs and Pavane semantic boundary

- Resumed the preserved T-0043 work on
  `feat/t-0043-miroirs-pavane-semantic-oracle` from current main. The named
  stash remained a read-only donor and was not applied or dropped.
- The donor's useful structural and verdict ideas were hand-ported. Its
  `latency_ns` execution requirement was rejected because it would make the
  intentionally timing-free T-0090 Sonatine baseline fail closed.
- Miroirs now requires the exact canonical compiler slate plus complete graph,
  contract, variant, candidate-set, ranking, and proposal bindings before any
  backend invocation. Graph identities, node identities, and integer dimension
  types were tightened at the same boundary.
- Pavane now independently regenerates the native/guest deterministic inputs,
  runs the bounded integer reference, and computes the expected FNV-1a checksum.
  A backend that returns a self-consistent but wrong checksum/reference pair is
  rejected. Oracle and validator injection were intentionally not exposed, so
  callers cannot replace mandatory checks with permissive implementations.
- Sonatine result parsing now rejects non-canonical decimal fields, negative or
  overflowing bindings, and a zero cookie. The native adapter requires the
  exact JSON schema, exact scalar types, nonnegative latency, and fixed lowercase
  hexadecimal checksums; malformed observations fail closed.
- Focused verification passed 29 tests with
  `python3 -m unittest tests.test_graph_mvp tests.test_sonatine_backend tests.test_native_backend -v`.
  Direct native and Sonatine/QEMU 8x8 GEMM runs both abstained safely and matched
  Pavane's independently computed checksum `c102cc36c72f8e5c`.
- This is host correctness and QEMU emulation correctness evidence only. It
  adds no performance, energy, FPGA, ASIC, or silicon claim, does not change
  EXP-0003's falsified conclusion, and does not complete full Gate 4.
- Final `scripts/ci-local.sh` exited 0 with all 91 tests, Sonatine RV64
  release/debug builds, DWARF checks, U-mode shell smoke, completion-journal
  replay, and native-versus-Sonatine graph differential. SHA-256 values were:
  QEMU smoke `8517f4ab46454c33ab26e80d55c7012a7c8f53db1000c7f97bc080a685ab53cb`,
  completion journal
  `a560ec64126cec3c6aebafd7f1b296d18c61bfe19bfaed06e5432ab22c4475fc`,
  Sonatine graph result
  `01a783fff4ed6273ccc962e2427e25386205d4083897ab495a8a3ff9381f090a`,
  and native graph result
  `859c31354917c85a31607a9eb4fb900210e12e99fd5bb86cc57854b800494c70`.
- Rebuilt `linux/Dockerfile.sonatine-graph-mvp` with the same 29 focused tests
  and actual QEMU/native differential under Linux arm64. Image
  `raveil-sonatine-graph:t-0043` has identity
  `sha256:cd6e787192be5c4984543ed5baac7be05518d27846d1cbbbdfb591f8f330ff85`;
  its default one-command demo exited 0 with explicit abstention.

## T-0034 Four-plane write authority firewall

- Implemented ADR-0030 on `feat/t-0034-four-plane-authority-enforcement`.
  Sonatine now gives Program, Graph, Data, and Experience distinct capability
  object types and type/right/owner/generation checks. Fixed boot registries
  preserve full Program and Graph identities; capability object IDs name only
  the authority domain and never truncate artifact identities.
- The normal graph path now requires Data authority for object registration,
  submission, and metadata publication, Program authority for the injected
  approval seam, and Experience authority for a byte-exact completion already
  consumed into an active shadow. The eight-entry Experience ledger rejects
  forged, duplicate, and over-capacity observations without overwriting prior
  entries.
- Unguarded job mutations are file-local. Focused core tests receive wrappers
  only under `SONATINE_JOB_AUTHORITY_TESTING`; a production RV64 ELF symbol
  audit found no raw or test mutation symbol. The public plane API is the only
  production mutation boundary added by this task.
- `scripts/ci-local.sh` exited 0 with 92 tests, release/debug RV64 builds, DWARF
  checks, U-mode command-shell smoke, completion-journal replay, and real
  native-versus-Sonatine/QEMU graph differential. The macOS arm64 environment
  used Python 3.14.6, Apple clang 21.0.0, `riscv64-elf-gcc` 16.1.0, Binutils
  2.47.20260726, and QEMU 11.0.3.
- Final SHA-256 values were QEMU smoke
  `b39dd1dc73c3530c3f4e513467e28a990fa728ea77185c12cd9b7cc51a8ecd03`,
  completion journal
  `f881698597e8436f6125e745a21169323ecb2a404ffd8d5ed44a8dca96944332`,
  Sonatine graph result
  `01a783fff4ed6273ccc962e2427e25386205d4083897ab495a8a3ff9381f090a`,
  and native graph result
  `859c31354917c85a31607a9eb4fb900210e12e99fd5bb86cc57854b800494c70`.
- A fresh GNU/Linux arm64 build of
  `linux/Dockerfile.sonatine-graph-mvp` ran the 29 focused tests and actual
  QEMU/native differential. Image `raveil-sonatine-graph:t-0034` has identity
  `sha256:0b3f596c2b48d2c91dd9235008d7ee5fcf697a173ae111ad4a4455a26dbafcd0`;
  its default one-command demo exited 0 with explicit abstention.
- The governance checker passed with 80 Markdown files, 72 task definitions,
  and 35 records; `git diff --check` exited 0. Evidence is host correctness and
  QEMU emulation correctness only. This adds no physical isolation,
  persistence, performance, energy, FPGA, ASIC, or silicon claim; EXP-0003's
  falsified conclusion was unchanged and T-0085 still owned byte shadows at
  that milestone.

## T-0085 bounded byte-shadow publication

- Implemented ADR-0031 on `feat/t-0085-byte-shadow-publication` without
  changing the pointer-free ObjectManifest, JobDescriptor, CompletionRecord,
  or graph transport schemas. Each object-table slot now owns a fixed maximum
  of 512 visible bytes, and registration copies an exact initial image or
  explicitly creates a zero image.
- Admission snapshots every referenced object. Trusted execution can read only
  exact READ ranges from the full dispatched binding. Data-authorized writes
  stage only into the exact WRITE range of a consumed `EXECUTED` completion;
  overlap, missing coverage, wrong owner/binding/object/range, cancellation,
  and post-approval writes fail closed.
- Program approval validates complete byte coverage and freezes it. Finalize
  revalidates the full transaction before publishing all bytes and exact-
  successor versions in one non-failing single-hart phase. Host tests directly
  cover stable snapshots across a competing writer, full-byte multi-output
  conflict atomicity, cancellation zeroization, rollback preservation, and
  fresh staging after slot reuse.
- The QEMU boot smoke now proves old bytes remain visible before commit, new
  bytes become visible with the version after commit, and a later rollback
  preserves them. The Sonatine graph adapter stages the actual GEMM int64
  output and rechecks the checksum from committed backing; an impossible
  post-commit readback mismatch is reported as an explicit fault.
- Final `scripts/ci-local.sh` exited 0 with 92 tests, RV64 release/debug builds,
  DWARF checks, U-mode shell smoke, completion-journal replay, byte-shadow
  smoke, and the native-versus-Sonatine/QEMU graph differential. The macOS
  arm64 environment used Python 3.14.6, Apple clang 21.0.0,
  `riscv64-elf-gcc` 16.1.0, Binutils 2.47.20260726, and QEMU 11.0.3.
- SHA-256 values were QEMU smoke
  `d2d2a94974d5468dc13c4297fc18ffdc47eda929286fcda134cd05b09f4c83f4`,
  completion journal
  `78afe3c467ef5d02b4b4e629895807043a50f4f2d0fb207e1e93f03d8dd01078`,
  Sonatine graph result
  `01a783fff4ed6273ccc962e2427e25386205d4083897ab495a8a3ff9381f090a`,
  and native graph result
  `d4fe4dd532727495102ab2a35e7b7f9814ef461763c6bd83cbbbb93ced6ae127`.
- Production `riscv64-elf-nm -g sonatine/build/sonatine.elf` exposed no raw or
  test byte/job mutation symbols. The guarded `plane_data_shadow_write` and
  trusted-core copy-out reads remain the bounded production seams.
- A fresh GNU/Linux arm64 build of
  `linux/Dockerfile.sonatine-graph-mvp` ran 29 focused tests and the actual
  QEMU/native differential. Image `raveil-sonatine-graph:t-0085` has identity
  `sha256:389e7b0ce10e03adaee54d2fb1b7f59782e3b05a1c0f5a8e9781df445fdbd439`;
  its default one-command demo exited 0 with explicit abstention.
- The governance checker passed with 81 Markdown files, 72 task definitions,
  and 36 records; `git diff --check` exited 0. Evidence is host correctness and
  QEMU emulation correctness only. There is no DMA, cache, persistence,
  multi-hart, performance, energy, FPGA, ASIC, or silicon claim, and EXP-0003's
  falsified conclusion remains unchanged.

## T-0040 pinned IREE/MLIR workload import

- Accepted ADR-0032 and added one repository-authored, digest-bound
  `tensor<8x8xi32>` GEMM fixture with explicit signed extension and i64
  accumulation. The graph result stays exact v1 while a separate strict
  `raveil.graph-import/v1` sidecar binds the canonical `GraphProgram`;
  MLIR/IREE
  objects, paths, diagnostics, and the VMFB stay private to the adapter.
- Locked `iree-base-compiler==3.11.0` wheel hashes for GNU/Linux arm64/x86-64
  and macOS universal development artifacts. The adapter checks version,
  source size/hash, fixed family/shape/entry point, bounded diagnostics,
  timeout, and a 16 MiB artifact ceiling before the unchanged Miroirs/Pavane
  graph loop executes.
- A native macOS development run used the isolated
  `/private/tmp/raveil-t0040-iree` environment. `iree-compile --version`
  reported `3.11.0rc20260316` at revision
  `e4a3b0405d7d23554da26403658d0e8c3c5ecf25`; the real fixture compiled to a
  7,095-byte VMFB and the graph MVP exited 0 with an exact semantic baseline
  and explicit abstention.
- The first Docker build against a new public Python base failed while loading
  registry metadata with `DeadlineExceeded`. No product artifact was changed.
  The retry used the already-audited local Debian arm64 graph image as the
  explicit base, installed the hash-locked wheel, ran 31 focused importer and
  graph tests, compiled the real fixture, and completed the one-command demo.
  The final GNU/Linux arm64 image `raveil-iree-import:t-0040` has identity
  `sha256:b525b868f8d6c40bff3a2e38cf71b73cb85dd8d99f7a3c96f5643b87127a988b`.
  Its one-command demo exited 0, selected the Linux arm64 wheel digest
  `c9cf39661b92bc0050f4ea9fef7d52a631294443bf2ae5d1978f0b9085be40b8`,
  produced the canonical graph identity, and abstained after the valid trusted
  baseline.
- `scripts/ci-local.sh` exited 0 with 99 tests, release/debug RV64 builds,
  DWARF, U-mode command-shell smoke, completion replay, byte-shadow checks, and
  native-versus-Sonatine/QEMU graph differential. Environment: macOS 26.5.1
  arm64, Python 3.14.6, Apple clang 21.0.0, `riscv64-elf-gcc` 16.1.0,
  Binutils 2.47.20260726, and QEMU 11.0.3.
- Evidence is compiler/import correctness, host correctness, and unchanged
  QEMU emulation correctness only. There is no IREE runtime, performance,
  energy, FPGA, ASIC, or silicon claim. EXP-0003 remains falsified and no
  existing Sonatine, Linux, graph, Experience, telemetry, or shadow artifact
  was removed.
