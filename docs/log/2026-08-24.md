# 2026-08-24

## T-0122 simulation-first Graph device MVP

- The owner authorized active use of independent sessions. ADR-0061 and
  T-0122/T-0123 entered canonical main through PR #18 at `206e341`; the live
  Project then gained S00/S01/S02 and T-0044/S14 cards with exact ownership,
  dependencies, commands, evidence classes, and status transitions.
- A low-reasoning implementation lane reconstructed only the eight authorized
  task-neutral files from donor `f5ea057` on current main. The public ABI is
  `raveil.graph-device-abi/v1`; no task-numbered public name remains. Existing
  compiler, oracle, RTL core, scratchpad, simulator adapter, and all T-0044
  files remained read-only.
- Commit `03523a9` passes 30 focused Python tests and the one-command
  `./hardware/chisel/run-graph-device-sim-mvp.sh` flow on linux/amd64. The flow
  compiles Chisel to SystemVerilog, builds the Verilator device plus
  transport-neutral runtime, runs two normal inputs, one cancellation, and a
  reset/restart, then independently finalizes the Pavane receipt.
- The primary run at ignored private locator
  `artifacts/graph_device/run.yOwyBF` has receipt SHA-256
  `92c2c403e7837d08e87b8efb3b6e459d784efb8bc07affde737af4f97c6f9f61`,
  source SHA-256
  `5e4df1490fc61547ad6bfd38bd4abb0d4f87ef7b1e2047ea270310fc4a75f899`,
  ABI SHA-256
  `fe01496e260f6504a9a04a0a54a5f65ad680fdded267293087f3edb929867277`,
  simulator SHA-256
  `f1b41511b4837ad1298dac874abde0f3fb34072890489987300c42e899815819`,
  and environment SHA-256
  `5b6b31d6806a7c0dd64d96b54bebb9f64f456efc137076af96cfd9a05ea30472`.
- Each normal seed staged 324 words, reached output-valid after 3,072 finite
  polls, and produced 256 private words byte-identical to its independent
  Pavane oracle. The cancel seed reported zero output and created no private
  output file. Reset/restart passed. An independent Tester then replayed exact
  commit `03523a9` from a fresh detached worktree and reproduced the same
  receipt SHA-256, source, ABI, simulator, environment, device-log, output,
  cancel, reset/restart, and poll identities. Its raw runner log is
  `/private/tmp/t0122-s02-runner-03523a9-replay.log`, SHA-256
  `c5b1c959a6184efacd75f2dbbdda83dcb41a45a4060f922ac4a55d81b350a5a0`.
- Verification used Python 3.14.6 plus linux/amd64 Scala CLI 1.10.1, Scala
  3.7.4, Temurin JDK 17.0.19, and Verilator 4.038. The final 30 focused tests,
  record checker with 144 Markdown files, 98 task definitions, and 72 records,
  and `git diff --check` passed.
- The result is `rtl-simulation-functional` only. It contains no performance,
  latency-comparison, resource, area, energy, AXI/UIO, FPGA, ASIC, silicon,
  publication, or product evidence. T-0123 remains P1 until separately
  promoted with an exact current-main mutation and acceptance packet.

## T-0044/S14 read-only common-memory strategy inventory

- The independent Librarian lane inspected the exact S13 seven-macro contract,
  pinned inventory receipt and runner, readiness validator, and public primary
  sources without changing tracked files or running any candidate flow.
- No verified public Liberty-plus-LEF set matched all seven exact names,
  dimensions, ports, pins, masks, technology, and PVT conditions. This is no
  candidate identified, not proof of nonexistence.
- OpenRAM can generate layout, timing, and P&R views and is a future proposal
  input, not a compatible asset. One public SKY130 32x512 1rw1r macro has
  Liberty and LEF references but does not match the seven-set; an open upstream
  issue also reports an infeasible 0.04 ns input-slew constraint for that
  generated Liberty.
- S14 closes as negative planning evidence. S13 remains Blocked, EXP-0011
  remains unallocated/unfrozen, and no synthesis, P&R, physical, performance,
  FPGA, ASIC, silicon, dependency-adoption, or legal-clearance claim follows.
  The review is retained at
  `docs/research/reviews/2026-08-24-T-0044-S14-common-memory-strategy-inventory.md`.
- Verification on Darwin 25.5.0 arm64 with Python 3.14.6 passed all 38
  `tests.test_t0044_integrated_rtl` tests, the record checker with 144 Markdown
  files, 98 task definitions, and 72 records, and `git diff --check`.

## T-0122 and ADR-0061 two-lane simulation-first reset

- The owner directed Raveil to make genuine use of independent sessions and
  to repair the GitHub Project as a live Kanban rather than only a detailed
  audit ledger.
- Live GitHub readback at remote main `acd2db99ede5` reported 38 Project items
  and 37 fields: 25 Backlog, 11 Done, one Ready Sprint-review item, one Blocked
  T-0044/S13 item, and no In Progress item. GitHub Issues remained empty and 17
  pull requests had merged. This made the Project useful for history and weak
  for current work allocation.
- Allocated T-0122 and ADR-0061 monotonically after canonical T-0121 and
  ADR-0060. T-0122 becomes the independent simulation-first Graph device MVP;
  T-0044 remains the independent Research lane and stays blocked before
  EXP-0011 data while its macro-view or common-memory inventory proceeds
  read-only. T-0123 is ordered after T-0122 for bounded generated-schedule and
  multi-DAG generality.
- ADR-0061 changes the two-item WIP rule: two mutation items are allowed only
  with disjoint task, branch/worktree, mutation owner, file allowlist,
  artifacts, tests, acceptance, and evidence. Project Manager records, PR
  acceptance, and merge remain serial. A worktree without a live Project item
  is donor material or provenance, not active WIP.
- The unmerged T-0113 branch at clean replay `f5ea057` is retained only as a
  low-risk implementation donor. It is 41 commits behind current main, and its
  seven Static Graph dependencies are byte-identical with current main. Old
  STATUS/TODO/ROADMAP/log changes will not be merged. T-0122 reconstructs only
  the current-main implementation and uses task-neutral public ABI names.
- The Project's populated historical fields remain preserved. ADR-0061 requires
  the default live pull surface to use Title, Status, Priority, Parent T-ID, Owner
  Role, Depends On, Sprint, Story Points, Demo Command, and Evidence Class.
  No field is deleted without a JSON snapshot and separate governance change.
- This taskization creates no implementation, simulation result, performance
  datum, EXP allocation, FPGA/ASIC/silicon result, or gate change.

Verification environment: Darwin 25.5.0 arm64, Python 3.14.6, authority base
`acd2db99ede5`.

- `python3 -m unittest tests.test_experiment.AgentBoundaryTests
  tests.test_static_region tests.test_simulation_adapter -v` passed 26 tests.
- `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  passed with 143 Markdown files, 98 task definitions, and 72 records.
- `git diff --check` passed.

- T-0117/S01 merged through PR #6 as canonical commit
  `bb1631109842f85b2a958ebcf30e5ee6a1b5312f` after a separate read-only
  `raveil-tester` review was posted on the PR. T-0117/S02 then used a new clean
  worktree at that exact revision. The normal TTY view, deterministic `jjq`
  navigation and quit, and explicit empty state exited 0; the malformed
  fixture was refused with the expected exit 2. The four raw transcript hashes
  are normal
  `161d51eda2ef133eab6f8a19b4e452aa49c12455a3d07732d18ea4d4ad9feb0d`,
  navigation
  `b0ccdf9647ee3edc45328f7f351037894d98e366b5ecf0472c04d2f8c9006d60`,
  empty
  `15e502499dfcdbd1d1aad81ab1bf4b5258afd8fd7a6b39e637ac178074f21ca1`,
  and malformed
  `782d046df2b9aeabc4d15941f8d2944b254fad354ae50a94132f99991dd43d3d`.
  `python3 -m unittest tests.test_garden_tui tests.test_graph_mvp -v` passed 35
  tests on macOS 26.5.1 / Darwin 25.5.0 arm64 with Python 3.14.6 and Git
  2.50.1 (Apple Git-155). T-0117 is complete as a bounded host-functional
  Playable. It makes no execution, performance, simulation, emulation, FPGA,
  ASIC, silicon, approval, evidence-promotion, research-gate, or hardware-gate
  claim.
- The owner rejected treating eight SP as the complete AI weekly capacity and
  required agent-specific scheduling plus active delegation. T-0118 and
  ADR-0059 retain eight SP as an under-utilization lower-bound check, establish
  a provisional 13-SP commitment and 13--21-SP warm range, and separate
  relative delivery risk from elapsed execution and the serial PM authority
  boundary. Project readback remains 33 items and grows from 32 to 37 fields
  with `AI Estimate`, `Observed Cycle`, `Agent Tier`, `Role Packets`, and
  `Resource Use`.
  T-0118 reuses the former Capacity Plan item, is five Initial/Current SP in
  S-0001, and occupies the mutation/operations WIP slot beside T-0106/S01
  review. T-0117/S01 preserves Initial SP 5 and changes Current SP to 3; S02
  preserves Initial SP 3 and changes Current SP to 2. Exact dated reasons and
  AI edit/verification/integration ranges are stored on both items. Current
  Sprint items also record role-packet allocation and disclose that token use
  was not instrumented. The first warm service forecast is 3--5 mutation
  packets, 5--8 Tester packets, 5--8 serial PM integration/PR packets, and
  4--8 bounded read-only reviews collectively per week; it will be recalibrated
  after two closed Sprints and is not a measured productivity claim.
- Role configuration is rebuilt from canonical `main`, not copied from the
  dirty T-0042 worktree. Experience, Measurement, Systems, the new dedicated
  Chisel implementer, and Tester use low reasoning; Librarian uses medium;
  Project Manager, Security and Performance Reviewers, and milestone
  Researcher use high. The executable agent-boundary test fixes this matrix and
  the Chisel file/authority boundary. `python3 -m unittest
  tests.test_experiment -v` passed 38 tests on Darwin arm64 with Python 3.14.6;
  the governance checker covered 141 Markdown files, 93 task definitions, and
  70 records, and `git diff --check` passed. This is development-workflow
  evidence only and changes no runtime dependency, implementation P0,
  benchmark, evidence class, EXP conclusion, or research/hardware gate.
- T-0044/S08 implemented the smallest integrated Static Graph/Rocket/common
  owned-memory prerequisite from canonical authority `5ce713be`. One generated
  ChipTop retains the Rocket fallback and cache/interconnect while adding the
  fixed Graph core/client, common fixture provider, common owned TileLink
  memory, and runtime selector. G1b elaboration, G1c Graph-active, G1d
  Rocket-active, and G1e two-mode/inactive-origin smokes passed in the pinned
  linux/amd64 Chipyard/Verilator environment. The final integrated and matched
  Rocket exports have RTL-tree SHA-256 values
  `b8df8a3fa2d379ce4d833f4b418eec05daae2a7378ffa63af0d9557c554c7582`
  and `d678fda0b687aeda1c55ee626a8e63a951f2df80f836dc0ec396fdb1255911b6`.
  Their full Yosys structural preflight exited 0 with equal external ports,
  one equal canonical Rocket module in each hierarchy, the same eleven
  admitted memory-macro instances, and common clock roots. Comparison report
  SHA-256 is
  `30ffeb2652ae459b33aca2f2b0ccee93d2380e23f871cdf6d437e5a6a2ba9fe2`;
  raw and derived manifest SHA-256 values are
  `14f1abb186e2f3592bb046ef133db96ebfbb2c633254fd81a53131c41647d54a`
  and `617803e806e3a01bbf6ef4a27650144c6ae09b57d9e103efb169e827d87af97f`.
  Two earlier append-only output paths retain an export quotation failure and
  one earlier preflight path retains its quotation failure; none is reused.
  The Project item moved from its low-confidence S-0003 forecast into S-0001
  at Current SP 8, bringing S-0001 to the calibrated 21-SP warm ceiling and
  delivery WIP to two of two.
  This is local development-only `rtl-simulation-functional` and
  `rtl-structural-preflight` evidence. EXP-0011 is not allocated or frozen,
  T-0044 remains open, and no performance, synthesis, timing, area, energy,
  FPGA, ASIC, or silicon claim follows.
- An independent high-reasoning T-0044/S08 fairness review used detached
  candidate `1602e53`, passed 41 focused source/checker tests, and confirmed
  that the current evidence must remain functional/structural only. It found no
  reason to reject the S08 prerequisite, but blocks EXP-0011 pre-data freeze
  until a hash-bound contract fixes the estimand and overhead ledger,
  common-module/delta connectivity, normalized clock/reset semantics, macro
  physical views and PVT/RC constraints, repetitions/seeds and uncertainty,
  and append-once raw/derived rules. It also requires the lawful Graph
  1,280-read/256-write and Rocket 800-read/256-write traffic difference to stay
  explicit rather than being labelled equal.
- The first independent clean S08 replay passed 62 focused tests, G1b through
  G1e, and both exports, but failed the final comparison because byte-identical
  `Rocket.sv` files produced different orderings for independent default
  assignments and synchronous updates in one parsed RTLIL process. The failed
  preflight log is retained at SHA-256
  `21ade950f7077be6393aaaeb7262018126c2636165f945c6a2c2f0a2b24eda1d`.
  After two bounded correction attempts failed, HCI-07 was raised and the owner
  authorized one narrow recovery. The resulting candidate retains dependency,
  duplicate-write, and control ordering while canonicalizing only independent
  statements within their original assignment or update runs. Sixty-three
  focused tests pass. Re-analysis of the retained clean raw pair gives equal
  canonical Rocket SHA-256
  `09a5cb7cc8214f4fd933eacc6343943b7eba41ab9c3a520075fbf2fa10c43713`
  and comparison-report SHA-256
  `5f1cc0685bb8d73b7949412fe59fa81b074d5e5ed6e97247a3f3d474290cae6d`.
  A subsequent independent clean replay at
  `bad871d95abcaed9d5589bf24ce40f4fa9666e87` passed 63 tests, G1b through
  G1e, both exports, the complete Yosys preflight, and all raw and derived
  manifest re-hashes. The preflight-log, raw-manifest, and derived-manifest
  SHA-256 values are respectively
  `59370e6a39cffeeff902f1a7833fe3815aab1ed0967870b121abed29a84b0354`,
  `9901949fc321d4a2a77f31c2113f54171cf0406e6533ed3750f34c329eff205f`,
  and `bc184ea0368c0ff3138b9b2e8359e1eb069a99de1592e77307f8db66000bc0ab`.
  This verifies only the S08 functional/structural prerequisite and carries no
  EXP freeze, synthesis, timing, area, energy, FPGA, ASIC, silicon, or
  performance claim.
- The owner established a strict service-cost stop after observing a current
  10,080-minute weekly Codex reading of 89 percent used and 11 percent
  remaining before T-0119 began. ADR-0060 adds HCI-09: calculate remaining as
  100 minus used percentage, continue cautiously at exactly five percent, and
  stop new tasks, subagents, long jobs, remote updates, and merges below five
  percent. Missing or unverifiable weekly telemetry fails closed for new costly
  work. A stop preserves the smallest safe receipt and notifies the owner;
  reset-credit use, capacity purchase, service-plan changes, or bypass require
  separate explicit authority. The threshold did not activate during this
  change. This is a development-workflow observation only and changes no SP,
  implementation P0, evidence class, EXP conclusion, performance claim, or
  research/hardware gate. No account, credential, or reset-credit identifier is
  retained. On Darwin 25.5.0 arm64 with Python 3.14.6 and Git 2.50.1,
  `python3 -m unittest tests.test_experiment -v` passed 39 tests,
  `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  passed 142 Markdown files, 94 task definitions, and 71 records, and
  `git diff --check` passed. This is development-workflow verification only.
- T-0106/S01 was replayed against current main before Sprint acceptance. Two
  retained attempts stopped while SBT loaded a missing synthetic build object;
  their raw-log SHA-256 values are
  `6595b4c037f7ef9f3afd1c9c437adb46473db0a7ea2218fbc249bd2315f06310`
  and `97ceb3ea7ce2d738aa971e8359f1ae3601796b41653662afb9f3892cc8bcab4f`.
  Those observations do not prove the historical event that invalidated the
  mutable volume. A newly versioned `build-v3` passed that boundary and exposed
  a separate deterministic source-closure defect: current main's origin
  overlay referenced integrated-Graph types while the CPU runner installed
  only three Scala overlay files. The failing fresh log is retained at
  SHA-256
  `6f9fe15a229c7a720ef14e0b9bd4afab1b04f9a98d2bd5ff6157c7928de45f00`.
- The accepted narrow correction adds the Static Stencil core and TileLink
  client as compile-time closure members, binds both into input/cache identity
  and post-install verification, and retains `build-v3`. It does not select or
  execute Graph hardware in the BOOM-only configuration. The primary fresh run
  and independent cached Tester replay both exited 0 after 10,916 simulation
  cycles with the same input/source/graph identities. Their raw-log SHA-256
  values are
  `16096756d0ab0a79c63c3ecb31da426c6c9111d5e756de9fc3587e12d6a2cbdf`
  and `e285807b317fd8214f6f30853251924affa2bf3678eaa7215558741bd69ba7b6`.
  The primary and independent scoped suites each passed 48 tests; the record
  checker covered 142 Markdown files, 94 task definitions, and 71 records;
  `git diff --check` passed. T-0106/S01 is accepted as bounded
  `rtl-simulation-functional` negative evidence only. T-0106 remains open and
  conditional; no general metadata, identity, resource, OoO, performance,
  FPGA, ASIC, or silicon conclusion follows.
- T-0044/S10 added a repository-only integrated-physical contract-readiness
  validator after the owner reported seven percent remaining in the current
  10,080-minute weekly Codex window. The stale S09 clean-replay item was already
  satisfied inside accepted S08 and is therefore retained at Initial SP 3 but
  Current SP zero; S10 preserves Initial SP 3 and is re-estimated at Current SP
  5. S-0001 consequently records 26 Current SP, above the warm planning band
  without changing that band or the independent weekly usage stop.
- The first low mutation packet twice reported negative-test acceptance without
  implementing the requested regression cases, triggering HCI-07. The owner
  selected option B: PM takeover inside the same two-file allowlist. The final
  validator rejects missing or unknown fields, allocated/frozen experiment
  state, equal or unrepresented traffic, duplicate accounting/connectivity,
  incomplete macro Liberty/LEF/PVT/RC identities, weak repetition policies,
  mutable seals, and incomplete decision rules. No tracked manifest or result
  was added.
- Primary verification passed 31 integrated-RTL tests, 21 physical-proxy tests,
  and all 102 T-0044 tests. An independent low Tester repeated those commands,
  the record checker, and `git diff --check` in both the normal and stripped
  clean environment. Its regular and clean-environment log SHA-256 values are
  `5f24f7545e80b7dc766c80b83eeb3c2436f3ead1f67ed37ff82a2ce17b05d94d`
  and `e25ba501f7eebf1b722f0f46aa7d478129374885bfedbc126acb84d3d8dc8d9b`.
  This is host contract-validation evidence only. EXP-0011 remains unallocated
  and unfrozen; T-0044 remains open; no physical, performance, energy, FPGA,
  ASIC, or silicon claim follows. The next actual allocation/freeze is a
  separate HCI-02 boundary.
- The owner reported that the 10,080-minute weekly Codex window had fully
  recovered, clearing HCI-09 for a new bounded task. T-0044/S11 was refined at
  Initial and Current SP 8, bringing S-0001 to 34 Current SP without changing
  the 13--21 warm planning band or treating agents as FTE.
- A high Performance pre-freeze audit accepted S10 only as narrow host contract
  validation and blocked a claim-bearing freeze. S11 therefore adds a separate
  v2 document validator for exact implementation/export/toolchain identities,
  the exhaustive common/Rocket/Graph denominator, workload-oracle separation,
  unequal traffic and CPU load reuse, common physical conditions, and explicit
  P&R/energy/hardware claim boundaries. The v1 S10 contract remains unchanged.
  No manifest, EXP ID, RUN-ID, threshold, physical tool invocation, or result
  was added.
- Primary and independent Python 3.14.6 runs passed 35 integrated-RTL tests, 21
  physical-proxy tests, all 106 T-0044 tests, the 142-file record checker, and
  `git diff --check`. The first stripped-environment Tester command selected
  system Python 3.9.6 because PATH was ordered incorrectly and hit two unrelated
  `zip(strict=True)` errors; that operational log remains at SHA-256
  `c1780b6fa98433fb1d08eebd988eef0d969b8ce916919db06d65f24bc7044e51`.
  Recovery attempt 1 pinned `/opt/homebrew/bin/python3` 3.14.6 and passed every
  command; its log SHA-256 is
  `9a55ad52858c37f447ca3cc23794a740f27b6a73f790f5b5425ba5f063dabc55`.
  Evidence remains host contract-validation only. S12 still owns typed
  estimator/threshold/run design and complete evidence sealing; actual
  EXP-0011 allocation/freeze remains HCI-02.
- Post-merge Project readback reports 34 items and 37 fields, with 24 Backlog,
  one Ready, and nine Done items. The corresponding STATUS summary is corrected
  from its pre-S11 33-item and 26-SP snapshot to 34 items and 34 Current SP for
  S-0001; this is a record-only correction and changes no evidence or capacity
  policy.
- The owner reported a fully recovered weekly service window, so T-0044/S12
  started as the sole implementation WIP at Initial and Current SP 8. The
  Performance pre-review separated integrated physical inference from the
  already-completed dynamic latency/traffic campaign: S12 fixes paired physical
  seeds 101 and 202 and explicitly excludes 1/4/16/64/256 fresh-input,
  energy, BOOM, and CGRA work. This avoids mixing deterministic P&R seed
  sensitivity with a runtime confidence interval.
- A low Measurement Implementer added the separate unallocated and unfrozen v3
  validator. PM review rejected its first draft because a duplicate test
  fixture, an incorrectly enabled dynamic scope, duplicate-tolerant decision
  comparisons, and an area-field/formula naming mismatch remained. One bounded
  correction removed all four defects. Primary and independent Python 3.14.6
  runs then passed 38 integrated-RTL tests, 21 physical-proxy tests, all 109
  T-0044 tests, the 142-file record checker, and `git diff --check`. The
  independent log SHA-256 is
  `782fed7aeb337fff0ea429bbbe59f7aa91f64544695a811eeae6b1c62468647b`.
  The high Performance closeout review approved the bounded fairness and
  non-claim boundary. This is host contract-validation evidence only: no EXP
  allocation/freeze, manifest, RUN-ID, physical invocation, result, or claim
  exists. The bounded slice completed within its 2--4 AI-working-hour estimate;
  exact elapsed time was not sealed. HCI-02 is the next boundary.
- The owner reported 96% remaining in the current 10,080-minute Codex window,
  clearing HCI-09 for T-0120. The PM allocated the next monotonic task to a
  bounded Garden Playable refinement rather than reopening completed T-0117.
  A clean worktree and `feat/t-0120-garden-multipane-workspace` branch were
  created from canonical `b60f76b8478c301e3097f2655a349f61b24a6e6b`; the
  dirty T-0042 worktree remained untouched. The Project now has 36 items and
  37 fields; T-0120 is an owner-priority five-SP S-0001 addition, bringing the
  observed Sprint to 47 Current SP without revising the 13--21 warm band.
- One low Experience mutation packet added an explicit 72--240-column render
  contract. Widths at or above 120 compose `Graph Navigator`, `Node Inspector`,
  and `Variants / Evidence` panes side by side; narrower widths stack them.
  Header and `Commands / Status` content remain full-width, authority/evidence
  text wraps, no ANSI or external dependency was added, and the default width
  is a deterministic 150 columns. PM primary verification on macOS 26.5.1 with
  Python 3.14.6 passed all 16 Garden tests, both 150- and 100-column demos, and
  `git diff --check`. This is provisional host-functional
  `development-non-claim` evidence pending independent review. A native host
  rendering of the exact 150-column command was visually inspected at
  `/private/tmp/raveil-garden-multipane.png`, SHA-256
  `85f785faf48332ef21f81d19210c3606806a9dec295e774d26c8edd500f3e6f7`;
  the first rendering exposed uneven pane bottoms, and the bounded correction
  aligned all three pane borders before the recorded capture. The PNG is an
  operational review aid, not canonical evidence or a tracked artifact. No
  graph execution, performance, RTL, FPGA, ASIC, or silicon claim follows.
- Independent Tester verification used a clean detached worktree at exact
  `7054e01f384a659774635d333c0e114adf6bd800` on macOS 26.5.1 ARM64 with
  Python 3.14.6 and Git 2.50.1. All 40 Garden and owned-graph tests, the 150-
  and 100-column CLI demos, the 142-file record checker, commit diff check, and
  clean-tree check passed. The combined log is
  `/private/tmp/raveil-t0120-garden-multipane-acceptance.log`, SHA-256
  `c82485df3a2e254bfa54f623fa94f2f88f6b90f7a3a958e3488f7bf05edb2f6f`.
  T-0120 therefore satisfies its bounded host-functional acceptance pending
  PR integration. `OPEN_QUESTIONS` needs no new entry because the slice leaves
  the accepted Garden authority and schema unchanged. No hardware or
  performance evidence was collected.
- The owner clarified that the existing dependency-free ASCII Garden must keep
  its CLI, but its priority may drop. T-0121's directed Graph Canvas draft was
  interrupted, verified only by 18 scoped tests, and preserved local-only at
  `d59bbc7`; it was not pushed, reviewed, or merged. The Project item moved to
  P2 Backlog and Unscheduled for a later Linux/macOS real-TUI reconsideration.
- The owner authorized HCI-02 for EXP-0011. T-0044/S13 began from canonical
  `7a48dc942db8800c3f6f25f7f0d2b60f697ca952` in the separate
  `research/exp-0011-integrated-physical` worktree. A medium Librarian packet
  and PM direct inspection confirmed that the existing export/preflight and
  S10--S12 validators are present, but no integrated P&R collector or frozen
  EXP-0011 manifest exists.
- The mandatory pre-data asset inventory used the pinned linux/amd64 physical
  image `sha256:7a0db885c100695626175931d3e053ba6a1602d949167b83e2ef60888eea7169`.
  OpenROAD, Yosys, OpenSTA, standard-cell Liberty/LEF, technology LEF, and the
  OpenRCX nominal rule were present. Both S08 candidates carry the same seven-
  macro contract at SHA-256
  `f318d6bd905d3c8b411082e9c33652593c96ae26d29669da8baf34e726b4de52`,
  but an exact recursive search found zero required Liberty timing or LEF
  geometry/pin matches for all seven macro names. Matching GDS files are also
  absent but are supplemental to the pause. The S12
  `required_physical_input_component_unavailable` rule therefore paused S13
  before EXP allocation or candidate data. No placeholder blackbox, synthesis,
  placement, routing, parasitic, timing, area, energy, FPGA, ASIC, or silicon
  result was created. The local operational inventory log is 3,956 bytes with
  SHA-256 `206f6330c630885a59c14bfad5652a7956d09996b34bd9795bc18c0395f0d2b2`.
  A subsequent high-reasoning fairness review rejected the first receipt
  because the exact search root and predicate were not independently
  replayable and GDS was not distinguished from required Liberty/LEF. The
  correction adds a pinned deterministic runner plus byte-matched tracked
  transcript, and treats GDS absence as supplemental only. The corrected
  receipt, runner, and transcript SHA-256 values are respectively
  `0c64aa343b6801c0846744364f2d5dece7af00e26648d53b437de51ea74f3945`,
  `9ff24ece4f418edb706857048507b8c63bd05ad6b4901a242bfbec3199583d18`,
  and `f44b4ec1da28110d60f693529f230d0809f1ab396b8c7c151b86ae740aca1d52`.
  Project count is now 38; S13 retains Initial SP 13 and records Current SP 3,
  bringing S-0001 to 50 Current SP without changing the capacity model.
