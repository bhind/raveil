# 2026-08-15

## EXP-0008 remotely durable evidence promotion

- Ran the committed verifier at
  `f08701398c0b87f9cc75e909952c809488a50b22` with rclone 1.75.0. Before any
  network operation it independently verified the failed and recovery seals,
  all source file sizes and SHA-256 values, both frozen manifests, derived
  report, failed-seal/import lineage, exact RUN-IDs, complete file sets, and
  absence of symlinks. The fixed source snapshot covers 20 files and
  738,617,303 bytes.
- The redacted command was `python3 -m
  raveil.t0044_evidence_promotion promote --remote-root
  <configured-remote>:Raveil/research-data --receipt
  docs/experiments/receipts/EXP-0008-evidence-promotion.json`. Both remote
  preflights returned expected missing-path exit 3; both immutable copies and
  both download-based one-way checks returned 0. Only after both checks passed,
  both completion-marker uploads and byte-for-byte readbacks returned 0.
- Failed-RUN marker SHA-256 is
  `4579d051c990becdb852c19d900027d86078b91dca4895fa87f355c9e768bd47`;
  recovery-RUN marker SHA-256 is
  `00fd5231eab983fbe21fd3fb9b3854dd82d75965e0cea8493212778cd1e1ee9a`.
  Verification completed at `2026-08-14T21:12:44Z`. The canonical receipt is
  `docs/experiments/receipts/EXP-0008-evidence-promotion.json`, SHA-256
  `3ea8b815fb0c83c9563f19c22820f14130be6cc9af5bcfa20508d3eb87699392`;
  its strict schema and sensitive-path scan pass.
- This closes only the ADR-0050 durability gap and establishes remotely durable
  RTL-simulation evidence. No simulation or derivation ran and no sealed byte
  changed. T-0044, RFC-0005 go, energy, timing, area, IP disposition, other
  organizations, product, FPGA, silicon, and general-workload claims remain
  open or explicitly unsupported.

## EXP-0008 local evidence-promotion verifier

- Implemented `raveil.t0044_evidence_promotion` under existing T-0044,
  EXP-0008, ADR-0009, and ADR-0050. It accepts only the two frozen RUN-IDs and
  expected seal/report/manifest hashes, validates every listed relative path,
  size and SHA-256 plus frozen-manifest and failed-seal recovery lineage, and
  rejects symlinks, special files, hardlinks, unknown seal/receipt fields, and
  unexpected file-set mutation.
- Promotion uses repository-external owner credentials, immutable rclone copy,
  download-based one-way checking, completion-marker-last upload and
  byte-for-byte readback. It re-verifies the sealed sources between network
  stages, records only redacted argv/status data, and requires the verifier to
  be committed in a clean tracked worktree before a receipt can be emitted.
- `python3 -m raveil.t0044_evidence_promotion verify` exited zero and
  independently recomputed 20 files and 738,617,303 bytes. Failed seal,
  recovery raw seal, derived report, base manifest, and recovery manifest
  matched their frozen SHA-256 values. No simulator or derive command ran.
- `python3 -m unittest tests.test_t0044_evidence_promotion
  tests.test_t0044_campaign_recovery tests.test_t0044_campaign
  tests.test_t0044_fixture tests.test_t0044_repeated -v` passed 43/43 tests on
  macOS arm64 with Python 3.14.6. Real remote copy, download verification,
  marker readback, and the receipt remain pending until this verifier is
  committed; EXP-0008 is still locally sealed and T-0044 remains open.

## ADR-0050 EXP-0008 evidence-promotion boundary

- Accepted ADR-0050 under existing T-0044. EXP-0008 already records the frozen
  manifests, failed and recovery RUN-IDs, exact commands, all-candidate
  256-input oracle result, nested 1/4/16/64/256 accounts, identity hashes,
  derived-report SHA-256, and raw-seal SHA-256. No new measurement or
  performance interpretation was introduced.
- The remaining reproducibility gap is durability: the generated raw bundles
  are ignored and currently machine-local, with no recorded immutable remote
  copy or download-based check. ADR-0050 applies ADR-0009's immutable-copy,
  marker-last, readback, and secret-exclusion rules to both the retained failed
  RUN and completed recovery RUN before external promotion.
- Added `docs/guides/T-0044-EXP-0008-EVIDENCE-PROMOTION.md` as the Jitro owner
  packet. It forbids simulator reruns and sealed-byte changes, requires exact
  local seal verification, immutable remote transfer, download verification,
  completion-marker readback, a small non-sensitive tracked receipt, mutation
  tests, and fail-closed handling when credentials or source evidence are
  unavailable.
- This is a documentation/decision change. EXP-0008 remains
  `advance-partial-latency-traffic`; T-0044, RFC-0005 go, energy, timing, area,
  IP disposition, other Graph organizations, FPGA, ASIC, and silicon remain
  open or non-claims.
- Read-only source-artifact checks ran `shasum -a 256` over the failed seal,
  recovery raw seal, and recovery report and reproduced, respectively,
  `88fe7959...8726`, `7c90f8a4...eeef`, and `1e52c4e2...7a65`. No sealed byte
  was changed. The focused command
  `python3 -m unittest tests.test_t0044_campaign_recovery
  tests.test_t0044_campaign tests.test_t0044_fixture
  tests.test_t0044_repeated -v` passed 31/31 tests. The governance checker
  passed over 127 Markdown files, 87 task definitions, and 60 records;
  `git diff --check` passed. No network, rclone, simulator, remote copy, or
  promotion receipt was produced by this decision-only change.

## EXP-0008 diagnostic timeout and recovery implementation

- The frozen EXP-0008 collection completed static Graph, Rocket in-order, and
  BOOM OoO for all 256 fresh inputs. BOOM serialize-dispatch, a diagnostic-only
  column, reached invocation 115 and exited 124 at exactly the CPU runner's
  hardcoded 3,600-second simulator timeout. No oracle, resource, traffic,
  accounting, or window violation preceded the exit.
- The collector failed closed, wrote no derived performance report, and sealed
  RUN-ID `20260814T130018Z-4368066-campaign256`. The RUN-ID and partial
  diagnostic are not reused. This is operational-abort evidence, not a
  performance or semantic no-go.
- Recovery keeps the runner's legacy 3,600-second default but exposes an
  explicit positive timeout. A new recovery collector validates the failed
  seal, imports all three completed primary logs by exact hash without
  deterministic rerun, and permits only the diagnostic retry at a frozen
  10,800 seconds. RTL, ELF, estimator, intervals, RFC-0005 thresholds, and
  sample counts remain unchanged. Focused recovery and campaign tests pass;
  recovery implementation authority is
  `1c6160bda7325f039ef88ca1efcc50eb3a572916`.
- Build-only identity collection exited zero without RTL execution. The
  recovery manifest binds source
  `f88406f8148fa70d0eb72757653c48476e17063dcc21967b20794b3b19228bfb`,
  build-only ELF observation
  `b0f152028d3543997c8e9508289c60f95cf73b166dc3115a032ca6c1a43a89d4`,
  failed seal
  `88fe79590c3ea98129d57363920686b084fb10b45e2a9c5fc0b53db3f3bc8726`,
  and all three primary raw hashes. Before retry data,
  `t0044-fixture-campaign-recovery-v1.json` is frozen at SHA-256
  `c9226d05f348c740801b7cbceb673514495c3f5fc15c1192629f31b2f58a1eb6`
  with RUN-ID `20260814T153738Z-0203248-campaign256-recovery`.
- The diagnostic retry completed 256 inputs and exited zero after
  8,005.834626 operations-only seconds. Post-processing then failed before any
  derived report because the recovery adapter reversed the existing parser's
  `path, variant` arguments. A focused test reproduces the API order and the
  adapter-only fix passes. The complete raw log SHA-256 is
  `5d1c3cd52e84184981fd865c87e084f4b1fa63ab1bc56c9699315b7d7d48deff`;
  no simulator rerun or raw modification is authorized for this fix.

## EXP-0008 full campaign result

- Adapter-only derivation fix
  `68ae28f4db067efd987b150bfb8a9792948404a4` parsed the same completed raw
  directory; no simulator command was repeated. All 256 fresh inputs pass the
  independent oracle for Graph, Rocket, BOOM, and diagnostic BOOM. Resource,
  fixture initiator, execution window, one-process/reset/install/no-reload,
  accounting, ordered input, and nested-prefix checks all pass.
- At prefix 256, median execution cycles are Graph 3,072, Rocket 14,539, and
  BOOM 21,893. Graph exposes 1,536 transactions per input; each CPU exposes
  1,056 through lawful load reuse. Useful operations remain identical: 1,280
  loads, 1,024 adds, 256 stores, and 256 outputs.
- At the claim-bearing 64-input prefix, cumulative six-phase Graph/Rocket
  correct-latency ratio is 0.2085927 with 95% interval
  [0.2085784, 0.2086002]. Its upper bound is below the frozen 1.05 no-go
  threshold, and configuration break-even is invocation 1. The bounded
  decision is `advance-partial-latency-traffic`.
- Report SHA-256 is
  `1e52c4e213cb19cb2455cfef67077d3d3acb959bfb834c24e6b12e932d2f7a65`;
  raw-seal SHA-256 is
  `7c90f8a4a09291f5269e19d1425d1eac1a7915b8b3abcc4f16eb7206f438eeef`.
  Successful evidence sessions used 12,824.035566 operations-only seconds;
  including the retained failed diagnostic attempt gives 16,438.087575
  seconds, within the original warm-cache matrix estimate.
- This does not establish RFC-0005 go. Energy, synthesis timing, area, IP
  disposition, VLIW/CGRA, elastic, stream, and hybrid remain unmeasured or
  unimplemented. T-0044 stays open.

## Closeout verification

- The non-performance three-way functional regression exited zero with the
  exact command below. It reconfirmed independent-oracle equality, the common
  owned-resource identity, complete accounting, and traffic conservation for
  Graph, Rocket, and BOOM; its single-run cycle values are not reused as
  EXP-0008 performance evidence.

  ```sh
  RAVEIL_CHIPYARD_SOURCE="$RAVEIL_PINNED_CHIPYARD" \
  RAVEIL_ROCKET_CHIP_SOURCE="$RAVEIL_PINNED_ROCKET_CHIP" \
  ./hardware/chisel/run-controlled-three-way-stencil.sh
  ```

  The two machine-local environment values resolved to the pinned external
  checkouts; the emitted source markers bind Chipyard
  `ac58f38d77c99e9d1cafa64dfd6d4b00bdcd43e1`, Rocket Chip
  `749a3eae9678bc70b029c5b9091fae33fad539c4`, BOOM
  `9459af0c1f6847f8411622dac770ac78fe10847c`, and the source/config/toolchain
  hashes captured in the log. Machine-local absolute paths are intentionally
  not canonical records.

- `python3 -m unittest tests.test_t0044_campaign_recovery
  tests.test_t0044_campaign tests.test_t0044_fixture
  tests.test_t0044_repeated tests.test_t0044_pilot
  tests.test_controlled_run tests.test_owned_memory_boundary
  tests.test_static_region -v` passed 94 tests. The separately required
  `python3 -m unittest tests.test_controlled_run -v` passed 10 tests.
  The record checker and `git diff --check` both exited zero.

## EXP-0009 physical-toolchain kickoff

- T-0044 remains P0 after EXP-0008. ADR-0048 accepts only the next partitioned
  Graph/Rocket area/timing lower-bound screen; it explicitly rejects a
  standalone Graph island as whole-system energy or area evidence.
- A repository-owned toy design now passes pinned linux/amd64 Yosys mapping and
  standalone OpenSTA timing at the Sky130 HD typical corner. The final image ID
  is `7a0db885c100695626175931d3e053ba6a1602d949167b83e2ef60888eea7169`;
  its size is 2,178,077,591 bytes. The toy netlist is
  `81aa811303fdfa1f1d8cca46ba0dad3f065dfe437d6e42d7b5740b2ff8ac5a09`.
- The cold commissioning retained three pre-candidate operational failures:
  classic Conda solve was bounded and interrupted, the old OpenROAD package
  needed explicit OpenMP/GL runtimes, and the OpenROAD database command needed
  technology input so the flow selected its packaged standalone OpenSTA 2.3.3
  engine. All failures exited nonzero; no candidate RTL or result existed.
- Two cached reruns of
  `./hardware/chisel/run-physical-proxy-toolchain-smoke.sh` exited zero with
  identical image, tool, library, environment, package, and mapped-netlist
  identities. This is `synthesis-toolchain-commissioning` with
  `performance=not-measured`.
- Stage-B now has a pre-data implementation for separate Graph/Rocket RTL
  export, manifest-derived top/black-box selection, offline pinned synthesis,
  runtime tool/Liberty identity checks, retained failure seals, append-once raw
  seals, separated derived reports, and fallback-plus-Graph composition. Nine
  focused unit tests pass. No candidate synthesis command has run; the next
  commit is the implementation authority, followed by RTL-only export and a
  separate manifest-freeze commit.
- Rocket RTL-only export then stopped before copying RTL: the dedicated
  worktree first lacked explicit external-source locators and the exporter had
  retained a malformed source-directory assignment. Both attempts were
  pre-synthesis and produced no candidate datum. The assignment was corrected
  and covered by a literal regression assertion; the next corrective commit is
  the Stage-B authority used for freeze.
- The following RTL copy succeeded but admitted the whole 993-file, roughly
  358-MB generated-source/build directory. Pre-freeze inspection rejected it.
  The exporter now copies only collision-checked `.top.f` RTL entries and binds
  both the canonical RTL tree and normalized file list; the rejected copy never
  became manifest or synthesis input.
- The first canonical-copy attempt also exited nonzero with an empty output
  because its `.top.f` locator was one directory too high. Read-only volume
  inspection corrected the locator; no candidate or RTL datum was admitted.
- Canonical pre-data export then succeeded from authority
  `f487259fbadc5dc35548c15d7c8967b7065cd466`: Graph has three RTL files at
  `b7ee4046...778f`; Rocket has 376 `.top.f` RTL files at `d98d3a78...1599`.
  Manifest `t0044-static-physical-screen-v1.json` freezes those trees, Graph
  common provider/scratchpad black-boxes, Rocket core denominator, 20 ns and
  1 ns I/O constraints, exact tool/library identities, failure/raw seals, and
  decision rules. Manifest SHA-256 is `681fd43e...d216`. Candidate synthesis
  remains unrun at freeze commit `d44c2e603ade69ceccb14ac0db2a77374d47ab7b`.
- The first frozen Graph candidate RUN-ID stopped before Yosys: shell
  redirection created `container.log` before launch, conflicting with the
  container's empty-raw-directory guard. The failed seal is
  `080f3ac9...b6f3`; it contains no candidate report. Recovery permits only an
  empty host-owned log at launch, keeps all scientific fields unchanged, and
  requires a new authority, recovery manifest, and RUN-ID.
- Recovery authority `b6c3125...0e94` changes only that guard. Recovery
  manifest `t0044-static-physical-screen-recovery-v1.json` has SHA-256
  `14f5786f...5786`, imports the original manifest hash, and otherwise retains
  the exact matrix, RTL, tools, partitions, constraints, and decisions.
  Recovery freeze commit is `64ffcc498c49ff30c7e282efbbe66616270adb39`.
- The recovery Graph RUN-ID reached Yosys and then failed on CIRCT-emitted
  block-local `automatic logic`; failed raw digest is `dcda0e56...094ec` and
  contains no area/timing result. A bounded next recovery adds a separate
  physical emitter with local-variable and packed-array lowering disabled,
  preserving the runtime emitter. Its source/RTL requires new authority and
  freeze before any candidate synthesis.
- Recovery-v2 authority `cba6226...7e6bf` adds only the separate physical
  emitter. Clean export removed the incompatible forms and produced Graph RTL
  `e95e2e02...5a41d`. Recovery-v2 manifest `293d83bf...1c04` keeps the exact
  Rocket/tool/partition/decision fields. Recovery-v2 freeze commit is
  `ba8ee88863750be9137bd519af0102ed0560cbb5`.
- The lowered Graph RUN-ID parsed RTL, then stopped because hierarchy pruning
  preceded the black-box existence assertions. Failed raw digest is
  `b4ed5c06...56c3d`; no synthesis/STA result exists. A final bounded recovery
  orders read, black-box assertion, then checked hierarchy without changing
  RTL or partitions.
- Final bounded recovery authority `daa984f...b226d` and recovery-v3 manifest
  `efe1ec48...806ed` bind that ordering-only change and inherit recovery-v2's
  exact generated RTL and scientific contract.
  Recovery-v3 freeze commit is `c238c9631f7e248960a2fa421e513764c60dcadd`.
- The final bounded Graph RUN-ID still selected zero modules for the frozen
  common scratchpad black-box after reading all three RTL modules. It exited 1
  before synthesis; files digest is `f203b7c8...7aed9`, failed-seal hash is
  `c0178010...0382`. Rocket was not started because the matrix was incomplete.
  EXP-0009 disposition is `pause-boundary`, with no candidate area/timing or
  energy claim. Derived pause report `340d0d0e...ce700` names the single resume
  condition: prove a Yosys-visible common-partition boundary under a new
  authority/manifest, not another recovery loop.

## EXP-0009 partition-boundary resume

- User direction resumed the single EXP-0009 pause boundary; the scientific
  Graph/Rocket partition, RTL, tool, clock, and decision contract remain fixed.
- A candidate-independent probe in the pinned Yosys 0.27+3 image showed `m:` is
  a memory-object selector. `N:<module>` selects each common module definition,
  `t:<module>` verifies exactly one instance, and `blackbox N:<module>` sets a
  verified black-box attribute before `hierarchy -check`.
- The collector-only repair writes
  `yosys-module-name-single-instance-v1` into raw tool identity. It creates no
  candidate area/timing datum. A committed authority and recovery-v4 manifest
  are required before a distinct Graph RUN-ID; Rocket remains gated on a
  complete Graph partition result.
- Recovery-v4 authority is `fe7b9f615c7ad7833fae00165297d0e7fc33c046`;
  frozen manifest SHA-256 is
  `de396d4f4d4bd81484c5a57b859ac59fdfca84a4cab7cc1a8834d3276c129d31`.
  It chains recovery-v3 and changes no candidate RTL, partition, tool,
  constraint, or decision field.
- Recovery-v4 freeze commit is
  `caa09835116cce0baadd2a12596b10e2b25fd4c3`; candidate collection is
  authorized only from that commit or a clean descendant.
- Recovery-v4 Graph `run-005` passed the named-module boundary and mapping but
  failed `check -assert` on 212 undriven-wire reports before mapped-netlist,
  stat, or STA publication. Files digest is `74898373...16ee3`; failed-seal
  SHA-256 is `7ecf3a41...4d51d1`. It is an ineligible operational failure, not
  a candidate datum.
- The same check failure reproduces on repository toy RTL (32 wires) and clears
  after loading the pinned Liberty as library cell definitions. A Graph
  diagnostic check also clears, without recording area/timing. Pinned OpenSTA
  2.3.3 rejects `remove_from_collection` and `report_clocks`; Tcl iteration over
  `all_inputs` with port-name filtering plus `report_clock_properties` is the
  supported equivalent. Freeze these collector-only compatibility corrections
  before a distinct Graph RUN-ID.
- Recovery-v5 authority is `104c6037fc13da99dbc73f07533488b3cebc7ef2`;
  manifest SHA-256 is
  `6973030c78c744fe5f59e37b4e7bce38735cc15358a10bd28a3c966fb76d051e`.
  The freeze chains recovery-v4 and changes no RTL, partition, Liberty, clock,
  I/O delay, estimator, or decision rule.
- Recovery-v5 freeze commit is
  `f1d62e95618d4a2107a1dfe20635098425b8abf5`; only it or a clean descendant
  may collect the next Graph RUN-ID.
- Recovery-v5 Graph `run-006` completed Yosys, strict mapped checking, and
  OpenSTA and sealed raw files, but derivation rejected two `module not found;
  creating black box` warnings for the intentional common partitions. Files
  digest is `6df847ec...e435f1`; raw-seal SHA-256 is
  `14847d3f...ad74c`. The contained area/STA text is ineligible and no derived
  result exists; Rocket remains unrun.
- Pinned-tool diagnostics show `write_verilog -noattr -blackboxes` emits exactly
  the two common declarations and removes both warnings when read before the
  unchanged mapped core. Bind explicit exactly-once stubs into raw identity and
  recovery-v6 before a distinct Graph RUN-ID.
- Recovery-v6 authority is `d4990d9265e37683a50fe61470e54481384d8cf9`;
  manifest SHA-256 is
  `135f30d6899742eb769d67c8a8dc6929db105f03e54e284e7e81900acebc398a`.
  It chains recovery-v5 and changes only explicit cut-declaration transport.
- Recovery-v6 freeze commit is
  `474c1b5c10fecdef5de0fefc2c5ff7199c22587c`; only it or a clean descendant
  may collect the next Graph RUN-ID.
- Recovery-v6 Graph `run-007` completed with a sealed partition-only synthesis
  estimate: 11,851.3664 um2, 1,592 cells, and +11.45991 ns setup slack at
  20 ns. Raw files digest is `e30beb18...1c072`, raw-seal SHA-256 is
  `634329f4...9fa1`, and derived-result SHA-256 is `c778d6fb...60d1`.
- Rocket `run-008` then failed before synthesis at
  `AXI4UserYanker.sv:255`; Yosys 0.27 rejected a multi-dimensional packed
  array. Files digest is `4065b750...1a80c` and failed-seal SHA-256 is
  `639bf2cc...186d`. It contributes no Rocket datum and leaves the matrix at
  `pause-boundary`.
- The pinned Chipyard `ENABLE_YOSYS_FLOW=1` path adds only
  `disallowPackedArrays` to its firtool lowering options. The next recovery is
  implemented as a physical-export-only, separate-volume path that refuses
  publication unless baseline and physical FIRRTL/annotation/SFC inputs match
  byte-for-byte. It does not mutate the runtime simulator path and requires a
  recovery-v7 freeze plus fresh Graph and Rocket RUN-IDs.
- The first isolated physical build completed at cache key
  `bdd35a34...15f1`, then export failed closed before RTL publication because
  annotations embed their absolute cache roots. The only annotation deltas are
  the two hierarchy filenames; normalized annotation hashes are
  `06064cd6...58d45` and `6d0bb759...a6b51`. FIRRTL/SFC inputs and all three
  hierarchy JSON files match. Top/model file lists retain identical 376/38
  filename sets in different order. The checker now normalizes only the exact
  frozen roots and sorts basename sets; a new authority is required before a
  distinct export.
- Normalization authority `c2842a0...d1035a` produced the distinct successful
  `rocket-in-order-yosys-v2` export: RTL tree `641735c1...86d2f`, 376-file list
  `d38af48c...1db46`, and lowering provenance `fd91b3b9...620da`. No frozen-
  pattern packed-array declaration remains. Pinned Yosys 0.27 read all 376
  files and passed checked `Rocket` hierarchy plus strict pre-synthesis check.
  These are compatibility/provenance observations, not candidate synthesis or
  performance data.
- Recovery-v7 authority is `0611b0431cf052fa1e30f1024f664dffbd1bee98`;
  manifest SHA-256 is `5b165299...ec1780`. It chains v6 and binds the common
  compatibility policy, Rocket generator/provenance hashes and new RTL while
  preserving Graph RTL and all physical-screen scientific fields. Both
  partitions require fresh RUN-IDs after the freeze commit.
- The first v7 Graph command allocated `run-009` but passed the export parent
  instead of `generated-src`. Actual tree hash `961002ee...fbeca` failed the
  frozen identity check after Docker completed and before seal/derivation. No
  candidate value was admitted. This exposed a collector-order incident: input
  identity was checked post-container. The correction moves exact authority,
  variant, and tree verification before raw-directory creation, re-verifies a
  private snapshot and mounts only that snapshot into Docker. Retrospective
  sealing is locked to the exact run-009 identities and pre-seal digest;
  another recovery freeze is required.
- Run-009 was sealed as historical, ineligible host evidence only:
  failure-metadata SHA-256 `5d242ee...d7162`, failed-seal SHA-256
  `d5b7023...9d2af`. Recovery-v8 authority `50639f0...aad938` and manifest
  SHA-256 `3dfb0dd...dd0b1` freeze the preflight/reverified-snapshot correction
  while retaining every v7 RTL, tool, constraint, estimator, and decision
  field. The focused 49-test suite, record checker, Python compile, shell
  syntax, and diff checks passed before the freeze.
- Recovery-v8 Graph `run-010` completed as a partition-only synthesis estimate:
  11,851.3664 um2, 1,592 cells, +11.45991 ns slack; raw seal
  `355aaea...01e77`, result `5280a4d...0b91`. All performance/energy/whole-
  system claim flags remain false.
- Rocket `run-011` passed collection and sealed raw evidence (`103dc16...4580`)
  but derivation failed closed: the log contains eight module area rows and the
  checker incorrectly required one globally. Exactly one row names frozen top
  `Rocket`; no result or matrix is admitted. The narrow correction selects only
  the exact frozen top, rejects duplicate log/stat aliases, and retains
  log/stat equality. Fresh paired recovery runs remain required.
- Recovery-v9 authority `558c7c0...9c6df` and manifest SHA-256
  `d052987...1fd63` freeze only the exact-top parser correction. All v8
  scientific and implementation identities are retained; both v9 partitions
  require new RUN-IDs.
- Recovery-v9 freeze `c9972af...a1fd9` produced fresh complete partitions:
  Graph `run-012` 11,851.3664 um2 / 1,592 cells / +11.45991 ns; Rocket
  `run-013` 51,625.7632 um2 / 6,549 cells / -12.768833 ns at 20 ns. Raw/result
  seals are `823926c...aa05d` / `00159e8...900f` and
  `41f4372...9529` / `73ae885...dc3b` respectively.
- Matrix SHA-256 `692ce50...0739` reports area ratio 0.229563 below 0.25, but
  Rocket misses the common clock. Frozen outcome is `pause-boundary`.
  Evidence is partition synthesis estimate only; no performance, energy, or
  whole-system claim and no full latency-account expansion is authorized.
- EXP-0010 was allocated as a new, explicitly exploratory timing follow-up so
  EXP-0009's 20 ns `pause-boundary` remains immutable. The collector now reads
  exact manifest timing values and admits only legacy 20/1/1 ns or one fixed
  40/1/1 ns tuple. EXP-0010 chains recovery-v9, forbids sweeps and prior-result
  reuse, and remains pre-data until a committed manifest freeze.
- EXP-0010 implementation authority `f662d68...1a9e3` now owns manifest-driven
  timing identity and fail-closed raw/result propagation. Manifest SHA-256
  `a09a641...b76ef` fixes one 40/1/1 ns pair chained to EXP-0009 recovery-v9;
  candidate collection remains prohibited until the freeze commit.
- EXP-0010 freeze `993f394` completed fresh 40 ns Graph `run-014` and Rocket
  `run-015`. Graph reports 11,851.3664 um2 / 1,592 cells / +31.459913 ns;
  Rocket reports 51,625.7632 um2 / 6,549 cells / +7.231165 ns. Both mapped
  netlists reproduce recovery-v9 byte identities.
- Matrix SHA-256 `28a79a3...5b48` has ratio 0.229563 and both timing flags true,
  yielding `advance-to-integrated-physical`. This does not rewrite EXP-0009's
  20 ns pause and carries no performance, energy, or whole-system claim.
