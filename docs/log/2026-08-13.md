# 2026-08-13

## T-0042 synthetic Rocket lifecycle observer

- A repository-owned standalone Chisel ledger now executes ADR-0045 token,
  epoch, replay/retry, A/D, retirement, store-authorization, kill, exception,
  reset, stale, duplicate, missing-metadata, error, and exhaustion transitions
  from synthetic events. It is deliberately separate from the pinned Rocket
  source and the ADR-0043 common bridge.
- `./hardware/chisel/run-rocket-lifecycle-observer.sh` completed with
  `input_sha256=d389bb150910f9c8f6eb8f61a9412823b9aaa29697ad983cb2985157d2cf2729`
  on assert-enabled `linux/amd64` Chisel 7.2.0/Verilator simulation. The exact
  marker conserved 21 allocations as 3 committed loads, 1 committed store, and
  17 noncommitted outcomes; it recorded 8 attempts, 1 core replay, 1 DCache
  retry, A/D 7/7, 5 retirements, 1 store authorization, 2 unknown inputs, and 8
  violations.
- Positive load/store cases and pre-A kill, post-A exception, reset outstanding,
  stale epoch, stripped metadata, duplicate token/outcome, untagged input, D
  error, sequence exhaustion, and invalid D completion were covered. The last
  case came from read-only review: commit eligibility had used the D event kind
  before validating its transition, which could promote a retired load with no
  accepted A request. The implementation retains the required load
  response-plus-retirement and store completion-plus-retirement-plus-
  authorization conjunctions, while also requiring the event that completes a
  conjunction to be a valid transition. The new RTL negative keeps the
  operation active until it terminates noncommitted.
- `verify_rocket_lifecycle_observer.py` accepted one exact marker, verified
  terminal conservation, and rejected field deletion, counter modification,
  and duplicate-marker mutations. Focused boundary tests passed 16/16 after the
  correction. The repository input hash does not claim complete reproducible
  provenance for the mutable Docker/APT/Scala cache substrate.
- Evidence is bounded RTL functional simulation with synthetic events only.
  Pinned Rocket execution and signal mapping, target-ELF semantic initiator,
  BOOM coverage, resource matching, matched comparison, performance, power,
  area, OoO effects, FPGA, silicon, novelty, non-infringement, patent clearance,
  and FTO remain unverified or out of scope. No EXP or new ADR was added.
- Closeout passed full discovery at 214/214 with one expected QEMU-integration
  skip, the record checker over 115 Markdown files and 53 records,
  `git diff --check`, and `./scripts/ci-local.sh`. The local CI used Python
  3.14.6, Apple clang 21.0.0, RISC-V GCC 16.1.0, binutils 2.47, and QEMU 11.0.3;
  it completed Sonatine release/debug builds, QEMU smokes, and native/QEMU Graph
  checks. These repository checks do not expand the synthetic RTL claim.

## T-0042 commit-aware semantic attribution decision

- ADR-0045 accepts a CPU-owned, non-software-writable token and epoch as the
  next semantic-attribution candidate. It explicitly rejects TileLink source,
  DCache origin, software phase, PC alone, and A/D completion alone as semantic
  identity.
- Pinned-source inspection located Rocket MEM/WB, DCache kill/replay/exception,
  and retirement signals, plus BOOM LSU request states, ROB/load/store queue
  identity, replay/kill/exception, and commit signals. This is read-only design
  evidence, not an implemented probe or functional result.
- Replay retains one token across distinct core and DCache attempts; token or
  epoch exhaustion and alias fail closed. Kill, exception, rollback, reset,
  stale epoch, or a duplicate outcome also fails closed. Loads require
  successful response plus retirement, while stores require retirement,
  CPU-specific store authorization, and actual owned-manager D completion.
  Loader/FESVR/Debug/untagged traffic cannot mint CPU tokens. A post-A exception
  cannot cancel transport, so any later side effect is recorded as an explicit
  lifecycle violation rather than attributed commit.
- Rocket and BOOM diagnostics, their positive/negative lifecycle stimuli, and
  connection to the ADR-0043 bridge remain future work. No EXP was added, and
  no semantic proof, resource match, OoO, performance, power, area, FPGA,
  silicon, novelty, non-infringement, patent-clearance, or FTO claim follows.
- A focused source survey records exact Chipyard, Rocket Chip, and BOOM
  revisions; Rocket MEM/WB/kill/replay/retirement and BOOM ROB/LSU/STQ/branch
  lifecycle locators; upstream license hashes; and the existing
  conventional-OoO plus EDGE/TRIPS similarity boundary. It permits no copied
  implementation or novelty/FTO inference. Generalized commit-frontier,
  block-atomic, target-encoding, or dependency-ready expansion requires a new
  claim-to-feature review before implementation.
- Closeout passed the record checker over 115 Markdown files and 53 records,
  `git diff --check`, full discovery at `212/212` with one expected skip, and
  `./scripts/ci-local.sh` including Sonatine release/debug builds, QEMU smokes,
  and native/QEMU Graph checks. These checks validate record and repository
  consistency; they do not implement or exercise the future CPU probes.

## T-0042 exact source is not ELF semantic identity

- A new fail-closed cross-workload verifier consumes the already exact regular
  CPU and PT_LOAD signatures plus both ELF binaries. It requires distinct ELF
  SHA-256 values and different semantic payload witnesses while requiring the
  exact same final tagged DCache source and valid origin accounting.
- Fresh integrated runs of `./hardware/chisel/run-owned-rocket-memory-smoke.sh`
  and `./hardware/chisel/run-owned-boom-memory-smoke.sh` succeeded at exact
  input SHA-256
  `b31048b2e3811a2c0bd67c2180ba04f874535e3492717aafb3372788cd4638aa`.
  Rocket reused source `8224,8224`; its regular and PT_LOAD ELF SHA-256 values
  were `3adf6845c8ba5f190c3c4fa8c4dd0ab22b6f3ff83fb26eab87a2b03462f5fea2`
  and `1def3cc8a5f859dd4c6d513c9a305cc37d562d8184e11b3d3b713af42db8ffe3`,
  and the generated graph SHA-256 was
  `8028e1c5f7a74d2faa38631e3465dc406ee4c18041e9cec8b080d5e9a3c7f51b`.
  BOOM reused source `8288,8288`; its ELF SHA-256 values were
  `5371029fc6e9be177ef16756979923a436e94301cf95ba453bef0fb44398f26d`
  and `15a98c8a6d32a764ff0986383b60d0b102e8cdff1a5af1b9e5901020469145a4`,
  and its graph SHA-256 was
  `8fbee170f393e6ed777484c9fbb3f6e0d6adddbf1802525fa5a1bb680756a948`.
- Both integrated runs retained regular-workload expected-source
  accepted/completed `8/8`, unexpected-source `0/0`, DCache-origin `8/8`, and
  last accepted/completed phase `2/2`. The PT_LOAD probe retained DCache-origin
  `1/1` and non-origin `2/2`; the cross-workload verifier then correlated the
  two already validated signatures without changing the RTL accounting.
- This bounded `rtl-simulation-functional` counterexample proves that the
  current source/origin metadata does not carry a unique target-ELF semantic
  identity. It does not define a replacement witness. A durable LSU/core
  witness would need explicit ownership plus replay, flush, exception, and
  commit rules and therefore requires a superseding ADR before implementation.
  No EXP was added; no resource, OoO, performance, power, area, FPGA, silicon,
  novelty, non-infringement, patent-clearance, or FTO claim follows.
- Closeout checks passed: focused owned-memory tests `14/14`, full discovery
  `212/212` with one expected skip, record checker, shell syntax, Python bytecode
  compilation, `git diff --check`, and full `./scripts/ci-local.sh` including
  Sonatine release/debug builds, QEMU smokes, and native/QEMU Graph checks.

## T-0042 concrete Debug SBA client-class path

- Dedicated repository-owned DMI drivers in the Rocket and BOOM Debug SBA
  configurations each activated the Debug Module, selected and read back
  8-bit `sbaccess`, wrote `0xa5` to owned address `0x08000000`, and checked the
  final SBA busy/error state. Both
  `./hardware/chisel/run-owned-rocket-debug-sba-smoke.sh` and
  `./hardware/chisel/run-owned-boom-debug-sba-smoke.sh` exited 0.
- Exact generated graph checks reported Debug `[8192,8224)` for both systems,
  Rocket DCache MMIO `[16416,16448)`, and BOOM DCache MMIO
  `[16480,16512)`. Graph SHA-256 values were
  `4b799fa12836b3b0ae31b0fee1567707115e66c64bcfeae17ecf08901827f16f`
  and `a53dce87f4f1b7ec2ffc767f7df7d7ba8b8cbc63f17a45cfd0e01dbd7cf98477`.
- Each exact 37-word signature observed the Debug write as
  unexpected/non-DCache-origin accepted/completed 1/1 at phase zero. The
  subsequent CPU read returned payload `0xa5` and completed as
  expected/DCache-origin 1/1 at phase zero, bringing aggregate conservation to
  2/2. Signature SHA-256 values were
  `f6529ba4225bbc99ff3205e466968b13cdf2e5d9644596703aea723259a5759c`
  for Rocket and
  `57fe2ad28fffcb7ad85801837f252d415aed95b65d020326f7a8642b71cbd740`
  for BOOM. The shared input SHA-256 was
  `5105c8a2643c71f00249bd141a5851d2c11b91632047953113ddd0c36ceb3fb8`.
- Negative iterations were retained as engineering evidence. Waiting-gated
  DMI response readiness prevented the first request because the pinned
  `DMIToTL`/TileLink buffering can propagate D readiness into A readiness.
  Keeping response ready high exposed a legal same-cycle request/response that
  an outstanding-only assertion initially rejected. The final driver admits
  only a prior or same-cycle paired response, rejects every unpaired response,
  polls `dmactive`, checks `sbaccess`, and has finite RTL and simulator
  watchdogs. A separate graph-verifier attempt also showed that GraphML repeats
  a master at multiple diplomacy stages; the final fail-closed check uses the
  first manager-list coordinate and compares every pinned range exactly.
- Manager regression commands also completed after the Debug SBA runs.
  `./hardware/chisel/run-owned-tl-protocol.sh` exited 0 with the raw-client
  classifier and test-only field-stripping negative intact.
  `./hardware/chisel/run-owned-rocket-memory-smoke.sh` and
  `./hardware/chisel/run-owned-boom-memory-smoke.sh` each exited 0 from shared
  input SHA-256
  `5105c8a2643c71f00249bd141a5851d2c11b91632047953113ddd0c36ceb3fb8`.
  Their existing CPU signatures retained DCache-origin 8/8, non-origin 0/0,
  config-specific last sources 8224 or 8288, and phases 2/2; their PT_LOAD
  probes retained serial non-origin 2/2 followed by DCache-origin 1/1.
- The implementation depends on the pinned Chipyard/Rocket-Chip `WithDebugSBA`,
  `DMIToTL`, `SBToTL`, and RISC-V Debug register encodings. Existing upstream
  license files locate source provenance only; this is not a patent,
  non-infringement, legal-clearance, or FTO conclusion.
- Evidence class is bounded `rtl-simulation-functional` plus exact
  `rtl-elaboration-topology`. It shows the tested Debug and DCache TileLink
  client classes and manager-local A/D metadata correlation. It does not halt
  or resume the hart, prove an instruction, PC, target-ELF semantic initiator,
  cover every loader/debug path, establish matched resources, isolate/remove
  OoO, or support performance, power, area, FPGA, silicon, novelty,
  non-infringement, patent-clearance, or FTO claims. No EXP or new ADR was
  added; this remains ADR-0044 diagnostic evidence.
- Final local verification used Python 3.14.6, Apple clang 21.0.0,
  `riscv64-elf-gcc` 16.1.0, GNU readelf 2.47.20260726, and QEMU 11.0.3.
  The focused boundary suite passed 13/13; quiet discovery passed 211 tests
  with one explicit skip; the record checker passed 113 Markdown files, 85
  task definitions, and 52 records; `git diff --check` passed; and
  `./scripts/ci-local.sh` exited 0 after its unit, release/debug build,
  normal/interrupt QEMU smoke, completion-telemetry, and native/QEMU Graph MVP
  sequence.

## T-0042 SimTSI/FESVR PT_LOAD structural-origin negative

- `./hardware/chisel/run-owned-rocket-memory-smoke.sh` and
  `./hardware/chisel/run-owned-boom-memory-smoke.sh` both exited 0 with final
  shared input SHA-256
  `0aa1771a198b15a8ed991a30e97905220b8cc848d58e209cb8b2b3524cfe080b`.
  Their independent content-addressed build volumes retained the exact pinned
  source checks and ephemeral origin/Xbar patches.
- The dedicated probe ELF has one writable `PT_LOAD` at virtual and physical
  address `0x08000000`, file and memory size four bytes, and payload
  `0x6c6f6164`. `_start` is `0x80000000`, the 33-word signature occupies
  `0x800001f0..0x80000274`, and `tohost` is `0x80000280`. The simulator command
  line contained no `+loadmem` override.
- Before CPU access, both runs observed serial sources `16,16`, aggregate
  accepted/completed 2/2, structural DCache-origin 0/0, non-origin 2/2, and
  software phase zero. After the CPU read the loaded payload, totals were 3/3,
  origin 1/1, and non-origin still 2/2. Rocket's last tagged source was 8224 in
  `[8224,8256)`; BOOM's was 8288 in `[8288,8320)`; both retained phase zero.
- Exact generated graph SHA-256 values were
  `8028e1c5f7a74d2faa38631e3465dc406ee4c18041e9cec8b080d5e9a3c7f51b`
  for Rocket and
  `8fbee170f393e6ed777484c9fbb3f6e0d6adddbf1802525fa5a1bb680756a948`
  for BOOM. Loader-probe signature SHA-256 values were
  `55b3fe5c66300e94805f40a0cea9328fe0fe873d3c2b6208ccbf4c8ea04b4b6f`
  and `a3601f0da57c0455b5267102523407eb3a969f469a1d356ab88a5c8e920e5c36`
  respectively.
- The existing CPU smoke in each wrapper also reproduced expected source and
  structural-origin accepted/completed 8/8, unexpected and non-origin 0/0,
  final phases 2/2, and signature SHA-256
  `cfdb4eada02f61cb5348803715d23a1bb46677ca9f192dd205ed2a67f1fd049e`
  for Rocket and
  `9bd9be12511b70fca0be51ed8dcc041d59d09a5fa23d2e95c3b5dcf54c87deef`
  for BOOM.
- Three fail-closed corrections preceded the final runs. The first one-request
  expectation failed because the pinned FESVR partial write performs an
  aligned transport read then write. A later exact-symbol check still used
  pre-layout addresses `0x800001e0..0x80000264`; `readelf`/`nm` showed the
  actual final signature at `0x800001f0..0x80000274`. Finally, an over-escaped
  `awk` field in the audit marker produced an empty graph hash even though the
  probe passed; replacing it with a fixed 64-character checksum extraction and
  rerunning both systems produced complete non-empty audit markers.
- This is bounded `rtl-simulation-functional` evidence that one pinned
  SimTSI/FESVR PT_LOAD transport reaches the owned manager as non-origin and a
  subsequent CPU read crosses the structural DCache marker in the same probe
  process. It does not prove the target ELF's semantic initiator, instruction
  or PC, cover Debug SBA or every loader/debug path, establish matched
  resources, isolate or remove OoO, or support performance, power, area, FPGA,
  silicon, novelty, non-infringement, patent-clearance, or FTO claims. No EXP
  or new ADR was added; the evidence remains within ADR-0044 diagnostics.
- Final local verification used Python 3.14.6, Apple clang 21.0.0,
  `riscv64-elf-gcc` 16.1.0, GNU readelf 2.47.20260726, and QEMU 11.0.3.
  `python3 -m unittest tests.test_owned_memory_boundary -v` passed 11/11;
  quiet discovery passed 209 tests with one explicit skip; the record checker
  passed 113 Markdown files, 85 task definitions, and 52 records;
  `git diff --check` passed; and `./scripts/ci-local.sh` exited 0 after the
  complete unit, release/debug/DWARF, normal/interrupt QEMU smoke, completion-
  telemetry, and native/QEMU Graph MVP sequence.

## T-0042 origin-sideband fail-closed transport negative

- `./hardware/chisel/run-owned-tl-protocol.sh` exited 0 with host input SHA-256
  `3f5401983c99f8ced3b7ff39da688483baa900142a89eaa4c1303270ae08255c`,
  overlay SHA-256
  `70e28cd6bbe7ab4d78104784e40fc60c4da822b6b3d4fb7a954e2212b71477dc`,
  and content-addressed assembly key
  `0f0116bf64dbc9898eb96f65b7f60f914c2c11c0e2d96171626aae084f10dde6`.
- Protocol V4 retained its raw-client absence control: 30 transactions,
  expected 3/3, unexpected 4/4, DCache-origin 0/0, and non-origin 7/7.
- The new test-only `RaveilOwnedTLOriginStripHarness` drove the structural
  origin field true upstream, removed it from downstream request-field
  negotiation, and ran the same driver. `OWNED-TL-ORIGIN-STRIP-V1` reported
  DCache-origin accepted/completed 0/0 and non-origin 7/7 with no assertion
  failure. This is bounded `rtl-simulation-functional` evidence that metadata
  loss fails closed through manager-local A/D accounting. It is not an actual
  loader, FESVR, debugger, CPU, or target-ELF semantic-initiator test.
- The initial sandboxed invocation stopped before Docker build because access
  to the Docker socket was denied. Re-running the exact script with Docker
  permission completed; no Scala, Chisel, Verilator, or RTL assertion failure
  occurred.
- `python3 -m unittest tests.test_owned_memory_boundary -v`, the record checker,
  and `git diff --check` passed. `./scripts/ci-local.sh` also exited 0 with its
  full unit, release/debug build, QEMU smoke, and Graph MVP sequence. The final
  quiet discovery rerun passed 207 tests with one explicit skip.

## T-0042 DCache-origin milestone publication

- Commit `0b58cdf478d94862c32b09f173025416a6f97e57` was pushed to
  `refs/heads/test/t-0042-dcache-origin-audit` and read back at the exact SHA.
- A fresh clone of that public branch ran `./scripts/ci-local.sh` to exit 0 with
  207 tests, one explicit skip, release/debug builds, QEMU smokes, and native/
  QEMU graph-MVP checks.
- New annotated tag `milestone/t-0042-dcache-origin-audit` was pushed without
  moving an existing tag. Remote tag object
  `41291e5327bde8dcabc760851d621c11a5d28096` peels to the exact commit above.
  No GitHub Release was created.

## T-0042 DCache-local structural-origin functional closeout

- The final Rocket and BOOM RTL smokes both exited 0 with shared input SHA-256
  `77e7d32d528e6233a34a01da36d8021ae8685921ea8c50d0b6d6d7f8a42fc430`.
  Exact generated graphs reported Rocket DCache-MMIO `[8224,8256)` with graph
  SHA-256 `8028e1c5f7a74d2faa38631e3465dc406ee4c18041e9cec8b080d5e9a3c7f51b`
  and BOOM `[8288,8320)` with graph SHA-256
  `8fbee170f393e6ed777484c9fbb3f6e0d6adddbf1802525fa5a1bb680756a948`;
  both retain SimTSI/FESVR `[0,8192)`.
- Both 30-word CPU signature V3 checks reported expected source accepted and
  completed 8/8, unexpected 0/0, structural DCache-origin 8/8, non-origin 0/0,
  in-range final sources (`8224` or `8288`), final phases 2/2, and unchanged
  data/phase/aggregate counters. Signature SHA-256 values were
  `cfdb4eada02f61cb5348803715d23a1bb46677ca9f192dd205ed2a67f1fd049e`
  for Rocket and
  `9bd9be12511b70fca0be51ed8dcc041d59d09a5fa23d2e95c3b5dcf54c87deef`
  for BOOM.
- The final raw-client Protocol V4 rerun exited 0 with 30 transactions,
  expected 3/3, unexpected 4/4, structural origin 0/0, non-origin 7/7, last
  source 3, and last phase 2. It is an untagged-manager negative control, not
  CPU execution evidence.
- The CPU runners did not mutate the checked-out pinned dependencies. They
  copied the exact Chipyard tree into the input-hash-keyed persistent build
  volume, required the unpatched `TLXbar.scala` SHA-256
  `7ef8f49ccb3b8df8ba3860d1a54d1eee6d964431b77aa147dd2511f97fe3a613`,
  applied the repository-owned request-default patch only to that ephemeral
  copy, and required the patched SHA-256
  `4867e293671c4df061637b01f358772595c0ec0efff359deeacb8572dde4cbe2`
  before elaboration and again before simulation. The source-hook patches,
  request-default patch, overlays, workload, verifiers, linker, runner, and
  Dockerfile all contribute to the shared CPU input SHA-256 above; an
  incomplete or drifted cache fails closed.
- A final hygiene correction removed whitespace-only added lines from the
  three patch artifacts and changed their byte hashes without changing any
  patched source result: the Rocket hook, BOOM hook, and `TLXbar.scala`
  post-patch SHA-256 values remained the required exact values. Both CPU RTL
  smokes were therefore rerun from the new shared input SHA-256 above and
  reproduced the same graph ranges, 8/8 positive origin counters, 0/0
  non-origin counters, phases, and signature SHA-256 values.
- Final local verification completed on macOS with Python 3.14.6, Apple clang
  21.0.0, `riscv64-elf-gcc` 16.1.0, GNU readelf 2.47.20260726, and QEMU
  11.0.3. `python3 -m unittest tests.test_owned_memory_boundary -v` passed
  9/9, the record checker passed across 113 Markdown files, 85 task
  definitions, and 52 records, `git diff --check` passed, and
  `./scripts/ci-local.sh` exited 0 after 207 tests with one explicitly skipped
  real-QEMU integration plus the release/debug Sonatine builds, bounded QEMU
  smokes, and native/QEMU graph-MVP checks.
- This closes only the current DCache-local structural marker diagnostic:
  exact topology, runtime client class, and A/D manager-local correlation work
  in the bounded functional RTL smokes. It does not prove target-ELF semantic
  initiator identity, an instruction or PC, complete loader/debug exclusion,
  matched resources, OoO effects/removal, performance, power, area, FPGA,
  silicon, novelty, non-infringement, patent clearance, or FTO. T-0042 remains
  open for semantic negative tests and matched common-resource work.

## T-0042 pinned Rocket request/response/WB positive

- `./hardware/chisel/run-owned-rocket-request-retire-witness.sh` completed
  twice at input SHA-256
  `09dcc09ac2257182d398eed0a231a276f8382906ecb282799dba9d1b77d07bd1`;
  the second invocation reused the validated simulator cache. The exact
  generated graph retained Rocket DCache-MMIO source range `[8224,8256)`,
  SimTSI/FESVR range `[0,8192)`, and graph SHA-256
  `8028e1c5f7a74d2faa38631e3465dc406ee4c18041e9cec8b080d5e9a3c7f51b`.
- The pinned `RocketCore` patch accepted one store at PC `0x80000018` with tag
  `0x00` and one load at PC `0x80000020` with tag `0x0c`. The exact seven-event
  stream was store `allocate/request/retire`, then load
  `allocate/request/retire/response`; the verifier also permits the load's
  response and retirement in the opposite order while requiring both and
  exact request/response tag equality. Both requests were first attempts.
- The simulator reported `*** PASSED ***` after 10,286 cycles. The exact
  signature was `(1, 0x4b1d2e3f)`, with signature SHA-256
  `1bf5a40e750d5662f6acad38a087f53c58ef6d7835dad04767678da03001565c`
  and ELF SHA-256
  `057d1872f3656f8ddd5cdd1c615d61e198c922797f2e857921dec3d9ec46ccf3`.
- Three fail-closed corrections preceded the final run. An initial assertion
  incorrectly required DCache response validity in the load WB cycle; the
  patch now tracks response and WB independently. `+verbose` was required to
  enable the Chisel diagnostic, and the runner then needed `2>&1` because the
  pinned simulator emitted the records on stderr while `tee` captured stdout.
  Finally, the verifier was corrected to normalize only Chisel's whitespace
  padding after `=` before applying its exact schema. These are retained in
  `docs/FAILURE_KNOWLEDGE.md`.
- This is bounded `rtl-simulation-functional` positive evidence for local
  DCache request acceptance, response-tag correlation, and WB observation. It
  does not carry a durable token through DCache/TileLink, prove store
  authorization or owned-manager D completion, execute replay/retry/kill/
  redirect/exception/reset negatives, implement the BOOM probe, prove target-
  ELF semantic initiator identity or resource matching, or measure performance,
  power, area, OoO effects/removal, FPGA, silicon, novelty, non-infringement,
  patent clearance, or FTO.
- Closeout verification passed `sh -n` for both witness runners, Python bytecode
  compilation, 18/18 focused owned-memory boundary tests, and 216 repository
  tests with one explicit real-QEMU integration skip. The governance checker
  passed 115 Markdown files, 85 task definitions, and 53 records;
  `git diff --check` passed. `./scripts/ci-local.sh` exited 0 with the same 216
  tests/one skip, release and debug Sonatine builds, bounded QEMU smokes, and
  native/QEMU Graph MVP checks. An initial `unittest discover` without
  `-s tests` found zero tests and exited 5; the corrected explicit test-root
  command produced the results above.

## T-0042 pinned Rocket same-cycle post-request redirect negative

- The preceding request/response/WB milestone was published without moving an
  existing ref or creating a GitHub Release. Remote branch
  `test/t-0042-rocket-request-retire-witness` read back as commit
  `a89f7b7a680636412fd692351c8311e754db8387`; annotated tag
  `milestone/t-0042-rocket-request-retire-witness` read back as tag object
  `3515f7fd048df98a6ba28d7ed036841c12d96aca` peeling to that exact commit.
  A fresh clone of the public tag ran `./scripts/ci-local.sh` with exit 0.
- A preliminary run placed an always-taken branch immediately before an
  owned-address store. Contrary to the initial pre-request-kill hypothesis, the
  pinned Rocket emitted `allocate/request` for the store at `0x08000100` while
  the older branch resolved in MEM. The ELF still completed with signature
  `(1, 0x4b1d2e3f)`, but the earlier diagnostic had no kill event and left that
  token unresolved. This expectation-model failure is retained in
  `docs/FAILURE_KNOWLEDGE.md`.
- The corrected hook now checks `take_pc_mem` in the accepted-request cycle,
  emits an exact `reason=mem-redirect` kill with branch, taken, and direction-
  misprediction qualification, records `promotion=blocked`, closes the killed
  sequence, and permits a following load to use the next sequence.
- The first exact run exposed an invalid zero-initialization assumption: the
  uninitialized `SyncReadMem` location returned `0xc5686cac`. Attempts to add a
  prior store changed request readiness and predictor history, so the expected
  accepted wrong-path request was not deterministic. Indirect-jump and
  self-trained conditional variants instead produced pre-request suppression.
- The final workload performs a load before and after the probe and requires
  equal non-magic readback. A true dependency on the first value keeps the
  single-live-token observer from seeing the next target request until the load
  has produced WB and its matching response. An intermediate run without that
  dependency correctly stopped on `Raveil Rocket witness live-token mismatch`;
  multi-live-token overlap remains unsupported.
- The source-cache SHA-256 is
  `ae104a177e448d96012ac33c9ded4465bd2dc1533f4ba96bbd694d055755653c`,
  the complete runner-input SHA-256 is
  `1b21cc88d4502c182abb14ddb4b12e78147cda8cecb39f96d93b6de625cfa8a5`,
  the pinned unpatched `RocketCore.scala` SHA-256 remains
  `0435dce882f4ad37ee566218fcd8b7d6f9e088c50448677d6eb6efac7e9029ac`,
  and the corrected post-patch SHA-256 is
  `29a1032a10aeb744853fdf50b0bfa962415461d253e7a74152852b020539b7a2`.
- Two consecutive cached executions of
  `./hardware/chisel/run-owned-rocket-postrequest-redirect-negative.sh` passed.
  Each trace contained eleven records: two completed loads around one
  `allocate/request/kill` store token. Both loads returned `0xc5686cac`; the
  wrong-path store had no WB retirement. The source-map verifier reported
  manager range `[8224,8256)`, serial range `[0,8192)`, fragmenter factor 32,
  and graph SHA-256
  `8028e1c5f7a74d2faa38631e3465dc406ee4c18041e9cec8b080d5e9a3c7f51b`.
  `./hardware/chisel/run-owned-rocket-request-retire-witness.sh` also passed as
  the seven-record positive regression against the same graph and input hash.
- This milestone is bounded functional RTL evidence for the exact simultaneous
  accepted-request/MEM-redirect bookkeeping and differential readback only. It
  is not multi-live-token support, pre-request-kill, DCache S1-kill, TileLink
  A/D, semantic initiator, resource match, performance, power, area, OoO,
  FPGA, silicon, novelty, non-infringement, patent-clearance, or FTO evidence.

## T-0042 pinned Rocket redirect DCache-fate diagnostic

- A dedicated `RaveilOwnedRocketFateConfig` now combines the existing bounded
  accepted-request/MEM-redirect workload with two direct but separate
  observations. The Rocket patch retains sequence, PC, address, and local tag
  for one cycle and samples `io.dmem.s1_kill`/`s2_kill`; the owned manager logs
  A on `fire` at `0x08000100` and holds source/opcode/size/phase until D `fire`.
- The first build completed Chipyard assembly, elaboration, firtool lowering,
  and Verilator compilation, then stopped fail closed because the source-map
  verifier admitted only the old Rocket config name. After exact-name admission
  of the fate config, the RTL completed but its verifier rejected a zero-padded
  hexadecimal address string. Numeric normalization and explicit Rocket
  request/S1 PC-address-tag correlation corrected both verifier defects; they
  are retained in `docs/FAILURE_KNOWLEDGE.md`.
- `./hardware/chisel/run-owned-rocket-redirect-dcache-fate.sh` then exited 0
  from the dedicated persistent volume. Source-cache SHA-256 was
  `6f71149621b114e6417e64c02bcfcff29ff8a6f5da242db76b38a5f4bfa0bfaf`,
  complete runner-input SHA-256 was
  `93010fada15a6478cb86e02eb030fcb78cf43abf75f3420af1a5453d4c540311`,
  and generated graph SHA-256 was
  `8028e1c5f7a74d2faa38631e3465dc406ee4c18041e9cec8b080d5e9a3c7f51b`.
  The graph verifier reported Rocket DCache manager range `[8224,8256)`,
  serial range `[0,8192)`, and fragmenter factor 32.
- The exact trace reports sequence 2 at PC `0x80000024`, accepted store request
  tag 0, `promotion=blocked`, following-cycle `s1_kill=1`, and `s2_kill=0`.
  The manager trace is exactly `A,D,A,D`: both As are `Get` size 2 with DCache
  origin/expected-source true and phase 0; both Ds are successful
  `AccessAckData`. Sources are 8240 then 8224 and match within each A/D pair.
  No probed-address `Put` appears. Both loads return `0x682513da`, equal and
  different from the wrong-path magic value.
- Against the same final source and input hashes, the cached base
  `RaveilOwnedRocketConfig` runs
  `./hardware/chisel/run-owned-rocket-postrequest-redirect-negative.sh` and
  `./hardware/chisel/run-owned-rocket-request-retire-witness.sh` also exited 0.
  The former retained the eleven-record redirect negative and the latter the
  seven-record positive request/response/WB witness; both reused graph SHA-256
  `8028e1c5f7a74d2faa38631e3465dc406ee4c18041e9cec8b080d5e9a3c7f51b`.
- This is bounded `rtl-simulation-functional` evidence for Rocket-local
  request/S1 correlation and separate manager-local A/D source correlation.
  `transport_token_correlation=not-carried` and
  `semantic_initiator=not-proven` remain explicit. It is not a resource match,
  performance, power, area, OoO, FPGA, silicon, novelty, non-infringement,
  patent-clearance, or FTO result, and no EXP was created.
- `sh -n` and Python bytecode compilation passed. The owned-memory boundary
  suite passed 22 tests; `python3 -m unittest discover -s tests` passed 220
  tests with one skip. The task-governance record checker passed 115 Markdown
  files, 85 task definitions, and 53 records; `git diff --check` passed.
  `./scripts/ci-local.sh` exited 0 with the same full unit suite, release/debug
  Sonatine builds, QEMU smokes, and native/QEMU Graph MVP checks.

## T-0042 redirect DCache-fate milestone publication

- Commit `ba6bd828a525be8fa74b303177ff3edd5438d95d` was pushed to
  `refs/heads/test/t-0042-rocket-redirect-dcache-fate` and read back at the
  exact SHA.
- Annotated tag `milestone/t-0042-rocket-redirect-dcache-fate` was created
  without moving an existing ref. Remote tag object
  `facd8d7864a7d32f6d9c26e5125e9dba7e790ef5` peels to the exact commit above.
  No GitHub Release was created.

## T-0042 pinned Rocket post-request exception diagnostic

- A read-only comparison against TODO and ADR-0045 selected the smallest
  non-duplicative next boundary: one accepted pinned-Rocket misaligned load
  followed by its WB exception. This remains under T-0042; no invariant or
  performance claim changed, so no new ADR or EXP was created.
- The first elaboration stopped because a broad patch hunk placed outcome logic
  before its Scala state definitions. After colocating the logic, the first
  complete RTL simulation emitted the intended exception but stopped fail
  closed because the new verifier assumed eight surrounding aligned-load
  records. The raw trace contained ten: each aligned load had exact attempts 1
  and 2 with matching PC, address, operation, and tag. The verifier and mutation
  fixture now require that exact shape. Both failures are retained in
  `docs/FAILURE_KNOWLEDGE.md`.
- `./hardware/chisel/run-owned-rocket-postrequest-exception.sh` then exited 0.
  Source-cache SHA-256 was
  `84bfa8e478c54aee515bca3491986eba6ac600fcfdae4502dcadacba4a0798c7`,
  complete runner-input SHA-256 was
  `7fe93531cf4672f5be9c785bb8ad64d1c79a4df3706ee988aeff1d8f2147b8cb`,
  and generated graph SHA-256 was
  `8028e1c5f7a74d2faa38631e3465dc406ee4c18041e9cec8b080d5e9a3c7f51b`.
- The exact Rocket-local trace records one accepted request at PC
  `0x80000028`, address `0x08000101`, and tag `0x0c`, followed by one matching
  WB misaligned-load exception with cause 4, `ma_ld=1`, `ma_st=0`,
  `take_pc_wb=1`, and `promotion=blocked`. The trap signature requires cause 4,
  `mtval=0x08000101`, one trap, and exact recovery; aligned before/after loads
  both returned `0x682513da`.
- This is bounded `rtl-simulation-functional` evidence for a Rocket-local
  accepted-request/WB-exception correlation. It does not show a TileLink A for
  the misaligned access, transport the token through DCache/TileLink, prove
  post-A rollback or general side-effect absence, identify a semantic
  initiator, match resources, or measure performance, power, area, OoO,
  FPGA, silicon, novelty, non-infringement, patent clearance, or FTO.
- `sh -n` and Python bytecode compilation passed. The owned-memory boundary
  suite passed 24 tests, including mutation rejection for exception
  qualification, PC/address/tag correlation, aligned-load completion fields,
  and duplicate records. `python3 -m unittest discover -s tests` passed 222
  tests with one skip. The task-governance record checker passed 115 Markdown
  files, 85 task definitions, and 53 records; `git diff --check` passed.
  `./scripts/ci-local.sh` exited 0 with the same full unit suite, Sonatine
  release/debug builds, QEMU smokes, completion telemetry, and native/QEMU
  Graph MVP checks. Three independent low-reasoning read-only reviews found no
  Critical or High issue; their verifier-field Low finding was closed before
  the final RTL readback, and their closeout-record Medium finding was closed
  by this paragraph.

## T-0042 post-request exception milestone publication

- Commit `69df120abef16eb55f44321825ec2f99bbf83686` was pushed to
  `refs/heads/test/t-0042-rocket-postrequest-exception` and read back at the
  exact SHA.
- Annotated tag `milestone/t-0042-rocket-postrequest-exception` was created
  without moving an existing ref. Remote tag object
  `ade6d9f251b5eab7c513b8ad2230d45cc13a42e9` peels to the exact commit above.
  No GitHub Release was created.

## T-0042 Rocket replay and post-A feasibility no-go

- A temporary pinned-source hook inspected the two second attempts already
  present around the Rocket post-request-exception workload. For PC
  `0x8000001c`, tag `0x24`, and PC `0x8000002c`, tag `0x26`, all surveyed
  qualifiers were zero: `ex_reg_replay`, `replay_ex_structural`,
  `replay_ex_load_use`, `replay_ex`, `mem_reg_replay`, `dcache_kill_mem`,
  `replay_mem`, `s2_nack`, `replay_next`, `wb_reg_replay`, and `replay_wb`.
  The discarded diagnostic run identified its source-cache and input hashes
  by prefixes `915272b94032` and `6b0a0d93`; it reused the exact Rocket graph
  SHA-256 `8028e1c5f7a74d2faa38631e3465dc406ee4c18041e9cec8b080d5e9a3c7f51b`.
  This rejects that exact trace as a direct replay witness; it does not prove
  that Rocket replay is absent generally. The exploratory patch was removed.
- Read-only source inspection also rejected a manager-only denied/error route
  to a precise post-A Rocket WB exception. The manager does not advertise
  `mayDenyGet`; more decisively, the pinned DCache sends uncached D
  denied/corrupt to `io.errors.bus` and does not derive `io.cpu.s2_xcpt.ae`
  from it. A precise DCache/Core exception conversion would be a new, larger
  mechanism requiring an ADR decision, not a minimal T-0042 diagnostic.

## T-0042 pinned BOOM load lifecycle positive

- Read-only comparison against TODO and ADR-0045 selected the smallest
  non-duplicative BOOM boundary: one exact load from LSU DCache request
  acceptance through response and architecturally valid ROB commit. This is an
  existing T-0042/ADR-0045 diagnostic with no performance question, so no new
  ADR or EXP was created.
- The first fresh-cache attempt stopped before patching because the runner used
  an incorrect pristine `lsu.scala` SHA-256. The corrected pristine hash is
  `1400d3997b94d1d5e4064d126d22aabd467905e1fd962896ac19cfbd1955371a`;
  the exact post-patch hash is
  `d96fa9f10ddc07c571826ef53638752ae78f967125d7a9199da3761227834e29`.
  The incomplete cache was not deleted; the final run used a new dedicated
  volume name.
- Fresh and cached executions of
  `./hardware/chisel/run-owned-boom-load-lifecycle.sh` exited 0. Source-cache
  SHA-256 was
  `66fb37eebe807c3a93eb46c2abb7c0c61fe9b414fae58f352541f7add494716c`,
  complete runner-input SHA-256 was
  `cc3c6e21cbc1dc2ebf0372c111e72f0bd82540d887112f9ced88cea4a65e0c37`,
  and generated graph SHA-256 was
  `8fbee170f393e6ed777484c9fbb3f6e0d6adddbf1802525fa5a1bb680756a948`.
  The graph verifier reported BOOM DCache manager range `[8288,8320)`, serial
  range `[0,8192)`, and fragmenter factor 32.
- The exact three-record trace is request, response, retire for repository
  epoch/sequence 1. PC is `0x80000010`, address `0x08000100`, ROB index 4,
  LDQ index 0, branch mask 0, and lane 0. The response and two-word signature
  agree on `0xdaab9780`; commit-valid and architectural-valid are both 1 and
  promotion is eligible.
- The independent review found that the first verifier required branch-mask
  and lane fields but did not reject their mutation. The final verifier now
  correlates both fields across all three records, fixes this exact trace to
  branch mask 0 and lane 0, and has separate mutation cases. Its cached RTL
  readback produced the final input hash above.
- This is bounded `rtl-simulation-functional` evidence for one BOOM-local load
  request/response/ROB-retirement correlation. The sequence is not carried
  through DCache/TileLink; ROB/LDQ/branch/lane fields are context only. It does
  not prove semantic initiator identity, store authorization, general BOOM
  replay/kill/exception/reset behavior, resource matching, OoO effects, or
  performance, power, area, FPGA, silicon, novelty, non-infringement, patent
  clearance, or FTO.
- `sh -n` passed for the shared and dedicated runners, Python bytecode
  compilation passed, and `git diff --check` passed. The owned-memory boundary
  suite passed 26 tests, including fail-closed lifecycle mutations;
  `python3 -m unittest discover -s tests` passed 224 tests with one explicit
  real-QEMU integration skip. The task-governance checker passed 115 Markdown
  files, 85 task definitions, and 53 records. `./scripts/ci-local.sh` exited 0
  with the same full unit suite, Sonatine release/debug builds, normal and
  interrupt QEMU smokes, completion telemetry, and native/QEMU Graph MVP
  checksum differential.

## T-0042 pinned BOOM misaligned exception/request/rollback ordering

- Read-only comparison against TODO and ADR-0045 selected a deterministic
  misaligned-load negative after the first BOOM positive. It remains within
  T-0042 and ADR-0045, changes no invariant, and asks no performance question,
  so no new ADR or EXP was created.
- The first source assembly stopped fail closed because the new BOOM mode
  applied its own LSU patch and then fell through to the neighboring load
  lifecycle patch. The conditional was corrected and the run moved to the
  dedicated `raveil-chipyard-owned-boom-misaligned-rollback-build-v2` volume;
  the incomplete cache and unrelated long-running Docker workload were not
  stopped or deleted.
- An initial later-request guard matched only the retained ROB index and could
  not distinguish complete retained context from index reuse. The final hook
  requires ROB index, LDQ index, PC, and address for a request, and requires
  every available retained context field for response/commit classification.
  These fields remain bounded context, not durable identity, because the
  repository sequence is not carried into DCache or TileLink.
- A final cached-diff check found one trailing-space patch context line. Its
  first removal made the patch hunk count inconsistent and the next source
  assembly stopped at `git apply` before building. The hunk was narrowed to
  exclude that context line, after which both `git apply --check` and
  `git diff --check` passed. Fresh source assembly and RTL simulation, followed
  by a final exact cached
  readback using
  `./hardware/chisel/run-owned-boom-misaligned-rollback.sh`, exited 0. The
  final source-cache SHA-256 was
  `1015fdd23fee239aa09b2b2b14f96934b7a522aa8b04d64fa0e3c8f19365be39`,
  complete runner-input SHA-256 was
  `9ab61eed9f0f04b5b7a10fdc96b569db980fc6e2a32af70830631b65677d814f`,
  generated graph SHA-256 was
  `8fbee170f393e6ed777484c9fbb3f6e0d6adddbf1802525fa5a1bb680756a948`,
  and post-patch `lsu.scala` SHA-256 was
  `1a12fdf33d797d2ae961ea5a1874d158c555372ee4f040cccb671d01ffb544e8`.
  The graph verifier retained BOOM DCache manager range `[8288,8320)`, serial
  range `[0,8192)`, and fragmenter factor 32.
- The exact four-record trace is `candidate`, `exception`, `request`, then
  `rollback` for epoch/sequence 1. PC is `0x80000028`, address `0x08000101`,
  ROB index 10, LDQ index 0, branch mask 0, and lane 0. The candidate has
  `ma_ld=1` and cause 4 with no same-cycle request. The LSU exception appears
  next; one matching DCache request is then accepted after the exception and
  before the global ROB rollback state. The terminal record requires request
  count 1, response seen 0, core exception seen 1, valid/architectural commit
  0, `matching_rbk=0`, and `promotion=blocked`. The software signature
  independently validates cause 4, `mtval=0x08000101`, one trap, exact resume,
  and equal aligned before/after reads.
- This is bounded `rtl-simulation-functional` evidence for that exact
  BOOM-local exception-before-request ordering. It does not close the required
  post-request exception/cancellation case because the exception occurs first;
  it does not prove transport cancellation, general side-effect absence,
  general rollback, token transport, semantic initiator identity, store
  authorization, resource matching, OoO effects, or performance, power, area,
  FPGA, silicon, novelty, non-infringement, patent clearance, or FTO.
- `sh -n` passed for the shared and dedicated runners; Python bytecode
  compilation and `git diff --check` passed. The owned-memory boundary suite
  passed 28 tests, including exact four-event and mutation rejection;
  `python3 -m unittest discover -s tests` passed 226 tests with one explicit
  real-QEMU integration skip. The task-governance checker passed 115 Markdown
  files, 85 task definitions, and 53 records. `./scripts/ci-local.sh` exited 0
  with the same full unit suite, Sonatine release/debug builds, normal and
  interrupt QEMU smokes, completion telemetry, and native/QEMU Graph MVP
  checksum differential.

## T-0042 pinned BOOM store authorization and separate manager A/D

- Read-only comparison against TODO and ADR-0045 selected the deterministic
  BOOM store path after the post-request-negative candidates remained
  stimulus-risky. This stays within T-0042 and ADR-0045, changes no invariant,
  and asks no performance question, so no new ADR or EXP was created.
- The first fresh source assembly stopped at patch application because the new
  store-mode branch also applied the neighboring misaligned patch. After the
  exact branches were separated, the first complete RTL trace emitted the
  intended lifecycle but stopped on a later unrelated store commit: stale
  address payload from an invalid STQ entry had qualified as another candidate.
  The final predicate requires both STQ-entry and address validity. The
  unrelated `raveil-t0105-rocket-smoke` container was not stopped or removed.
- Fresh source assembly/RTL simulation and an exact cached readback using
  `./hardware/chisel/run-owned-boom-store-authorization.sh` both exited 0. The
  final source-cache SHA-256 is
  `1150017a43a01765791fb4a1ec18fb1fdb35c9a57b9edb9213051f6216e0472b`,
  complete input SHA-256 is
  `3a52bd50f3e487d24d97f3cf05e2b55fbcf908ad191f51296ae75df7a2beff05`,
  graph SHA-256 is
  `8fbee170f393e6ed777484c9fbb3f6e0d6adddbf1802525fa5a1bb680756a948`,
  and post-patch `lsu.scala` SHA-256 is
  `beaf195dfed4457315b14aad5cb054f09bc894a6a997d63467e5fec0570154fb`.
- The exact BOOM-local order is `authorize`, `request`, `response`, `clear`
  for sequence 1, PC `0x8000001c`, address `0x08000100`, ROB index 7, STQ
  index 3, branch mask 0, and lane 0. The manager independently records
  `PutFullData`/`AccessAck` with source 8304, then `Get`/`AccessAckData` with
  source 8288; both are in `[8288,8320)`, have matching A/D sources, and have
  denied/corrupt zero. Software readback equals stored value `0x51a7c0de`.
- This is bounded `rtl-simulation-functional` evidence for BOOM-local
  authorization/request/response/clear correlation and separate manager-local
  A/D source correlation. The sequence is not carried through DCache/TileLink;
  same-token manager D completion, complete store attribution, semantic
  initiator, general store behavior, resource matching, OoO effects,
  performance, power, area, FPGA, silicon, novelty, non-infringement, patent
  clearance, and FTO remain unproven.
- Closeout commands and results were:
  - `sh -n hardware/chisel/run-owned-cpu-memory-smoke.sh hardware/chisel/run-owned-boom-store-authorization.sh`: exit 0;
  - `python3 -m py_compile hardware/chisel/verify_owned_boom_store_authorization.py hardware/chisel/verify_owned_cpu_source_map.py`: exit 0;
  - `python3 -m unittest tests.test_owned_memory_boundary -v`: exit 0, 30 tests passed;
  - `python3 -m unittest discover -s tests`: exit 0, 228 tests passed with one explicit real-QEMU skip;
  - `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`: exit 0 over 115 Markdown files, 85 task definitions, and 53 records;
  - `git diff --check`: exit 0; and
  - `./scripts/ci-local.sh`: exit 0, including the same full unit suite,
    Sonatine release/debug builds, normal/interrupt QEMU smokes, completion
    telemetry, and native/QEMU Graph MVP checks.
