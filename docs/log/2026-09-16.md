# 2026-09-16

## T-0121 kickoff and queue reconciliation

- Owner requested continuation and previously approved T-0121 promotion.
  Weekly usage monitoring remains disabled by explicit owner instruction.
- Canonical `project_queue.py prepare 179` and `start 179` succeeded with
  Experience Implementer, dependencies T-0120/T-0161/T-0192, S-0004, 5 SP,
  Host Functional evidence, and the Garden/project focused-test demo.
- Isolated worktree `/private/tmp/raveil-t0121-ascii`, branch
  `feat/t-0121-ascii-canvas`, starts from main `58de6bc`.
  The historical dirty root and T-0191 external-cancel draft are preserved.
- Reconciled stale TODO/ROADMAP deferral with the accepted bounded ASCII
  scope. This is a Playable UI refinement, with no new architecture or EXP.
- Main `58de6bc` has `c0dc979` as its second parent and identical tree. The
  earlier interpretation as another session's conflicting update was wrong.
  Closed stale OPEN PR #247 with that evidence; T-0191 remains In Progress.
  No candidate was reimplemented or pushed directly to main.

## T-0121 implementation verification

- Renderer-only ASCII edges share the admitted fixture/dynamic/saved topology.
  External LOAD selectors and only the STORE output are shown for lowered
  programs. Repeated-node and dependency-arrow legends prevent interpretation
  as an execution schedule. Wrapped edges retain continuation indentation.
- Fixture canvas is in `d` details; the existing compact fusion comparison
  remains unchanged. No dependencies, admission, compiler, schema or RTL change.
- Environment: macOS 26.6.2 arm64, Python 3.14.6. Implementer command
  `python3 -m unittest tests.test_garden_tui tests.test_project_graph` passed
  73 tests. Primary repeated the same modules with `PYTHONDONTWRITEBYTECODE=1`
  and `-q`: 73 tests passed in 11.403 seconds. Record checker passed 209 Markdown
  files, 157 task definitions and 110 records; `git diff --check` passed.
- Primary rendered the actual fixture CLI with `--keys dq --width 72` and
  inspected the external inputs, selected node and final output. Evidence is
  Host Functional only. No research gate or EXP changes; OPEN_QUESTIONS remains
  unchanged because this is the already approved dependency-free UI slice.
