# 2026-08-30

## T-0093 integration acceptance and closeout

- The owner explicitly accepted T-0093 integration after the primary verified
  PR #70 exact head `64ca94bc135ae656406a748ecee9f3c866c1dec4`, its
  allowlisted files, mergeability, empty review/check surfaces, 61 passing
  focused/regression tests, and deterministic manifest SHA-256
  `b6ab732f9305212cc39510eb0f7e01ab3c4172bb2f209da636937b994bc92ca0`.
- PR #70 merged at 09:28 JST as canonical commit
  `4db3a1e57d97ea92b0e840c1d9ce203849e82109`; Issue #69 closed at the same
  boundary. The closeout preserves host-functional evidence only and no
  execution, mutation, Experience, filesystem-security, FUSE/VFS,
  performance, hardware, publication, or product-readiness claim.
- Closeout replay on macOS 26.5.1 arm64 with Python 3.14.6 and Apple Git
  2.50.1 ran `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest
  tests.test_graph_directory tests.test_graph_mvp tests.test_garden_tui
  tests.test_interactive_shell -q`: all 61 tests passed in 3.516 seconds.
  The governance record checker passed 161 Markdown files, 110 task
  definitions, and 81 records; `git diff --check` passed.
- The PM initially treated the owner-visible command as a per-task acceptance
  gate. The owner corrected the boundary: ordinary task PRs integrate after
  technical acceptance while work continues; the weekly Sprint Review is the
  Saturday ceremony that aggregates the Sprint's runnable outcomes, shows
  them to the owner, collects feedback, and receives the Sprint disposition.
  ADR-0069 already scoped its human-visible boundary to that ceremony. The
  ambiguous sprint-operator wording is a process defect to correct separately;
  S-0003 remains open and was not accepted by the T-0093 task disposition.
- Retrospective for this task boundary: `Keep` exact-head verification and
  truthful evidence labels; `Problem` conflation of task review with weekly
  Sprint Review plus an unavailable Jitro browser/telemetry connector; `Try`
  separate those phases explicitly and let the PM take over bounded work when
  current owner-supplied HCI-09 evidence clears the usage guard.

## T-0137 task-review and Sprint-review boundary correction

- Current owner-supplied HCI-09 evidence at 08:55 JST showed 100% weekly
  remaining. After T-0093 closeout, the live queue audit passed with zero WIP.
  Issue #72 and clean current-main branch
  `fix/t-0137-sprint-review-boundary` bind the two-SP S-0003 correction to the
  Project Manager and the exact skill/workflow/record allowlist. Canonical
  start dry-run and apply set it as the sole P0/In Progress item; the following
  branch audit passed with one active item.
- The defect was an ambiguous combined `Review/closeout` phase in the Sprint
  operator, not ADR-0069 itself. The correction separates continuous task PR
  integration from the scheduled owner-visible weekly ceremony. Ordinary
  tasks no longer wait for a per-task owner demo by default; their merge and
  `Done` state cannot accept or close the weekly Sprint Review.
- No ADR, ROADMAP, OPEN_QUESTIONS, RFC, EXP, product, research, hardware, or
  evidence-class change is required. This is Host Functional governance
  validation only and makes no delivery-speed or product-readiness claim.
- The bundled `skill-creator` Python validator stopped before validation with
  `ModuleNotFoundError: No module named 'yaml'`; the host has no PyYAML. The PM
  did not add an unrelated repository or user dependency. A Ruby YAML
  equivalent checked the same frontmatter dictionary, allowed keys, name,
  description, and unfinished-placeholder invariants and passed. The 23
  `tests.test_project_queue` regressions, governance record checker over 161
  Markdown files/111 task definitions/81 records, and `git diff --check` also
  passed. The remaining acceptance boundary is exact-head PR integration, not
  an owner-attended per-task demo.
- PR #73 exact head `74b1f27e735b6dc4aaecc2562128f2addd2639bf`
  was mergeable and clean with the seven allowlisted files and no check or
  review-thread objection. Technical acceptance was recorded without a
  per-task owner demo, then the PR merged at 09:42 JST as canonical commit
  `e8b500ec03bb7154e50fa0cb737dc25d95fca684`; Issue #72 closed immediately
  after merge. T-0137 is complete, but S-0003 and its Saturday owner-visible
  Sprint Review remain open so delivery can continue.

## T-0132/S02 AXI installation path kickoff

- Current owner-provided HCI-09 evidence at 08:55 JST still showed 100% weekly
  remaining at this task boundary. T-0137 closeout left zero active WIP, and
  no live Project item was Ready. The PM refined the next mainline slice from
  accepted ADR-0067 rather than pulling the deferred Garden or stale T-0044
  plans.
- Issue #75 and clean current-main branch `feat/t-0132-s02-axi-install` bind
  S-0003, eight SP, the Systems Implementer, Tester/PM support, unchanged
  ABI/aperture dependencies, exact file/artifact/test allowlists, and RTL
  Simulation evidence. Docker 29.6.2 is running and the immutable offline
  Chisel/Verilator image is already cached. The canonical start dry-run and
  apply succeeded; the live branch audit passed with T-0132/S02 as the sole
  P0/In Progress item.
- The slice enables only affine/program installation control, ordered payload,
  installed digest, status, and count through actual AXI pins. It stops before
  execution I/O/start/cancel, Linux, absolute platform mapping, KV260/FPGA,
  performance, resources, Experience, schema, or ABI changes. No new ADR is
  required because ADR-0067 already reserves S02 promotion over the unchanged
  installation ABIs and relative aperture.
- The first RTL edit added queued config/program mutation commands and digest
  reads. The initial real S01 offline replay failed in private artifact
  `artifacts/graph_device_axi4lite_control/run.3sCXjF`: execution word 7
  returned digest data instead of the existing output count. The PM restored
  the execution-count priority and reran the same operator command.
- Corrected private replay
  `artifacts/graph_device_axi4lite_control/run.nqf2e9` exited zero. It retained
  the exact S01 marker and produced identical double-emitted RTL manifest
  SHA-256 `59d4b5c94a8959756c4877d2fa1dd0b3514d0e72dc516d8ce70c71d2812f11bd`.
  Eighteen focused host regressions and `git diff --check` also pass. This is
  only a regression-clean implementation checkpoint; it does not yet prove the
  new S02 install sequence and makes no performance or hardware claim.
- The first complete S02 AXI transcript passed in private run
  `artifacts/graph_device_axi4lite_install/run.5WUYuO`. Expanding its negatives
  then produced an intentional failing replay at `run.zPPHgw`: a misaligned
  config-control write returned DECERR but the rounded internal word still
  queued a clear. Response classification and mutation authorization had used
  separate predicates. The RTL now authorizes reset/install side effects only
  from the same aligned, in-aperture accepted predicate that yields OKAY.
- A second negative replay at `run.0mQ8CZ` was a test-oracle correction, not an
  RTL defect: the affine installer correctly retains its loading bit together
  with sticky fault after a duplicate word, so the assertion now tests the
  fault bit rather than requiring the whole status word to equal four.
- Corrected private run
  `artifacts/graph_device_axi4lite_install/run.7rQAIH` exits zero. It covers
  retained-B admission, compact affine and bounded-program install/digest,
  partial/misaligned/read-only/out-of-range rejection, wrong order, duplicate,
  premature commit, namespace isolation, and factory restore. Its exact marker
  labels `rtl-simulation-functional` and `performance=not-measured`; both RTL
  emissions have manifest SHA-256
  `02e010b141d7ce9d05eb0ee1df307c13d12a77823dfc6b1473cef0c0beffaf89`.
  Twenty-one focused/regression host tests and `git diff --check` pass. The
  candidate now proceeds to independent review and exact-head integration;
  it establishes no Linux, FPGA, performance, resource, or silicon result.
- Independent security and measurement reviews found no blocking AXI
  authorization, path, credential, or claim-label defect. They identified
  bounded acceptance gaps for invalid control, payload overflow, partial-AW/W
  retention, unbound runtime stderr, dynamic cache-image identity, and lack of
  receipt re-verification. The S02 candidate now covers those sequences,
  requires empty device/container stderr, pins immutable image ID
  `sha256:2efc059cf07eb054d93fc1fa32decd7a13c2cdb97069dac29138275b22e5c57c`,
  and re-verifies the append-once receipt after creation.
- Final corrected private run
  `artifacts/graph_device_axi4lite_install/run.9i9v74` exits zero with the same
  exact functional/non-performance marker and identical double-emitted RTL
  manifest SHA-256
  `02e010b141d7ce9d05eb0ee1df307c13d12a77823dfc6b1473cef0c0beffaf89`.
  The general inherited evidence-bundle size-ceiling concern remains outside
  this install-only slice; no unbounded input is introduced to the AXI design.
