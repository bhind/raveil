# 2026-08-23

- ADR-0055 first accepted a Markdown board, then ADR-0056 superseded it after
  the owner selected the private, repository-linked GitHub Project at
  `https://github.com/users/bhind/projects/1` as the sole live sprint board.
- Configured six Status options, six seven-day Sprint Iterations, initial and
  current story points, Pillar, Demo Command, Evidence Class, estimate review
  and change reason, Review Outcome, and Retro Action fields. Added the
  `Sprint Board` Kanban view.
- The sprint board remains a coordination view over stable T-IDs. It cannot
  promote STATUS, ROADMAP, ADR, RFC, or EXP state, and weekly review does not
  replace milestone-driven research review.
- S-0001 is planned for 2026-08-24 through 2026-08-30 with an eight-point
  capacity and WIP limit two. T-0116 is three Operations points in Review; the
  current bounded T-0042 negative is five Research points In Progress. An
  unpointed Sunday review/demo/retrospective item is Ready. The RTL item remains
  unverified.
- The Project remains private. No hosted CI/CD or calendar-triggered agent
  execution was enabled.
- Owner review found the initial Project incomplete because it contained only
  the S-0001 execution items and no Product Backlog. T-0116 corrective work
  added 30 decomposed planning items, bringing the Project to 33 total items,
  and expanded it to 32 fields. The Backlog now records owner/support roles,
  parent T-ID, dependencies, priority, work type, initial/current SP, committed
  or forecast Sprint, date, confidence, evidence class, and demo/evidence path.
  The original table view was renamed and filtered as `Product Backlog`; the
  separate `Sprint Board` remains the execution Kanban.
- S-0002 is an eight-point Playable plan for T-0117, a read-only Garden TUI
  plus clean terminal acceptance. Garden initially appeared under T-0093 in
  the Project, but governance review corrected it because T-0093 owns a static
  directory snapshot and cannot silently absorb a TUI. T-0117 was allocated
  only after confirming no existing task owned Garden. S-0003 through S-0006
  are eight-point T-0042 slices.
  The low-confidence dependency forecast reaches T-0042 readiness, T-0044
  matched-comparison review and decision by S-0011, and a conditional KV260
  feasibility packet in S-0012. Custom RISC-V remains an unpointed Icebox epic.
- Capacity assumes one human integration and acceptance lane at eight SP per
  week for the first three closed sprints. Specialist agent roles are ownership
  boundaries rather than independently additive FTE. Parent epics carry no SP
  so their child slices are not counted twice.
- A later canonical-authority audit found that the Project had misclassified
  the active BOOM stripped-token negative and its proposed follow-ups under
  completed T-0042. The active five-point slice is corrected to bounded
  T-0106 carry-in evidence; it does not activate conditional T-0106. The
  unstarted token-hardening sequence and stale T-0044 latency/traffic replay
  forecast return to `Unscheduled`. S-0003 is instead refined to five points
  for the minimal post-EXP-0010 integrated Static Graph/Rocket/common-boundary
  top and three points for clean structural plus oracle verification. No
  performance, physical, FPGA, silicon, EXP, Gate, or T-0044 conclusion changes.
- Private Project synchronization preserved all 33 items and 32 fields. Final
  readback reported 27 Backlog, four Ready, one In Progress, and one Review;
  WIP is two of two. T-0116 returned from Done to Review because the current
  local integration branch is not yet canonical repository history. S-0001,
  S-0002, and S-0003
  each total eight SP. No slice retains T-0042 as parent. T-0106/S02 through
  S07 are P2, Unscheduled, and carry no forecast date. The superseded
  T-0044/S00 through S07 planning items
  remain preserved with no forecast dates, while T-0044/S08 and S09 form the
  S-0003 five-plus-three-point integrated-boundary plan. KV260 is conditional
  and unscheduled. No repository push, merge, release, or visibility change
  occurred.
- After explicit owner approval, integration branch
  `fix/t-0116-sprint-governance-integration` and `origin/main` both advanced
  without force from `648eea4` to `b64086c`; remote readback matched exactly.
  T-0116 may therefore close as canonical Done. Project WIP returns to one of
  two, with only T-0106/S01 In Progress. No tag, release, force push, history
  rewrite, visibility change, or dirty-worktree mutation occurred.
- T-0106/S01 carried the already-active BOOM stripped-token negative onto the
  current-main descendant branch
  `test/t-0106-s01-boom-stripped-token-carry-in`, based on
  `8ac41af64b38832eb0ec3ab2533753540e4f7f11`. Implementation commit `32aab73`
  adds one test-only post-producer value-clearing patch, an exact runner,
  fail-closed verifier, regression tests, and user-facing boundary notes while
  retaining the historical T-0042 patch filename as provenance rather than
  task authority.
- The replay command is
  `RAVEIL_CHIPYARD_SOURCE="$RAVEIL_DEPENDENCY_ROOT/chipyard" RAVEIL_ROCKET_CHIP_SOURCE="$RAVEIL_DEPENDENCY_ROOT/chipyard/generators/rocket-chip" ./hardware/chisel/run-owned-boom-store-token-stripped-after-valid.sh`,
  where `RAVEIL_DEPENDENCY_ROOT` names the existing read-only dependency root.
  It exited 0 on Darwin arm64 with Docker 29.6.2, using a pinned `linux/amd64`
  image `raveil-boom-functional-sim:v1` with local image identity
  `sha256:6fadc432541ecf5283be6c8709198cfe5046ce17c48fa89087b8b3ab7157092c`,
  Chipyard revision `ac58f38d77c99e9d1cafa64dfd6d4b00bdcd43e1`, and conda lock SHA-256
  `5248d0e404ab5ac0884ffd03934e31b757c6999c9987009e5cfd5d80fc21da3d`.
  The terminal marker reported input SHA-256
  `6a8af8096a6378cbb4ac1504e9d960166d5bddec3d07b4124ebfae729863bba6`,
  source SHA-256
  `7a3b0abd29a3c81150e16ef3913dda5cbc20928edd3f67c6b506d1b47c3c0c05`,
  graph SHA-256
  `8fbee170f393e6ed777484c9fbb3f6e0d6adddbf1802525fa5a1bb680756a948`,
  and successful completion after 10,916 simulator cycles. The raw log in
  Docker volume
  `raveil-chipyard-owned-boom-store-token-stripped-after-valid-build-v2` at
  `7a3b0abd29a3c81150e16ef3913dda5cbc20928edd3f67c6b506d1b47c3c0c05/owned_memory_boom_store_token_stripped_after_valid.log`
  has SHA-256
  `3769c3c621132a9e2e396ae9040c63ad69de3ef509119f32fcbcd88047413e0a`;
  its signature SHA-256 is
  `81f57f1057625948bd687708f89290422f376fdad77a8334afa7c7454b8aa257`.
- Scoped verification passed 48 tests with
  `python3 -m unittest tests.test_static_region tests.test_owned_memory_boundary -v`;
  the governance checker covered 137 Markdown files, 92 task definitions, and
  67 records; `git diff --check` passed. This is
  `rtl-simulation-functional` evidence only. It proves one fixed
  valid-to-invalid/zero negative, unknown manager classification, transaction
  completion, and readback; it does not prove field removal, malformed nonzero,
  epoch/reset/stale/replay/backpressure behavior, semantic initiator identity,
  resource matching, OoO effects, or performance. T-0042 remains complete,
  later T-0106 work remains conditional, and Sprint acceptance remains open.
- Detached clean-checkout replay at `efc9e34` reused the pinned warm build and
  repeated the exact RTL terminal markers and input/source/graph identities;
  the demo exited 0 in 16.9 seconds of operational wall-clock. The clean tree
  also passed the same 48 tests, record checker, diff check, and ancestry from
  `8ac41af64b38832eb0ec3ab2533753540e4f7f11`. Wall-clock is operational
  information only and is not performance evidence.
- The owner replaced one-off direct `main` pushes with a PR-only integration
  policy. GitHub ruleset 20582491 was changed from disabled to active for
  `refs/heads/main`: it has no bypass actors, requires a pull request with zero
  mandatory approvals, requires review-thread resolution, and blocks deletion
  and non-fast-forward updates. Zero approvals avoids deadlocking the current
  single-human integration lane; it does not authorize agent merge. ADR-0057
  records that dedicated branch push and PR creation are normal steps for an
  already authorized bounded change, while PR merge remains human-authorized.
- The owner replaced per-PR merge acknowledgement with an incident-free merge
  audit. ADR-0058 now gives the Project Manager standing authority to merge a
  verified, current, record-complete, GitHub-mergeable PR immediately when no
  ADR-0051 HCI class applies. Direct `main` push, remote branch deletion, gate
  or EXP decisions, evidence ambiguity, destructive operations, user-work
  overlap, external cost or credentials, legal risk, and material scope forks
  retain their existing stop boundaries.
- T-0117/S01 began on dedicated branch
  `feat/t-0117-garden-read-only-browser` from authority
  `d23c9cb88fd76500976eee1b81e8306396a52be0`; implementation candidate
  `412f70d` adds a dependency-minimal read-only Garden browser, strict
  `raveil.garden-snapshot/v1` fixture validation, canonical variant-slate
  comparison, deterministic bounded navigation, and explicit empty/error
  states. The fixture SHA-256 is
  `e318d6e1b8ae4f5db5200de896c1e3405ee1c0697a0513596b6987238899922f`.
  `python3 -m unittest tests.test_garden_tui tests.test_graph_mvp -v` passed 32
  tests on Darwin arm64 with Python 3.14.6. Both
  `python3 -m raveil garden --fixture tests/fixtures/garden/minimal.json` and
  `python3 -m raveil garden --empty` exited 0, and `git diff --check` passed.
  This is `host-functional` development evidence only: Garden has no graph
  execution, mutation, approval, publication, evidence-promotion, performance,
  FPGA, ASIC, silicon, task, or gate authority. T-0117 remains open pending
  S02 independent clean-terminal acceptance.
