# 2026-09-09

## T-0191 first external-simulator execution

- Built exact OpenASIP v2.2 and llvmtce revisions in the pinned linux/amd64
  image. The first hardened compile correctly failed closed because OpenASIP
  attempted to generate a target plugin below `HOME`; the fixed-ADF plugin was
  then generated at image-build time, preserving a read-only, networkless,
  non-root and capability-free runtime.
- Changed all three sealed programs to volatile inputs after independent review
  identified legal whole-program constant folding. TPEF disassembly now gates
  minimum load witnesses and the elementwise multiply witness.
- One frozen ADF compiled and `ttasim` executed neighborhood, elementwise and
  reduction programs. Results 170, 740 and 374 matched independent uint32
  oracles. EXP-0012 records exact image, ADF, manifest and TPEF identities.
- Private raw evidence retains compiler/simulator output, TPEFs and
  disassembly. Its marker-last receipt SHA-256 is
  `5201f55e8c788601a74802484b58617984173eb6a053edf11939679aa265e272`;
  it is not yet a durable published artifact.
- Added a Raveil-owned lifecycle boundary demonstrating cancel-before-start,
  candidate failure/oracle-mismatch fallback, unpublished candidate output and
  Raveil-only publication, then wired the actual simulator entrypoint through
  that boundary. The accepted receipt hash is
  `e85cfeae2232a432c5162a4ad38bef1392cede97936b4954cd5a6da0b8b94543`.
  A forced candidate-failure diagnostic invoked the actual native C runner;
  all three CPU results matched and Raveil published the fallback receipt.
- Generated ten VHDL files totalling 76,211 bytes twice from the same ADF.
  Raw RF hashes differed only by generated-on comments; the normalized tree
  identity matched as
  `19b6128e75369e8f3198f0dfceb15804a0a00b0d55138e2bc768c8be3149689b`.
  TPEF sizes and bounded n=1 host load diagnostics were also recorded without
  performance or device-time meaning. T-0191 remains open for durable-artifact
  promotion, in-flight cancellation parity and review.
- The local evidence verifier rehashed the exact 26-file authorized run bundle
  and 13-file RTL bundle, including every raw log, TPEF, generated VHDL and
  receipt, and rejected any extra-file or identity mismatch by construction.
- The full repository regression passed 843 tests in 155.455 seconds with two
  skips. The focused T-0191 suite passed 17 tests; the record checker and
  `git diff --check` passed after the final evidence changes.

## T-0191 OpenASIP feasibility successor

- After T-0044/S00 closeout, the live Project had one independent active item
  and no Ready successor. HCI-09 read 18% used and 82% remaining in the
  10,080-minute weekly window at 2026-09-08T15:53:53Z.
- Issue #239 registers the smallest executable follow-up at 8 initial/current
  SP in S-0003. It is Ready with Systems Implementer mutation ownership and
  Host Functional evidence, explicitly labelled as external simulator output.
- The packet pins OpenASIP v2.2, one ADF/generated-RTL identity, three distinct
  programs, configuration-cost receipts, CPU/candidate lifecycle parity and an
  independent Raveil authority property. It excludes source copying, adoption,
  devices, claim-bearing performance/PPA and T-0044 gate closure.
- Registration and record reconciliation performed no external build,
  simulation, source import or experiment collection.

## T-0044/S00 public configurable candidate qualification

- The owner resumed work after the weekly usage window recovered. The latest
  HCI-09 reading available before kickoff was 16% used and 84% remaining in
  the 10,080-minute window.
- Issue #190 entered S-0003 In Progress as a Planning-only, read-only public
  source qualification. The primary and two independent reviews screened no
  more than three final candidates and added no upstream source to the Raveil
  repository.
- Exact remote identities were resolved as OpenASIP v2.2
  `f2282048f4c78d18190a9b77e7497e29a164721d`, OpenEdgeCGRA main
  `4a179fcf6faee18765f66baa0a30af91e3840728`, and Apache VTA main
  `d4a15f627d5cc9762a82270d1be60a136e6af9c2`.
- OpenASIP remains the only bounded feasibility-spike candidate, not an
  adoption: a successor must pin Linux dependencies, freeze one ADF/RTL and
  demonstrate three distinct programs without regeneration. OpenEdgeCGRA's
  core manifest references `LICENSE.md`, which returned HTTP 404 at the exact
  revision; mixed file-header license identifiers remain unresolved. VTA
  remains a tensor-specific public control. Review added an explicit
  independent contract-lifetime/authority test and preserved the later full
  installation/configuration/execution/fallback and PPA-proxy comparison.
  Copyright metadata was separated from unreviewed patent and FTO questions.
- No build, source import, simulator run, experiment freeze, architecture
  decision, FPGA access or claim-bearing measurement occurred.
- The repository regression suite passed 826 tests in 119.555 seconds with two
  skips on macOS arm64 and Python 3.14.6. The record checker passed 205
  Markdown files, 155 task definitions and 109 records; `git diff --check`
  passed. Independent review and Vreji required the successor to exercise
  explicit CPU/candidate lifecycle parity, retain ADR-0049's independent
  authority-property test, defer the full-cost comparison and distinguish a
  candidate-attributable build failure from retryable infrastructure failure.
- Exact candidate `e5610ff` was merged by PR #237 as canonical `8c6fb00`.
  Issue #190 closed and `project_queue.py complete` recorded the unchanged
  Planning evidence boundary, observed cycle and actual resource use before
  Project Done was verified. T-0044 remains open and no successor experiment
  was started by this closeout.

## T-0181 Graph extension decision spike

- After T-0185 completion, HCI-09 read the 10,080-minute Codex window at 14%
  used and 86% remaining at 2026-09-08T14:57:27Z. T-0181 then started in
  S-0003 at 3 SP with Planning evidence and no compiler/RTL mutation authority.
- Three fixtures attempt a full-width source-free fill, unsigned saturating
  dark-frame subtraction and raw-intensity threshold plus T-0185 cross
  dilation. The current validator rejects every proposed opcode and also
  rejects a `0xF1234567` ADD_IMM attempt above the 22-bit maximum.
- Planning recommends only `GE_IMM_U32` for a later decision. It yields exact
  uint32 zero/one from an unsigned 22-bit threshold, adds no conditional
  effect, fits the threshold-dilation discriminator in 15/16 instructions and
  reuses the existing unary-immediate word shape. Constants, saturation and
  general SELECT remain deferred. No adoption decision is made here. Review
  corrected an unsupported first-LOAD rationale: current rejection proves an
  unknown opcode and insufficient 22-bit encoding, not a first-node rule.
- `check_vectors.py` passed all three rejected programs, four exact current
  attempts, 15
  hand-calculated truth vectors and five proposed-rejection vectors. The existing Graph DAG and
  Project Graph regression subset passed 53 tests in 11.140 seconds. The
  record checker passed 204 Markdown files, 155 task definitions and 109
  records; `git diff --check` passed. Evidence is Planning plus existing Host
  Functional regression, not RTL execution of any proposed operation.
- Before publication review, `python3 -m unittest discover -s tests` passed 826
  tests in 155.406 seconds with two skips. After correcting the planning
  rationale and adding rejection vectors, the 53 scoped regressions passed
  again in 11.850 seconds; the updated vector checker and working-tree
  `git diff --check` passed.
- After all review corrections, the full 826-test suite passed again in
  149.816 seconds with two skips. A mistyped follow-on record-check path made
  that compound shell command exit 2 after the successful suite; the canonical
  record checker and diff checks were rerun separately below rather than
  misreporting the compound command as success.
- Vreji and independent re-review cleared exact `ed32f61` after the correction.
  PR #235 merged T-0181 to canonical main as `69589f0`; Issue #180 closed and
  `project_queue.py complete` recorded the Planning outcome, unchanged 3 SP
  estimate and actual resources before Project Done was verified.
