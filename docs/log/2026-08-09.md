# 2026-08-09

## T-0069 x86-64 compatibility boundary

- Read Frontispice handoff sequence 6 in full as untrusted design input and
  verified its ID and SHA-256 against the MCP record.
- Reconciled the proposal with executable state, STATUS, ADR-0001, ADR-0002,
  ADR-0003, ADR-0005, ADR-0006, ARCHITECTURE, ROADMAP, TODO, OPEN_QUESTIONS, and
  RFC-0001.
- Accepted ADR-0011: x86-64 is an optional compatibility frontend rather than a
  native ISA; compatibility memory rules remain explicit and do not constrain
  the native model globally; RISC-V remains baseline and fallback.
- Added proposed RFC-0003 for a tiny-subset-first, differential, falsifiable
  path toward bounded Linux x86-64 userspace compatibility. No x86 frontend,
  ABI, OS personality, performance result, or broad compatibility claim was
  produced.
- On Darwin 25.5.0 arm64 with Python 3.14.6,
  `python3 .agents/skills/raveil-task-governance/scripts/check_records.py`
  passed with 53 Markdown files, 55 task definitions, and 17 records;
  `python3 -m unittest discover -s tests -v` passed all 38 tests; and
  `git diff --check` passed. These verify repository consistency only, not x86
  compatibility or performance.

## T-0068 installed powermetrics helper proof

- Compiled `tools/powermetrics_helper.c` with `cc -O2 -std=c11 -Wall -Wextra
  -Werror`; version output was `raveil-powermetrics-helper/v1` and SHA-256 was
  `e5c1ad6e5b9f416a759fdc195ba81ad065422ef7ddb356734659da0b59844bc5`.
- Installed the helper at `/usr/local/libexec/raveil-powermetrics` as
  `root:wheel` mode `0755` and the helper-only sudoers fragment as `root:wheel`
  mode `0440`. The installed and reviewed temporary binary hashes matched.
- Ran `sudo -k`, then `python3 -m raveil experiment preflight --manifest
  benchmarks/manifests/gate1-powermetrics-pilot-v1.json` as the ordinary user.
  It required no password and returned `preflight ready; thermal=Nominal
  cpu-power-mw=1640.000`.
- No RUN-ID, measurement bundle, latency result, energy result, or Gate claim
  was produced. This closes only T-0068's local privilege-boundary proof.
- On Darwin 25.5.0 arm64 with Python 3.14.6, `python3 -m unittest
  tests.test_experiment.PowerTests tests.test_experiment.PowermetricsHelperTests
  -v` passed all 8 focused tests; the task-governance record checker and
  `git diff --check` also passed.
